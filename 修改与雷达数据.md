以下是 8 月 10 日至 8 月 15 日的更新汇总，共 24 个已提交更新；8 月 15 日暂无新提交。

### 8 月 10 日

- 公共线索 AI 评分最多尝试 3 次，取消跨周期延迟重试。
- 材料生成失败后展示真实失败状态，不再伪装成功。
- 统一材料文件下载逻辑，支持正确文件名。
- 恢复历史 PPT、Word、Excel 材料下载。
- 调整生成文件目录权限，解决 Nginx 无法读取问题。

### 8 月 11 日

- 新增 Radar 数据源钉钉告警。
- 优化钉钉告警摘要和采集结果统计。
- 区分候选数据总量、增量、有效线索等同步指标。
- 增加独立数据源接入和历史公众号数据导入能力。
- OpenAlex 数据源当天试接后已移除，当前最终版本不使用。
- 论文采集改为每日定时执行，默认每天 14:00，失败后延迟重试。

### 8 月 12 日

- 投资提案生成切换为 VC Agent 专用链路。
- 补充内容提取不完整时的恢复机制。
- 禁止内部字段、流程字段出现在对外投资提案中。
- 投资提案统一只通过 VC Agent 生成，清理旧链路。
- 公共线索池删除“已有多源证据”统计卡片。

### 8 月 13 日

- 修复投资提案任务中断后的恢复。
- 支持项目上传 Markdown 文件并进入知识库。
- 修复 MiniMax-M3 在评分工作流中的结构化输出问题。
- 评分恢复队列覆盖失败和历史未评分线索。
- 每 15 分钟自动扫描并恢复异常评分任务。
- 自愈范围扩展到 36氪、BP 上传等非 Radar 来源线索。
- 短品牌名存在完整工商主体时允许进入评分。
- 更新合规性说明、投资提案、投资建议书等文档生成 Skill。
- 投资提案支持事实检查点复用和分批恢复。

### 8 月 14 日

- AI 文档生成整体迁移到 Agent Skills 架构。
- 合规性说明、投资提案、尽调报告、Q&A、投资建议书统一由对应 Agent Skill 执行。
- 清理大量旧的 Express 内置文档流水线和重复服务。
- 调整 AI 任务、产物下载、任务卡片和助手页面。
- 增加 Web Research 服务。
- 更新数据库结构、部署脚本、Agent 工具和文件发布流程。

### 当前本地状态

当前另外存在尚未提交的本地变更：

- 9 个文件被修改。
- 968 个已跟踪文件显示删除。
- 13 个新目录或生成产物未跟踪。

这批内容尚未进入远程 `main`，主要涉及旧文档、历史生成产物、旧 Skills 清理以及投资建议书流程修改，不能视为已经上线。提交前建议先单独核对，避免误删交付资料。

## 项目 Radar 数据与运行位置

Radar 已并入 `/www/sbl/server-dist/index.js` 对应的唯一主服务，不再存在
`project-discovery` 运行目录、8121 端口、独立同步服务或 timer。

MySQL 是唯一权威源：

- `sbl_radar_raw_events`：不可变采集证据；
- `sbl_radar_candidates`：当前候选投影；
- `sbl_radar_source_registry`：公共来源、高校来源和公众号账户；
- `sbl_radar_collector_states`：采集器状态；
- `sbl_radar_wechat_chat_messages`：微信群聊 Push 原始消息；
- `sbl_runtime_jobs` / `sbl_runtime_job_runs`：调度配置与执行历史；
- `sbl_radar_sync_state` / `sbl_radar_ai_reviews`：线索同步游标与 AI 评审。

GSData 凭据和微信群聊 Push 密钥仅保存在生产 `.env` 或受控密钥系统，不再从
JSON 文件读取，也不得写入文档、聊天、仓库或日志。历史 JSONL/Excel 仅能存放在
受限迁移归档目录，并通过显式 `RADAR_DATA_DIR` 用于审计，不参与线上运行。
