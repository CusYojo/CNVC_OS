# 共享线索池投资证据画像迭代实现报告

> 日期：2026-09-02
> 范围：源码、0092至0094正式库迁移、V4有界补全写回、契约与集成测试、只读正式库审计、活动构建切换和隔离浏览器DOM验收
> 未执行范围：更大范围全量回补、业务字典代客定义、正式登录会话的页面DOM验收、代码提交/推送及对外生产发布
> 2026-09-03 产品口径更新：线索池七列调整为“线索主体、行业 / 产品路线、机构 / 高校背景、融资进展、最新估值、最新动态、更新时间”；顶部仍保持“线索类型、行业、阶段、地区、渠道、更新时间”六个旧版筛选；大客户验证和数据状态仅从列表移除，详情页与服务端画像继续保留

## 1. 本轮已完成

- 共享线索池列表固定为七列：线索主体、行业 / 产品路线、机构 / 高校背景、融资进展、最新估值、最新动态、更新时间。
- 列表移除一句话摘要、核心信息和独立推荐理由/信号；评级移入线索主体，最新动态与更新时间保留为后两列。
- 大客户验证和数据状态只退出线索池列表；客户阶段、脱敏、聚合计数、画像覆盖/冲突/过期状态仍由服务端画像保留，并在详情页继续展示和核对。
- 新增 `lead-investment-profile-v1` 确定性画像契约，只消费冻结快照中已验证且至少绑定一条公开证据的事实。
- 融资、估值按稳定 `instanceKey` 和日期归并；计划融资、目标估值和媒体估算不进入已完成融资或最新有效估值。
- “待交割/尚未完成/传闻/意向”轮次不会因存在轮次名称和日期而计入已完成融资；计划、目标、预计、媒体、第三方推算和传闻语义优先于 `pre_money`/`post_money` 字段名，不会进入最新有效估值。
- “融资中/在融/进行中/ongoing/in progress”同样保持为未完成融资，不产生最新已完成轮次、金额或估值。
- 同一融资实例存在多种估值候选时先剔除计划和估算值，再按已确认投后、投前、通用估值选择；目标投后估值不会遮蔽同轮已有证据支持的有效投前估值。
- 客户验证阶段由意向、试点、合同、订单/交付、回款/复购事实确定，模型不能直接指定等级；在客户确认 L1 口径前，意向/科研合作只保留在事实层，服务端画像的已验证客户按 L2 起算；保密客户在投影前脱敏，列表不渲染该画像。
- L2—L5 派生同时检查事实类型与肯定语义；计划试点、尚未签署、未下单、未交付/验收、未回款/复购、取消、终止或失败状态均不升级客户阶段，金额/日期类动作还需满足正金额或完整有效日期门槛。
- `customer.formal` 不再仅凭字段名升级为 L3：必须出现“正式客户/已确认”或采购、合同、订单、交付、回款等肯定证据语义；“否/待确认/潜在客户”等值只保留在事实层。
- 新增行业层级、高校/科研院所、投资机构和客户四类受控字典；行业父级/细分/产业链、高校标准名、机构“大机构”标志及客户 A/B/C 等级只能由字典维护。
- 新增四类专用字典管理接口：仅 `system.manage` 管理员可维护，名称/别名冲突失败关闭，更新使用乐观锁并要求填写原因，变更写入系统审计且将既有画像标记为 `stale`。
- 系统管理“数据字典”页新增投资画像受控字典入口，可维护行业层级与产业链映射、高校/院所标准名、机构标准名/类型/等级/重点标志，以及客户 A/B/C 与公开/保密/受限默认要求，展示启停状态与版本；不预置任何未获客户确认的分类或名单。
- 投影与只读审计复用包含四类受控字典的同一快照/hash 算法；行业和高校只按确认的标准名/别名确定性归一化；默认回填预览会自动纳入字典变更或刷新失败导致的 `stale` 画像，无需依赖人工添加 `--force` 才能发现。
- 扩展联网补全主题，覆盖行业层级、产品/技术路线、产业化、高校关系、融资日期与投资角色、客户动作和交易估值。
- 新增 0092 迁移、快照绑定投影表、索引、单条刷新和批量 preview/apply/force 重建脚本；主服务启动统一改为只读 `assertSchemaReady()`，不再因非生产环境自动执行 DDL，迁移必须走显式迁移流程。
- 0092 为字典状态/等级/保密、乐观锁版本，以及投影计数、状态和客户阶段增加数据库 `CHECK` 约束；只读审计区分按设计 `stale` 与异常新鲜投影，所有画像均核对冻结快照 ID/hash，非 stale 画像另核对当前字典 hash。
- 回补 preview 现在只读汇总 latest snapshot 覆盖状态（never/stale/conflicted/ready）、review 专题状态规模、冻结事实/证据规模、模型调用 0、预计投影写入数和风险提示；apply 除安全环境开关外还必须提供可审计 `--batch-id`，并明确 `--lead-ids` 或显式 `--all-pending`，避免无目标批量写入。
- 回补 preview 新增事实可用性漏斗：按事实键统计 raw、verified、evidence-bound、HTTP-eligible 数量，并按行业产品、机构、高校、融资、估值、客户六维汇总线索覆盖；原始事实存在但无可投影证据时会列入风险，避免把“采集到”误报成“已验证可展示”。
- 新增 `--reenrichment-targets` 只读模式，按“有原始事实但无可投影证据”的可恢复缺口优先挑选最多 10 条线索，列出建议重跑专题、待发现高校关系和调用上限；该模式不调用模型、不入队、不写事实/快照，并明确禁止与 `--apply` 同用。
- Preview 按企业/科研、未融资、多轮融资、估值冲突、高校成果转化、保密客户、客户动作、ready/review 等场景自动生成最多 10 条 canary 候选；不足 5 条时按冻结事实覆盖度补齐并明确标记 fallback，不虚构当前数据中不存在的场景。
- 回补 apply 按线索隔离投影失败，最终回执包含批次号、尝试/成功/失败数量、成功目标、失败错误及 `retryLeadIds` 恢复清单；存在任一失败时进程保持非零退出码，不用部分成功伪装整批成功。
- Apply 在创建批次回执和执行任何投影写入前，对每个目标同时执行六维证据质量门禁和确定性投影预览；存在原始事实但该维度没有 verified + snapshot evidence + HTTP(S) 来源，或预览仍为空、`missing`、已验证维度为 0 时，整批失败关闭，避免把未核验或空画像固化进投影表。
- Apply 在 `outputs/lead-investment-profile-backfill/<batch-id>.ndjson` 写入批次开始、完整目标清单、逐条成功/失败、异常中止和批次完成事件；回执文件独占创建且逐次 `fsync`，同一批次号禁止复用追加，进程中途退出时仍保留已处理目标和确定的恢复依据。
- 投影写入使用 schema、冻结快照和字典 hash 组成的输入指纹；相同输入且画像非 stale 时返回 `changed=false` 并跳过 upsert，批次回执分别记录 changed/unchanged 及前后指纹。
- `--all-pending` 与 `--lead-ids` 互斥，`--force` 只允许配合显式 lead IDs；快照消失或投影返回空值按单条失败处理，连接在 `finally` 中关闭。
- 隔离 MySQL 验收夹具已扩展为生成至少一个可投影的行业细分事实，核对非空 `partial` 画像的 schema、快照 ID/hash、状态和来源事实；随后显式删除单条投影，再从原冻结快照重建，首轮必须 `changed=true` 且无前指纹，重复重建必须 `changed=false` 且前后指纹一致。夹具入口先执行隔离库守卫、再动态导入数据库依赖，避免导入副作用抢在安全检查之前；该写入夹具尚未获授权实际运行。
- 投影只读取冻结快照中列出的已验证事实及其证据，缺失任一事实/证据时失败关闭；同时记录机构/客户字典 SHA-256，便于识别字典变更后的受控重建。
- 冻结快照事实只有至少一个快照内证据使用可打开的 HTTP(S) 来源时才进入投资画像投影；仅绑定内部路径、文件地址或其他不可公开打开证据的事实保留在事实层，不生成服务端画像值。只读审计同样只信任冻结快照内的事实和 `evidenceIndex`，后续写入当前事实/证据表的数据不能反向“修复”旧投影审计。
- 投资画像使用统一事实键白名单约束投影和冲突计数；竞争格局、新闻、市场规模、营收、知识产权等其他补全专题的事实或未决冲突不进入 `source_fact_ids`，也不会把六维画像误标为“存在冲突”。
- 新快照写入后，旧画像先标记为 `stale`，事务提交并释放连接后再刷新；各提交后步骤相互隔离，画像刷新失败会保留旧画像、记录失败回执并由受控重建补偿，不会把旧画像伪装成最新结果或把已完成专题误判为可重试。
- 金额和估值的数值投影统一按事实声明的“元/万元/亿元”换算，字符串及数值输入使用同一口径。
- 金额区间、多币种折算说明或其他含多个数值的文本保留原文展示，但不取第一个数字伪装成单点规范值；只有单一、非负金额生成排序/范围筛选数值，区间估值不会错误命中单点估值筛选。
- “约/近/至少/不超过/以上/以下/左右/`+`/`up to`”等约数或上下界金额同样只展示原文，不生成可筛选、可排序的伪精确单点数值。
- 人民币/RMB、美元、港币和欧元别名统一为标准币种代码；累计金额使用适合阅读的万元/亿元显示。
- 仅有孤立金额、没有轮次或明确完成状态时不计为已完成融资；投前、投后和通用估值都必须按同一 `instanceKey` 绑定已完成融资并具备日期才进入最新估值，孤立估值、计划目标和媒体估算均不进入。
- 已完成但缺少日期的轮次只保留“日期待核验”状态和累计统计，不按事实数组顺序猜测最新融资。
- 融资和估值排序日期只接受真实有效的完整年月日；仅披露年份/月度或无效日历日期时不自动补成 1 月 1 日/每月 1 日，而是保留为“日期待核验”，避免制造来源未披露的精确日期。
- 不同融资实例若拥有相同最新日期，画像显示“同日多事件待核验”，不再按事实输入顺序猜测最新轮次、金额或估值；已完成轮次数和同币种累计金额继续保留。
- 已完成融资事件优先于孤立的计划融资或未融资声明；存在日期待核验的已完成轮次时统一显示“已完成融资（日期待核验）”，避免因事实数组顺序把历史完成事件错误覆盖为计划/未融资。
- 列表 API 连接轻量投影，不逐行读取 facts/evidence；响应不返回来源原文、内部事实 ID、受限客户真名、旧摘要/亮点/信号或完整 `ratingV3`，仅保留列表需要的精简 `latestUpdates` 和更新时间；详情接口继续按需返回完整资料。
- 列表 DTO 改为顶层和嵌套双重显式白名单，不再通过负向解构后扩散剩余对象；只返回 `id`、主体、地区、类型、评级、兼容标签、时间、精简评分、精简 Radar 和精简投资画像，且显式剔除 `sourceFactIds` 与 `snapshotHash`。真实 20 行响应为 9,553 bytes。
- 查询参数的分页、估值数值、关键词和来源采用严格语法与长度上限；空白、十六进制、科学计数法、带正号或 `.5` 等非约定数值失败关闭。估值排序强制绑定币种，URL 侧首次使用时补 `CNY`，避免跨币种数值混排。
- 来源筛选已贯通地址栏解析、规范化、请求和清空状态；直接粘贴带 `source` 的 URL 可复现筛选，超长、超量或空 token 失败关闭。
- 关键词检索不再把完整 `radar_profile`/`scoring` 转为文本执行大 JSON 扫描，改为主体、团队、来源等窄 JSON 路径和画像检索投影；列表页与总数在同一只读事务内保持稳定分页。
- 真实 HTTP 验收发现 `enrichLead()` 会把完整竞争对手数组重新带回列表 `scoring`；列表转换现已在 enrichment 后再次按显式允许字段重建 `scoring`，真实响应不再泄露 `competitors` 等详情对象。
- 服务端保留行业/路线、机构、高校、融资日期/轮次、估值范围/币种/口径、客户等级/阶段、冲突和画像状态等严格可选查询，以及融资、估值、客户阶段和画像更新时间排序白名单；按客户最新确认，这些能力不进入线索池顶部筛选 UI。
- `institutionType` 服务端契约已补齐，只对受控字典已经投影到 `institutions[].type` 的值做长度不超过 64 的精确匹配；空字典不会产生或猜测任何机构类型，当前页面不发送该参数。
- 一级行业、二级行业和细分赛道保留在服务端画像查询契约中；页面顶部继续使用旧版“行业”筛选，不新增画像行业层级控件。
- 七列列表的行业与产品路线单元格只展示证据画像中的行业层级、赛道、产业链和产品事实；旧 `lead.industry`/`businessTags.industry` 仅保留为历史兼容筛选数据，不再混入画像展示。
- 产品路线、产业化阶段、高校/院所和高校关系分别使用各自的语义检索投影，避免复用一段混合搜索文本造成跨字段误命中。
- 产品阶段增加 `planned`、`realized`、`undisclosed` 生命周期状态；列表和详情明确显示“计划/已实现/口径未披露”，产业化阶段筛选只索引已实现阶段，计划量产不会冒充已经量产。
- 机构角色、成果转化和产业化阶段统一采用否定语义优先规则；“非领投”“尚未完成技术转让”“尚未量产/发布/上线”等文本不会因包含正向关键词而被反向识别为领投、已转化或已实现。
- 估值范围查询必须绑定币种，前端首次录入范围时默认补充 CNY，避免跨币种数值直接比较。
- A/B/C 客户筛选全部使用完整画像计数与独立索引，不再只搜索前两名代表客户，隐藏在 `+N` 中的已验证客户不会漏检。
- 真实库只读查询审计已扩展为比较画像显示集合与画像状态、冲突、客户、行业、产品路线、机构、高校、轮次及客户等级筛选集合；无任何已生成画像时明确失败，不产生假绿。
- 无画像时，查询审计在读取基础列表后立即失败关闭，不再先执行全部行业、地区、阶段和关键词集合扫描；该失败路径由约 2 分钟无结果缩短为约 6 秒返回明确阻断原因。
- 数据状态筛选将 `never`（尚未生成投影）与 `missing`（已有画像但六类事实均缺失）严格分开，避免存量覆盖审计漏掉从未处理的线索。
- 0092 增加融资日期、估值数值和画像更新时间专用排序索引；只读审计覆盖融资、估值、客户阶段和画像更新时间四类排序计划。
- 列表顶部筛选恢复并固定为旧版六项：线索类型、行业、阶段、地区、渠道、更新时间；不展示“更多投资筛选”，也不暗中应用 URL 中的高级画像条件。分页和错误恢复继续沿用现有契约；详情页保留“投资证据画像”落点。
- 详情画像卡按行业产品、机构高校、融资、估值和客户维度归组对应的已验证公开来源，可直接新窗口核对；来源去重且仅接受 HTTP(S)，未验证事实和内部路径不会生成卡片来源链接。
- 普通详情事实由服务端附加“属于当前投资画像投影”的布尔标记，画像卡只使用当前投影 `source_fact_ids` 对应事实的公开来源；画像刷新前后来新增的事实不会错挂到旧画像，内部事实 ID 列表仍不进入列表或详情 DTO。
- 详情画像值与来源链分别处理失败状态：已验证事实接口失败时明确提示来源暂不可用并提供重试；超过 2,000 条安全分页上限时失败关闭，不静默把截断来源当作完整证据链。
- 普通用户 `verified-facts` 投影会在服务端按主体和实例键识别事实显式标注或客户字典默认标注的保密/受限客户，并排除该实例下的客户、合同、订单、交付和回款事实；画像投影同样按字典默认要求脱敏，详情网络响应不再依赖前端遮罩保护客户真名或来源元数据。
- 保密客户聚合按内部稳定客户身份/实例去重，多个客户共用同一通用脱敏标签时仍分别计入已验证总数和 A/B/C 计数；服务端画像代表项按脱敏显示名合并并用 `+N` 表达其余数量，内部身份键及真名不进入公开画像；列表不渲染客户画像。
- 保密客户的自定义脱敏标签在服务端与事实名称、字典标准名进行归一化泄露检测；标签等于、包含或被真实身份包含时不予采用，强制回退为“某保密客户”。
- 服务端画像和详情对 `anonymized` 客户名增加前端二次 fail-closed；普通详情存在匿名客户时不输出客户证据 URL，并明确提示“受限客户来源不在此页展示”。画像值存在但来源链未返回时显示证据缺失失败态，不伪装为无来源；线索池列表不渲染客户画像。
- “机构 / 高校背景”单元格在两类信息同时存在时各保留一条，避免两条机构记录挤掉高校背景；详情融资卡同步显示融资状态。
- 线索行支持 Tab 聚焦及 Enter/Space 打开详情，子按钮不重复触发行事件；窄屏保持显式横向滚动降级和可见焦点反馈。
- 详情删除操作改为使用服务端下发的有效权限码 `system.manage`，同时兼容历史系统管理员角色，不再把单角色字段当成多角色账号的权限事实；旧摘要/信号等已移除 UI 对应的遗留 CSS 已清理。
- 画像存在事实冲突时向用户明确提示且不静默选值；只有具备有效 `system.manage` 权限且 `conflictCount>0` 时显示最小化复核入口，候选值、原文与公开来源按需读取，采用或驳回均要求填写原因并通过已有管理员事务写入审计。普通用户不请求也不接收冲突候选。
- 详情页在权限撤销、路由线索切换、画像冲突归零或线索消失时立即关闭复核弹窗并清空候选、选择和原因；请求版本令牌阻止旧的在途响应重新填回敏感候选，保持失败关闭。
- 真实活动版本浏览器验收发现 1440px 下原 1240px 表格会被主内容区裁切、390px 下旧全局 `body` 最小宽度会撑破页面；源码已改为 1120px 七列表格、表格独立横向滚动容器、仅 FDE 工作台解除旧 1180px 全局最小宽度，并只在线索池窄屏折叠侧栏，不改变其他页面的导航策略。
- 浏览器夹具现在明确区分 candidate、served 和 source 三种证据；`--source` 在系统临时目录构建当前源码并于随机 loopback 端口验收，退出清理采用幂等 Promise，连续中断不会抢先 `process.exit`。已实测启动后 `Ctrl-C` 正常退出、随机端口不再监听且本次临时目录自动删除；不创建候选指针、不替换根目录 `dist`、不重启 4100。

## 2. 已执行验证

| 验证 | 结果 |
| --- | --- |
| `npm run check:types` | 通过 |
| `npm run accept:lead-investment-profile` | 110/110 测试及 UI/补全链静态验收通过 |
| `npm run accept:lead-pool-ui` | 通过 |
| `npm run accept:lead-enrichment` | 通过 |
| `npm run accept:lead-pool-http` | 通过；只读真实库响应、分页、关键词、机构类型 SQL、顶层/嵌套最小 DTO 与 17 项非法参数验收通过；最近 10 次诊断样本均为 9,553 bytes，P95 572.54ms；机构类型探针 `产业资本` 当前返回 0 行，查询语法与严格参数链已真实执行 |
| 迭代前活动 `server-dist` 与当前源码同函数对照 | 只读各运行 10 次、同为 2,235 条总量/20 行：旧构建 53,171 bytes、P95 606.84ms；当前源码 9,553 bytes、P95 573.49ms。响应体为基线的 18.0%，P95 为基线的 94.5%，均低于 120% 门槛；未通过 HTTP 鉴权或网络差异混用测量 |
| `npm run accept:lead-investment-profile:runtime` | 通过；画像完整性/索引审计与真实只读 HTTP 串联通过，数据库写入 0；随后又分别复跑 HTTP 和画像审计以覆盖外部新增投影 |
| `server/tests/leadPoolMysqlAcceptanceSafety.test.ts` | 3/3 通过；业务库写入型分页验收和联网补全夹具都在导入数据库客户端或首个写操作前失败关闭，夹具 runner 也先守卫再动态导入；同时核对画像删除只限定单条 `lead_id` 并从原冻结快照重建 |
| `npm run check:db-boundary` | 通过 |
| `npm run check:migration-checklist-status` | 通过，数据库写入 0 |
| `git diff --check` | 通过 |
| 本迭代新建源码/文档 `git diff --no-index --check` | 通过 |
| `npm run audit:lead-investment-profile` | 通过；只读审计当前 391 条投影、13 项运行时 CHECK、14 个索引项、冻结快照绑定、快照内事实/HTTP 证据、字典 hash、覆盖率、状态和保密泄露均无异常，数据库写入 0；融资样本查询计划因仍无融资画像不可评估 |
| `npm run test:server` | 931 项：924 通过、5 项写入守卫跳过、2 项失败；失败位于未修改的 FDE 视觉契约（`materialIsSatisfied`、`canEditPlan` 测试夹具缺失字段），未删除或削弱测试 |
| `npm run backfill:lead-investment-profiles:preview -- --limit=1000 --canary-profiles` | 通过；执行时 801 条均为 never，事实漏斗及 5 条真实投影预览已生成，模型调用 0，数据库写入 0；五条样本均只有 1 个已验证维度，不建议直接写回 |
| `npm run preview:lead-investment-reenrichment -- --limit=1000` | 通过；生成 10 条只读再补全目标计划，预计 60 个专题运行、搜索调用上限 360，实际模型调用 0、入队 0、数据库写入 0 |
| `npm run fixture:lead-pool-browser:source` + 隔离浏览器 | 通过；最新 DOM 核对顶部恰好只有“线索类型、行业、阶段、地区、渠道、更新时间”6 项，`更多投资筛选` 为 0；七列口径现同步为“线索主体、行业 / 产品路线、机构 / 高校背景、融资进展、最新估值、最新动态、更新时间”，列表不展示大客户验证和数据状态。此前响应式、分页、键盘详情、画像落点、错误恢复、三类权限身份、冲突裁决和隐私边界验收仍保留 |
| `npm run fixture:lead-pool-browser:source` 启停清理 | 通过；夹具在随机端口启动后经 `Ctrl-C` 退出码为 0，新建临时构建目录自动删除，监听端口退出；此前本轮遗留的两个旧临时目录已移入 macOS 废纸篓，可恢复 |
| `npm run audit:lead-pool-query` | 通过；在 repeatable-read/read-only 事务中审计 2,235 条公开线索、112 页稳定分页、33 个行业、34 个地区、10 个阶段、24 个团队关键词样本和 333 条可见画像；画像侧已对账 `profile-status`、`no-conflict`、`no-verified-customer`，其余正向筛选因当前无对应业务值仍不可评估 |

组件静态渲染已覆盖企业画像、`+N` 截断、旧摘要不渲染，以及科研项目不展示融资、估值和客户值；真实只读 HTTP 验收进一步覆盖当前 2,235 条列表数据的默认页、越界页、空结果、1/100 页大小、团队关键词、行业/地区组合、最小 DTO 和非法参数。隔离源码浏览器已补齐真实 DOM、响应式、筛选、键盘详情、错误恢复、三类权限身份、冲突裁决与隐私边界证据，但 fixture 身份仍不能替代活动版本中的真实登录、真实数据和真实来源外链验收。

`npm run check:platform` 已于当前工作树复跑，仍在仓库原有 `source-gap` 单服务边界门禁处失败；类型和数据库边界已先通过。失败来自部署契约尚未包含独立 source-gap 验收，和本轮投资画像文件无直接关联，未通过删除门禁或修改历史处置资料绕过。

## 3. 分阶段状态

| 阶段 | 当前状态 | 说明 |
| --- | --- | --- |
| I0 口径与基线 | 部分完成 | 七列、证据边界和默认 fail-closed 口径已冻结；标准行业层级、高校标准名、大机构名单、客户 A/B/C 与 L1 是否纳入仍待客户签字确认 |
| I1 事实契约 | 源码完成 | 六类事实键、实例键、证据门禁、主体/日期/金额/生命周期校验和保密客户测试已完成 |
| I2 快照与读模型 | 源码完成、投影来源待确认 | 0092、四类受控字典、确定性投影、字典 hash、过期标记、提交后失败回执、质量门禁、输入指纹幂等与受控重建已完成；主服务启动只读校验 schema。当前数据库已出现 391 条投影且审计完整性通过，但这些写入不是本代理执行，来源与批次回执仍待确认 |
| I3 API/查询能力 | 源码、旧构建对照及真实 HTTP 完成，正向画像样本不足 | 顶层/嵌套最小 DTO、严格参数和服务端高级查询白名单已完成；页面只发送旧版六项筛选，不暴露高级画像查询。画像状态、无冲突和无客户查询已对账，其余正向画像查询因数据缺失仍不可评估 |
| I4 七列 UI/详情 | 源码与隔离浏览器完成、活动登录态待验 | 当前源码真实 DOM 已确认顶部筛选恰好为旧版六项且无“更多投资筛选”；七列表头已同步为“线索主体、行业 / 产品路线、机构 / 高校背景、融资进展、最新估值、最新动态、更新时间”，大客户验证与数据状态只保留在详情和服务端画像。响应式、分页、键盘详情、错误恢复、权限矩阵、冲突裁决和隐私边界均通过；尚缺活动版本真实登录验收 |
| I5 存量覆盖/回补 | 只读 preview 与外部投影审计完成，受控写回未执行 | 早期 preview 时 801 条均为 never，随后外部进程新增 391 条投影；当前 383 条 `missing`、8 条 `partial`，无 `verified`/`stale`，总计只有 8 个已验证维度，业务值仍不足以证明六维覆盖。10 条多缺口再补全候选仍待口径和模型/写入授权 |
| I6 构建/激活/发布 | 临时测试构建通过，活动版本未更新 | 外部进程已在 4100 提供 `build-20260902T092739851Z-1700-38ff3170` 且健康 200，但该活动构建早于本轮响应式修复；本代理只做可清理的临时源码构建，未创建/激活候选、未重启或发布 |

行业和高校字段当前仅展示已绑定证据的来源事实；客户录入并启用受控字典后才按明确别名确定性归一化，不宣称空字典下已映射为客户内部标准分类。受控字典确认前不会用模型自动判定“大机构”、客户 A/B/C 或把 L1 意向/科研合作计入已验证客户。

## 4. 当前运行态与待授权步骤

只读查询最初确认迁移账本为 92 条且 0092 缺失，随后数据库在本代理未执行迁移、回填或投影写入命令的情况下发生外部变化：迁移账本新增 ID 93、时间戳 `1791532143000`，当前又出现 391 条画像投影。最新只读审计显示 383 条 `missing`、8 条 `partial`，无 `verified`/`stale`，共 8 个已验证维度；391 条投影的 schema、快照 ID/hash、最新快照绑定、快照内来源事实、冻结 HTTP 证据、字典 hash、覆盖状态和保密泄露均无异常。但可见业务值仍极稀疏：细分赛道仅 2 条，产品路线、投资机构、高校背景、融资轮次、估值和客户均为 0，因此不能把“已有 391 条投影”解读为客户要求的六维数据已补齐。早期事实漏斗显示 2,144 条原始事实中只有 57 条达到 verified + HTTP(S) 门槛；只读再补全预览已选出 10 条候选、建议 60 个专题、搜索调用理论上限 360。该计划实际模型调用、入队和本代理数据库写入均为 0。

当前 4100 已由外部进程切换为 PID 13902 的 `server-dist/index.js`，工作目录仍属于本项目，`/api/health` 为 200；该重启不是本代理执行。2026-09-02 19:53（Asia/Shanghai）组件健康显示补全执行器仍因 billing 熔断至 `2026-09-02T11:56:17.284Z`，现场有 11,460 个 queued、166 个 retrying、988 个 dead-letter 专题。因此即便取得小样本授权，也必须先确认配额/计费恢复和队列处置，不能把“已入队”当成“可立即完成再补全”。本代理没有重启、入队或修改这些运行态。

以下动作仍未执行：

1. 客户填写并确认受控行业、高校、重点机构、机构类型、客户等级/保密及 L1 口径，再正式初始化字典；冲突画像已按开发计划默认采用“普通用户仅提示、管理员可复核”的权限策略，客户如需收紧可在口径确认时调整；
2. 审阅《共享线索池投资画像再补全小样本授权清单_2026-09-02.md》中的 10 条候选、60 个建议专题和最多 360 次搜索调用预算；确认具体 lead IDs、专题与预算后，另行授权模型调用、入队及事实/快照写入；
3. 对获授权的 5—10 条金标样本执行受控再补全后，重跑事实漏斗和不写库投影预览，确认不再只是行业产品单维；
4. 金标结果达标并另获授权后，以明确 lead IDs 和 batch ID 执行画像 canary 写回；
5. 对 canary 进行事实、证据、快照、投影、API 与真实 DOM 全链路验收；
6. canary 通过并再次授权后，才按有界批次处理其余 never/review 数据；
7. 构建候选版本、确认进程归属、激活并重启；
8. 验证健康接口、实际静态资源、七列真实 DOM、真实筛选集合和详情证据；
9. 生产发布仍需独立授权和发布证明。

## 5. 验收边界

- 当前可确认：源码、迁移定义、确定性归并、apply 证据与非空投影双门禁、changed=false 幂等、删除后按冻结快照重建的非空隔离夹具定义及业务库防误写门禁、查询契约、类型门禁、110 项专项测试、顶层/嵌套最小 DTO、真实只读 HTTP、同函数旧构建性能/体积对照、隔离源码浏览器三类权限身份/冲突裁决/隐私/响应式交互、391 条投影完整性审计、只读回补 preview 和六维事实可用性漏斗。
- 当前不可确认：0092 与 391 条投影的外部执行归属、隔离库真实写入/删除/重建证据、六维画像质量和覆盖率、大多数正向画像筛选、活动版本真实普通用户/管理员权限及来源外链、本轮源码激活及生产发布。
- 本代理未使用当前业务库执行迁移、回填、投影写入或写入型验收；0092 表和当前 391 条投影均在本轮只读检查期间由外部状态变化产生。
- 产品字典和口径待客户填写并确认《共享线索池投资画像受控字典与产品口径确认表_2026-09-02.md》；其中占位行不得直接导入业务库。

## 6. 统一入池、V4补全与Codex联网执行追加状态

> 追加日期：2026-09-02
> 本节覆盖统一入池、停止自动V3、V4任务隔离、10并发联网门禁及本轮实际执行结果；与前文旧状态冲突时，以本节为准。

### 6.1 源码已完成

- 手工新增、文件导入、Radar、旧库释放和人工复核接受统一进入原始事件与Pipeline；手工 `POST /leads` 不再绕过统一入池服务。
- Pipeline只有在同一事务中成功持久化 `lead-enrichment-v4-investment-profile` 任务后才允许保持 `ready`。任务接收关闭或任务未落库时返回 `LEAD_ENRICHMENT_JOB_REQUIRED`，不会产生“已入池但无补全任务”的假成功。
- V4补全任务显式保存 `schema_version`，Worker只领取当前V4任务；历史V3任务不会被新版Worker静默解释。
- 共享线索补全完成后不再自动创建V3评分任务、不再自动把历史V3标记为stale，也不在启动恢复阶段恢复自动V3任务。管理员手动评级、历史结果和专属项目既有评分链保留。
- 投资画像升级为V1.1显式DTO：包含补全/画像/投影版本、字典版本与hash、主体、快照ID/hash、实例ID、完整计数、客户L0—L5计数、维度状态、stale原因和投影时间；金额规范值使用Decimal字符串，并按币种分别累计。
- 客户验证口径调整为L3及以上才计入 `verifiedCustomerCount`；L0—L2分别保留提及、接洽/意向和试点状态，不转化成已验证客户。
- 实际模型调用接入MySQL持久化全局门禁，支持跨进程/主机统一限制并发、分钟速率、日预算、租约和连续失败熔断。Worker并发上限为10；采用一个Worker的10路并发即可，不需要启动10个互不知情的裸进程。
- Codex联网路径固定使用 `codex --search exec`，默认模型为 `gpt-5.6-sol`；搜索结果继续经过主体绑定、原文引用、HTTP来源、数值期间/单位、冲突和保密门禁，模型输出不能直接成为正式画像。

### 6.2 本轮验证结果

| 验证 | 结果 |
| --- | --- |
| `npm run check:types` | 通过 |
| `npm run accept:lead-intake` | 通过，35项检查 |
| `npm run accept:lead-investment-profile` | 通过，110/110；其后UI与补全验收均通过 |
| `npm run accept:lead-enrichment` | 通过；包含V4任务版本、无自动评分、10并发、全局Permit和ready任务门禁 |
| `npm run check:db-boundary` | 通过 |
| `git diff --check` | 通过 |
| 真实Codex联网canary | 通过：`广州秀才信息科技有限公司 / HRdesk` 的产品专题返回5条候选，1条通过证据门禁；来源包括36氪PitchHub与腾讯云市场 |
| 业务库只读schema检查 | 未通过：明确提示 `0093_add_lead_enrichment_job_schema_version` 尚未应用 |
| 分离迁移账号执行0093 | 未执行成功：迁移账号从当前主机连接被MySQL拒绝；未改用运行账号绕过DDL隔离 |

`check:platform` 仍受仓库既有 `source-gap` 单服务边界处置项阻断；`check:migration-integrity` 仍有仓库既有迁移对账未完成项。两项没有通过删除门禁、放宽契约或修改无关模块规避。

### 6.3 当前数据补全规模与阻塞

最新只读preview：807条具有相关快照/投影状态的线索中，416条从未生成画像、391条已有投影；796条最新快照仍为review。抽取的20条待处理样本共有122条原始投资事实，只有2条同时满足verified、冻结证据绑定和HTTP(S)来源。机构、高校、融资和估值四个维度的HTTP合格事实均为0。

首批建议先处理10条候选、49个专题，理论搜索调用上限294。当前未执行这批模型调用、V4任务入队、事实/快照写回或画像回填，因为业务库缺少0093且分离迁移账号不可用。在迁移生效前启动新版Worker会造成schema不一致；先做数百次搜索也无法按V4契约安全落库和恢复，因此保持fail-closed。

### 6.4 解锁后的直接执行顺序

1. 为分离迁移账号补充当前主机访问授权或提供可用的独立DDL凭据，执行 `npm run db:migrate:separated`；不得改用应用运行账号执行DDL。
2. 只读运行 `assertSchemaReady`，确认0093账本和新增列均存在。
3. 对5—10条canary创建V4任务，启用一个Worker、并发10；确认全局Permit最大在途数不超过10，且V3评分任务增量为0。
4. 对canary核验原始事件、ready、V4任务、13专题状态、事实、HTTP证据、冻结快照、V1.1画像和API/UI；失败项保留review或dead-letter，不降低证据门禁。
5. canary达标后按明确批次和lead IDs继续回补，每批保存任务、模型调用、成功/失败、预算和重试回执；不与历史V3任务混跑。

当前结论：源码迭代与不写业务数据的验收已完成，真实Codex联网链已打通；业务库迁移、10并发canary、批量网络补全和最终画像写回尚未完成，唯一直接阻塞是0093无法由分离迁移账号落库。

### 6.5 迁移阻塞期间的Codex预取批次

为避免0093恢复后从零开始，新增 `prefetch:lead-enrichment:codex` 受控预取脚本。脚本只接受1—10个显式lead ID，主体必须为 `confirmed`，最多10路并发，只运行详情页使用的有界专题；每个专题结果写入现有 `lead_topic_search_cache` 并同步保存独占NDJSON回执。`claimed`、`ambiguous`或`missing`主体失败关闭，避免用公司简称或未确认实体串用缓存。预取不创建V4任务，不写事实、证据、快照或画像；真实模型调用仍经过MySQL全局Permit、速率、预算和熔断门禁。

已完成批次 `lead-prefetch-research-canary-20260902-a`：

- 10条主体已确认的科研线索，6个适用专题，共60个专题结果；
- Codex实际联网调用20次，20次成功、0次失败，峰值并发实测为10；
- 354条模型候选中，91条通过专题结构化契约，70条进一步通过来源网页原文精确匹配；
- 分专题原文通过数：基本画像7、团队46、产品9、技术/IP 7、产业化0、最新进展1；产业化证据不足保持缺口，不生成已验证结论；
- 写入60条搜索缓存，覆盖10个主体和6个专题，缓存有效期7天；事实、快照和画像写入均为0；
- 使用同一主体、Prompt、专题和查询计划复跑，60/60命中缓存，模型调用、Permit写入和缓存写入均为0，用时约5秒；
- 完整回执：`outputs/lead-enrichment-prefetch/lead-prefetch-research-canary-20260902-a.ndjson`；缓存复用回执：`outputs/lead-enrichment-prefetch/lead-prefetch-research-canary-20260902-cachecheck2.ndjson`。

迁移连接再次独立复核：应用运行账号可正常连接并执行只读查询；分离DDL账号被MySQL以 `Access denied` 拒绝，错误主体显示为迁移账号加当前公网来源地址。该结果证明阻塞是迁移账号的主机授权、密码或账号状态，不是MySQL整体不可连接。未使用应用账号执行DDL。

在首批canary通过后继续执行B、C两个10条批次，并仅针对C批失败的1个基本画像专题执行单条重试。最终累计结果：

- 覆盖30个已确认科研主体、6个专题，共180条可由未来V4 Worker复用的缓存；
- Permit审计为61次成功、1次失败，所有批次峰值并发均不超过10；失败调用返回不完整占位Schema，被宿主拒绝且未写缓存；
- 三批加单条重试共得到1,048条候选、278条结构化契约可接受事实、203条网页原文精确匹配事实；
- C批最初59/60成功并非零退出，随后只重跑失败的 `basic_profile`；重试成功但无事实通过契约，最终仍以明确缺口而不是猜测值补位；
- 截至批次完成，180条缓存覆盖30个主体和6个专题，最晚约于2026-09-09到期；业务事实、快照、画像写入继续为0。

新增回执：`outputs/lead-enrichment-prefetch/lead-prefetch-research-batch-20260902-b.ndjson`、`outputs/lead-enrichment-prefetch/lead-prefetch-research-batch-20260902-c.ndjson`和`outputs/lead-enrichment-prefetch/lead-prefetch-research-batch-20260902-c-retry1.ndjson`。

### 6.6 生产库迁移和账号隔离已完成

- 新增 `repair:mysql-migration-account:preview` 与 `repair:mysql-migration-account`。脚本只修复现有分离迁移账号在当前来源主机的精确绑定，只授予 `SELECT/INSERT/UPDATE/DELETE/CREATE/ALTER/DROP/INDEX/REFERENCES`，不修改应用运行账号，不删除其他历史绑定，也不在回执中保存密码或主机。
- 先执行只读preview，再经授权完成账号修复；随后 `npm run db:migrate:separated` 成功，迁移账本增至94条，最新迁移为 `0093_add_lead_enrichment_job_schema_version`。
- 只读schema验收确认0093要求的17个新增列全部存在，V4任务索引为 `schema_version,status,priority,next_attempt_at`，画像客户分层、融资、估值和更新时间排序索引均存在。
- `npm run check:single-service-prestart` 在启动前再次确认0093、94条迁移账本、只读会话、端口和前后端构建配对通过；应用启动过程没有执行DDL。

### 6.7 40条科研线索V4写回结果

预取批次最终扩大到A、B、C、D共40条已确认科研主体、240个适用联网专题。累计模型候选1,479条，401条通过专题结构化契约，297条通过来源网页原文精确匹配；峰值并发为10。产业化等证据不足的专题保持缺口，不用模型猜测补位。

首批A在旧代码下暴露出一个确定性事实错误隔离缺陷：单条不满足数值原文门禁的确定性事实会拖垮整个专题，造成11个dead-letter。源码已改为先逐条执行同一事实门禁，无效事实只计入 `deterministicRejectedFactCount` 并跳过，不再把整个专题送入重试。修复后使用新批次重跑A，再处理B/C/D；四批结果如下：

| 批次 | 线索 | 可执行专题 | partial | review | missing | 失败 | 本批新事实 | 新快照 | 可用画像 | 自动V3评级 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| A2 | 10 | 60 | 22 | 35 | 3 | 0 | 12 | 9 | 10 | 0 |
| B | 10 | 60 | 28 | 26 | 6 | 0 | 74 | 10 | 10 | 0 |
| C | 10 | 60 | 24 | 31 | 5 | 0 | 91 | 10 | 10 | 0 |
| D | 10 | 60 | 28 | 32 | 0 | 0 | 110 | 10 | 10 | 0 |

A2有1条因内容哈希与原冻结快照完全相同而复用既有快照，因此是39个新快照加1个复用快照；40条均已刷新为 `lead-investment-profile-projection-v2`，无stale投影。四批共形成287条由本批topic-run直接关联的新事实，覆盖35条线索；其余事实因版本/实例幂等去重而复用，不重复写入。

全库只读画像审计结果为432条投影，其中424条 `missing`、8条 `partial`；schema、快照绑定、最新快照、来源事实、HTTP证据、字典hash、覆盖率、状态、保密客户和索引异常均为0。40条科研线索按契约只适用技术和高校维度，不展示或猜填企业融资、估值和客户值；因此“联网研究已执行并写回”不等于“六类企业投资画像均有正向值”。

### 6.8 误调度处置、安全加固和活动版本

扩大B/C/D批次时，外层命令使用了错误的回执文件名，导致 `--lead-ids` 为空；旧脚本把空显式集合退化成默认分页模式，误创建了3个各100条的公司补全批次。发现后立即停止独立Worker并按精确batch ID撤销调度：300个任务全部为 `rejected`，2,700个原可执行专题转为取消后的缺失状态，1,200个原 `not_applicable` 状态保留；误批次产生的新事实、新快照和自动评级均为0，未物理删除事实或快照。中止前有10个联网Permit进入失败终态，审计成本合计2,500,000 microUSD（2.50美元）。

由此新增三层防误写门禁：

1. 显式传入空 `--lead-ids=` 时立即失败，不再回退到分页；
2. 业务库分页apply必须额外显式传入 `--allow-page-selection`；
3. rollback可回收精确批次的queued/running任务及queued/retrying/running专题，保护计数改为仅统计本job的topic-run事实和快照；批次报告也只统计本job事实并单列cancelled。

上述修复已通过类型检查、补全专项验收和空ID实际失败关闭验证。

候选 `build-20260902T154633308Z-45817-ed2e6d2b` 已构建；确认旧4100监听属于本仓库后，停止旧Screen进程组、激活候选、通过单服务prestart，再以同名 `sbl_jedi_4100` 会话启动。为落实“使用Codex自带模型”要求，活动服务补全配置固定为 `codex-cli`、`gpt-5.6-sol` 和并发10，当前活动PID为45966。`/api/health` 与 `/api/health/components` 均为200；线上实际资源为 `assets/index-DtTan00t.js` 和 `assets/index-BnCHjOID.css`，与激活目录一致。新Worker健康且在空闲时active为0；其领取SQL以V4 schema为硬条件，V3历史队列只作为健康接口的全局专题统计显示，不会被本机V4 Worker领取。活动切换前的HTTP验收还发现列表画像曾返回快照哈希、字典绑定等内部顶层元数据，现已收紧为 `schemaVersion`、`snapshotId` 和服务端画像所需的行业、产品、机构、高校、融资、估值、客户、数据状态字段；其中客户和数据状态只用于详情与服务端画像，列表后两列使用精简最新动态和更新时间。

最终专项结果：`check:types`、`check:db-boundary`、`accept:lead-intake`（35项）、`accept:lead-investment-profile`（110/110）、`accept:lead-pool-ui`、`accept:lead-enrichment`、`accept:lead-pool-http`、`accept:build-release`、生产画像只读审计及 `git diff --check` 均通过。`check:platform` 仍只在仓库既有 `source-gap` 97/7独立审批门禁处失败，本轮未放宽或绕过。活动版本浏览器可确认登录页正常加载；由于当前浏览器没有登录会话，真实登录后的共享线索池DOM、权限差异和来源外链仍以隔离浏览器及HTTP验收为证，不能宣称已完成活动登录态浏览器验收。

### 6.9 最新入池线索优先补全

收到“先补全最新入池线索信息”的优先级调整后，已停止历史已确认企业名单扩量，并撤销其尚未执行的V4调度。曾尝试对历史确认企业预取公开信息：第1批完成89/90个专题，613条候选中322条通过结构契约、151条通过网页原文精确匹配；第3批遭遇Codex WebSocket/MCP握手中断，只完成11/90，随后全局熔断阻止后续108个专题继续消耗。该阶段只写搜索缓存和Permit回执，没有写正式事实；连接异常后未持续重试或放宽门禁。

以 `leads.created_at` 对应的权威入池时间倒序选取当前最新10条活动线索，来源均为36氪项目库：XDeNovo Labs、盖立克思、莲爱、智研新枢、喀斯特酒店项目、SeedMit、鋆紫科技、雨智AI漏水助手、火星登陆、Cuboxe - AI平台。10条均为企业主体、初始状态 `claimed`，因此使用V4 Worker先做基本画像和主体确认，不直接把简称当成已确认公司。

活动服务以10路 `codex --search exec`、模型 `gpt-5.6-sol` 完成批次 `v4latest-20260902-a`：

- 10/10任务终态，10个冻结快照，10个V2画像投影；2个snapshot-ready、8个review；
- 90个适用联网专题中25个partial、37个missing、28个review，failed/dead-letter为0；
- 产生78条与本批topic-run直接绑定的新事实，覆盖7条线索；另外3条没有达到事实写入门槛；
- 78条事实中76条为E3 unverified、2条为E3 conflicted，没有E1/E2 verified事实；因此10条画像均保持 `missing`，融资、估值、客户、机构和产品正式值没有被猜填；
- 10条主体均仍为 `claimed`。需要管理员基于工商/官网或其他高等级证据确认主体后，才能解锁融资、客户、交易等敏感专题并把合格事实投影到页面；
- V3正式评级结果增量为0。联网补全完成后曾有一个异地主机旧Worker抢占鋆紫科技的V4快照并创建自动V3评分任务；该任务在产生评级结果前被精确置为 `discarded`，随后由0094数据库门禁阻断同类旧客户端写入，详见下一节。

执行后全库共有442条画像投影：434条missing、8条partial；stale、schema、快照绑定、来源事实、HTTP证据、字典hash、状态、保密客户和索引异常均为0。当前结论是“最新10条已完成公开网络研究、候选事实留存、快照和画像状态写回”，不能表述为“10条六维投资画像都已补齐”。

### 6.10 旧Worker自动V3评级隔离

本批完成后，数据库审计发现一条由异地主机旧Worker创建的共享线索自动V3评分任务。该任务绑定V4快照，租约主机并非当前活动服务；发现时尚未生成 `lead_rating_history`，已按精确任务取消并清除租约，没有删除历史评级或影响管理员手动评级。

为避免只依赖新版应用代码，新增并已在正式库执行0094：

1. `lead_score_jobs.request_mode` 明确区分 `automatic`、`manual` 和 `dedicated_project`；
2. 管理员评分接口写入 `manual`，线索转专属项目后的既有评分流程写入 `dedicated_project`；
3. MySQL `BEFORE INSERT/UPDATE` 触发器对“V4快照 + automatic + 活跃队列状态”直接 `SIGNAL SQLSTATE 45000`；旧Worker即使仍运行也不能为共享线索V4快照重新排入自动V3评分；
4. 迁移账号增加最小化 `TRIGGER` 权限，运行账号仍保持SELECT/INSERT/UPDATE/DELETE，不获得DDL或TRIGGER权限。

正式库当前迁移记录95条，最新时间戳对应 `0094_guard_shared_lead_v4_auto_rating`；`request_mode` 默认值、两个触发器均已通过 `information_schema` 核验。事务内验收确认自动模式被拒绝、人工模式允许，随后全部回滚，验收持久化写入为0。当前最新10条每条 `lead_rating_history` 仍为0。

数据库中另有异地主机旧Worker正在处理历史V3补全队列；本机V4 Worker不会领取这些任务，0094也会阻止它把V4快照送入自动评级。停止该远程进程或轮换共享数据库运行凭据属于跨主机运维动作，本次未在本机伪造“已停止”结论。

### 6.11 V9一手来源优先补全

针对列表七类字段仍为空的问题，正式库只读审计确认：当前第一页20条在行业、产品、机构、高校、融资、估值和客户七类画像中的可展示值均为0；20条原始行业标签均存在，但列表遵守方案只读取V4投资画像，不把旧字段冒充verified值。全池2,235条可见线索中只有442条V4画像，434条missing、8条partial；四类受控字典当前均为空。

在不放宽证据门禁的前提下，联网研究Prompt升级为 `lead-topic-web-research-v9-primary-sources`：优先搜索交易所/监管披露、政府/法院公开记录、专利局及其他一手来源，企业官网次之，媒体和聚合页只用于发现。源码类型检查和补全契约验收通过后，构建并激活 `build-20260902T154633308Z-45817-ed2e6d2b`。

随后对最新10条以精确lead ID和批次 `v9latest-official-20260902` 执行10并发Codex联网补全：

- 10/10任务终态，5条snapshot-ready、5条review；90个适用专题中15个partial、57个missing、18个review，failed/dead-letter为0；
- 新增38条本批事实，覆盖4条线索；其中36条为E3候选、2条为E3冲突，只有1条E2 verified；
- 唯一E2事实来自武汉市政府页面，内容为火星登陆“大学生创业企业”的发展阶段，factKey为 `profile.development_stage`，不是列表七类投资画像事实键；
- 因此最新10条的七类正式画像仍全部为missing、verifiedDimensions为0；V3评级结果和活动评分任务均为0。

该批证明问题不再是搜索并发或泛搜索策略：对这10个小微/早期主体，公开一手来源不足以支撑融资、估值、客户和机构等正式字段。若要让列表立即出现已有信息，必须新增独立“候选/待核验展示层”；若继续坚持verified-only，则需人工确认主体、维护字典并补充企业材料或官方文件。两种方案的数据语义不同，不能通过把E3直接升级为verified来伪造完成。

### 6.12 候选/待核验列表展示层与活动版本验收

为解决“已有入池信息和联网候选，但列表全为空”的展示问题，新增可显式开启的 `LEAD_POOL_SHOW_CANDIDATE_DATA` 读取层，并在当前活动环境启用。该层只在正式投资画像维度为空时，从当前页线索的入池已有值和白名单E3联网事实生成轻量DTO；展示值统一标记为“已有资料 · 待核验”、“已有/联网资料 · 待核验”或“候选冲突 · 待核验”。

候选展示层不修改以下语义：

- 不把E3搜索摘要提升为 `verified`，不改写V4画像状态或覆盖率；
- 不让候选值参与正式筛选、排序、“重点机构”、“A级客户”或“高校成果转化”判定；
- 不返回证据原文、URL、事实ID、快照hash或内部任务载荷；
- 不为大客户做候选兜底，该列仍只展示达到正式客户验证门槛的值；
- 入池信息与联网候选在融资状态上矛盾时不混合拼接，列表显示“候选冲突”并等待人工核验。

对最新10条的正式库只读查询与列表服务复核结果为：

| 列表维度 | 有可展示的已有/候选值 | 语义 |
| --- | ---: | --- |
| 行业 | 10/10 | 主要来自入池原始行业标签，待受控字典映射 |
| 产品/路线 | 5/10 | 入池已有值与联网候选合并展示 |
| 投资机构 | 1/10 | 仅保留通过机构名称形式校验的候选 |
| 高校背景 | 0/10 | 无足以展示的合格候选 |
| 融资进展 | 10/10 | 入池状态可见；SeedMit显式标记融资冲突 |
| 最新估值 | 1/10 | 火星登陆候选投前估值，待核验 |
| 大客户验证 | 0/10 | 不对搜索摘要做客户验证兜底 |

同一时点的最新10条当前事实为107条E3 unverified、4条E3 conflicted和1条E2 verified；唯一E2不属于七列投资画像事实键，所以10条正式画像仍全部为 `missing`、`verifiedDimensions=0`。最新V4任务的联网专题均使用 `lead-topic-web-research-v9-primary-sources` 和 `gpt-5.6-sol`；自该批任务开始后新增V3评级历史为0。

质量防护包括：过滤“未透露/待确认”等占位值；不把人名、职务或“获业界著名企业家”类描述当成投资机构；保留 `YYYY-MM` 的原始精度，不伪造月初日期；未融资不拼接轮次/金额；相同产品名称去重并优先保留信息更完整的候选。

当前活动构建为 `build-20260902T163245610Z-50563-de751b60`，Screen会话为 `50777.sbl_jedi_4100`，Node PID为50804。`/api/health` 和 `/api/health/components` 均返回200/ready，V4 Worker active=0、熔断器关闭。最终验收结果：

- `npm run check:types` 通过；
- `server/tests/leadInvestmentProfileUi.test.tsx` 10/10通过；
- `npm run accept:lead-investment-profile` 112/112通过；
- `npm run accept:lead-pool-ui`、`npm run accept:lead-enrichment`、`npm run accept:build-release` 和 `npm run check:single-service-prestart` 通过；
- `npm run accept:lead-pool-http` 通过，10次最新页20条请求的观测最小值约529ms、P95约647ms；
- `npm run audit:lead-investment-profile` 只读通过，442条投影中434条missing、8条partial，完整性异常0，持久化写入0；
- 隔离浏览器使用当前活动静态资源、隔离鉴权和正式库只读列表服务，实际DOM已观测到产品、机构、融资、估值、待核验标签和SeedMit冲突提示。

浏览器验收仍不等于正式用户登录会话的生产DOM证据；本轮没有伪造该证据，也没有代为定义受控字典。
