# 微信公众号融资与企业主体信息抽取优化开发方案

> 版本：V1.3
> 日期：2026-08-26
> 项目：SBL Jedi 共享线索库 / 专属项目
> 状态：P0 已实施并完成实际服务验收；P1 安全可评分线索已完成 Codex 重处理，原始候选实体治理待后续执行
> 配套文档：`微信公众号融资与企业主体信息抽取优化验收文档.md`
> 文档性质：本文件同时记录目标方案与 2026-08-26 实施结果；最终验收口径见配套验收文档。

## 实施结果摘要（2026-08-26）

- 已使用本机 Codex CLI `gpt-5.6-sol` 四路并发完成 101 条安全可评分微信公众号正式线索的重提取与 V3 重评分；未调用项目网关模型。
- 已增加源内融资事实硬门禁：完成态、连续原文引文、轮次和金额逐字匹配、投资方同引文约束；共写入 62 条 Codex 融资事实，验收错误为 0。
- 已将无法评级记录的顶层分数统一清零，并终止已处理记录的旧评分/丰富队列覆盖；最终遗漏和待处理失败均为 0。
- 已保留逐条写前快照、原始 Codex JSON、结果 JSONL、失败 JSONL、批次 ID、模型和抽取器版本，支持审计与定向恢复。
- 尚未形成正式线索的原始候选及多文章错误聚合记录不做盲目批量写入，后续先完成候选到实体的一对一拆分，再复用同一 Codex v3 流程。

- 已完成统一融资抽取、NFKC/轮次归一化、完成融资语境识别、金额/币种与领投方提取、原文证据和幂等键。
- 已完成事实值与核验状态分离，页面不再用“待核验”覆盖真实金额，并直接展示轮次、金额、领投方和原文入口。
- 已完成品牌、法定主体、品牌成立叙述、工商成立日期、官网和文章来源的语义分离；工商字段按字段展示直接来源。
- 已完成“李银川/银川团队”地区消歧，工商强证据已把诺因智能从错误的“宁夏”纠正为“广东”。
- 已转专属项目继续参与补全；项目空值/占位值可安全同步，人工有效值受保护，重复执行幂等。
- 已提供默认预览、显式 `--apply`、定向/批量范围、批次前后快照、条件式批次回滚和回滚冲突保护。
- 诺因智能线索与项目已定向修复；专项测试、全量服务端回归、隔离 MySQL 集成验收、构建、激活、重启、健康检查和真实浏览器验收通过。
- P1 未扩大执行：多转载同一融资事件的跨来源证据集合合并、全历史微信公众号小批次回填和持续质量指标，留作后续治理，不影响本次 P0 正确性。

### V1.2 补充修复：微信公众号候选的可靠同步交接

本轮进一步修复了“采集成功但没有稳定进入 Radar 同步/融资抽取”的运行时断点。修复后，当前系统内所有正式微信公众号入口均执行同一交接契约：

```text
微信公众号定时采集 / 失败重试 / 机构账号采集 / 手工采集 / 历史导入
  -> 写入 radar_candidates
  -> 持久化标记 radar-collect-sync 为立即到期
  -> syncOnly 同步（不重复抓取公共源）
  -> 融资事实抽取、主体审核、线索创建或合并
```

具体实现包括：

- 采集产生候选写入后，不再只依赖固定周期，而是持久化投递 `radar-collect-sync` 交接任务；进程重启后交接仍可继续。
- 交接任务使用 `syncOnly` 标记，只同步已写入候选，不再次执行公共数据源采集，避免重复采集和同步循环。
- 同进程内“已有同步正在运行”被折叠为幂等并发，不再累计失败并进入永久死信。
- 启动时自动恢复历史上由该良性并发错误形成的死信任务。
- Radar AI 审核决策幂等键绑定本次审核运行，复核键绑定触发决策，消除跨运行重试时的不可变输出冲突。
- 组件健康检查在任一启用的运行任务处于死信时返回不健康，避免死信存在但总健康仍显示正常。

实际重放结果：`radar-collect-sync` 运行 `91fd3e0e-697a-4614-a593-122d3e73cbe8` 成功，结果中的采集阶段为 `skipped=true / reason=wechat_candidate_handoff / candidateWrites=1`，任务死信数恢复为 0。诺因智能最终记录保留 `天使++轮`、`5亿元人民币`、`经纬创投`及微信原文证据；企业主体页已落库深圳诺因智能有限公司、官网、成立日期、注册资本、法定代表人、信用代码、登记状态、公司类型和注册地址及字段级来源。

## 0. 结论与优先级

本次问题不是微信公众号正文抓取失败，而是正文已经入库后，融资事实、企业主体和页面展示之间缺少一条稳定、可追溯的数据链路。

以“诺因智能”样本为例：

- 微信正文已抓取成功，库内正文包含“完成天使 ++ 轮融资，单笔金额 5 亿元人民币，本轮融资由经纬创投领投”。
- 融资金额曾进入 `funding_rounds`，但轮次仍为“待核验”，投资方字段被“经纬观点”等文章结构文本污染。
- 数据质量结果又生成“融资轮次待核验”“融资金额待核验”，页面优先展示了这些占位结论，覆盖了已存在的 `5 亿元`。
- 微信文章链接被作为 `officialSite` 的兜底值，导致资讯来源被误标为“公司官网”。
- 工商主体结构化字段仍为空，且已转为专属项目的线索被现有批处理条件排除。
- “银川团队”或人名“李银川”曾被错误解释为地名，形成“宁夏”污染。

因此建议按以下优先级开发：

1. **P0：融资事实抽取、证据绑定和页面展示纠偏。**
2. **P0：企业主体、官网、地区语义分离，阻止继续产生错误数据。**
3. **P0：已转专属项目仍可补全，并安全同步到项目。**
4. **P1：对历史微信公众号线索执行可预览、可审计、可重复运行的回填。**
5. **P1：增加质量指标、冲突队列和持续回归样本。**

## 1. 目标与完成定义

### 1.1 目标

建立一条与来源平台无关的事实处理链：

```text
原始文章
  -> 正文归一化
  -> 融资/主体候选事实抽取
  -> 原文证据定位与语境校验
  -> 企业实体解析
  -> 冲突裁决与置信度
  -> 结构化事实入库
  -> 线索/项目安全投影
  -> 页面按事实与证据状态展示
```

### 1.2 完成定义

只有同时满足以下条件，才可视为本次优化完成：

- 新进入系统的微信公众号文章可以抽取融资轮次、金额、投资方和对应原文证据。
- `天使 ++ 轮`、`天使++轮`、全角符号和异常空格等写法得到同一标准结果。
- 页面有真实金额时不再被“融资金额待核验”覆盖。
- 资讯来源链接与公司官网分字段展示，微信文章不得冒充公司官网。
- 企业简称、工商全称、品牌成立时间、工商注册时间不再混用。
- 人名或团队名中的“银川”不会直接推导出“宁夏”。
- 已转专属项目的线索仍能完成事实补全，并按字段优先级安全同步到项目。
- 诺因智能存量样本完成定向回填；重复执行不新增重复融资记录，不覆盖更高优先级人工数据。
- 自动化测试、专用测试库集成测试、页面验收和实际部署验收全部通过。

## 2. 当前基线与断点

### 2.1 已确认的样本基线

| 项目 | 当前状态 |
|---|---|
| 候选记录 | `wechat_api:78f0f36abf46521148717d4c235d633b` |
| 文章抓取状态 | `gsdata_content_ok` |
| 库内正文长度 | 1393 字符 |
| 线索 ID | `daf2a843-797c-44da-a13b-3eec6a4dd5b7` |
| 已转项目 ID | `6bb3ac47-192f-4964-9c71-90a854588aa6` |
| 正文融资事实 | 天使 ++ 轮、5 亿元人民币、经纬创投领投 |
| 当前融资结构 | 金额已有 `5 亿元`，轮次仍为待核验，投资方字段被正文栏目文本污染 |
| 当前页面状态 | 融资金额和轮次显示待核验；微信文章被显示为公司官网 |
| 当前工商结构 | `registry` 为空，补全证据为空 |
| 当前地区 | 错误显示宁夏，疑似来自“银川”语义误判和历史脏数据 |

### 2.2 主要断点

#### 断点 A：抓取结果没有进入统一事实抽取

`server/src/services/radarCollectorService.ts` 已能获取并保存正文，但后续研究证据构建仍可能产出空 `fundingRounds` 和空工商信息，正文只被当作普通研究文本使用。

#### 断点 B：融资抽取规则对来源做了过早限制

当前数据质量优化逻辑对金额可信来源设置了平台白名单。微信公众号即使原文存在明确金额，也可能在核验正文之前直接落为“融资金额待核验”。

#### 断点 C：轮次标准化没有先处理内部空格

`天使 ++ 轮` 未先归一化为 `天使++轮`，导致确定性规则无法命中，只能依赖后续模型，最终容易保留占位值。

#### 断点 D：展示优先级把占位值当成高优先级事实

`src/pages/LeadDetailPage.tsx` 优先读取数据质量结论；当结论是“融资金额待核验”时，仍会覆盖已有融资记录中的 `5 亿元`。

#### 断点 E：来源链接与官网字段混用

页面把 `radarProfile.link` 作为 `officialSite` 的兜底，导致微信公众号来源页被标记为公司官网。

#### 断点 F：工商补全不覆盖已转项目和品牌简称

现有批处理排除了 `pool_status='已转专属项目'`，部分脚本还要求输入名称具有法定企业后缀，因此“诺因智能”这类品牌名容易被跳过。

#### 断点 G：地区历史污染无法自动被更强证据纠正

历史资料中的“银川团队”可能被地名别名规则吸收为“宁夏”；当前中等置信度画像值又可能先于新证据返回，错误被持续保留。

## 3. 开发范围

### 3.1 本期纳入

- 微信公众号正文的融资候选事实抽取和原文证据绑定。
- 融资轮次、金额、币种、投资方和领投方标准化。
- 资讯语境、市场规模、订单金额、估值、募资目标与已完成融资的分类。
- 品牌名、工商主体、官网、注册地、业务所在地和文章来源的语义分离。
- 已转专属项目线索的补全及受控同步。
- 页面展示优先级和证据状态展示。
- 诺因智能定向回填及微信公众号历史记录批量回填工具。
- 单元、集成、回归、页面和部署验收。

### 3.2 本期不纳入

- 自动认定未被可靠来源支持的股权比例、估值或交易条款。
- 仅凭文章叙述的“成立于”覆盖工商登记成立日期。
- 无证据时自动生成统一社会信用代码、法定代表人或注册资本。
- 未经字段优先级判断，批量覆盖项目经理已人工维护的专属项目数据。
- 把微信公众号文章、媒体主页或搜索结果页认定为公司官网。

## 4. 核心设计原则

### 4.1 事实与核验状态分离

“5 亿元”是候选事实，“单一来源已标注”或“多源交叉核验”是证据状态。不得用“融资金额待核验”替换已经抽取到的事实值。

推荐显示：

```text
5 亿元人民币 · 原文已标注，待交叉核验
天使++轮 · 原文已标注
```

### 4.2 来源无关，证据有级别

是否抽取融资事实不应由“是不是 36 氪”决定。来源类型用于计算证据级别和是否需要交叉核验，不用于在看到原文之前丢弃事实。

### 4.3 法律事实与商业叙述分离

至少区分：

- `brandName`：品牌或项目简称。
- `legalName`：工商登记主体全称。
- `claimedFoundedAt`：官网、文章等叙述的品牌/团队成立时间。
- `registeredAt`：工商登记成立日期。
- `registeredAddress`：工商注册地址。
- `operatingRegion`：实际办公或经营地区。

不同语义的日期或地区允许同时存在，不应互相覆盖。

### 4.4 所有重要字段必须可回到证据

融资金额、轮次、投资方、工商主体、官网、注册地址等字段应保留：

- 来源 URL；
- 来源类型；
- 原文片段或结构化查询证据；
- 抽取方式和规则/模型版本；
- 抽取时间；
- 置信度和冲突状态。

### 4.5 回填必须幂等、可预览、可审计

任何历史修复先输出差异预览，再允许应用；重复运行不新增重复事实，所有覆盖或拒绝覆盖都应留下审计记录。

## 5. 目标数据契约

### 5.1 融资事实

建议为融资记录补齐或统一以下字段：

| 字段 | 说明 |
|---|---|
| `roundRaw` | 原文轮次，如“天使 ++ 轮” |
| `roundNormalized` | 标准轮次，如“天使++轮” |
| `amountRaw` | 原文金额，如“5 亿元人民币” |
| `amountValue` | 数值化金额，无法可靠数值化时允许为空 |
| `amountUnit` | 元、万元、亿元等 |
| `currency` | CNY、USD 等 |
| `amountDisplay` | 面向页面的规范文本 |
| `investors` | 去重后的投资机构数组 |
| `leadInvestors` | 领投方数组 |
| `eventDate` | 融资事件时间；无明确时间时为空 |
| `subjectName` | 融资主体名称 |
| `sourceUrl` | 事实来源页 |
| `evidenceQuote` | 支持该事实的最小充分原文片段 |
| `evidenceStatus` | 单源已标注、多源已核验、存在冲突、证据不足 |
| `extractionMethod` | rule、model、registry、manual |
| `extractorVersion` | 便于回放和回归 |

融资记录的幂等键建议由“主体 + 标准轮次 + 金额 + 事件时间窗口 + 来源事实指纹”构成，不得仅依赖文章 ID。

### 5.2 主体与企业信息

建议结构至少包括：

```text
entityIdentity
  brandName
  legalName
  aliases[]
  officialWebsite
  officialWebsiteEvidence
  claimedFoundedAt[]
  registry
    registeredAt
    registrationStatus
    legalRepresentative
    registeredCapital
    unifiedSocialCreditCode
    registeredAddress
    companyType
  operatingLocations[]
  conflicts[]
```

当仅有“诺因智能”而没有可靠工商证据时，页面应显示“主体名称：诺因智能”“工商全称：待核验”，不得把品牌名直接标成“公司全称”。

### 5.3 证据优先级

推荐优先级从高到低：

1. 人工确认且保留审计来源的数据。
2. 工商登记或政府公开信息。
3. 公司自有域名官网、正式公告或官方认证账号。
4. 投资机构官方公告。
5. 权威媒体原文。
6. 聚合平台、搜索摘要和二次转载。
7. 模型推断或无来源旧画像。

低优先级数据只能补空、形成候选或进入冲突队列，不得静默覆盖高优先级数据。

## 6. 详细开发任务

### 6.1 P0-A：正文归一化

新增统一文本预处理，供确定性规则和模型共同使用：

- Unicode NFKC 归一化。
- 全角/半角加号、连字符、币种符号统一。
- 删除轮次词内部无语义空格：`天使 ++ 轮` → `天使++轮`。
- 保留原始正文和字符偏移映射，避免证据片段无法回指。
- 统一人民币、美元及中文数量单位，但不得把估值或市场规模改写成融资金额。

### 6.2 P0-B：融资候选抽取与语境分类

建立独立的 `financingFactExtractor`，不要把融资抽取散落在页面、同步脚本和数据质量脚本中。

候选抽取至少覆盖：

- `完成/宣布/获得/获/交割/关闭 + 轮次 + 融资`；
- `金额 + 融资`、`单笔金额`、`融资规模`；
- `由 X 领投`、`X 参投/跟投/追加投资`；
- 同句、相邻句和段落标题之间的组合关系。

语境分类至少包含：

- 已完成融资；
- 拟融资/募资目标；
- 估值；
- 订单或合同金额；
- 市场规模；
- 累计融资；
- 历史融资；
- 无法判断。

只有“已完成融资”可以直接进入当前融资事实；其余类型必须分字段保存或进入人工复核。

### 6.3 P0-C：原文证据校验

每个候选事实在入库前执行：

- 金额、轮次或投资方必须能在证据片段中定位。
- 融资主体必须与线索主体或已解析别名一致。
- 证据片段不得只来自标题拼接、网页导航或评论区。
- 投资方抽取在“经纬观点”等栏目标题前截断，过滤“融资由经纬创投”等重复残片。
- 模型输出中的新增事实若无法回到原文，降级为无效候选。

### 6.4 P0-D：企业主体解析与联网补全

- 支持用品牌简称进入实体解析，不再要求输入名称必须含“有限公司”等后缀。
- 先生成主体候选，再使用官网、投资方公告、权威媒体和工商来源交叉验证。
- 官网必须通过公司名称、品牌、版权主体、备案信息或官方外链等至少一种所有权证据验证。
- 微信文章仅保存在 `sourceUrl/articleUrl`，不得写入 `officialWebsite`。
- “成立于 2025 年 8 月”和工商“成立日期 2025-06-23”应分别保存，并标记语义或来源冲突。
- 对法定代表人、注册资本、信用代码、登记状态等字段，坚持无可靠结构化证据不填。

### 6.5 P0-E：地区解析纠偏

- 人名、团队名、机构名中的行政区词不得直接作为地区证据。
- `李银川`、`银川团队`等命中需要进行实体类型判定。
- 仅正文一般叙述命中的地区不高于“低置信候选”。
- 工商注册地址、官网联系地址和明确“总部位于/公司位于”具有更高优先级。
- 允许更强的新证据纠正历史中等/低置信度画像，并记录旧值、原因和新证据。

### 6.6 P0-F：数据质量与页面展示

页面显示规则调整为：

1. 有结构化事实值时展示事实值。
2. 同时展示证据状态徽标。
3. 只有事实值为空时才展示“待核验”。
4. 存在冲突时展示已知值和“存在冲突”，而不是抹掉全部值。

具体要求：

- `5 亿元` 不得被 `融资金额待核验` 覆盖。
- `天使++轮` 可显示“原文已标注，待交叉核验”。
- 投资方列表去重，不显示“经纬观点”“融资由经纬创投”等残片。
- 公司官网与文章来源分行展示。
- 工商全称未确认时，不把品牌名冒充工商全称。
- 页面能查看每个重要事实的来源和证据摘要。

### 6.7 P0-G：线索与专属项目同步

已转专属项目的线索不能被补全任务排除。建议：

- 原始证据和事实统一记录在线索事实层。
- 项目字段作为投影，不重复生成另一套来源不明的事实。
- 新事实仅在项目字段为空、仍是系统占位值或旧值证据优先级更低时自动同步。
- 对人工编辑过的项目字段，只生成“建议更新”，不自动覆盖。
- 每次同步记录来源线索、事实 ID、旧值、新值、决策和操作者。
- 线索和项目之间的重复执行必须幂等。

### 6.8 P1-H：历史数据回填

新增专用回填入口，至少支持：

```text
--candidate-id
--lead-id
--project-id
--source=wechat
--from/--to
--limit
--preview
--apply
--resume-from
```

回填流程：

1. 读取历史原始正文，不重新抓取即可先回放。
2. 输出字段级差异、证据和覆盖决策。
3. 定向处理诺因智能样本。
4. 定向验收通过后，再按小批次处理历史微信公众号记录。
5. 批次保存检查点、成功数、跳过数、冲突数和失败原因。
6. 重复运行验证零重复、零非预期覆盖。

### 6.9 P1-I：指标和告警

建议增加：

- 正文抓取成功但融资事实为空的比例。
- 正文包含融资触发词但进入“全部待核验”的比例。
- 有事实值却被占位值覆盖的记录数。
- 来源页被识别为官网的异常数。
- 企业主体未解析率、冲突率和人工复核积压。
- 回填重复记录数和高优先级字段覆盖拦截数。

## 7. 代码改造建议

以下为建议影响范围，开发时需结合当前工作树逐项确认，避免覆盖已有未提交修改。

| 模块 | 建议改造 |
|---|---|
| `server/src/services/radarCollectorService.ts` | 抓取成功后触发统一事实抽取或写入待抽取队列 |
| `server/src/routes/meta.ts` | 研究证据不再固定返回空融资/工商结构；同步改用事实层 |
| `server/src/services/leadDataQualityService.ts` | 轮次归一化、占位值语义和证据状态分离 |
| `server/src/services/leadCompanyIntelExtractionService.ts` | 扩展主体、融资和证据契约，强化原文绑定 |
| `server/src/services/leadPublicIntelService.ts` | 按证据优先级合并工商、官网和地区数据 |
| `server/src/services/leadRegion.ts` | 人名/组织名消歧和历史污染纠偏 |
| `server/src/scripts/optimizeLeadDataQualityWithCodex.ts` | 移除过早来源白名单，改为来源分级 |
| `server/src/scripts/enrichLeadRegistryWithCodex.ts` | 支持品牌简称和已转项目；保留预览模式 |
| `server/src/scripts/enrichAllLeadCompaniesWithCodex.ts` | 补全范围包含已转项目，增加字段级覆盖策略 |
| `server/src/services/aiSummaryService.ts` | 项目转换/同步使用统一事实和审计规则 |
| `src/pages/LeadDetailPage.tsx` | 事实优先展示、证据徽标、官网与来源分离 |
| 新增回填脚本与测试 | 提供定向预览、批量回填、幂等和审计验证 |

## 8. 实施阶段

### 阶段 0：锁定契约与回归样本

- 固化诺因智能原文和当前错误表现为金标夹具。
- 固化融资字段、主体字段、证据状态和覆盖优先级。
- 先写失败测试，证明当前逻辑会漏抽或错显。

### 阶段 1：阻止新增错误

- 上线正文归一化、融资抽取、证据校验。
- 修正页面占位覆盖和官网兜底。
- 修正地区人名误判。
- 新记录全部走新契约。

### 阶段 2：打通主体与项目投影

- 支持品牌简称实体解析。
- 支持已转专属项目继续补全。
- 加入人工字段保护和同步审计。

### 阶段 3：定向回填

- 对诺因智能执行 `preview`。
- 人工确认字段级差异后执行 `apply`。
- 验证线索页、项目页、事实证据和重复执行结果。

### 阶段 4：历史批量回填与发布

- 按小批次回填微信公众号历史记录。
- 监控冲突率、失败率和重复率。
- 完成候选构建、激活、重启、健康检查和实际页面验证。

## 9. 测试策略

### 9.1 单元测试

- 轮次空格和符号归一化。
- 中文金额、币种和单位解析。
- 融资语境与市场规模/订单/估值区分。
- 投资方和领投方抽取、去重及栏目文本过滤。
- 品牌名与工商全称分离。
- 人名“李银川”和“银川团队”不产生宁夏地区。
- 有事实值时占位值不覆盖。
- 来源 URL 不得成为官网。

### 9.2 集成测试

- 使用专用测试数据库走完候选文章 → 线索事实 → 项目投影。
- 测试新线索、已转项目、人工字段已编辑、重复回放和证据冲突。
- 禁止对业务数据库执行写入型集成测试。

### 9.3 页面与端到端测试

- 线索页显示融资金额、轮次、领投方和证据状态。
- 企业信息页区分工商全称、品牌、官网和文章来源。
- 专属项目页显示经允许同步后的融资信息。
- 刷新、重新补全和重复回填后页面结果稳定。

### 9.4 发布检查

至少执行并保存结果：

```bash
npm run check:types
npm run test:server
npm run build
```

建议新增并纳入发布门禁：

```bash
npm run test:wechat-lead-extraction
npm run accept:wechat-lead-extraction
npm run backfill:wechat-lead-facts -- --lead-id=<id> --preview
```

上述新增命令在实现前仅是目标接口，不代表当前仓库已经存在。

## 10. 发布、回滚与数据安全

- 数据结构优先采用向前兼容的新增字段，不直接删除旧字段。
- 新抽取器先通过开关或按来源灰度，只处理新增微信公众号记录。
- 历史回填默认 `preview`，必须显式 `--apply` 才写入。
- 回填前保存批次清单和字段快照；支持按批次恢复旧投影值。
- 发生异常时可以关闭新抽取和同步，但保留已写入的原始证据和审计记录。
- 发布不能只以测试通过或进程存活为准，还需确认最新构建已激活、服务已重启、健康接口正常、实际页面加载的是新资源。

## 11. 风险与控制

| 风险 | 控制措施 |
|---|---|
| 模型编造融资事实 | 所有关键值必须能回指原文；无法回指即拒绝入库 |
| 媒体栏目污染投资方 | 最小证据片段、栏目边界识别、机构名去重与黑名单 |
| 历史回填覆盖人工数据 | 字段来源优先级、人工锁、差异预览和审计日志 |
| 同一融资多篇转载造成重复 | 主体、轮次、金额、时间窗口和事实指纹联合去重 |
| 品牌名绑定错主体 | 多证据实体解析；低置信度进入人工复核 |
| 官网误认 | 域名所有权证据校验，媒体和平台域名明确排除 |
| 地区由人名误判 | 命名实体类型判断，正文弱证据不得覆盖注册地址 |
| 已转项目线索状态分叉 | 统一事实层，项目只做受控投影并记录来源 |

## 12. 开发交付物

开发完成时应交付：

- 统一融资事实抽取与证据绑定实现。
- 企业主体、官网和地区解析改造。
- 线索/项目投影及字段覆盖审计。
- 页面展示改造。
- 定向和批量历史回填工具。
- 金标夹具、单元测试、集成测试和 E2E 测试。
- 诺因智能修复前后差异、回填批次报告和页面截图。
- 构建、部署、健康检查和实际服务验收记录。

最终是否通过，以配套验收文档全部 P0 条目通过为准。
