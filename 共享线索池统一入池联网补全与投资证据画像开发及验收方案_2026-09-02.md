# 共享线索池统一入池、联网补全与投资证据画像开发及验收方案

> 文档版本：V1.5
> 编制日期：2026-09-02
> 适用项目：`/Users/hyw/Desktop/sbl_jedi`
> 适用分支：当前 `main` 工作树
> 当前状态：源码、0092至0094正式库迁移、40条科研线索、最新10条企业线索V4补全及V9一手来源优先复核、画像写回、旧Worker自动V3数据库隔离、候选/待核验列表展示层和活动版本切换已完成；受控字典业务口径、最新企业主体人工确认、正式登录会话的活动页面DOM验收及更大范围回补仍待后续确认
> 本文性质：开发与验收基线；实际执行证据、批次结果、异常处置和未完成边界见《共享线索池投资证据画像迭代实现报告_2026-09-02.md》
> V1.1修订：补充主体匹配审计、金额规范化、客户计数、证据时间、字典绑定、明确DTO、字段全链路矩阵及对应验收项
> V1.2执行更新：记录0093已迁移、V4金丝雀与扩大批次已写回、无自动V3评级、候选已激活及活动登录态仍待验的边界
> V1.3执行更新：记录最新10条优先补全结果、0094 `request_mode` 与数据库触发器门禁，以及异地主机历史V3 Worker仍需单独运维处置的边界
> V1.4执行更新：记录V9一手来源优先Prompt、最新10条10并发复核及“官方证据不足仍不得伪造verified字段”的数据质量结论
> V1.5执行更新：新增候选/待核验展示层，将入池已有值与E3联网候选以来源标签和冲突标记显式展示，不改变正式画像、筛选、verified覆盖率或V3评级语义

## 1. 文档结论

本方案将共享线索池主链路调整为：

```text
多来源项目进入系统
  → 原始事件留档与幂等校验
  → 主体识别、工商准入、实体匹配和人工复核
  → 正式进入共享线索池
  → 自动创建投资画像联网补全任务
  → AI按专题发现公开来源并提取候选事实
  → 宿主重新抓取、验证原文、主体、时间、单位和业务口径
  → 事实、证据和冲突版本化入库
  → 冻结不可变补全快照
  → 生成确定性投资证据画像投影
  → 列表、筛选、排序和详情证据展示
  → 管理员可按需触发V3评级，但不再自动执行
```

本方案确认以下产品原则：

1. 共享线索池以投资证据画像和数据验证状态作为主要筛选依据。
2. 数据完整度只表达“掌握了多少可信信息”，不转换成投资分数。
3. V3评级退出自动主链路；历史评级只读保留，重点线索可由管理员按需评级。
4. 所有入池入口统一经过原始事件、主体准入、实体匹配和Pipeline状态迁移。
5. AI只负责搜索和候选提取，不能直接把搜索结果写成verified事实。
6. 列表展示和筛选必须使用同一份可重建投资画像投影。
7. 未经授权不得迁移业务库、批量回补、重启、激活或发布生产。

## 2. 与已有文档的关系

本方案基于并调整以下已有文档：

- `共享线索池投资证据画像开发与验收计划_2026-09-02.md`；
- `共享线索库联网补全与V3评分迭代开发方案.md`；
- `共享线索库联网补全与V3评分验收文档.md`；
- `共享线索池查询与分页缺陷修复及验收计划_2026-09-02.md`；
- `共享线索池投资证据画像迭代实现报告_2026-09-02.md`。

如旧文档与本方案冲突，以本方案确认的新产品决策为准：

- 不再要求补全快照自动触发V3评级；
- 不再要求新事实自动使旧评级过期并重评；
- 普通列表不再展示评级徽标或提供评级分数排序；
- 投资画像生成不依赖评级成功；
- 13个后台研究专题不等于13个前台区块，也不要求每条新线索同步执行全部13个专题。

旧V3文档仍可作为历史事实模型、证据门禁、主体建模和管理员按需评级的技术参考，不得作为自动评级继续存在的依据。

## 3. 当前实现基线与差距

### 3.1 已有能力

- 已有 `lead_pipeline_raw_events`、Pipeline状态迁移、人工复核和实体匹配基础。
- Radar、36氪、BP、批量导入和公开情报等入口已具备部分正式入池链路。
- Pipeline进入 `ready` 时可以自动创建补全任务。
- `lead-enrichment-v3` 已定义13个后台专题和事实契约。
- 已有主体、事实、原始文档、证据、冲突和不可变快照模型。
- 已有联网搜索、宿主抓取、quote匹配、主体匹配和字段校验。
- 已有补全任务租约、重试、死信、熔断、预算和审计能力。
- 当前工作树已有 `lead-investment-profile-v1`、0092迁移、投影服务、列表查询和七列UI相关未提交实现。
- 当前详情页已隐藏普通用户的V3评级操作区，管理员评分接口仍存在。

### 3.2 当前关键差距

1. 手工 `POST /leads` 仍直接写入 `leads`，没有完整原始事件和自动补全链路。
2. 不同入口使用的实体匹配能力不完全一致，36氪仍偏重来源键去重。
3. 当前源码和已有构建产物执行的补全专题集合不一致。
4. 新投资画像源码已经引用0092表，但当前业务数据库尚未应用0092。
5. 当前列表仍显示评级徽标，并保留“评级分数”排序。
6. 快照冻结后仍可能把历史 `ratingV3` 标记为stale并自动创建评分任务。
7. 人工复核、Radar批处理和36氪导入仍存在自动评分触发点。
8. 历史V3排队任务与未来投资画像版本缺少明确的schema隔离和终止策略。
9. 联网补全存在大量排队、review、dead-letter和额度/预算失败，不能直接全量重跑。
10. 当前工作树包含多项未提交修改，实施时必须增量合并，不得覆盖查询分页和投资画像既有工作。

## 4. 范围与边界

### 4.1 本期包含

- 所有共享线索入口统一走原始事件和Pipeline；
- 主体准入、实体匹配、去重和人工复核统一；
- 新投资画像补全schema和核心专题执行策略；
- AI联网搜索、来源抓取、事实校验和冲突处理；
- 事实快照到投资画像投影的确定性生成；
- 投资画像表、字典、索引、API、筛选和排序；
- 七列列表和详情证据落点；
- 停止共享线索自动V3评级；
- 保留管理员按需评级和历史评级审计；
- 存量V3任务、快照、事实和画像覆盖的只读审计；
- preview、canary、有界回补、幂等和恢复方案；
- 单元、契约、服务端、隔离MySQL、HTTP、浏览器和运行态验收；
- 构建、可选本地激活、生产发布和回滚计划。

### 4.2 本期不包含

- 删除V3评级源码、算法、历史表或历史结果；
- 修改专属项目的既有评分流程；
- 把画像覆盖率转换成线索投资评级；
- 未经确认执行13专题全量联网研究；
- 未经授权迁移、回填、取消历史任务或写业务库；
- 未经授权启动、停止、重启服务或激活候选；
- 未经授权发布生产、提交或推送代码；
- 让AI自行定义“大机构”“A级客户”或“高校成果转化”；
- 为通过验收放宽鉴权、CSRF、Origin、SSRF或证据门禁；
- 对查询分页修复以外的无关模块进行重构。

## 5. 目标业务流程

### 5.1 统一入池流程

所有入口使用同一正式提交边界：

| 入口 | 原始来源类型 | 目标处理 |
|---|---|---|
| Radar公开源 | `radar` | 原始事件→主体识别→实体匹配→ready |
| 论文/科研来源 | `paper`或Radar子类型 | 保留论文身份，区分科研主体与公司主体 |
| 36氪储备库 | `lead_reserve` | 原始事件→通用实体匹配→ready |
| BP上传 | `bp-upload` | 自报信息标记unverified，关键事实联网核验 |
| Excel/CSV | `batch-import` | 单行单事件，错误行不影响其他行 |
| 公开情报采集 | `public-intel` | 搜索结果先留档，再正式提交 |
| 手工新增 | `manual` | 不再直接插入，必须生成原始事件和审计 |
| 人工复核接受 | `manual-review` | 绑定原事件和决策后进入ready |

统一步骤：

1. 保存来源类型、来源ID、发生时间、原始payload和内容hash。
2. 生成稳定幂等键；同键不同内容必须拒绝或进入审计冲突。
3. 校验主体名称是否具体、可识别。
4. 执行工商注销等确定性准入规则。
5. 解析公司、项目、团队、科研成果等主体类型。
6. 按信用代码、标准名、官网域名、来源键和URL执行实体匹配。
7. 唯一命中则合并；无命中则创建；多命中则进入review。
8. 正式线索和Pipeline `ready`在同一事务中提交。
9. `ready`事务内创建对应schema的补全任务。
10. 原始事件、正式线索和补全任务必须可相互追溯。

### 5.2 实体匹配顺序

建议采用以下确定性顺序：

1. 统一社会信用代码；
2. 已验证原始来源唯一键；
3. 工商主体标准全称；
4. 官方网站域名；
5. 已确认的曾用名和品牌别名；
6. 项目名与运营主体关系；
7. 规范化来源URL；
8. 其他低置信候选进入人工复核。

项目名、团队名和实验室名不得混入法律公司别名；人物同名不得仅凭名称跨项目合并。

### 5.3 补全任务状态

```text
queued
  → running
    → resolving_entity
    → planning
    → searching
    → fetching
    → extracting
    → validating
      → completed / partial / missing / not_applicable / review
      → retrying → running
      → dead_letter
```

规则：

- `basic_profile`优先执行；
- 主体为ambiguous/missing时，融资、客户、技术权属和交易等重大专题进入review；
- 单一专题失败不得抹除其他专题成功结果；
- 最新动态失败不得阻塞核心投资画像；
- 计费/额度错误不无限重试；
- Worker、任务和专题均使用持久化租约；
- 同一事件和schema不得重复创建等价任务。

## 6. 需要补充的数据

### 6.1 主体与基础身份

| 字段组 | 最低字段 | 用途 |
|---|---|---|
| 线索身份 | `lead_id`、项目/品牌名、主体类型 | 全链路主键和展示 |
| 工商身份 | 公司标准名、信用代码、登记状态、公司类型 | 主体确认和准入 |
| 别名 | 简称、品牌名、曾用名 | 搜索与去重 |
| 地区 | 注册地、实际业务地区、地区来源和置信度 | 列表筛选 |
| 官网 | 官方域名、来源和验证状态 | 实体消歧 |
| 关系 | 项目—公司、团队—项目、高校—人物等 | 事实路由 |
| 匹配决策 | `matchedSubjectId`、`matchMethod`、`matchConfidence` | 解释自动合并依据 |
| 复核状态 | `reviewStatus`、`reviewReason`、`reviewedBy`、`reviewedAt` | 多主体或低置信复核 |

成立时间、注册资本、法人和注册地址保留在详情页，不占用列表独立列。

主体和别名还必须遵守：

- `aliasType`至少区分法律曾用名、品牌名、项目名、来源原名和人工确认别名；
- 项目名、团队名、实验室名不得作为公司的法律别名参与高置信自动合并；
- `matchConfidence`只表达候选匹配置信度，不能替代确定性标识或人工确认；
- 自动合并必须保存所用标识及匹配规则版本；多候选、标识冲突或低置信结果进入review；
- 普通列表不返回内部匹配分数和复核原因，管理员审计接口按权限读取。

### 6.2 行业、产品与产业化

| 事实键 | 含义 | 规则 |
|---|---|---|
| `industry.level1` | 一级行业 | 批准枚举 |
| `industry.level2` | 二级行业 | 可映射一级行业 |
| `industry.segment` | 细分赛道 | 不接受“科技”等宽泛词 |
| `industry.chain_position` | 产业链位置 | 受控字典 |
| `product.name` | 核心产品 | 稳定instanceKey |
| `product.route` | 产品路线 | 与技术路线分开 |
| `product.form` | 产品形态 | 软件、设备、材料、服务等 |
| `technology.route` | 技术实现路线 | 绑定产品或项目主体 |
| `product.use_case` | 应用场景 | 不从行业名称直接推断 |
| `product.stage` / `production.stage` | 产业化阶段 | 规划、实验室、中试、小批量、量产分开 |
| `production.capacity` | 产能 | 计划和实际口径分开 |
| `production.yield` | 良率 | 实验和量产条件分开 |
| `qualification.certification` | 资质/认证 | 名称、编号、签发机构、适用范围、状态和有效期必填 |

同一产品的`product.name`、`product.route`、`product.form`、`technology.route`、`product.use_case`和产业化阶段必须共享同一`productInstanceKey`。产能、良率和认证必须绑定产品、工厂/产线或适用主体及明确期间，不能脱离测试条件或适用范围单独展示。

字段落点：

- 列表投影：行业层级、产业链位置、最多两个核心产品、产品路线、技术路线和产业化阶段；
- 详情投影：产品形态、应用场景、产能、良率和资质认证；
- 筛选字段：行业层级、产业链位置、产品路线、产品形态和产业化阶段；
- 产能、良率和认证本期默认不做范围筛选，避免单位、条件或适用范围未统一时产生误导。

### 6.3 投资机构

每个投资事件至少保存：

- 标准机构名称和别名；
- 融资事件instanceKey；
- 对应轮次和日期；
- 领投、跟投、战略投资或未披露角色；
- 机构类型；
- 机构等级和重点机构标志；
- 来源、原文引用、证据等级、验证和冲突状态。

“重点机构”只能由受控机构字典决定，AI只能提出归一化候选。

机构字典至少保存标准名、别名、机构类型、等级、重点机构标志、版本、生效状态、生效时间、修改原因和审计人。列表最多返回两家代表机构，同时返回`institutionTotalCount`；代表机构按已验证轮次时间倒序、投资角色和稳定机构ID确定性排序，不能按模型生成顺序选择。

### 6.4 高校与科研背景

必须区分：

- 学历/校友；
- 当前教师或研究人员；
- 历史任职；
- 院系、实验室和课题组；
- 联合研发；
- 高校成果转化；
- 专利许可或转让；
- 孵化平台或校办企业。

每条关系至少保存稳定`academicLinkInstanceKey`、人物、机构、院系/实验室、关系类型、起止时间、当前/历史标志、对应项目/公司、来源和验证状态。只有学校列表但无人物对应时，不建立虚假的人物—学校关系。

专利许可或转让还必须保存专利/专利族标识、许可方、被许可方或转让双方、权利类型、生效日期和适用范围。列表最多返回两条代表高校关系并返回`academicLinkTotalCount`；历史任职不得显示为当前关系，过期许可不得显示为当前有效成果转化依据。

### 6.5 融资进展

同一轮融资共享稳定`financingInstanceKey`，至少包含：

| 字段 | 含义 | 处理规则 |
|---|---|---|
| `status` | 融资状态 | 已完成/正在交割/计划融资/未融资/未披露 |
| `roundRaw` | 来源原始轮次 | 保留“Pre-A+”“战略轮”等原文 |
| `roundNormalized` | 标准轮次 | 受控枚举，无法映射时保持未标准化 |
| `announcedAt` | 宣布日期 | 不得替代融资完成日期 |
| `completedAt` | 完成/交割日期 | 最新已完成轮次的主要排序时间 |
| `amountRaw` | 来源金额原文 | 保留“近亿美元”“数千万元”等原始表述 |
| `amountValue` | 可计算规范数值 | 使用Decimal语义，不使用浮点近似 |
| `amountUnit` | 元/万元/亿元等 | 与规范值换算规则绑定 |
| `currency` | 原始币种 | CNY/USD/HKD等受控值 |
| `amountUndisclosed` | 金额未披露 | true时不得生成伪数值0 |
| `investors` | 投资方与角色 | 每家机构保存标准ID、领投/跟投/战略/未披露 |
| 证据字段 | 原文、来源、状态 | 按公共证据契约处理 |

派生规则：

- 最新轮次只从`status=completed`且具有有效`completedAt`的已验证事件中选择；同日多个候选无法确认先后时进入冲突；
- `announcedAt`、来源发布时间和抓取时间不能冒充`completedAt`；
- 累计融资额和完成轮次数由已验证融资事件确定性计算，不人工重复维护；
- `cumulativeAmountByCurrency`按币种分别累计，金额未披露的轮次只计轮次数、不计金额；
- 默认不跨币种相加；如业务需要统一币种，必须同时保存`fxRate`、`fxRateDate`、`fxRateSource`和`convertedCurrency`，展示时明确“折算”；
- 计划融资、市场规模、合同金额和营业收入不得写成已完成融资金额。

### 6.6 估值

至少保存：

- `valueRaw`、规范数值或估值区间`valueMin/valueMax`；
- 元/万元/亿元等`unit`；
- 币种；
- 投前或投后口径；
- 对应融资事件instanceKey；
- 对应轮次；
- 估值基准日`asOfDate`和来源发布日期；
- 已完成融资估值、计划估值或第三方估算类型；
- 来源、验证和冲突状态。

普通列表默认只展示最新的已验证、已完成融资估值。计划估值和第三方估算不得冒充成交估值。估值范围不得擅自取中位数；无法确认投前/投后口径时保持未披露或review。估值筛选必须绑定币种，跨币种比较只允许使用有汇率日期和来源的受控折算值。

### 6.7 客户与商业验证

客户重要性与验证阶段分开维护：

| 阶段 | 含义 | 最低证据 |
|---:|---|---|
| L0 | 提及/宣传案例 | 仅来源提及，不算已验证客户 |
| L1 | 接触/意向 | 洽谈、意向或明确交流依据 |
| L2 | 送样/测试/PoC/试点 | 测试、试点、送样或PoC依据 |
| L3 | 框架协议/正式合同 | 合同、采购或正式合作依据 |
| L4 | 订单/交付/验收 | 订单、交付或验收依据 |
| L5 | 回款/复购 | 回款、复购或持续收入依据 |

客户记录至少包含：

- 稳定客户内部标识；
- 标准名称和别名；
- A/B/C客户等级；
- 保密状态和脱敏标签；
- 当前最高验证阶段；
- 合同、订单、交付、验收和回款事实；
- 时间、金额、币种、范围和来源；
- 访问权限和审计状态。

“战略合作”“联合实验室”“研究伙伴”不得直接升级为正式客户。保密客户真名不得出现在普通API响应或DOM中。

客户聚合口径：

- `mentionedCount`：最高阶段达到L0的去重客户数；
- `engagedCount`：最高阶段达到L1的去重客户数；
- `trialCount`：最高阶段达到L2的去重客户数；
- `contractedCount`：最高阶段达到L3的去重客户数；
- `deliveredCount`：最高阶段达到L4的去重客户数；
- `payingCount`：最高阶段达到L5的去重客户数；
- `verifiedCustomerCount`：本方案推荐按L3及以上计算，最终以`LP-DEC-011`确认结果为准；未确认时fail-closed，不把L0—L2计为已验证客户；
- `tierACount`：只统计同时满足A级字典和`verifiedCustomerCount`门槛的客户，不能统计仅宣传提及的A级公司。

代表客户最多返回两家，同时返回`customerTotalCount`。排序依次使用最高验证阶段、客户等级、最新有效证据时间和稳定客户ID；不能按模型输出顺序选择。阶段只表示已取得的最高证据，不因后续证据缺失自动降级；合同失效、合作终止或证据过期使用有效期和状态单独表达。

### 6.8 每条事实的公共证据字段

所有可展示事实必须同时保存：

- `leadId`、`subjectId`和主体类型；
- `topicKey`、`factKey`和稳定`instanceKey`；
- 原始值、规范值和value hash；
- 事实发生时间`factOccurredAt`；
- 期间`periodStart/periodEnd`和有效期`validFrom/validTo`；
- 单位、币种和适用范围；
- 来源标题、发布主体/站点、最终落地域名和来源类型；
- 原始来源URL和规范URL；
- 来源发布时间`sourcePublishedAt`和抓取时间`sourceRetrievedAt`；
- 原始文档ID和内容hash；
- 可定位的连续原文quote及页码/章节/字符偏移`quoteLocator`；
- 证据方向`supports/refutes`，不得把反驳材料当成支持证据；
- E1/E2/E3证据等级；
- verified/unverified/conflicted/missing/not_applicable/rejected状态；
- 事实版本、替代关系、有效期；
- 冲突候选和裁决结果；
- 验证时间`verifiedAt`、验证规则版本和验证主体；
- Prompt、模型、工具、提取置信度和运行批次审计信息。

搜索命中不等于事实验证成功；缺少原文、主体、时间或口径时必须保持空值、missing或review。

提取置信度只用于排序人工复核优先级，不能直接把事实升级为verified。未来日期、结束时间早于开始时间、来源发布时间晚于抓取时间等异常必须拒绝或进入review。

### 6.9 数据状态与字典绑定

投资画像的数据状态至少包含：

| 字段 | 含义 |
|---|---|
| `verifiedDimensions` | 已达到verified门槛的适用维度数量 |
| `applicableDimensions` | 当前主体适用的画像维度数量 |
| `dimensionStates` | 每个维度的verified/partial/conflicted/missing/not_applicable状态 |
| `conflictCount` | 当前未裁决冲突数量 |
| `stale` | 是否因新快照或字典变化过期 |
| `staleReason` | snapshot_changed/dictionary_changed/projection_failed等受控原因 |
| `factUpdatedAt` | 画像所用最新事实时间 |
| `snapshotCreatedAt` | 来源冻结快照时间 |
| `projectedAt` | 当前投影生成时间 |
| `sourceFreshnessAt` | 当前画像所用最新来源发布时间，不得用抓取时间冒充 |

画像投影必须绑定：

- `snapshotId`、`snapshotHash`和补全`schemaVersion`；
- 行业、机构、客户和高校字典的版本或Hash；
- 投影规则版本`projectionVersion`；
- 画像DTO版本`profileSchemaVersion`。

字典或快照变化后旧投影必须标记stale并受控重建；重建失败时保留上一份有效投影，但API必须返回其过期状态，不能伪装为最新结果。

### 6.10 字段全链路处理矩阵

所有新增字段在开发前必须进入字段字典并明确以下处理链路：

| 字段组 | 事实/事件键 | 实例粒度 | 主要验证 | 快照 | 列表投影 | 详情 | 筛选 |
|---|---|---|---|---|---|---|---|
| 主体匹配 | 主体决策记录 | 原始事件+候选主体 | 确定性标识/人工复核 | 审计索引 | 仅状态 | 管理审计 | 否 |
| 行业 | `industry.*` | 项目/公司 | 字典层级一致 | 是 | 是 | 是 | 是 |
| 产品路线 | `product.*`、`technology.route` | 产品 | 稳定产品实例键 | 是 | 最多2项+总数 | 是 | 是 |
| 产能/良率 | `production.*` | 产品+产线+期间 | 实际/计划和测试条件 | 是 | 否 | 是 | 否 |
| 资质认证 | `qualification.certification` | 编号+主体 | 签发机构、状态、有效期 | 是 | 摘要状态 | 是 | 可选 |
| 投资机构 | 融资事件投资方 | 融资事件+机构 | 字典、轮次和角色 | 是 | 最多2项+总数 | 是 | 是 |
| 高校关系 | 高校关系事实 | 人物/项目+机构+关系 | 关系类型和有效期 | 是 | 最多2项+总数 | 是 | 是 |
| 融资 | 融资事件 | 轮次事件 | 状态、完成时间、金额口径 | 是 | 最新+累计 | 是 | 是 |
| 估值 | 交易估值 | 融资事件/估值事件 | 口径、基准日、币种 | 是 | 最新有效值 | 是 | 是 |
| 客户 | 客户行为事实 | 客户+行为+期间 | L0—L5证据门槛 | 是 | 代表项+分层计数 | 是 | 是 |
| 数据状态 | 画像派生字段 | lead+快照+字典版本 | 纯函数和幂等 | 是 | 是 | 是 | 是 |

字段不得只出现在Prompt或自由文本中。每个字段都必须有权威来源、可空语义、冲突规则、权限、生命周期、投影落点和至少一个验收样本；未进入列表的字段也必须在详情或管理员审计区有明确落点。

## 7. 专题执行策略

### 7.1 核心画像专题

每条适用线索优先执行：

1. `basic_profile`；
2. `products`；
3. `technology_ip`；
4. `industrialization`；
5. `team`；
6. `financing`；
7. `transaction_exit`；
8. `customers_contracts`。

### 7.2 非阻塞专题

`latest_developments`作为详情页增量专题，低优先级执行。失败、缺失或额度不足时，不阻塞核心投资画像投影。

### 7.3 暂不随入池强制执行的专题

- `ownership`；
- `financial_operations`；
- `competition`；
- `market_policy`。

这些专题继续保留事实合同，可在重点线索、管理员手工研究或后续尽调中触发，但不作为每条共享线索入池后的默认成本。

科研主体不适用融资、客户和交易专题时，必须记录`not_applicable`，不能伪造企业经营事实。

## 8. 技术设计

### 8.1 版本策略

- 历史 `lead-enrichment-v3` 快照保持原语义。
- 新流程建议使用 `lead-enrichment-v4-investment-profile`。
- 补全任务表应显式保存`schema_version`，不只将版本隐藏在幂等hash中。
- 联网研究Prompt单独升级版本，历史Prompt结果保留。
- 投资画像投影使用 `lead-investment-profile-v1`。
- 同一语义事实和证据集生成相同快照hash。
- 运行ID、重试次数和耗时保留在快照审计中，但不改变语义hash。

### 8.2 统一入池服务

建议将来源无关的正式提交边界收敛为：

```ts
commitLeadPipelineReady({
  eventId,
  sourceType,
  candidate,
  decision,
  actor,
})
```

该服务负责：

- 主体准入；
- 实体匹配；
- 创建或合并`leads`；
- 记录实体匹配和决策；
- Pipeline迁移到ready；
- 创建V4补全任务；
- 保证事务、幂等和审计一致。

### 8.3 联网研究

AI Gateway和`codex-cli`均可作为搜索后端，但必须遵守同一宿主契约：

- 只允许返回专题白名单factKey；
- 只允许引用本轮工具返回URL；
- 除基本画像外必须使用稳定instanceKey；
- 数字事实必须带期间、单位、币种或scope；
- 无法确认进入gaps；
- 多值冲突进入conflicts；
- 页面中的Prompt Injection文本一律视为不可信数据；
- 搜索和抓取必须遵守SSRF、重定向、robots、大小和超时边界。

### 8.4 证据验证与持久化

宿主按以下顺序校验：

1. URL属于本轮搜索来源集合；
2. 受控抓取成功并持久化原始文档；
3. quote可在文档中定位；
4. 文档包含确认主体名称或别名；
5. factKey属于专题契约；
6. instanceKey、时间、单位、币种和scope完整；
7. 融资、估值、合同、收入和市场口径不混淆；
8. 客户阶段满足最低证据门槛；
9. 证据等级由最终落地域名保守判定；
10. 与当前事实值冲突时不静默覆盖。

E3单独支持的重大融资、估值、合同、订单、回款和专利权属事实不得进入verified投影。

### 8.5 快照

快照至少包含：

- schema版本；
- 线索和主体身份；
- 实体和关系；
- 专题状态；
- 当前事实；
- 证据索引；
- gaps；
- conflicts；
- coverage；
- snapshot hash；
- 事实发生、来源发布、抓取和验证时间；
- 字典候选及未归一化项，不把运行时字典结果固化为事实值；
- Prompt、模型、工具、运行和时间审计。

快照状态：

- `ready`：无未决重大冲突，可生成正常投影；
- `review`：存在未决冲突或主体复核，只投影无冲突verified事实；
- 单一专题missing/partial不阻止其他有效事实进入投影；
- 投影失败时保留上一份有效投影。

### 8.6 投资画像投影

投影完全由当前verified事实、冲突、字典和快照确定性生成，至少包含：

- 身份：lead、补全schema、画像schema、projection版本、snapshot ID/hash；
- 字典绑定：行业、机构、客户和高校字典版本/Hash；
- 行业：一级、二级、细分赛道、产业链位置；
- 产品：最多两个核心产品、产品路线、技术路线、产业化阶段和产品总数；
- 机构：最多两个代表机构、轮次、角色、类型、等级、重点机构标志和机构总数；
- 高校：最多两个代表关系、人物、机构、关系类型、实验室、有效期、成果转化标志和关系总数；
- 融资：状态、最新已完成轮次、完成日期、金额原文、规范数值、单位、币种、分币种累计轮次/金额；
- 估值：金额原文、规范数值/区间、单位、口径、币种、基准日和轮次；
- 客户：最高阶段、最多两个代表客户、L0—L5分层计数、A级已验证客户数和客户总数；
- 状态：适用维度、已验证维度、逐维度状态、冲突数、过期状态/原因、事实/来源/快照/投影时间。

投影不得保存原文、内部材料路径、受限客户真名或完整facts/evidence大对象。

### 8.7 V3评级解耦

主流程调整为：

- `LEAD_ENRICHMENT_AUTO_SCORE=false`作为默认运行配置；
- 快照冻结后不自动`enqueueLeadScoreJob()`；
- 新快照不再自动把历史V3改成stale；
- 人工复核接受后不自动`scheduleLeadScoring()`；
- Radar、36氪和批量导入不再自动补偿评分；
- 启动恢复不再恢复非人工V3任务；
- 管理员手工评分接口保留；
- 手工评分仍必须绑定准确快照ID/hash/schema；
- 历史V3结果、任务和评级历史不物理删除；
- 专属项目评分流程保持不变。

### 8.8 API和查询

`GET /api/leads`返回轻量投资画像摘要，不返回完整事实和证据。

建议固定DTO契约；金额数值使用Decimal字符串，避免JavaScript浮点精度损失：

```ts
type ProfileDimensionState =
  | 'verified'
  | 'partial'
  | 'conflicted'
  | 'missing'
  | 'not_applicable'

type MoneySummary = {
  raw?: string
  value?: string
  minValue?: string
  maxValue?: string
  unit?: 'yuan' | 'wan_yuan' | 'yi_yuan' | 'base'
  currency?: string
  undisclosed?: boolean
}

type LeadInvestmentProfileSummary = {
  profileSchemaVersion: 'lead-investment-profile-v1'
  enrichmentSchemaVersion: string
  projectionVersion: string
  snapshotId: string
  snapshotHash: string
  dictionaryBinding: {
    industryHash?: string
    institutionHash?: string
    customerHash?: string
    academicHash?: string
  }
  subject: {
    leadId: string
    name: string
    legalEntityName?: string
    subjectType: string
    region?: string
    profileReviewStatus?: 'clear' | 'review'
  }
  industry: {
    level1?: string
    level2?: string
    segment?: string
    chainPosition?: string
  }
  products: Array<{
    instanceKey: string
    name: string
    productRoute?: string
    technologyRoute?: string
    productionStage?: string
  }>
  productTotalCount: number
  institutions: Array<{
    institutionId: string
    name: string
    round?: string
    role?: 'lead' | 'follow' | 'strategic' | 'undisclosed'
    type?: string
    tier?: string
    major: boolean
  }>
  institutionTotalCount: number
  academicLinks: Array<{
    instanceKey: string
    institution: string
    relationType: string
    person?: string
    departmentLab?: string
    validFrom?: string
    validTo?: string
    current?: boolean
    commercialization?: boolean
  }>
  academicLinkTotalCount: number
  financing: {
    status: string
    latestCompletedRound?: string
    latestCompletedAt?: string
    latestAmount?: MoneySummary
    cumulativeAmountByCurrency: Array<{
      currency: string
      value: string
      completedRoundCount: number
    }>
    completedRoundCount: number
  }
  valuation: {
    amount?: MoneySummary
    type?: 'pre_money' | 'post_money' | 'planned' | 'estimated'
    asOfDate?: string
    round?: string
  }
  customers: {
    highestStage?: 'L0' | 'L1' | 'L2' | 'L3' | 'L4' | 'L5'
    mentionedCount: number
    engagedCount: number
    trialCount: number
    contractedCount: number
    deliveredCount: number
    payingCount: number
    verifiedCustomerCount: number
    tierACount: number
    customerTotalCount: number
    representatives: Array<{
      customerId: string
      displayName: string
      tier?: 'A' | 'B' | 'C'
      stage: 'L0' | 'L1' | 'L2' | 'L3' | 'L4' | 'L5'
      anonymized: boolean
    }>
  }
  dataStatus: {
    verifiedDimensions: number
    applicableDimensions: number
    dimensionStates: Record<string, ProfileDimensionState>
    conflictCount: number
    stale: boolean
    staleReason?: string
    factUpdatedAt?: string
    sourceFreshnessAt?: string
    snapshotCreatedAt: string
    projectedAt: string
  }
}
```

`LeadListItem`中的`investmentProfile`允许整体缺失；没有可用投影时返回明确的missing状态或不返回该对象，不能构造空快照ID/hash。数组只返回确定性排序后的前两项；`*TotalCount`返回脱敏后的可见总数，不得通过总数差异推断受限客户或受限关系。普通DTO不返回`matchConfidence`、受限客户真名、原文quote、内部事实ID、Prompt或审计私有字段。

支持的主要筛选：

- 行业一级、二级和细分赛道；
- 产品路线和产业化阶段；
- 投资机构、类型和重点机构；
- 高校、关系类型和成果转化；
- 最新轮次和融资日期；
- 估值范围、币种和口径；
- 客户等级、最低验证阶段和是否有已验证客户；
- 画像状态和是否存在冲突。

支持的排序：

- 最新入池；
- 最新融资；
- 最新有效估值；
- 客户验证阶段；
- 画像更新时间。

移除普通列表的“评级分数”排序。所有筛选参数必须由服务端严格验证，非法参数返回400；列表和total使用相同筛选语义。

### 8.9 前端

列表固定为七列：

1. 线索主体；
2. 行业 / 产品路线；
3. 机构 / 高校背景；
4. 融资进展；
5. 最新估值；
6. 大客户验证；
7. 数据状态。

调整要求：

- 删除评级徽标；
- 删除“评级分数”排序；
- 线索主体显示名称、工商主体、地区和主体类型；
- 如需小状态，显示画像已验证/部分验证/存在冲突；
- 多值产品、机构、高校和客户只显示前两项，超出使用`+N`，N基于当前用户可见总数；
- 数据状态展示已验证维度/适用维度、冲突、过期原因和画像更新时间，不用抓取时间冒充事实新鲜度；
- 不显示“待评级、评级中、评级过期、评级失败”；
- 详情页不显示普通用户V3操作入口；
- 历史评级如保留，只在管理员审计区显示；
- 外部来源新窗口打开并使用`rel="noreferrer"`；
- 保密客户真名不进入响应和DOM；
- 保持行点击、详情按钮、Enter/Space和现有权限行为；
- 桌面端无非预期横向溢出，窄屏按相同信息优先级降级。

## 9. 数据库与迁移

### 9.1 迁移内容

在现有0092投资画像迁移基础上评审是否需要补充：

- 补全任务`schema_version`；
- V3历史任务`superseded/cancelled`终态或等效审计字段；
- 投资机构字典；
- 客户字典；
- 投资画像投影；
- 主体匹配决策和人工复核审计；
- 融资/估值原值、Decimal规范值、单位、币种、事件时间和未披露标志；
- 客户L0—L5分层计数及计数口径版本；
- 事实发生、来源发布、抓取、验证和有效期时间字段；
- 快照、字典Hash、投影版本和过期原因绑定字段；
- 必要的筛选和排序索引；
- 字典版本、生效状态和修改审计。

金额规范值必须使用足够精度的DECIMAL或无损字符串契约，不使用FLOAT/DOUBLE保存投资金额和估值。所有时间使用项目统一时区存储规范，API输出ISO 8601；来源未披露具体日期时不得虚构月日。不得在应用启动时自动执行DDL。迁移编号以实施时仓库迁移顺序为准，不能盲目复用已占用编号。

### 9.2 存量处理

先执行只读审计，区分：

- V3 queued/running/retrying/dead-letter任务；
- V3 ready/review快照；
- 当前verified/unverified/conflicted事实；
- 历史V3评级；
- 可直接生成投资画像的线索；
- 缺少核心画像事实的线索；
- never/ready/stale/conflicted/missing/not_applicable状态。

处理原则：

- 不删除历史事实、证据、快照和评级；
- 旧V3活动任务不得由V4 Worker静默解释；
- 终止或归档旧任务前必须preview并获得单独授权；
- 能从现有verified事实直接投影的，先投影、不重新联网；
- 事实不足的才创建V4补全任务；
- 先5—10条canary，再有界并发；
- 额度不足立即停止，不擅自换模型或无限重试；
- 重跑应返回`changed=0`，或明确解释新增来源差异。

## 10. 分阶段开发计划

### I0：产品和版本冻结

交付：决策记录、字段字典、专题范围、V3退出口径和现状审计。

- `LP-I0-001` 确认停止自动V3评级。
- `LP-I0-002` 确认管理员按需评级是否保留。
- `LP-I0-003` 确认8个核心专题和最新动态非阻塞策略。
- `LP-I0-004` 确认行业、机构、客户、高校和阶段字典。
- `LP-I0-005` 确认V4 schema、Prompt和任务版本策略。
- `LP-I0-006` 只读审计入口、V3任务、快照、事实、画像和运行队列。
- `LP-I0-007` 确认已验证客户、A级客户和各阶段计数口径。
- `LP-I0-008` 确认跨币种融资/估值默认不折算及可选汇率来源。
- `LP-I0-009` 冻结字段全链路矩阵和DTO版本。

退出条件：所有关键产品口径和迁移边界已确认；未确认项fail-closed。

### I1：统一入池

交付：通用提交服务、手工入口改造、实体匹配和入口契约测试。

- `LP-I1-001` 抽取来源无关的`commitLeadPipelineReady()`。
- `LP-I1-002` 手工新增生成raw event并进入Pipeline。
- `LP-I1-003` 36氪复用通用实体匹配。
- `LP-I1-004` BP/Excel/Radar/公开情报入口对账。
- `LP-I1-005` 多候选统一进入review。
- `LP-I1-006` 验证同事件重放幂等。
- `LP-I1-007` 保存匹配方法、规则版本、候选主体和人工复核审计。

退出条件：所有接受入池的入口都有raw event、ready状态和对应补全任务。

### I2：V4补全与证据契约

交付：V4 schema、Prompt、专题计划、验证器和金标样本。

- `LP-I2-001` 任务显式保存schema版本。
- `LP-I2-002` 冻结核心和非阻塞专题集合。
- `LP-I2-003` 扩展行业、机构、高校、融资、估值和客户事实键。
- `LP-I2-004` 增加主体、日期、金额、币种、scope和生命周期门禁。
- `LP-I2-005` 增加客户L0—L5确定性派生。
- `LP-I2-006` 增加保密客户投影安全测试。
- `LP-I2-007` 建立企业、科研、未融资、多轮融资和冲突金标。
- `LP-I2-008` 实现融资/估值原值、Decimal规范值、单位和分币种累计规则。
- `LP-I2-009` 区分事实发生、来源发布、抓取、验证和有效期时间。
- `LP-I2-010` 补齐产品形态、应用场景、产能、良率和认证的详情落点。

退出条件：错误主体、计划融资、宣传客户和估值混淆被拒绝或进入review。

### I3：快照、投影和V3解耦

交付：迁移、快照投影、自动评分退出和恢复测试。

- `LP-I3-001` 评审0092和补充迁移。
- `LP-I3-002` 实现ready/review快照安全投影。
- `LP-I3-003` 停止快照自动评分和stale写入。
- `LP-I3-004` 移除各入口自动评分触发。
- `LP-I3-005` 保留管理员手工评分和历史审计。
- `LP-I3-006` 验证投影可删除重建且不覆盖事实层。
- `LP-I3-007` 投影绑定快照、字典Hash、规则版本并返回stale原因。
- `LP-I3-008` 实现客户分层计数、多值总数和确定性代表项排序。

退出条件：补全完成自动生成画像，但`lead_score_jobs`无自动增量。

### I4：API和七列页面

交付：轻量DTO、严格筛选、七列UI和详情证据落点。

- `LP-I4-001` 接入投资画像投影查询。
- `LP-I4-002` 移除评级DTO依赖、评级徽标和分数排序。
- `LP-I4-003` 实现行业、产品、机构、高校、融资、估值和客户筛选。
- `LP-I4-004` 实现画像状态、冲突、缺失和过期状态。
- `LP-I4-005` 补齐详情页资本、高校和客户证据落点。
- `LP-I4-006` 验证权限、外链、键盘和响应式布局。
- `LP-I4-007` 前后端共同使用明确的`LeadInvestmentProfileSummary`契约。
- `LP-I4-008` 实现`+N`、适用维度、字典/快照过期和四类时间语义。

退出条件：普通页面无V3状态文案，显示集合与筛选集合对账差异为0。

### I5：隔离验收和canary

交付：单元、契约、隔离MySQL、只读真实搜索和端到端canary报告。

- `LP-I5-001` 在隔离MySQL执行迁移和回滚测试。
- `LP-I5-002` 不写库运行5—10条金标搜索。
- `LP-I5-003` 在隔离库验证写回→快照→投影。
- `LP-I5-004` 证明自动评分任务增量为0。
- `LP-I5-005` 验证管理员按需评分仍可用。
- `LP-I5-006` 对业务库生成回补preview。

退出条件：canary精度、证据、幂等、安全和成本达标。

### I6：灰度、构建和发布

交付：灰度批次、候选构建、可选本地激活、发布和回滚记录。

- `LP-I6-001` 经授权执行小批V4回补。
- `LP-I6-002` 验证真实API、DOM和来源证据。
- `LP-I6-003` 完成类型、测试、平台门禁和构建。
- `LP-I6-004` 经授权激活并验证健康、资源和队列。
- `LP-I6-005` 再次确认后有界并发回补。
- `LP-I6-006` 生产发布获得独立授权并验证在线schema与静态资源。

退出条件：源码、测试、迁移、数据回补、本地激活和生产发布分别有独立证据。

## 11. 验收环境与安全前提

### 11.1 数据库

- 写入型测试只允许使用名称明确包含`test`、`tests`或`acceptance`的隔离MySQL数据库。
- 不得对业务库开启`ALLOW_MYSQL_INTEGRATION_TESTS`或`ALLOW_MYSQL_ACCEPTANCE_WRITES`。
- 业务库只允许只读审计和经授权的明确批次写入。
- 迁移、回填和任务归档必须记录目标库、批次ID、lead IDs和恢复依据。

### 11.2 联网搜索

- 真实联网验收默认不执行。
- 必须明确主体、专题、模型、预计调用和费用边界。
- 先单专题，再多专题；不得从1条canary直接扩大到全量。
- 搜索结果只作为候选，真实联网成功不等于事实写回成功。

### 11.3 发布

- 构建成功不等于本地生效。
- 本地生效不等于生产发布。
- 激活前必须确认4100监听和PID归属。
- 不使用宽泛进程匹配停止其他项目。
- 验证`/api/health`、`/api/health/components`、实际静态资源和真实DOM。

## 12. 验收清单

### 12.1 产品与范围

- [ ] `LP-SCOPE-001` 普通共享线索池不展示评级徽标。
- [ ] `LP-SCOPE-002` 普通页面不出现待评级、评级中、评级过期或评级失败。
- [ ] `LP-SCOPE-003` 列表不提供评级分数排序。
- [ ] `LP-SCOPE-004` 投资画像生成不依赖V3评级。
- [ ] `LP-SCOPE-005` 数据完整度不改变项目评级。
- [ ] `LP-SCOPE-006` 专属项目评分未被修改。
- [ ] `LP-SCOPE-007` 历史V3结果保留且可审计。
- [ ] `LP-SCOPE-008` 管理员按需评级行为已经明确并受权限保护。

### 12.2 入池与幂等

- [ ] `LP-FLOW-001` Radar接受入池时生成raw event、ready记录和V4任务。
- [ ] `LP-FLOW-002` 36氪接受入池时经过通用实体匹配。
- [ ] `LP-FLOW-003` BP和Excel每条记录可追溯原始来源。
- [ ] `LP-FLOW-004` 手工新增不再直接绕过Pipeline。
- [ ] `LP-FLOW-005` 人工复核接受时绑定原事件和决策。
- [ ] `LP-FLOW-006` 注销主体被拒绝且不创建补全任务。
- [ ] `LP-FLOW-007` 同一事件重复提交不创建重复线索或任务。
- [ ] `LP-FLOW-008` 幂等键内容冲突不会静默覆盖。
- [ ] `LP-FLOW-009` 多主体候选进入review，不随机合并。
- [ ] `LP-FLOW-010` 线索删除/合并/转项目后不会继续领取补全任务。

### 12.3 主体与关系

- [ ] `LP-ENTITY-001` 公司、项目、团队和科研成果分别建模。
- [ ] `LP-ENTITY-002` 项目名和团队名不进入公司法律别名。
- [ ] `LP-ENTITY-003` 信用代码、官网域名和标准名参与消歧。
- [ ] `LP-ENTITY-004` 同名公司不能串入融资和客户事实。
- [ ] `LP-ENTITY-005` 人物同名无法确认时保持ambiguous。
- [ ] `LP-ENTITY-006` 人物—高校关系有明确关系类型。
- [ ] `LP-ENTITY-007` 学历不会被误写成高校成果转化。
- [ ] `LP-ENTITY-008` 人工确认主体后只恢复主体阻断专题。
- [ ] `LP-ENTITY-009` 每次自动合并保存matchMethod、规则版本和命中标识。
- [ ] `LP-ENTITY-010` matchConfidence不能绕过确定性标识或人工复核门槛。
- [ ] `LP-ENTITY-011` 多候选复核保存reviewReason、操作人和操作时间。
- [ ] `LP-ENTITY-012` 普通列表响应不包含内部匹配分数和复核原因。

### 12.4 行业、产品和产业化

- [ ] `LP-DATA-IND-001` 行业层级使用批准字典。
- [ ] `LP-DATA-IND-002` 细分赛道不使用宽泛营销词。
- [ ] `LP-DATA-IND-003` 产品路线和技术路线分别保存。
- [ ] `LP-DATA-IND-004` 同一产品使用稳定instanceKey。
- [ ] `LP-DATA-IND-005` 规划、在研、中试和量产状态不混淆。
- [ ] `LP-DATA-IND-006` 产能和良率带实际/计划及测试条件。
- [ ] `LP-DATA-IND-007` 列表标签可在详情事实或来源中核对。
- [ ] `LP-DATA-IND-008` 行业和路线筛选结果与展示集合一致。
- [ ] `LP-DATA-IND-009` 同一产品名称、形态、路线、技术、场景和阶段共享productInstanceKey。
- [ ] `LP-DATA-IND-010` 产能、良率绑定产品/产线、期间、单位和测试条件。
- [ ] `LP-DATA-IND-011` 认证绑定编号、签发机构、范围、状态和有效期。
- [ ] `LP-DATA-IND-012` 产品形态、场景、产能、良率和认证在详情页有明确落点。

### 12.5 机构与高校

- [ ] `LP-DATA-BKG-001` 重点机构只由受控字典判定。
- [ ] `LP-DATA-BKG-002` 投资机构绑定具体轮次。
- [ ] `LP-DATA-BKG-003` 未披露角色不显示为领投。
- [ ] `LP-DATA-BKG-004` 机构类型和等级可审计。
- [ ] `LP-DATA-BKG-005` 高校关系绑定人物或项目主体。
- [ ] `LP-DATA-BKG-006` 院系、实验室、任职和成果转化分别保存。
- [ ] `LP-DATA-BKG-007` 只有机构列表时不强行分配人物关系。
- [ ] `LP-DATA-BKG-008` 机构和高校筛选使用标准名和别名归一化。
- [ ] `LP-DATA-BKG-009` 机构和高校返回值具有确定性排序、可见总数和`+N`语义。
- [ ] `LP-DATA-BKG-010` 高校任职和成果转化关系保存有效期，历史关系不显示为当前关系。
- [ ] `LP-DATA-BKG-011` 专利许可/转让关系绑定权利标识、双方主体、生效日和范围。

### 12.6 融资与估值

- [ ] `LP-DATA-CAP-001` 轮次、日期、金额、币种和投资方共享融资事件instanceKey。
- [ ] `LP-DATA-CAP-002` 最新轮次按已完成融资日期确定。
- [ ] `LP-DATA-CAP-003` 计划融资和已完成融资严格分开。
- [ ] `LP-DATA-CAP-004` 融资、估值、合同、收入和市场规模不会互相冒充。
- [ ] `LP-DATA-CAP-005` 投前、投后、计划和估算估值口径明确。
- [ ] `LP-DATA-CAP-006` 未融资显示为有效状态，金额和估值显示`-`。
- [ ] `LP-DATA-CAP-007` 未披露和待核验不渲染伪金额。
- [ ] `LP-DATA-CAP-008` 估值范围筛选使用规范数值和币种。
- [ ] `LP-DATA-CAP-009` roundRaw保留原文且roundNormalized只使用受控映射。
- [ ] `LP-DATA-CAP-010` announcedAt、completedAt、sourcePublishedAt和sourceRetrievedAt不会混用。
- [ ] `LP-DATA-CAP-011` 金额原文、Decimal规范值、单位、币种和未披露标志一致。
- [ ] `LP-DATA-CAP-012` 累计融资按币种分别计算，未披露金额不按0计入金额。
- [ ] `LP-DATA-CAP-013` 未提供汇率来源和日期时不做跨币种累计或范围比较。
- [ ] `LP-DATA-CAP-014` 估值区间不擅自取中位数，投前/投后不明确时进入review。

### 12.7 客户与商业验证

- [ ] `LP-DATA-CUS-001` 客户标准名、别名、等级和保密状态受控维护。
- [ ] `LP-DATA-CUS-002` L0—L5由宿主证据规则派生。
- [ ] `LP-DATA-CUS-003` 宣传案例和战略合作不自动成为正式客户。
- [ ] `LP-DATA-CUS-004` L3至少有合同、采购或正式合作依据。
- [ ] `LP-DATA-CUS-005` L4至少有订单、交付或验收依据。
- [ ] `LP-DATA-CUS-006` L5至少有回款、复购或持续收入依据。
- [ ] `LP-DATA-CUS-007` 保密客户普通响应和DOM不包含真名。
- [ ] `LP-DATA-CUS-008` 客户阶段和等级筛选与展示集合一致。
- [ ] `LP-DATA-CUS-009` mentioned/engaged/trial/contracted/delivered/paying计数分别按门槛去重计算。
- [ ] `LP-DATA-CUS-010` verifiedCustomerCount使用已确认门槛，未确认时不计入L0—L2。
- [ ] `LP-DATA-CUS-011` tierACount只统计同时满足A级字典和已验证门槛的客户。
- [ ] `LP-DATA-CUS-012` 代表客户按阶段、等级、证据时间和稳定ID确定性排序。
- [ ] `LP-DATA-CUS-013` customerTotalCount基于当前用户可见集合，不泄露受限客户存在性。
- [ ] `LP-DATA-CUS-014` 合同失效或合作终止通过状态/有效期表达，不篡改历史最高阶段证据。

### 12.8 搜索、证据和冲突

- [ ] `LP-EVID-001` 每个verified值至少绑定一个持久化来源文档。
- [ ] `LP-EVID-002` 每个verified值具有可定位quote。
- [ ] `LP-EVID-003` 来源正文命中确认主体或别名。
- [ ] `LP-EVID-004` 数字事实具有期间、单位、币种或scope。
- [ ] `LP-EVID-005` 搜索摘要命中但原页失败时不入verified。
- [ ] `LP-EVID-006` E3重大事实不进入verified投影。
- [ ] `LP-EVID-007` 同实例不同值进入冲突，不静默覆盖。
- [ ] `LP-EVID-008` 冲突候选分别保存值、quote和来源。
- [ ] `LP-EVID-009` 低等级证据不能覆盖高等级当前事实。
- [ ] `LP-EVID-010` Prompt Injection不会改变系统规则和工具权限。
- [ ] `LP-EVID-011` 原始文档抓取遵守SSRF和重定向限制。
- [ ] `LP-EVID-012` 事实和证据重跑幂等。
- [ ] `LP-EVID-013` factOccurredAt、period、validity、published、retrieved和verified时间分别保存。
- [ ] `LP-EVID-014` 来源发布时间晚于抓取时间等时间矛盾被拒绝或进入review。
- [ ] `LP-EVID-015` quoteLocator能定位页码、章节或字符偏移，不只保存不可定位文本。
- [ ] `LP-EVID-016` supports和refutes证据方向分开，反驳材料不作为支持证据。
- [ ] `LP-EVID-017` 提取置信度只用于复核排序，不能直接升级verified。
- [ ] `LP-EVID-018` 来源标题、发布主体、最终域名和来源类型进入证据元数据。

### 12.9 快照与投影

- [ ] `LP-SNAP-001` 快照绑定准确schema、lead、事实和证据。
- [ ] `LP-SNAP-002` 同一语义事实集生成相同snapshot hash。
- [ ] `LP-SNAP-003` 运行次数和耗时变化不改变语义hash。
- [ ] `LP-SNAP-004` review快照只投影无冲突verified事实。
- [ ] `LP-SNAP-005` 投影失败保留上一份有效投影。
- [ ] `LP-SNAP-006` 投影可删除后从事实和快照重建。
- [ ] `LP-SNAP-007` 投影不包含来源原文和受限字段。
- [ ] `LP-SNAP-008` 同一快照重复投影结果一致。
- [ ] `LP-SNAP-009` 新快照使旧投影过期并成功替换。
- [ ] `LP-SNAP-010` 列表数据状态与快照/冲突状态一致。
- [ ] `LP-SNAP-011` 投影绑定行业、机构、客户和高校字典版本或Hash。
- [ ] `LP-SNAP-012` 字典变化只重投影受影响记录并返回dictionary_changed原因。
- [ ] `LP-SNAP-013` dataStatus同时返回verifiedDimensions和applicableDimensions。
- [ ] `LP-SNAP-014` dimensionStates逐维度区分partial/conflicted/missing/not_applicable。
- [ ] `LP-SNAP-015` 事实、来源、快照和投影四类时间不会互相冒充。
- [ ] `LP-SNAP-016` 投影规则版本变化可审计、可受控重建且失败保留旧投影。

### 12.10 V3解耦

- [ ] `LP-RATE-001` 补全快照完成后没有自动新增评分任务。
- [ ] `LP-RATE-002` 人工复核接受后没有自动评分。
- [ ] `LP-RATE-003` Radar和36氪导入后没有自动评分补偿。
- [ ] `LP-RATE-004` 新快照不会自动把历史V3标记为stale。
- [ ] `LP-RATE-005` 启动恢复不会恢复非人工评分任务。
- [ ] `LP-RATE-006` 管理员手工评分仍要求ready快照。
- [ ] `LP-RATE-007` 手工评分绑定快照ID/hash/schema。
- [ ] `LP-RATE-008` 历史V3结果和评级历史未被删除。
- [ ] `LP-RATE-009` 普通API不依赖ratingV3生成投资画像。
- [ ] `LP-RATE-010` 专属项目评分回归通过。

### 12.11 API、查询和性能

- [ ] `LP-API-001` 列表DTO返回投资画像摘要，不返回完整facts/evidence/scoring。
- [ ] `LP-API-002` 非法筛选参数返回400。
- [ ] `LP-API-003` 多条件组合、total和分页使用同一语义。
- [ ] `LP-API-004` URL前进/后退和详情返回状态一致。
- [ ] `LP-API-005` 显示值与筛选结果ID集合差异为0。
- [ ] `LP-API-006` 列表查询无每行N+1。
- [ ] `LP-API-007` 常用范围和排序具有索引/查询计划证据。
- [ ] `LP-API-008` 20条响应不包含受限字段。
- [ ] `LP-API-009` DTO符合明确的profile/enrichment/projection版本契约。
- [ ] `LP-API-010` 金额数值使用Decimal字符串且金额范围筛选绑定币种。
- [ ] `LP-API-011` 多值数组最多两项，totalCount与当前用户可见集合一致。
- [ ] `LP-API-012` DTO不返回matchConfidence、quote、Prompt或内部事实/审计ID。
- [ ] `LP-PERF-001` P95不超过同规模基线120%，或已确认新的绝对预算。
- [ ] `LP-PERF-002` 响应大小不超过基线120%。
- [ ] `LP-PERF-003` Worker并发有明确上限和熔断。

### 12.12 页面和权限

- [ ] `LP-UI-001` 表头恰好为七个目标列。
- [ ] `LP-UI-002` 列表DOM没有评级徽标。
- [ ] `LP-UI-003` 筛选中没有评级分数排序。
- [ ] `LP-UI-004` 行业、机构、融资、估值、客户和状态正常展示。
- [ ] `LP-UI-005` 无值显示`-`，未融资状态保留。
- [ ] `LP-UI-006` 冲突显示“存在冲突”，不静默选值。
- [ ] `LP-UI-007` 行点击、按钮和键盘行为保持一致。
- [ ] `LP-UI-008` 桌面无非预期横向溢出。
- [ ] `LP-UI-009` 普通用户看不到管理复核和手工评分入口。
- [ ] `LP-UI-010` 外链新窗口打开且包含`rel="noreferrer"`。
- [ ] `LP-UI-011` 产品、机构、高校和客户超出两项时显示准确`+N`。
- [ ] `LP-UI-012` 数据状态展示已验证/适用维度、冲突、过期原因和画像时间。
- [ ] `LP-UI-013` 页面不把来源抓取时间显示为事实发生或来源发布时间。
- [ ] `LP-SEC-001` 普通用户不能读取受限客户真名。
- [ ] `LP-SEC-002` 管理写接口继续要求会话、权限和CSRF。
- [ ] `LP-SEC-003` 普通详情不返回内部路径、Prompt、hash和审计ID。

### 12.13 存量、回补和运行态

- [ ] `LP-MIG-001` 迁移在隔离MySQL执行成功并可清理。
- [ ] `LP-MIG-002` 业务库迁移前有目标、版本和回滚记录。
- [ ] `LP-MIG-003` 旧V3任务归档有preview和单独授权。
- [ ] `LP-MIG-004` 可复用事实不会重复联网。
- [ ] `LP-MIG-005` 回补preview包含目标、专题、来源、调用和风险。
- [ ] `LP-MIG-006` canary限定明确lead IDs和batch ID。
- [ ] `LP-MIG-007` 失败保留模型、Prompt、结果和恢复依据。
- [ ] `LP-MIG-008` 幂等重跑为changed=0或可解释差异。
- [ ] `LP-OPS-001` 健康接口显示补全Worker、队列和熔断状态。
- [ ] `LP-OPS-002` Worker运行不被当作覆盖率证明。
- [ ] `LP-OPS-003` 额度不足时停止领取新网关任务。
- [ ] `LP-OPS-004` 过期租约可以恢复且同一任务只有一个领取者。
- [ ] `LP-OPS-005` 真实canary形成事实、快照、投影、API和DOM证据。

## 13. 金标样本

至少准备：

| 编号 | 样本 | 验证重点 |
|---|---|---|
| G-01 | 资料完整企业 | 完整核心画像 |
| G-02 | 未融资企业 | 未融资有效，金额/估值为`-` |
| G-03 | 多轮融资企业 | 轮次instanceKey、累计金额和最新轮次 |
| G-04 | 估值冲突企业 | 冲突保留，不静默选择 |
| G-05 | 高校成果转化项目 | 人物—高校—实验室—转化关系 |
| G-06 | 只有学历背景 | 不误判成果转化 |
| G-07 | 宣传客户 | L0，不进入已验证客户 |
| G-08 | 试点客户 | L2和试点证据 |
| G-09 | 订单与回款客户 | L4/L5确定性派生 |
| G-10 | 保密客户 | 响应和DOM只出现脱敏标签 |
| G-11 | 科研成果无公司 | 融资、客户、交易为not_applicable |
| G-12 | 同名企业 | 主体ambiguous，不串事实 |
| G-13 | 注销企业 | 入池拒绝且不创建任务 |
| G-14 | 网络/额度失败 | 重试、死信和恢复正确 |
| G-15 | 重复来源事件 | 幂等，不重复任务、事实和投影 |
| G-16 | 管理员手工评级 | 按需评分可用且不自动触发 |
| G-17 | 跨币种多轮融资 | 分币种累计，无依据时不强制折算 |
| G-18 | 金额未披露融资 | 计轮次、不按0计入累计金额 |
| G-19 | 同日两条融资报道 | 完成时间不足时冲突，不按抓取顺序选最新 |
| G-20 | 历史高校任职/过期许可 | 不显示为当前有效关系 |
| G-21 | A级宣传客户 | L0且不进入tierACount/verifiedCustomerCount |
| G-22 | 已过期认证和条件化良率 | 详情标明有效期、产品/产线和测试条件 |

## 14. 测试与证据矩阵

| 层级 | 建议命令/方式 | 证明内容 | 写入边界 |
|---|---|---|---|
| 类型检查 | `npm run check:types` | 前后端契约一致 | 不写业务库 |
| 入池契约 | `npm run accept:lead-pipeline-events`等 | raw event、ready、幂等和准入 | 默认fixture/隔离 |
| 补全契约 | `npm run accept:lead-enrichment` | 专题、证据、快照和无自动评分 | 不写业务库 |
| 投资画像单测 | 目标`node --test` | 归并、阶段、冲突和脱敏 | 不写业务库 |
| UI源码验收 | `npm run accept:lead-pool-ui` | 七列、无评级、空值和结构 | 不写业务库 |
| HTTP验收 | `npm run accept:lead-pool-http` | 查询、分页、权限和轻量DTO | 默认fixture |
| 服务端全测 | `npm run test:server` | 路由、服务和回归 | 先核对`.env` |
| 隔离MySQL | `npm run accept:lead-enrichment:mysql`及新增画像验收 | 迁移、事实、快照、投影和幂等 | 仅隔离库 |
| 真实搜索 | `npm run accept:lead-enrichment:real-web` | 真实搜索和抓取门禁 | 默认只读，不写库 |
| 只读业务审计 | `npm run audit:lead-investment-profile` | 覆盖、冲突和字典缺口 | 只读 |
| 回补预览 | `npm run backfill:lead-investment-profiles:preview` | 目标和样本 | 只读 |
| 平台门禁 | `npm run check:platform` | 类型、数据库和单服务边界 | 按现有脚本边界 |
| 构建 | `npm run build` | 候选Web/Server | 可能只生成候选 |
| 浏览器 | 已登录普通用户和管理员 | DOM、权限、外链和交互 | 不写业务数据 |

建议新增或扩展：

- 手工新增自动补全契约测试；
- V4任务schema与V3任务隔离测试；
- 无自动V3评分验收脚本；
- 投资画像迁移和查询隔离MySQL验收；
- 投资画像集合对账脚本；
- 保密客户网络响应和DOM安全测试；
- 管理员按需评级回归测试。
- 字段全链路矩阵和DTO契约测试；
- 融资/估值Decimal、单位、币种、时间语义和分币种累计测试；
- 客户L0—L5分层计数、A级交叉口径和可见总数测试；
- 字典Hash、stale原因、适用维度和多时间字段测试；
- 历史高校关系、过期认证和条件化产能/良率详情测试。

## 15. 发布与回滚

### 15.1 发布顺序

1. 冻结产品口径和schema版本；
2. 在隔离MySQL验证迁移；
3. 完成源码和直接测试；
4. 对业务库生成只读审计和迁移preview；
5. 经授权执行向前兼容迁移；
6. 构建候选并记录build ID；
7. 经授权确认旧进程归属并停止；
8. 激活候选并启动统一服务；
9. 验证健康、组件、静态资源、API和真实DOM；
10. 运行小批canary；
11. 观察错误率、额度、队列和画像质量；
12. 再决定全量回补和生产发布。

### 15.2 回滚

- Web/Server构建可恢复上一构建指针；
- 新增表保持向前兼容，旧服务不依赖新字段；
- 投影是可重建读模型，可回退到上一快照投影；
- 事实和证据不物理删除，错误事实使用rejected/superseded/冲突裁决；
- 历史V3结果不因本次调整删除；
- 旧任务归档必须保留审计和恢复依据；
- 失败时不得以清空事实表、快照表或评分历史作为常规回滚。

## 16. 交付物

- 本开发与验收方案；
- 产品决策和字段字典；
- 统一入池契约与入口对账表；
- V4补全schema、Prompt和事实契约；
- 迁移、投影服务和重建脚本；
- API DTO、筛选、排序和索引；
- 七列列表和详情证据落点；
- V3自动评级退出和管理员按需评级能力；
- 单元、契约、HTTP、MySQL、UI和浏览器验收；
- 只读覆盖审计和存量任务preview；
- canary和批次报告；
- 构建、激活、健康、静态资源和真实DOM证据；
- 回滚记录和未完成项清单。

## 17. 开发前必须确认的问题

| 决策ID | 问题 | 推荐方案 | 未确认时处理 |
|---|---|---|---|
| `LP-DEC-001` | 是否停止自动V3评级 | 停止 | 保持现状但不得启动回补 |
| `LP-DEC-002` | 是否保留管理员按需评级 | 保留 | 仅保留历史只读，不开放新评分 |
| `LP-DEC-003` | 新补全schema名称 | `lead-enrichment-v4-investment-profile` | 不复用V3语义 |
| `LP-DEC-004` | 默认专题 | 8个核心专题 | 未确认专题不自动执行 |
| `LP-DEC-005` | 最新动态 | 非阻塞低优先级 | 不阻塞画像 |
| `LP-DEC-006` | 重点机构定义 | 客户维护机构字典 | 不显示重点机构标签 |
| `LP-DEC-007` | 大客户定义 | A/B/C + L0—L5二维 | 不以规模替代验证阶段 |
| `LP-DEC-008` | 保密客户 | 脱敏标签+权限 | 默认只显示“某头部客户” |
| `LP-DEC-009` | 旧V3任务 | preview后审计归档 | 保持暂停，不直接删除 |
| `LP-DEC-010` | 业务库回补 | canary后有界执行 | 默认只读preview |
| `LP-DEC-011` | 已验证客户门槛 | L3及以上 | 未确认时L0—L2不计入已验证客户 |
| `LP-DEC-012` | A级客户计数 | A级且达到已验证客户门槛 | 不统计A级宣传提及 |
| `LP-DEC-013` | 跨币种累计 | 默认分币种展示 | 无汇率来源和日期时不折算 |
| `LP-DEC-014` | 融资最新日期 | 已完成事件的completedAt | 不用报道或抓取时间替代 |
| `LP-DEC-015` | 多值展示 | 前两项+当前用户可见`+N` | 不返回可能泄露受限关系的总数 |

## 18. 完成定义

只有同时满足以下条件，才能标记为完成：

1. 产品决策、字典和schema已确认；
2. 所有入池入口统一通过Pipeline；
3. 手工新增可以自动创建补全任务；
4. 核心画像事实、证据、冲突、快照和投影验收通过；
5. 快照完成后不自动创建V3评分任务；
6. 普通列表和详情不依赖或展示V3评级；
7. 显示集合与筛选集合对账差异为0；
8. 企业、科研、未融资、多轮融资、冲突、高校转化和保密客户金标通过；
9. 未出现受限客户、证据正文、内部路径或密钥泄露；
10. 直接测试、类型检查、服务端测试、平台门禁和构建结果明确；
11. 若执行数据回补，canary、幂等、失败恢复和批次报告齐全；
12. 若要求本地生效，已验证监听、健康、资源、API和真实DOM；
13. 若要求生产发布，已验证生产schema、在线资源和真实业务样本；
14. 源码、测试、迁移、数据回补、本地激活和生产发布分别有独立证据；
15. 主体匹配、金额规范化、客户计数、证据时间、字典绑定和明确DTO验收通过；
16. 所有新增字段都有事实键、实例粒度、验证规则、快照、投影/详情落点、权限和验收样本；
17. 未完成、跳过、环境缺失和需授权事项全部显式记录。
