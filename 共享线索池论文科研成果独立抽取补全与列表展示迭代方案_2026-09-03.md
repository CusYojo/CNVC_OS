# 共享线索池论文/科研成果独立抽取补全与列表展示迭代方案

> 日期：2026-09-03
>
> 项目：`sbl_jedi` 共享线索池
>
> 文档性质：开发修改方案；实际实施状态与未完成门禁见第 16 节

## 1. 结论

论文和科研成果需要单独的数据抽取、联网补全、证据投影与列表展示契约。

现有论文链路不是从零开始：系统已经能够从 arXiv、OpenAlex、Crossref、DataCite、Zenodo 等来源获取标题、摘要、作者、机构、公开时间、成果类型、许可、代码和数据集线索，并已具备作者保护合并、论文身份规范化、来源正文抓取、事实证据、冲突、快照和主体关系能力。

本次迭代不重建整套基础设施，而是在现有链路上补齐三个断点：

1. 新增科研专用的事实字典、查询计划和适用性规则，不再直接使用企业化的查询词与字段语义；
2. 新增 `lead-research-profile-v1` 科研成果画像投影，不再把论文强行投影为企业投资画像；
3. 共享线索池改为统一七列、按主体类型自适应展示，论文行展示研究方向、团队机构、成果进展和转化信号，不展示论文自身不存在的融资与估值。

本方案与 V3 评级解耦。科研成果画像可以独立生成和展示，不以 V3 评级是否执行为前提，也不自动触发 V3 评级。

## 2. 本次只读基线

### 2.1 源码基线

现有能力包括：

- `paperMetadata.ts`：刷新论文元数据时保护已有作者列表，空刷新不能覆盖有效作者；
- `paperIdentity.ts`：从论文标题提取简洁成果名，同时保留完整论文标题；
- `paperResearchMetadataService.ts`：解析 arXiv 页面中的机构、作者贡献、论文许可、代码和数据集链接；
- `externalPaperMetadataService.ts`：适配 Crossref、DataCite、Zenodo 的 DOI 元数据、作者、机构、日期、成果类型和许可；
- `radarCollectorService.ts`：采集 arXiv/OpenAlex，保存来源身份、作者稳定 ID、逐人机构、研究团队、许可和内容链接；
- `leadEnrichmentWorkerService.ts`：把确定性论文元数据写入 `paper.*`、`research.*` 事实，并进入统一证据、冲突和快照链路；
- `leadEnrichmentContract.ts`：科研主体已排除融资、客户和交易等不适用专题；
- `LeadDetailPage.tsx`：详情页已按科研主体展示作者、机构、成果类型和权利信息。

### 2.2 当前真实数据基线

以下为 2026-09-03 对当前连接库的只读快照，数量会随采集和任务执行变化：

| 指标 | 当前数量 | 覆盖率 |
| --- | ---: | ---: |
| 有效论文线索 | 273 | 100% |
| 作者 | 273 | 100% |
| 研究分类 | 273 | 100% |
| 成果公开时间 | 273 | 100% |
| 研究团队 | 273 | 100% |
| 所属机构 | 154 | 56.4% |
| 逐人作者—机构关系 | 0 | 0% |
| 成果类型 | 7 | 2.6% |
| 期刊/会议/仓储 | 13 | 4.8% |
| DOI | 7 | 2.6% |
| 论文许可 | 269 | 98.5% |
| 代码链接 | 6 | 2.2% |
| 数据集链接 | 3 | 1.1% |
| 已确认知识产权归属 | 0 | 0% |

当前事实层中：

- 40 条论文形成了当前有效的 `paper.*` 已验证事实，共 84 条；
- 仅 2 条论文形成了 `research.*` 已验证事实，共 2 条；
- 273 条旧 `lead-enrichment-v3` 论文任务仍为 `queued`；
- 40 条论文存在 V4 研究结果，但状态均为 `review`，尚未形成可直接支撑新列表的完整科研画像；
- 当前列表接口最终只保留论文渠道和实验室轻量信息，没有输出作者、机构、发表状态、成果资源和转化信号的独立列表投影。

### 2.3 核心问题

1. 论文有元数据，但列表没有消费这些数据；
2. 通用专题名称可复用，但 `basic_profile`、`products`、`technology_ip`、`industrialization` 的查询词仍偏企业；
3. `research` 主体只是跳过融资、客户和交易，没有形成科研专用的事实输出合同；
4. 论文标题或成果简称可能不唯一，仅用项目名进行全网搜索容易串主体；
5. 作者、机构、代码、数据集和论文许可已有一定覆盖，但方法、应用场景、TRL、复现、原型验证、专利、转化和关联企业覆盖很低；
6. 现有 `lead-investment-profile-v1` 包含融资、估值、客户等企业维度，不适合作为论文列表的主投影；
7. 论文作者不等于商业化团队，论文机构不等于知识产权所有人，开放许可不等于数据、代码或模型许可。

## 3. 目标与边界

### 3.1 本期目标

- 建立论文/科研成果独立的抽取补全路径；
- 建立 `lead-research-profile-v1` 科研成果画像；
- 复用现有事实、证据、冲突、快照和主体关系基础设施；
- 为共享线索池提供轻量、稳定、可分页的科研列表 DTO；
- 企业和论文使用同一张七列表格，但按主体类型展示不同业务语义；
- 缺失内容统一显示 `-`，不在普通列表显示“待核验”“联网候选”“已有资料”等运营文案；
- 论文没有关联经营主体时，不查询、不展示、不猜测企业融资、收入、股权、估值和客户；
- 论文存在明确 Spin-off 或成果转化公司时，建立主体关系，由公司实体单独运行企业补全。

### 3.2 本期不包含

- 删除现有 `paperMeta`、论文回填脚本、历史事实、快照或评级结果；
- 把论文分类直接认定为投资行业；
- 把论文作者自动认定为创始人、公司员工或商业化团队；
- 把作者单位自动认定为知识产权所有人；
- 把论文许可复制为代码、数据集、模型或专利许可；
- 把科研资助、联合研究、数据提供方或试点方认定为企业融资或正式客户；
- 未经单独确认，对 273 条论文执行全量联网研究；
- 自动执行 V3 评级或用科研画像覆盖历史评级；
- 放宽鉴权、SSRF、Origin、引用匹配、来源抓取或证据等级门禁；
- 修改专属项目既有评分流程；
- 为本次列表调整重构无关模块。

## 4. 总体架构

```text
论文来源
  → arXiv / OpenAlex / DOI / 出版社 / 仓储适配
  → 论文身份规范化与正文/附件分类
  → 确定性元数据抽取 paperMeta
  → 科研主体、研究团队、关联企业实体图
  → 科研专用缺口规划
  → 按缺口执行受控联网研究
  → 原文回抓与连续引用校验
  → lead_facts / lead_fact_evidence / conflicts
  → 不可变 lead_enrichment_snapshot
  → lead-research-profile-v1 投影
  → 列表 researchProfile DTO
  → 企业/科研自适应七列展示
```

数据职责：

| 层级 | 权威内容 | 是否直接用于列表 |
| --- | --- | --- |
| `radarProfile.paperMeta` | 来源适配后的确定性论文元数据 | 只作投影输入，不直接铺满列表 |
| `lead_facts` + evidence | 经过主体、原文引用和来源校验的事实 | 是，科研画像的事实权威层 |
| `lead_enrichment_snapshots` | 某次研究的不可变事实快照 | 是，绑定科研画像版本 |
| `lead_research_profile_projections` | 面向查询和展示的派生投影 | 是，列表主读取层 |
| `lead_investment_profile_projections` | 企业投资画像 | 仅企业主体使用 |

## 5. 科研成果画像契约

新增服务端契约：

```ts
type LeadResearchProfile = {
  schemaVersion: 'lead-research-profile-v1'
  projectionVersion: 'lead-research-profile-projection-v1'
  enrichmentSchemaVersion: string
  snapshotId?: string
  snapshotHash?: string

  subject: {
    leadId: string
    name: string
    subjectType: 'research'
    provider?: 'arxiv' | 'openalex' | 'crossref' | 'datacite' | 'zenodo' | 'publisher'
    sourceId?: string
    arxivId?: string
    openAlexId?: string
    doi?: string
    version?: string
  }

  identity: {
    titleZh?: string
    titleOriginal?: string
    projectName?: string
    resourceType?: string
    publicationStatus?: 'preprint' | 'published' | 'accepted' | 'repository_record' | 'undisclosed'
    venue?: string
    publishedAt?: string
  }

  direction: {
    researchCategories: string[]
    researchDirections: string[]
    researchProblem?: string
    methods: string[]
    primaryMethod?: string
  }

  team: {
    authors: Array<{
      name: string
      role?: string
      orcid?: string
      openAlexAuthorId?: string
      identityStatus?: 'confirmed' | 'claimed' | 'ambiguous'
    }>
    authorCount: number
    affiliations: string[]
    authorAffiliations: Array<{ author: string; affiliation: string }>
    lab?: string
  }

  progress: {
    resourceType?: string
    venue?: string
    publishedAt?: string
    doi?: string
    codeStatus?: 'available' | 'missing'
    datasetStatus?: 'available' | 'missing'
    modelStatus?: 'available' | 'missing'
    reproducibility?: string
  }

  valueAndTransfer: {
    applicationScenarios: string[]
    trl?: string
    prototypeStatus?: string
    validationSignals: string[]
    commercializationStage?: string
    transferStatus?: string
    spinOffCompanies: Array<{ entityId?: string; name: string; relationStatus: string }>
    researchPartners: string[]
  }

  rights: {
    articleLicense?: string
    datasetLicense?: string
    codeLicense?: string
    modelLicense?: string
    patentCount: number
    patentStatus?: string
    intellectualPropertyStatus?: string
    intellectualPropertyOwner?: string
  }

  latestDevelopments: Array<{
    occurredAt?: string
    title: string
    eventType?: string
  }>

  dataStatus: {
    status: 'verified' | 'partial' | 'conflicted' | 'missing' | 'stale'
    dimensionStates: Record<string, 'verified' | 'partial' | 'conflicted' | 'missing' | 'not_applicable'>
    conflictCount: number
    updatedAt?: string
    projectedAt: string
  }

  sourceFactIds: string[]
}
```

约束：

- 投影只接受当前有效且满足展示门禁的事实；
- 原始搜索结果、提示词、模型推理、内部任务状态和完整引用正文不进入普通列表 DTO；
- 数组字段必须去重、限长并保留总数；
- `sourceFactIds`、快照 ID、快照哈希必须保留在服务端投影，用于审计和失效判断；
- 投影是派生数据，事实和证据仍是权威来源；
- 论文更新版本、作者、机构、许可、代码、数据集、专利或转化关系变化后生成新快照并刷新投影；
- 科研画像不能写入企业融资、估值和客户字段。

## 6. 确定性抽取改造

### 6.1 保留并统一现有来源适配器

优先级：

1. 来源原文或官方出版页面；
2. arXiv/OpenAlex 的稳定身份和元数据；
3. Crossref/DataCite/Zenodo 等 DOI 或仓储元数据；
4. 其他公开页面仅用于补缺，不覆盖高优先级确定性字段。

统一输出 `NormalizedPaperSource`：

```text
provider
sourceId
arxivId/openAlexId/doi/version
landingPageUrl/fullTextUrl/pdfUrl
title/abstract/authors/categories
publishedAt/updatedAt/venue/resourceType
paperAuthors/affiliations/authorAffiliations
rights/contentLinks/sourceStatus/reviewReasons
```

### 6.2 内容类型必须分离

- `landing_page`：论文或成果落地页；
- `paper_html`：论文正文 HTML；
- `paper_pdf`：论文正文 PDF；
- `dataset`：数据集；
- `code_repository`：代码仓库；
- `model_repository`：模型或权重；
- `supplement`：补充材料；
- `unknown`：无法安全判断。

XLSX、ZIP、图片、补充材料不能作为论文正文。链接类型错误时进入 `review`，不得继续生成“已解析论文正文”的结论。

### 6.3 作者与机构

- 作者顺序以来源原文为准，模型不能重排；
- 空来源不能覆盖已有作者；
- OpenAlex Author ID、ORCID 作为稳定标识；
- arXiv 只有作者名且无法消歧时，`identityStatus=ambiguous`；
- 机构列表和逐人作者—机构关系分开保存；
- 没有明确逐人关系时只展示机构列表，不推断作者归属；
- 通讯作者、共同第一作者、共同资深作者只接受原文明示。

### 6.4 论文、代码、数据、模型和知识产权

分别抽取和验证：

- 论文许可；
- 数据集地址与数据许可；
- 代码地址与代码许可；
- 模型/权重地址与模型许可；
- 专利号、申请人、权利人和法律状态；
- 知识产权所有人；
- 技术转让、许可、作价入股和 Spin-off 关系。

任一项缺少独立来源时，不得从另一项许可或作者机构推导。

## 7. 科研专用联网补全

### 7.1 保留通用专题枚举，新增科研合同

本期不新增第二套任务表，也不改变 13 专题历史枚举。由 `entityType=research` 选择独立的 `RESEARCH_TOPIC_CONTRACTS`：

| 现有专题键 | 科研专用目标 | 允许的主要事实 |
| --- | --- | --- |
| `basic_profile` | 成果身份、研究问题、方向和方法 | `paper.*`、`research.problem`、`research.direction`、`research.method` |
| `team` | 作者、贡献、机构、实验室和稳定身份 | `paper.authors`、`paper.author_contributions`、`paper.affiliations`、`paper.author_affiliations`、`team.institution` |
| `products` | 数据集、代码、模型、工具、原型和可交付成果 | `research.artifact`、`research.prototype_status`、`product.form`、`product.use_case` |
| `technology_ip` | 技术指标、许可、专利、IP和技术转让 | `technology.*`、`license.*`、`patent.*`、`ip.owner`、`technology.transfer_status` |
| `industrialization` | TRL、复现性、工程化、场景验证 | `research.trl`、`research.reproducibility`、`research.validation`、`profile.application_scenario` |
| `latest_developments` | 新版本、接收、获奖、开源、合作和转化 | `news.event`、`news.event_date` |

论文主体默认保持不适用：

- `financing`；
- `ownership`；
- `customers_contracts`；
- `financial_operations`；
- `transaction_exit`。

当确认存在关联公司时，不把这些专题改为论文适用，而是给关联公司实体单独建立企业补全任务。

### 7.2 科研查询计划

每次查询至少绑定两个稳定条件，优先组合：

- 完整论文标题；
- arXiv ID、OpenAlex Work ID 或 DOI；
- 作者名 + 机构；
- 项目/模型/数据集专名；
- 官方代码仓库或项目主页域名。

示例：

```text
"完整论文标题" DOI OR arXiv
"完整论文标题" code dataset model weights
"成果专名" site:github.com OR site:huggingface.co
"作者姓名" "机构名称" ORCID laboratory
"完整论文标题" patent technology transfer spin-off
"成果专名" prototype pilot validation commercialization
```

禁止只用“人工智能、机器人、芯片”等泛行业词搜索并绑定到单篇论文。

### 7.3 缺口驱动

确定性抽取完成后生成 `researchGapPlan`，只搜索缺失维度：

- 作者和机构已由来源确认时，不再交给模型重复猜测；
- 论文许可已确认时，只补数据、代码或模型的独立许可；
- 没有代码、数据集或转化线索时，可以搜索是否存在，不允许把“未搜到”写成“不存在”；
- 主体身份为 `ambiguous` 时，只运行身份修复，不运行专利、转化或关联公司研究；
- 同一快照、同一科研合同版本、同一缺口计划只能创建一个有效任务。

### 7.4 证据门禁

每条联网事实必须同时满足：

1. 使用本轮实际返回的直接 URL；
2. 宿主重新抓取公开页面或 PDF；
3. `quote` 能在抓取正文中连续匹配；
4. 页面正文能够绑定目标论文、成果、作者或稳定 ID；
5. 事实键属于当前科研专题允许集合；
6. 专利、转让、关联公司等重大事实必须绑定对应法律主体；
7. 证据冲突进入候选冲突，不自动拼接成一个确定结论。

## 8. 主体关系

科研链路至少维护：

```text
research --authored_by_team--> team
research --affiliated_with--> institution
research --has_artifact--> dataset/code/model/prototype
research --has_patent--> patent
research --licensed_to--> company/institution
research --commercialized_by--> company
research --validated_by--> partner
```

规则：

- 论文作者属于研究团队，不自动属于商业化团队；
- 作者在公司任职或创业必须有单独来源；
- `commercialized_by`、`licensed_to`、`spin_off` 不能只由名称相似产生；
- 关联公司确认后，企业融资、估值、客户和经营事实绑定公司实体；
- 论文列表可以显示“关联企业：某公司”，但不能显示成“论文融资”。

## 9. 科研画像投影与数据库

### 9.1 新增投影表

建议在下一个可用迁移中新增：

```text
lead_research_profile_projections
```

核心字段：

```text
lead_id
profile_schema_version
projection_version
enrichment_schema_version
snapshot_id
snapshot_hash
provider/source_id/arxiv_id/openalex_id/doi/version
title_zh/title_original/project_name
resource_type/publication_status/venue/published_at
research_categories/research_directions/research_problem/methods
authors/author_count/affiliations/author_affiliations/lab
artifacts/reproducibility
application_scenarios/trl/prototype_status/validation_signals
commercialization_stage/transfer_status/spin_off_companies/research_partners
rights_summary/patent_count
dimension_states/profile_status/conflict_count
source_fact_ids/facts_updated_at/source_freshness_at/projected_at/updated_at
```

索引至少包括：

- `lead_id` 主键；
- `profile_status, updated_at`；
- `published_at, lead_id`；
- `provider, source_id`；
- `facts_updated_at, lead_id`。

### 9.2 投影触发

- 补全快照提交后，根据主体类型二选一：
  - `company/project/team` 进入投资画像投影；
  - `research` 进入科研成果画像投影；
- 同一 `leadId + snapshotHash + projectionVersion` 幂等；
- 新事实、冲突裁决、论文版本或来源变化后重建；
- 投影失败不能影响事实和快照提交，进入可恢复任务；
- 关闭科研画像功能开关时，列表可回退现有论文基本信息，不删除投影数据。

## 10. 列表 API 与页面展示

### 10.1 统一七列表头

推荐改为：

| 列 | 企业线索 | 论文/科研成果 |
| --- | --- | --- |
| 线索主体 | 项目名、公司名、地区 | 成果名、原始论文名、成果类型 |
| 方向 / 产品 | 行业、产品路线 | 研究方向、核心方法、成果形态 |
| 团队 / 机构 | 投资机构、高校背景 | 第一/通讯作者、实验室、所属机构 |
| 进展 / 阶段 | 融资状态、轮次、本轮金额 | 发表状态、时间、期刊/会议、代码/数据状态 |
| 价值 / 转化 | 最新估值 | 应用场景、TRL、验证、专利、转化或关联企业 |
| 最新动态 | 企业动态 | 论文版本、接收、开源、获奖、合作、转化动态 |
| 更新时间 | 数据更新时间 | 数据更新时间 |

### 10.2 列表 DTO

`LeadListItem` 新增：

```ts
researchProfile?: LeadResearchProfileListItem
```

轻量 DTO 只包含每列需要的前两项、总数和聚合状态，目标单条不超过 4KB：

```ts
type LeadResearchProfileListItem = {
  identity: { titleOriginal?: string; resourceType?: string }
  direction: { directions: string[]; primaryMethod?: string }
  team: { authors: Array<{ name: string; role?: string }>; authorCount: number; affiliations: string[] }
  progress: { publicationStatus?: string; venue?: string; publishedAt?: string; codeAvailable: boolean; datasetAvailable: boolean }
  valueAndTransfer: { applicationScenarios: string[]; trl?: string; signals: string[]; relatedCompanies: string[] }
  dataStatus: { status: string; updatedAt?: string }
}
```

普通列表不返回：

- 完整摘要和正文；
- 完整作者、机构、事实和来源列表；
- 内部提示词、任务指标、错误栈；
- 冲突候选原文；
- 未验证的敏感关系和关联公司猜测。

### 10.3 展示优先级

论文行：

1. 有科研画像时使用科研画像；
2. 科研画像缺失时，回退 `paperMeta` 中确定性元数据；
3. 两者都没有时显示 `-`；
4. 不回退企业融资、估值、客户或企业投资机构；
5. 不显示“联网候选”“已有资料”“待核验”“未披露”等运营提示；
6. 冲突事实不进入普通确定值，管理员仍可在审计入口复核。

## 11. 代码修改清单

### 11.1 新增

- `server/src/contracts/leadResearchProfileContract.ts`
- `server/src/services/leadResearchProfileProjectionService.ts`
- `server/src/scripts/backfillLeadResearchProfiles.ts`
- 下一可用数据库迁移：`lead_research_profile_projections`
- `server/tests/leadResearchProfileProjection.test.ts`
- `server/tests/leadResearchTopicContract.test.ts`
- `server/tests/leadResearchProfileUi.test.tsx`
- `server/src/scripts/leadResearchProfileAudit.ts`

### 11.2 修改

- `server/src/services/leadEnrichmentContract.ts`
  - 明确科研适用专题；
  - 增加科研合同版本和缺口计划版本。
- `server/src/services/leadTopicWebResearchService.ts`
  - 新增 `RESEARCH_TOPIC_CONTRACTS`；
  - 根据 `entityType` 切换事实键、查询词和提示词；
  - 查询绑定论文稳定 ID。
- `server/src/services/leadEnrichmentWorkerService.ts`
  - 确定性论文事实优先；
  - 按科研缺口执行联网补全；
  - 关联公司事实路由到公司实体。
- `server/src/services/leadEnrichmentSnapshotPostCommit.ts`
  - `research` 快照提交后生成科研画像投影。
- `server/src/services/paperResearchMetadataService.ts`
  - 补齐代码、数据、模型和原型内容类型；
  - 保持许可相互独立。
- `server/src/services/radarCollectorService.ts`
  - 统一 arXiv/OpenAlex 标准输出；
  - 保存版本和更新时间。
- `server/src/services/aiSummaryService.ts`
  - 查询科研投影；
  - 输出轻量 `researchProfile`；
  - 论文不构建企业 `availableData` 回退。
- `src/types/index.ts`
  - 增加科研列表与详情类型。
- `src/pages/SourcingPage.tsx`
  - 使用中性七列表头；
  - 企业和科研分别渲染；
  - 统一空值规则。
- `src/pages/LeadDetailPage.tsx`
  - 保留来源严谨的科研详情；
  - 可逐步切换至科研画像摘要 + 已验证事实来源。
- `server/src/scripts/leadPoolUiAcceptance.ts`
  - 增加企业/科研双路径静态验收。

### 11.3 复用且不重写

- `paperMetadata.ts` 的作者保护合并；
- `paperIdentity.ts` 的成果名解析；
- arXiv/OpenAlex/Crossref/DataCite/Zenodo 来源适配；
- `lead_facts`、`lead_fact_evidence`、`lead_fact_conflicts`；
- `lead_enrichment_snapshots`；
- 现有主体图、来源抓取、SSRF、连续引用和冲突门禁；
- 现有管理员复核接口。

## 12. 实施阶段

### I0：契约冻结与只读盘点

- 确认科研画像字段、事实键和展示优先级；
- 生成 273 条论文只读覆盖率报告；
- 建立 arXiv、OpenAlex、DOI、Zenodo、机构缺失、同名作者、未来日期、补充附件、关联企业等金标样本；
- 不写库。

退出条件：字段合同和样本清单确认，当前缺口可复现。

### I1：科研画像契约和纯函数投影

- 新增科研画像类型、状态和投影服务；
- 使用内存事实样本验证确定性结果、缺失、冲突和不适用；
- 不连接业务库。

退出条件：单元测试和类型检查通过。

### I2：隔离库迁移与投影验收

- 在确认的隔离数据库执行下一可用迁移；
- 验证建表、索引、回滚、幂等和外键；
- 验证同一快照重复投影不产生重复数据；
- 清理隔离样本。

退出条件：隔离库 DDL/DML 验收通过且残留为 0。

### I3：科研专用研究合同

- 新增科研事实键、查询词、提示词和缺口计划；
- 仅运行 fixture 和受控公开来源样本；
- 验证不会调用企业融资、客户和交易查询；
- 验证重大事实主体绑定和引用门禁。

退出条件：科研合同自动化验收通过。

### I4：列表 API 和 UI

- 列表查询关联科研投影；
- DTO 限长并保持分页性能；
- 页面改为统一中性列和双路径渲染；
- 论文无融资、估值、候选状态文案；
- 真实浏览器验证混合列表和科研筛选。

退出条件：API、组件、真实 DOM 和响应式验收通过。

### I5：历史确定性回填

- 先执行 `--preview`，只从已有 `paperMeta` 和已验证事实生成科研投影；
- 不联网、不改原始事实；
- 经确认后分批 `--apply`；
- 重跑应 `changed=0`。

退出条件：273 条范围对账一致，无覆盖已有人工裁决。

### I6：联网补全灰度

- 选择 10 至 20 条覆盖不同来源和缺口的 canary；
- 只研究缺失维度；
- 核查搜索来源、抓取正文、quote、事实、快照和科研投影；
- canary 通过后，再决定是否扩大批次；
- 未经确认不执行 273 条全量联网研究。

退出条件：准确率、来源完整性、主体误绑定率和可恢复性达到门禁。

## 13. 验收方案

### 13.1 数据与来源

- [ ] `RP-DATA-001` 论文身份优先使用 arXiv/OpenAlex/DOI 稳定 ID。
- [ ] `RP-DATA-002` arXiv 版本号不被静默丢弃。
- [ ] `RP-DATA-003` OpenAlex Work 不被标记为 arXiv。
- [ ] `RP-DATA-004` 未来发表日期进入 review，不作为正常公开时间。
- [ ] `RP-DATA-005` XLSX、ZIP、图片和 supplement 不作为论文正文。
- [ ] `RP-DATA-006` 空刷新不覆盖已有作者。
- [ ] `RP-DATA-007` 作者顺序和贡献角色不由模型重排。
- [ ] `RP-DATA-008` 无逐人证据时不生成作者—机构关系。
- [ ] `RP-DATA-009` 论文、代码、数据、模型和 IP 许可彼此独立。

### 13.2 科研联网补全

- [ ] `RP-WEB-001` 科研主体使用科研专用查询词和事实键。
- [ ] `RP-WEB-002` 查询至少绑定论文标题、稳定 ID、作者机构等两个身份条件。
- [ ] `RP-WEB-003` 已确定字段不会重复调用模型猜测。
- [ ] `RP-WEB-004` 每条事实的 URL 来自本轮搜索结果。
- [ ] `RP-WEB-005` quote 能在宿主抓取正文中连续匹配。
- [ ] `RP-WEB-006` 泛行业页面不能绑定到单篇论文。
- [ ] `RP-WEB-007` 未搜到代码或转化不被写成“不存在”。
- [ ] `RP-WEB-008` 专利、转让和 Spin-off 必须绑定法律主体。
- [ ] `RP-WEB-009` 冲突进入冲突表，不拼接成确定结论。

### 13.3 投影

- [ ] `RP-PROJ-001` 科研快照生成 `lead-research-profile-v1`。
- [ ] `RP-PROJ-002` 投影绑定正确快照 ID、哈希和事实 ID。
- [ ] `RP-PROJ-003` 同一快照重复执行幂等。
- [ ] `RP-PROJ-004` 新快照使旧投影失效或被原子替换。
- [ ] `RP-PROJ-005` 冲突事实不进入普通确定值。
- [ ] `RP-PROJ-006` 论文不存在企业融资、估值和客户投影。
- [ ] `RP-PROJ-007` 关联公司融资只绑定公司实体。

### 13.4 API 与页面

- [ ] `RP-UI-001` 混合列表使用统一中性七列表头。
- [ ] `RP-UI-002` 企业行继续显示行业、产品、融资和估值。
- [ ] `RP-UI-003` 论文行显示研究方向、方法、作者、机构、成果进展和转化信号。
- [ ] `RP-UI-004` 论文行不显示论文融资或论文估值。
- [ ] `RP-UI-005` 缺失值显示 `-`。
- [ ] `RP-UI-006` DOM 不出现“联网候选”“已有资料”“待核验”等运营文案。
- [ ] `RP-UI-007` 只有合法外部来源链接可在新窗口打开。
- [ ] `RP-UI-008` 列表 DTO 不泄露原始提示词、内部任务和完整审计事实。
- [ ] `RP-UI-009` 单条科研列表 DTO 不超过 4KB。
- [ ] `RP-UI-010` 20/50/100 条分页不出现明显性能回退。

### 13.5 金标样本

至少覆盖：

| 样本 | 预期 |
| --- | --- |
| arXiv 纯论文，无公司 | 显示方向、作者、机构和发表信息；融资估值不出现 |
| OpenAlex 论文 | 保留 OpenAlex Work ID、作者稳定 ID 和逐人机构 |
| Crossref/DataCite/Zenodo | 正确识别 DOI、仓储、成果类型和日期 |
| 机构列表但无法逐人对应 | 展示机构列表，不分配给具体作者 |
| 同名作者 | 保持 ambiguous，不拼接履历 |
| 未来发表日期 | 使用元数据创建时间或进入 review |
| 链接指向补充 XLSX | 不作为论文正文 |
| 有代码无 License | 显示代码存在，许可不显示为已确认 |
| 论文许可与数据许可不同 | 分别展示，不互相覆盖 |
| 有技术转让或 Spin-off | 建立关联企业，企业事实独立补全 |
| 只有联合研究或科研资助 | 不显示为融资或正式客户 |
| 新版本变更作者或许可 | 新快照、新投影，旧证据可追溯 |

## 14. 发布、开关与回滚

建议增加：

```text
LEAD_RESEARCH_PROFILE_ENABLED=false
LEAD_RESEARCH_WEB_ENRICHMENT_ENABLED=false
LEAD_POOL_RESEARCH_ADAPTIVE_COLUMNS=false
```

发布顺序：

1. 先发布兼容新表但默认关闭的后端；
2. 执行并核验数据库迁移；
3. 开启科研投影，保持联网研究关闭；
4. 对历史确定性数据执行小批回填；
5. 开启自适应列表；
6. 执行受控联网 canary；
7. 根据验收决定扩大范围。

回滚：

- 关闭自适应列表，恢复当前企业列展示；
- 关闭科研画像读取，回退 `paperMeta` 基础字段；
- 停止新增科研联网任务；
- 保留已生成事实、证据、快照和投影，不删除审计数据；
- 数据库表暂不删除，待稳定期结束后另行决策。

## 15. 完成定义

只有同时满足以下条件，才能称为本次迭代完成：

1. 论文使用科研专用抽取和联网补全合同；
2. 科研画像与企业投资画像物理或逻辑分离；
3. 现有 273 条论文可由确定性元数据和已验证事实幂等生成科研投影；
4. 列表真实 DOM 按科研语义展示，不出现论文融资、论文估值或候选运营文案；
5. 作者、机构、许可、代码、数据、专利和转化关系不发生越权推断；
6. API、隔离库、受控联网 canary、真实页面、权限和回滚验收均有证据；
7. 明确区分源码完成、迁移完成、历史投影回填、联网补全覆盖、运行版本生效和生产发布。

## 16. 2026-09-03 实施与验收记录

本节记录本次迭代的实际执行结果；第 1—15 节保留为设计与验收基线。

### 16.1 已完成

- 已新增 `lead-research-profile-v1` 科研画像契约和独立投影表，科研画像不复用企业投资画像；
- 已按论文元数据和 V4 已验证事实生成科研画像，投影只读取当前 V4 快照；
- 已新增科研专用联网查询合同，查询绑定完整论文标题和 DOI、arXiv、OpenAlex、作者、机构等稳定身份；
- 已实现缺口驱动和新任务截止时间开关：截止时间前的历史任务只做确定性抽取，截止时间后的新入池科研任务才允许联网；
- 已保持 V3 评级解耦，科研画像生成、回填和展示均不触发 V3 自动评级；
- 已将列表改为统一中性七列，并增加独立科研行；缺失值显示 `-`，普通列表不显示候选或待核验运营文案；
- 已将科研画像接入列表 DTO，列表响应不输出 `sourceFactIds`、原始快照或内部任务数据；
- 已增加迁移、投影、联网合同、UI、HTTP、审计、回填和受控预取脚本。

### 16.2 数据库与回填结果

| 项目 | 实际结果 |
| --- | --- |
| 当前迁移 | `0095_add_lead_research_profile_projections` |
| 迁移总数 | 96（`0000`—`0095`） |
| 有效论文线索 | 273 |
| 已生成科研投影 | 273 |
| 投影覆盖率 | 100% |
| 当前状态 | `partial` 273；未把字段不全误报为完整验证 |
| 企业融资字段泄漏 | 0 |
| 超过 4KB 的投影 | 0 |
| 回填模型调用 | 0 |
| 二次幂等回填 | 变更 0、未变更 273、失败 0 |

生产连接库已执行并核验迁移、外键和回填。迁移账户无 `CREATE DATABASE` 权限，因此未能创建一次性隔离库运行完整 DDL/DML 验收；这不是生产迁移失败，但 `RP-DB-009` 隔离库自动清理验收仍需由具备临时建库权限的账户补做。

### 16.3 受控联网 canary

- 仅对 2 条最新论文执行预取验证，不做 273 条历史论文的全量联网研究；
- 首轮共 12 个专题：10 个完成、2 个因上下文预算失败；得到 43 条候选事实、10 条模型接受事实、7 条连续原文引用通过事实；
- 调整科研专题批处理后，仅重试 2 个 `latest_developments` 专题，2 个均正常完成；未发现满足证据门禁的最新动态，因此写入 0 条，不把“未搜到”写成“不存在”；
- canary 只写搜索缓存和运行审计，不写业务事实、快照或科研画像。

### 16.4 自动化与运行验收

- TypeScript 前后端类型检查通过；
- 科研迁移、投影、截止时间、缺口规划、科研/企业查询合同和科研 UI 共 8 项专项测试通过；
- 既有投资画像和统一补全回归测试通过；
- 列表 UI 静态验收、登录态 HTTP 验收、鉴权和分页边界验收通过；
- 真实登录页面已核验统一七列表头、20 条科研行、每行 7 个单元格，未出现企业融资/估值字段和候选运营文案；
- 候选版本 `build-20260903T022757832Z-70907-59d0260e` 已激活，本地统一服务 `127.0.0.1:4100` 健康；
- 平台总门禁中的类型检查和数据库边界检查通过；单服务源码检查仍因仓库既有的 `97/7 source gaps` 精确绑定断言失败，本次未放宽或绕过该门禁。

### 16.5 当前结论

论文独立抽取、科研画像、列表展示和“新入池才按缺口联网”的主流程已经实现并在当前本地运行版本生效。历史 273 条论文已完成确定性画像回填，但没有执行全量历史联网补全；后续新入池论文会按截止时间、适用专题和真实字段缺口进入受控联网流程。
