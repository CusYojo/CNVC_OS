# 共享线索池 1:1 复刻开发与验收计划

> 文档版本：V1.0
> 编制日期：2026-08-25
> 适用项目：`/Users/hyw/Desktop/sbl_jedi`
> 参考项目：`/Users/hyw/Desktop/sbl-product-prototype`
> 评分规范：`/Users/hyw/Desktop/sbl_jedi/评分规则.md`
> 当前状态：待评审
> 本期范围：只迭代“共享线索池”列表页、线索详情页及其专用数据/评级契约，其他业务板块暂不修改。

## 1. 项目目标

本期将当前项目的共享线索池改造成参考项目同板块的 1:1 版本，并接入当前项目的真实数据、权限、线索转换和 AI 评级能力。

目标结果：

1. 普通用户看到的共享线索池列表、筛选、分页、详情和返回行为，与参考项目保持一致。
2. 页面不使用参考项目中的模拟线索、模拟总数、模拟融资、模拟联系人或固定日期。
3. 列表按照《评分规则.md》只展示 A+ 至 C- 或“待评级”，不展示 100 分制分数和长篇分析。
4. 详情页使用同一评级结果展示完整评级依据，主列表等级与详情等级严格一致。
5. 本期页面不展示“批量导入”“上传 BP”“人工复核”“Radar 同步”等运营按钮或入口。
6. 上述运营能力的后端接口、权限、任务、恢复和审计链继续保留，本期不删除、不降级。
7. “我的专属项目”、AI 助手、会议、OA、风险、系统管理等其他板块保持原样。

## 2. 已确认的产品决策

### 2.1 1:1 复刻的含义

“1:1 复刻”包括：

- 页面信息架构一致；
- 列表列数、列顺序和主要宽度比例一致；
- 标题、搜索、筛选、列表、分页、详情页的视觉层级一致；
- 行点击、键盘打开、详情返回、筛选恢复、滚动恢复等交互一致；
- 企业线索与科研项目使用参考项目的差异化展示方式；
- 空状态、加载状态、失败状态和不可用状态沿用相同视觉语言。

“1:1 复刻”不包括：

- 复制参考项目的 `mockData`；
- 复制硬编码的公司、联系人、估值、动态、评级和总数；
- 将参考项目的前端模拟评分逻辑带入正式项目；
- 绕过当前项目的服务端分页、权限、审计和真实任务状态；
- 为了填满页面而编造缺失字段。

### 2.2 运营按钮隐藏规则

本期共享线索池页面不渲染以下按钮、菜单项或快捷入口：

- 批量导入；
- 上传 BP；
- 人工复核；
- Radar 同步；
- 与上述能力等价的图标按钮、更多菜单项、悬浮入口或快捷键入口。

实施要求：

- 不采用 `display: none`、透明度、移出屏幕等 CSS 方式隐藏；
- 不把按钮保留在 DOM 中；
- 不保留可被 Tab 聚焦、读屏识别或自动化点击的隐形入口；
- 不通过 URL 参数、前端角色判断或本地存储重新显示这些入口；
- 暂不删除后端 API、数据库表、任务调度、重试、审计记录及现有权限校验；
- 若未来恢复入口，应作为新的产品迭代重新评审，不在本期预留隐藏开关。

### 2.3 评分规范的使用边界

《评分规则.md》中的角色、事实、证据、缺失信息、七维评分、等级和输出结构属于“评级引擎业务规范”。其中“只允许输出合法 JSON”等要求用于约束模型输出，不是本开发计划文档的写作指令。

本期评级必须满足：

- 主列表只显示 A、B、C、D 等级或任务状态；
- 详情页不展示评级研判、分数或发起评级入口；
- 未达到正式评级门槛或证据不足的线索统一显示 D 级，并在列表推荐理由中解释证据缺口；
- 不得将信息缺失按 0 分处理；
- 不得直接把当前旧评分总分换算为新等级；
- 等级、覆盖率、加权分和评级状态由后端确定性计算，不直接信任模型自行计算的结果。

### 2.4 线索删除规则

- 列表为系统管理员提供单条线索删除入口，普通用户不渲染该入口；
- 删除按钮与整行详情跳转隔离，点击后必须先展示包含线索名称的二次确认；
- 确认后调用现有 `DELETE /api/leads/:id`，前端不直接操作数据库；
- 服务端继续执行系统管理员权限校验，并将线索状态软删除为“已删除”；
- 已删除线索从共享池列表和统计中移除，同时清理尚未完成的评级任务；
- 历史导入、Pipeline、实体映射、删除审计和已转化的专属项目继续保留；
- 删除成功后刷新真实总数和当前页；若删除的是非首屏最后一条，则回退到上一页；失败时不移除当前行并展示服务端错误。

## 3. 当前实现与参考实现差异

### 3.1 当前项目已有能力

当前项目已经具备：

- 真实登录、权限和服务端会话；
- 共享线索服务端分页；
- 关键词、行业、地区、渠道筛选；
- 线索详情按需读取；
- AI 评分排队、租约、重试、死信和恢复；
- 线索转为专属项目；
- 线索删除、Radar、BP 上传、批量导入和人工复核的后端业务链；
- 不可变原始事件、模型运行、决策、证据和人工操作审计。

### 3.2 参考项目的目标体验

参考项目提供：

- 独立的“共享线索池”标题区；
- 页面级关键词搜索；
- 线索类型、行业、阶段、地区、渠道、更新时间六项筛选；
- 七列线索列表；
- 企业线索/科研项目两类展示口径；
- 推荐理由、信号和线索评级；
- 最新动态时间线；
- 页码和每页条数；
- 独立线索详情路由；
- 返回后恢复筛选、页码和列表滚动位置；
- 项目简介、主体基础信息、产品与商业化、团队、动态及转项目操作。

### 3.3 必须补齐的数据能力

当前列表接口还需要补齐：

- `leadType`：企业线索或科研项目；
- `stage`：融资阶段或项目阶段；
- `updatedRange`：近 7 天、近 30 天、近 90 天；
- 团队人数或团队信息可用状态；
- 成立时间或成果公开时间；
- 可验证的背景标签；
- 最多两条最新动态；
- V3 主列表等级及评级任务状态。

## 4. 本期范围

### 4.1 本期包含

1. 共享线索池列表页 1:1 重构。
2. 新增独立线索详情页和 `/sourcing/:id` 路由。
3. 服务端筛选和列表展示 DTO 扩展。
4. 共享线索池专用 V3 评级流程。
5. 评级结果版本化与旧结果兼容。
6. 列表状态、详情状态、评分状态和错误状态完整展示。
7. 转为专属项目操作继续可用。
8. 系统管理员可在列表中二次确认后软删除无效线索。
9. 共享线索池相关类型、API、测试和发布验收更新。
10. 对参考页和实现页进行浏览器截图比对。

### 4.2 本期不包含

- 修改其他页面的布局、文字、样式或导航；
- 修改“我的专属项目”的现有评分规则；
- 修改 AI 助手、会议、OA、风险、知识库或系统管理功能；
- 删除 BP 上传、批量导入、人工复核、Radar 同步后端能力；
- 新增第三方工商、联系人或社交媒体数据源；
- 为缺失数据制造默认公司、团队、融资、估值或动态；
- 未经预览直接批量重评全部历史线索；
- 将旧评分强制映射为 V3 正式等级；
- 调整全站侧边栏、顶部栏或设计系统。

## 5. 目标用户流程

```text
进入共享线索池
  → 服务端读取第一页真实线索
  → 展示搜索、六项筛选和七列列表
  → 用户搜索或筛选
  → 服务端按条件返回分页结果
  → 用户点击整行或详情箭头
  → 保存列表查询、页码和滚动位置
  → 进入 /sourcing/:id
  → 按需读取完整线索详情
  → 展示主体、产品、团队、动态和完整评级
  → 用户可转为我的专属项目
  → 返回共享线索池
  → 恢复原筛选、页码和滚动位置
```

## 6. 页面详细设计

### 6.1 列表页结构

页面从上到下分为：

1. 标题及说明；
2. 页面级搜索框；
3. 六项筛选栏；
4. 共享线索列表；
5. 总数、页码和每页条数；
6. 加载、空数据、错误等状态。

页面中不出现统计卡片、批量导入、上传 BP、人工复核、Radar 同步等当前运营入口。

### 6.2 搜索与筛选

搜索范围：

- 项目名称；
- 公司/机构主体；
- 行业；
- 摘要；
- 来源；
- 团队、机构、投资方等已入库结构化字段。

筛选项：

| 筛选 | 默认值 | 服务端参数 | 行为 |
|---|---|---|---|
| 线索类型 | 全部线索 | `leadType` | 企业线索、科研项目 |
| 行业 | 全部行业 | `industry` | 使用当前标准行业映射 |
| 阶段 | 全部阶段 | `stage` | 融资阶段或项目阶段 |
| 地区 | 全部地区 | `region` | 使用权威业务地区字段 |
| 渠道 | 全部渠道 | `channel` | 保留 36氪等互斥渠道规则 |
| 更新时间 | 全部时间 | `updatedRange` | 近 7/30/90 天 |

交互要求：

- 搜索和任一筛选变化后回到第 1 页；
- 多个筛选条件按 AND 组合；
- 输入搜索词采用防抖或显式提交，避免每个按键产生失控请求；
- 只允许最后一次请求更新列表，旧请求后返回不得覆盖新条件；
- 清空筛选时恢复默认条件和第 1 页；
- 筛选选项不得从当前页数据临时推导，避免翻页后选项变化；
- 查询失败保留用户当前条件，不自动清空。

### 6.3 七列数据映射

| 列 | 企业线索 | 科研项目 | 缺失处理 |
|---|---|---|---|
| 项目概况 | 项目名、公司主体、企业线索、行业、背景标签；复合行业按逗号、顿号、分号或竖线拆为独立标签 | 项目名、机构/实验室、科研项目、行业、背景标签；使用相同拆分规则 | 主体不明显示“主体待核验”；标准行业名“具身智能/机器人”保留为一个标签 |
| 一句话摘要 | `summary` | `summary` | 显示“暂无可验证摘要” |
| 核心信息 | 团队、成立、地点 | 成果、验证、转化 | 每项独立显示“待核验” |
| 推荐理由/信号 | V3 正式/参考评级优先取最多两条 `detailView.investmentThesis[].thesis`；D/无法评级取 `detailView.rating.oneSentenceJudgment`；无投资逻辑时取最新一句话判断；无 V3 才回退最多两条历史 `highlights`；另展示最多三个信号和线索评级 | 同左 | 不把历史正向 `highlights` 当作 D 级线索的当前推荐理由；全部缺失时显示“推荐依据待进一步核验” |
| 融资/估值 | 固定展示融资状态、融资金额、估值三项，并与“核心信息”使用一致的左标签、右内容三行键值排列；融资状态使用独立 `fundingStatusDisplay`，不得用融资轮次冒充 | 科研项目三项均显示“不适用”；企业状态为“未融资”时，金额和估值显示“—” | 已融资、未融资、未披露、待核验和不适用严格区分；金额与估值缺失时仍保留对应子项 |
| 最新动态 | 最多两条可追溯动态 | 最多两条论文/成果动态 | 无动态显示“暂无更新” |
| 更新时间 | 数据更新时间、入池相对时间 | 同左 | 使用统一时区和格式 |

一句话摘要默认最多显示三行并通过 `title` 查看全文；推荐理由默认最多显示两行，超出部分以省略号截断，并使用无延迟的页面内浮层在鼠标悬停或键盘聚焦时立即显示全文。推荐理由区域的标签优先取最新 V3 `detailView.rating.coreTags`，再兼容 Radar 信号与历史风险标签，去重后最多显示三个。

列表不得展示：

- AI 综合评分数字；
- 维度分；
- 风险长文；
- 免责声明长文；
- 模型内部原因；
- 无来源的估值、团队人数、背景标签或最新动态。

### 6.4 列表评级状态

| 状态 | 列表展示 | 允许操作 |
|---|---|---|
| V3 正式评级 | A+ 至 C- | 打开详情 |
| V3 参考评级 | A+ 至 C- | 打开详情 |
| 无法评级 | D | 打开详情查看基础资料；列表推荐理由解释证据缺口 |
| 未运行 V3 | 待评级 | 打开详情 |
| 排队/运行/重试 | 评级中 | 打开详情 |
| 失败 | 评级失败 | 打开详情 |
| 死信 | 评级失败 | 打开详情；后台保留受权运维处理能力 |

正式等级使用参考项目的品牌蓝色文字；“待评级”使用灰色；任务失败使用克制的错误状态，不把失败伪装成待评级。

### 6.5 分页

- 使用服务端真实 `total`、`page`、`pageSize`、`totalPages`；
- 不使用参考项目的固定 326 条或固定 17 页；
- 默认每页 20 条；
- 支持 10、20、50 条/页；
- 页码超出总页数时回退至最后有效页；
- 排序必须以唯一 ID 作为最终稳定键，防止翻页重复或遗漏；
- 筛选后总数必须是过滤后的真实总数。

### 6.6 独立详情页

新增 `/sourcing/:id`，页面包括：

1. 返回共享线索池；
2. 主体名称、地区、定位、官网、行业和背景标签；
3. 转为我的专属项目；
4. 项目简介；
5. 主体基础信息或科研主体信息；
6. 产品与商业化；
7. 团队成员及有证据的公开主页；
8. 动态路径和相关新闻。

详情数据要求：

- 进入页面后按 `leadId` 读取完整详情；
- 不依赖列表精简 DTO 充当完整详情；
- 404 展示“线索不存在或已移除”，不得回退到其他线索；
- 接口失败展示重试，不制造本地详情；
- 官网、新闻和公开主页必须使用安全外链属性；
- 联系方式仅在数据真实、公开、授权且有来源时展示；
- 参考项目中的演示手机号、邮箱和社交主页不得复制。

### 6.7 返回状态恢复

从列表进入详情前保存：

- 搜索词；
- 六项筛选；
- 当前页；
- 每页条数；
- 列表滚动位置。

推荐优先将查询条件写入 URL Query，滚动位置写入会话级 UI 状态。不得把业务线索正文或详情数据写入浏览器存储。

直接访问 `/sourcing/:id` 时，返回按钮进入 `/sourcing` 默认状态；从列表进入时，返回原查询和滚动位置。

## 7. V3 评级方案

### 7.1 隔离原则

当前 `score-project` 同时被专属项目评分复用。本期不得直接替换现有评分标准，否则会改变其他板块。

新增共享线索池专用流程：

```text
score-lead-rating-v3
```

仅共享线索池的新评分和重新评分使用该流程。专属项目继续使用原流程，除非后续另行立项。

### 7.2 版本化结果

建议在现有 `leads.scoring` 中增加命名空间：

```json
{
  "ratingV3": {
    "schemaVersion": "lead-rating-v3",
    "mainView": {
      "displayGrade": "A"
    },
    "detailView": {},
    "computed": {
      "score": 87.5,
      "assessableWeight": 85,
      "informationCoverage": 85,
      "ratingStatus": "正式评级"
    },
    "scoreJob": {},
    "scoredAt": ""
  }
}
```

兼容要求：

- 不覆盖旧评分 JSON；
- V3 页面只信任 `schemaVersion=lead-rating-v3` 的结果；
- 旧结果继续保留用于审计和回滚；
- 不符合 V3 版本的旧结果在列表显示“待评级”；
- 不能只写 `displayGrade` 而缺失详情和计算证据。

### 7.3 七个维度

| 维度 | 权重 |
|---|---:|
| 财务经营状况 | 15% |
| 核心产品体系与产品竞争力 | 15% |
| 核心技术壁垒与研发能力 | 15% |
| 行业赛道、政策环境与发展空间 | 15% |
| 技术产业化能力与行业适配性 | 10% |
| 商业模式与市场落地能力 | 20% |
| 创始人及核心团队 | 10% |

每个维度为 0 至 10 分或 `null`。信息缺失必须为 `null`，不得默认为 0。

### 7.4 后端确定性计算

综合得分：

```text
score =
  Σ（dimensionScore / 10 × dimensionWeight）
  ÷ assessableWeight
  × 100
```

信息覆盖率：

```text
informationCoverage = assessableWeight / 100 × 100%
```

评级资格：

- 正式评级：至少 5 个维度可评估，信息覆盖率不低于 60%；
- 参考评级：至少 4 个维度可评估，信息覆盖率为 50% 至 59.9%；
- 无法评级：不满足参考评级条件。

无法评级时：

- `mainView.displayGrade = 待评级`；
- `detailView.rating.grade = null`；
- `detailView.rating.score = null`；
- `detailView.rating.status = 无法评级`。

等级区间：

| 等级 | 分数区间 |
|---|---:|
| A+ | 90–100 |
| A | 85–89.9 |
| A- | 80–84.9 |
| B+ | 75–79.9 |
| B | 70–74.9 |
| B- | 65–69.9 |
| C+ | 60–64.9 |
| C | 50–59.9 |
| C- | 低于 50 |

A+ 还必须同时满足：

- 信息覆盖率不低于 80%；
- 置信度为高；
- 至少 3 个核心维度达到 9 分；
- 不存在未解决的重大合规、诚信、知识产权或持续经营风险。

若总分达到 90 但不满足 A+ 附加条件，必须按明确的后端规则降为 A，不能交由模型自由决定。

### 7.5 证据与缺失信息

- E1、E2、E3 证据等级进入结构化结果；
- 仅由 E3 支持的单项不得超过 8 分；
- 缺失维度使用 `null`；
- 冲突信息单独记录，不通过删掉不利材料解决；
- 重大红线独立展示，不能被其他维度高分抵消；
- 模型只使用宿主提供的授权材料和证据；
- 评级输入、模型运行、决策、证据和最终计算结果必须可审计。

### 7.6 旧数据处理

本期发布不自动批量改写历史评分。

历史重评流程必须分为：

1. 预览：统计待重评数量、来源、预计任务量和不具备评分资格的记录；
2. 小批量试跑：覆盖企业线索、科研项目、信息完整、信息不足、失败重试等样本；
3. 验证：检查等级分布、覆盖率、失败率和审计完整性；
4. 分批执行：限制并发，使用现有租约、重试和死信能力；
5. 回滚：页面停止读取 V3 结果，但不删除已经产生的审计记录。

任何全量重评都必须另行得到明确执行授权。

## 8. API 与数据契约

### 8.1 列表请求

建议扩展现有 `GET /leads`：

```text
GET /leads?page=1&pageSize=20
  &keyword=
  &leadType=
  &industry=
  &stage=
  &region=
  &channel=
  &updatedRange=
```

要求：

- `pageSize` 最大 100；
- 所有字符串 trim 后校验长度；
- `leadType`、`updatedRange` 使用枚举；
- 行业、地区、渠道继续使用现有标准化规则；
- 所有条件进入服务端查询，不能先取当前页再前端过滤；
- 最终排序包含唯一 ID；
- 保留旧参数兼容，不影响其他调用方。

### 8.2 列表响应

建议新增显式列表 DTO：

```ts
type LeadPoolListItem = {
  id: string
  leadType: 'company' | 'research'
  projectName: string
  subjectName: string | null
  industry: string
  region: string | null
  backgroundTags: string[]
  summary: string | null
  coreFacts: Array<{ label: string; value: string | null; status: 'ready' | 'pending' }>
  reasons: string[]
  signals: string[]
  funding: {
    stage: string | null
    amount: string | null
    valuation: string | null
    status: 'available' | 'pending' | 'not_applicable'
  }
  latestUpdates: Array<{ occurredAt: string; title: string; sourceUrl?: string }>
  rating: {
    displayGrade: 'A+' | 'A' | 'A-' | 'B+' | 'B' | 'B-' | 'C+' | 'C' | 'C-' | '待评级'
    status: 'ready' | 'pending' | 'running' | 'failed'
  }
  poolEnteredAt: string
  dataUpdatedAt: string
}
```

列表 DTO 不返回：

- 完整评分维度和证据；
- 完整 Radar 正文；
- 完整融资历史；
- 完整团队和股东；
- 完整来源列表；
- 内部错误堆栈、路径、模型请求或审计详情。

### 8.3 详情请求

继续使用 `GET /leads/:id`，但应明确增加 V3 详情字段。列表与详情的评级字段必须由同一个后端投影函数生成，避免两套映射产生不一致。

### 8.4 转专属项目

继续使用现有转换接口：

- 转换成功后按钮显示“已转为我的专属项目”；
- 重复点击必须幂等；
- 转换不能制造重复项目；
- 转换后返回列表仍保留共享线索状态的权威投影；
- 不改变专属项目页面布局和业务逻辑。

## 9. 前端实现计划

### 9.1 页面拆分

建议结构：

```text
src/pages/SourcingPage.tsx
src/pages/LeadDetailPage.tsx
src/components/lead-pool/LeadPoolFilters.tsx
src/components/lead-pool/LeadPoolTable.tsx
src/components/lead-pool/LeadPoolRow.tsx
src/components/lead-pool/LeadRatingDetail.tsx
src/lib/leadPoolPresentation.ts
```

拆分原则：

- 业务请求和状态归页面/Store；
- 纯展示映射放入 `leadPoolPresentation`；
- 不在组件内硬编码项目 ID 对应的特殊展示；
- 不复制参考项目的 `LEAD_PRESENTATIONS`、`CONTACTS`、`TEAM_MEMBERS` 等模拟映射；
- 不为通过截图而写固定总数、固定页码或固定日期。

### 9.2 路由

在 `src/App.tsx` 增加：

```tsx
<Route path="/sourcing/:id" element={<LeadDetailPage />} />
```

不修改其他路由，不调整全站导航。现有 `/sourcing` 导航在详情路径下应继续保持选中状态。

### 9.3 样式隔离

新增样式统一限定在：

```text
.lead-pool-page
.lead-pool-*
.lead-review-page
.lead-review-*
```

禁止：

- 修改通用 `button`、`table`、`input`、`main` 等裸选择器；
- 调整 AppLayout 全局间距以适配单个页面；
- 修改其他页面复用的通用组件默认样式；
- 使用 `!important` 覆盖全站样式；
- 用固定高度裁掉真实内容。

### 9.4 状态

前端必须覆盖：

- 首次加载；
- 条件切换加载；
- 空列表；
- 列表请求失败；
- 详情加载；
- 详情 404；
- 详情请求失败；
- 待评级；
- 评级中；
- 评级失败；
- 转项目处理中；
- 已转项目。

## 10. 后端实现计划

### 10.1 查询扩展

在列表查询 Schema 和 `listLeads` 中增加：

- `leadType`；
- `stage`；
- `updatedRange`；
- 列表最小字段投影；
- 最新动态轻量投影；
- V3 评级轻量投影。

不得把完整 `scoring`、`radarProfile`、`sources`、`fundingRounds` 大 JSON 返回到列表。

### 10.2 展示映射

新增统一的服务端展示映射：

- 企业/科研类型判定；
- 主体名称；
- 业务地区；
- 融资/估值状态；
- 可验证背景标签；
- 最新动态；
- V3 列表等级；
- 数据更新时间。

同一事实只允许一个权威映射函数。列表和详情不得各自发明兜底值。

### 10.3 评分执行

新增 V3 标准配置、结构化输出 Schema、宿主归一化和确定性计算器。现有评分任务的租约、重试、恢复和审计能力尽量复用，不新建第二套不可靠队列。

需要新增或更新：

- V3 评分标准配置；
- V3 输出校验；
- N/A 维度处理；
- 覆盖率和置信度计算；
- 等级计算；
- A+ 附加条件；
- 红线处理；
- 主列表/详情一致性校验；
- 评分输入和输出版本标记；
- V3 针对企业和早期科研成果的阶段适配。

### 10.4 失败与重试

- 模型结构无效时任务失败，不保存半份正式评级；
- 失败时保留上一个有效 V3 结果，但页面标明最新任务失败；
- 自动重试和死信使用现有策略；
- 人工重试必须有权限并写审计；
- 页面停止轮询不等于取消后端任务；
- 服务重启后能够恢复排队、运行或重试状态。

## 11. 代码影响范围

预计修改：

- `src/pages/SourcingPage.tsx`；
- 新增 `src/pages/LeadDetailPage.tsx`；
- `src/App.tsx`；
- `src/styles.css`；
- `src/types/index.ts`；
- `src/store/useAppStore.ts`；
- `server/src/routes/meta.ts`；
- `server/src/services/aiSummaryService.ts`；
- `server/src/services/inProcessAiWorkflowService.ts` 或新增 V3 专用评分服务；
- 新增 V3 评分标准配置；
- 共享线索池及评分相关验收脚本。

不应修改：

- `src/pages/ProjectsPage.tsx`；
- `src/pages/ProjectDetailPage.tsx` 的页面行为和视觉；
- `src/pages/AIAssistantPage.tsx`；
- `src/pages/MeetingsPage.tsx`；
- `src/pages/WorkflowPage.tsx`；
- `src/pages/RisksPage.tsx`；
- `src/pages/SystemPage.tsx`；
- 全站侧边栏和顶部栏视觉；
- 专属项目现有评分标准和任务契约。

如确需修改范围外文件，必须在开发记录中说明原因、影响和验证结果。

## 12. 实施阶段

### 阶段 0：基线冻结

- 保存参考项目列表和详情截图；
- 记录当前 `/sourcing` 的 API、权限和任务契约；
- 记录当前 Git 状态，保留用户未跟踪的《评分规则.md》；
- 确认其他页面基线截图和关键功能检查；
- 建立本期改动文件白名单。

完成条件：参考视觉、当前业务能力、范围边界均可追溯。

### 阶段 1：列表 DTO 和服务端筛选

- 扩展查询参数；
- 实现企业/科研类型；
- 增加阶段和更新时间过滤；
- 增加最小列表 DTO；
- 保证分页和排序稳定；
- 增加 API 单元/验收测试。

完成条件：不用前端模拟数据即可渲染目标七列。

### 阶段 2：列表页 1:1 重构

- 重建标题、搜索、筛选、七列列表和分页；
- 删除页面中的统计卡片；
- 不渲染四类运营入口；
- 实现整行点击和键盘操作；
- 增加系统管理员单条删除入口、二次确认和删除后分页刷新；
- 实现加载、空数据、错误状态；
- 完成 1440、1280 和窄屏检查。

完成条件：标准用户页面达到参考视觉和交互基线。

### 阶段 3：独立详情页

- 新增详情路由；
- 接入真实详情接口；
- 复刻主体、简介、基础信息、产品、团队和动态；
- 接入转项目；
- 实现返回状态恢复；
- 禁止模拟联系人和动态。

完成条件：列表到详情再返回形成完整闭环。

### 阶段 4：V3 评级

- 新增专用工作流；
- 实现 V3 Schema 和确定性计算；
- 列表接入等级状态；
- 详情接入完整研判；
- 实现失败、重试和恢复；
- 完成边界、覆盖率、A+和红线测试。

完成条件：新评分端到端满足《评分规则.md》，且不改变专属项目评分。

### 阶段 5：回归与发布候选

- 类型检查；
- 相关验收脚本；
- 视觉截图比对；
- 浏览器真实 API 测试；
- 构建发布候选；
- 验证候选静态资源和服务接口；
- 形成验收证据清单。

完成条件：本计划第 14 节所有 P0 验收项通过。

## 13. 测试策略

### 13.1 单元测试

覆盖：

- 线索类型判定；
- 企业/科研字段映射；
- 融资、估值三态；
- 最新动态排序和截断；
- 筛选参数校验；
- 更新时间范围边界；
- V3 加权分；
- N/A 分母；
- 覆盖率；
- 等级区间；
- A+附加条件；
- 红线降级；
- 主列表和详情等级一致性；
- 旧评分不被误识别为 V3。

### 13.2 API 测试

覆盖：

- 单筛选和多筛选；
- 关键词跨字段；
- 真实总数；
- 稳定分页；
- 越界页；
- 详情 200/404；
- 未授权访问；
- 转项目幂等；
- 列表不泄露大 JSON 和内部错误；
- V3 结果轻量投影。

### 13.3 浏览器测试

覆盖：

- 首次进入；
- 搜索；
- 六项筛选；
- 清空筛选；
- 分页和每页条数；
- 整行点击、按钮点击、Enter 和 Space；
- 进入详情；
- 返回后状态恢复；
- 直接访问详情；
- 转项目；
- 待评级、评级中、评级失败和已评级；
- 四类运营入口不存在；
- 无模拟联系人和模拟数据。

### 13.4 视觉测试

至少生成：

- 1440×1000 列表页；
- 1440×1000 详情首屏；
- 1440×1000 详情中下部；
- 1280 宽列表页；
- 900 宽或产品约定最小宽度页面；
- 空列表；
- 待评级、评级中和评级失败样本。

对比项：

- 页面左右边距；
- 标题和说明位置；
- 搜索框尺寸；
- 六项筛选尺寸和间距；
- 表头高度；
- 七列比例；
- 行高和分割线；
- 标签、等级、时间线；
- 分页位置；
- 详情卡片间距和文字层级。

## 14. 验收标准

### 14.1 P0 范围验收

- [ ] `SCOPE-001` 本期只改变共享线索池列表、详情及其专用数据/评级能力。
- [ ] `SCOPE-002` “我的专属项目”等其他板块没有视觉和行为变化。
- [ ] `SCOPE-003` 批量导入、上传 BP、人工复核、Radar 同步入口未在页面 DOM 中渲染。
- [ ] `SCOPE-004` 隐藏入口没有快捷键、URL 参数或角色开关可重新显示。
- [ ] `SCOPE-005` 对应后端接口、权限、任务和审计能力仍存在且未被弱化。
- [ ] `SCOPE-006` 参考项目模拟数据未进入当前项目代码、数据库或页面。
- [ ] `SCOPE-007` 线索删除仅新增于共享线索池列表，没有改变其他板块的数据删除行为。

### 14.2 P0 列表视觉与交互验收

- [ ] `UI-001` 标题、说明、搜索、筛选、列表和分页顺序与参考项目一致。
- [ ] `UI-002` 六项筛选的顺序、默认值和交互与参考项目一致。
- [ ] `UI-003` 列表为七列，列标题、顺序和主要比例与参考项目一致。
- [ ] `UI-004` 企业线索和科研项目按各自信息口径展示。
- [ ] `UI-005` 整行、项目名称和详情箭头均可进入详情，且不会重复触发。
- [ ] `UI-006` 行支持 Enter/Space 打开，筛选和分页支持键盘操作。
- [ ] `UI-007` 搜索和筛选后真实总数、页码及行内容正确。
- [ ] `UI-008` 返回列表后恢复搜索、筛选、页码、每页条数和滚动位置。
- [ ] `UI-009` 加载、空数据、请求失败均有明确状态。
- [ ] `UI-010` 1440×1000 关键布局与参考截图一致，关键间距误差不超过 4px，文字和列内容因真实数据产生的自然换行除外。
- [ ] `UI-011` 页面不存在横向内容遮挡、文字溢出、不可见操作或错误叠层。
- [ ] `UI-012` 一句话摘要最多显示三行，推荐理由最多显示两行；两者溢出均显示省略号，并共用无延迟的悬停/键盘聚焦浮层即时展示完整内容。
- [ ] `UI-013` D 级线索的推荐理由显示最新证据不足判断，不显示历史正向推荐语。
- [ ] `UI-014` “融资 / 估值”列逐条固定展示“融资状态、融资金额、估值”三个带标签子项，长值安全省略并可悬停查看全文。
- [ ] `UI-015` 推荐理由区域展示最多三个最新 V3 核心标签；没有 V3 标签时再回退 Radar 信号或历史风险标签。
- [ ] `UI-016` “融资 / 估值”与“核心信息”均采用左标签、右内容的三行键值布局，标签与内容基线、行距一致。
- [ ] `UI-017` 列表项目概况和详情页使用同一行业拆分规则，“企业服务、前沿技术”显示为两个独立标签，重复标签自动去重。
- [ ] `UI-018` 企业融资状态为“未融资”时，融资金额和估值均显示“—”；科研项目才使用“不适用”。
- [ ] `UI-019` 系统管理员可看到单条删除按钮；普通用户的 DOM 中不存在该按钮。
- [ ] `UI-020` 删除按钮不会触发行级详情跳转，确认弹窗明确展示待删除线索名称和保留范围。
- [ ] `UI-021` 删除成功后当前列表、总数和分页刷新；非首屏最后一条被删除后回退上一页；失败时原行保留并显示错误。

### 14.3 P0 数据验收

- [ ] `DATA-001` 列表所有业务数据来自当前项目 API。
- [ ] `DATA-002` 总数、页数和页码不是固定模拟值。
- [ ] `DATA-003` 线索类型、阶段、地区、渠道和更新时间筛选由服务端执行。
- [ ] `DATA-004` 多条件筛选按 AND 组合，结果与数据库查询一致。
- [ ] `DATA-005` 分页排序有唯一 ID 兜底，不重复、不漏项。
- [ ] `DATA-006` 未披露、待核验和不适用使用不同状态。
- [ ] `DATA-007` 列表不返回完整大 JSON、内部路径、错误堆栈或模型请求。
- [ ] `DATA-008` 团队人数、背景标签、估值和动态没有证据时不编造。
- [ ] `DATA-009` 列表和详情对同一字段使用同一权威映射口径。
- [ ] `DATA-010` 推荐理由按“D/无法评级一句话判断 → 正式/参考评级投资逻辑 → 最新一句话判断 → 历史 highlights”的适用分支取值，且每个分支均有自动化验收。
- [ ] `DATA-011` 列表 DTO 返回独立 `fundingStatusDisplay`；融资状态不得由前端直接把融资轮次改名得到。

### 14.4 P0 评级验收

- [ ] `RATE-001` 列表只显示 A+ 至 D、待评级、评级中或评级失败，不显示数字总分和维度分。
- [ ] `RATE-002` 只有 `lead-rating-v3` 结果可以显示正式等级。
- [ ] `RATE-003` 旧评分没有被直接按分数区间换算为 V3 等级。
- [ ] `RATE-004` 七个维度均存在，无法评估的维度为 `null`。
- [ ] `RATE-005` `null` 维度不进入综合评分分母。
- [ ] `RATE-006` 正式、参考、无法评级资格按维度数量和覆盖率确定性计算。
- [ ] `RATE-007` 90、85、80、75、70、65、60、50 和低于 50 的边界测试通过。
- [ ] `RATE-008` 覆盖率 49.9%、50%、59.9%、60%、79.9%和80%的边界测试通过。
- [ ] `RATE-009` A+ 的覆盖率、置信度、三个核心维度和无重大红线条件全部验证。
- [ ] `RATE-010` 仅由 E3 证据支持的单维度不能超过 8 分。
- [ ] `RATE-011` 无法评级时列表显示 D，评分结果仍保持 `grade=null`、`score=null`，并用一句话判断解释未达到评级门槛的原因。
- [ ] `RATE-012` 主列表等级使用同一份 V3 后端评级投影，不从旧总分二次推导。
- [ ] `RATE-013` 评级输入、模型运行、证据、决策和宿主计算结果可追溯。
- [ ] `RATE-014` 共享线索 V3 评分没有改变专属项目现有评分结果或任务契约。

### 14.5 P0 详情验收

- [ ] `DETAIL-001` `/sourcing/:id` 可直接访问和刷新。
- [ ] `DETAIL-002` 详情数据来自 `GET /leads/:id`，不是列表 DTO 或模拟对象。
- [ ] `DETAIL-003` 项目简介、主体信息、产品商业化、团队和动态区块完整。
- [ ] `DETAIL-004` 官网、新闻和公开主页链接安全且来源可追溯。
- [ ] `DETAIL-005` 不存在参考项目模拟电话、邮箱、微信号和社交链接。
- [ ] `DETAIL-006` 转为专属项目成功、失败、处理中和已转换状态正确。
- [ ] `DETAIL-007` 重复转换不创建重复专属项目。
- [ ] `DETAIL-008` 详情 404 和接口失败不回退到错误线索。
- [ ] `DETAIL-009` 详情页不展示评级研判、等级、分数或发起评级入口。
- [ ] `DETAIL-010` 科研项目将论文全部署名作者去重后展示为团队成员，第一位标记“第一作者”、其余标记“共同作者”，姓名可跳转至对应 arXiv 作者检索页；没有作者数据时才回退其他团队字段。

### 14.6 P0 安全与权限验收

- [ ] `SEC-001` 隐藏前端入口不等于取消后端权限，相关接口仍执行原授权检查。
- [ ] `SEC-002` 评级失败信息经过公开错误转换，不展示凭据、路径或内部堆栈。
- [ ] `SEC-003` 外链使用 `rel="noreferrer"` 或等价安全策略。
- [ ] `SEC-004` 联系方式仅在真实、授权且允许当前用户访问时展示。
- [ ] `SEC-005` 详情和转项目不存在跨用户、跨权限访问绕过。
- [ ] `SEC-006` 浏览器存储中没有线索正文、联系人、评分证据等业务数据。
- [ ] `SEC-007` `DELETE /api/leads/:id` 继续要求系统管理员权限，前端隐藏按钮不替代服务端鉴权。
- [ ] `SEC-008` 删除采用 `poolStatus=已删除` 的软删除并写入“删除公共线索”审计；历史导入、实体映射和已转专属项目不被级联删除。

### 14.7 P0 性能验收

- [ ] `PERF-001` 列表接口不读取或返回完整详情大字段。
- [ ] `PERF-002` 页面首次只读取当前页，不全量加载全部线索。
- [ ] `PERF-003` 快速切换搜索或筛选时旧请求不能覆盖新结果。
- [ ] `PERF-004` 最新动态最多返回两条轻量信息。
- [ ] `PERF-005` 默认 20 条、最大 100 条的分页限制有效。
- [ ] `PERF-006` 列表关键查询有可接受的执行计划，未引入无边界全表 JSON 扫描；如保留 JSON 搜索须记录当前性能基线和后续索引计划。

### 14.8 P0 回归与发布验收

- [ ] `REG-001` `npm run check:types` 通过。
- [ ] `REG-002` 客户端权威状态验收通过。
- [ ] `REG-003` 线索评分 Agent、评分审计和评分生命周期验收通过。
- [ ] `REG-004` 线索转换验收通过。
- [ ] `REG-005` 线索删除、人工复核、BP 上传、批量导入和 Radar 后端验收未因隐藏入口而失败。
- [ ] `REG-006` 单服务边界检查通过；静态扫描契约已按组件拆分安全更新，而不是简单删除门禁。
- [ ] `REG-007` 当前项目生产构建通过。
- [ ] `REG-008` 发布候选已激活，并验证真实服务健康和实际提供的前端资源版本。
- [ ] `REG-009` 首页、专属项目、AI 助手、会议、OA、风险和系统管理完成只读冒烟回归。
- [ ] `REG-010` Git diff 只包含本期确认文件，用户原有未跟踪或无关修改未被覆盖、删除或误提交。

## 15. 评分边界验收样例

| 样例 | 可评估维度/覆盖率 | 原始得分 | 附加条件 | 预期 |
|---|---|---:|---|---|
| R-01 | 7维/100% | 90.0 | A+全部满足 | A+ |
| R-02 | 7维/100% | 90.0 | 置信度中 | A |
| R-03 | 6维/85% | 85.0 | 正式评级 | A |
| R-04 | 6维/85% | 80.0 | 正式评级 | A- |
| R-05 | 5维/60% | 75.0 | 正式评级 | B+ |
| R-06 | 4维/55% | 70.0 | 参考评级 | B |
| R-07 | 4维/50% | 60.0 | 参考评级 | C+ |
| R-08 | 4维/50% | 49.9 | 参考评级 | C- |
| R-09 | 3维/45% | 88.0 | 不满足参考评级 | 待评级 |
| R-10 | 4维/49.9% | 88.0 | 不满足参考评级 | 待评级 |
| R-11 | 7维/100% | 92.0 | 存在重大未解决红线 | 不得 A+，详情突出红线 |
| R-12 | 维度仅有E3证据 | 模型返回9.0 | 宿主证据规则 | 评分无效或封顶8.0 |

## 16. 视觉验收证据清单

交付时至少保存：

| 编号 | 证据 |
|---|---|
| V-01 | 参考项目列表页 1440×1000 截图 |
| V-02 | 当前项目实现后列表页 1440×1000 截图 |
| V-03 | 参考项目详情首屏截图 |
| V-04 | 当前项目实现后详情首屏截图 |
| V-05 | 六项筛选展开/选中状态截图 |
| V-06 | 企业线索和科研项目同屏截图 |
| V-07 | 待评级、评级中、评级失败、正式等级状态截图 |
| V-08 | 空列表和接口失败状态截图 |
| V-09 | 页面不存在四类运营入口的 DOM/可访问性证据 |
| V-10 | 其他主要页面未发生视觉变化的抽样截图 |

## 17. 发布与回滚

### 17.1 发布前

- 记录当前生产构建和数据库版本；
- 确认本期是否需要数据库迁移；
- 如需迁移，先执行预览和备份；
- 构建发布候选，不在正在服务的静态目录上直接覆盖；
- 完成类型、验收、视觉和真实 API 检查；
- 确认旧评分仍可读取和审计。

### 17.2 发布

- 激活已验证的候选构建；
- 重启唯一正式服务；
- 检查健康接口、监听端口和进程；
- 检查 Nginx/静态资源实际提供的新版本；
- 登录真实页面执行列表、详情、返回和转项目冒烟；
- 检查评分任务和错误日志。

### 17.3 回滚触发条件

出现以下任一问题立即回滚：

- 共享线索列表无法读取或分页明显错误；
- 其他板块发生路由、样式或业务回归；
- V3 等级与详情不一致；
- 旧评分被不可逆覆盖；
- 转项目出现重复写入；
- 权限绕过或敏感信息泄漏；
- 服务无法稳定启动或静态资源仍提供旧/混合版本。

### 17.4 回滚方式

- 激活上一份已验证构建；
- 页面停止读取 `ratingV3`，回到旧共享线索展示；
- 不删除新产生的评分运行、决策和证据审计；
- 数据库变更仅在有经过验证的回滚脚本时执行回滚；
- 记录失败版本、影响范围和恢复时间。

## 18. Definition of Done

同时满足以下条件才可声明本期完成：

1. 参考视觉和交互已经在当前真实项目中复现；
2. 页面没有模拟业务数据；
3. 四类运营入口在页面中不可见、不可聚焦、不可触达；
4. 对应后端能力和审计链未被删除或弱化；
5. V3 等级计算、缺失信息、覆盖率、A+和红线规则通过测试；
6. 列表等级与详情评级完全一致；
7. 其他业务板块没有功能和视觉回归；
8. 相关自动化检查、生产构建和浏览器验收通过；
9. 发布候选已实际激活，真实服务和静态资源验证通过；
10. 交付包含截图、测试结果、构建标识、提交哈希和回滚说明。

未完成历史全量重评不阻止页面版本发布，但必须明确说明：旧评分线索将显示“待评级”，且历史批量重评尚未执行，不能把页面上线描述为“全部线索已经完成 V3 评级”。

## 19. 评审与签字

| 角色 | 关注项 | 结论 | 签字/日期 |
|---|---|---|---|
| 产品负责人 | 1:1 范围、按钮隐藏、详情内容 | 待评审 |  |
| 投资业务负责人 | A/B/C 规则、证据和红线 | 待评审 |  |
| 前端负责人 | 视觉、交互、状态恢复、无跨页影响 | 待评审 |  |
| 后端负责人 | DTO、筛选、评级、任务和审计 | 待评审 |  |
| 测试负责人 | P0 验收项和证据完整性 | 待评审 |  |
| 发布负责人 | 构建、激活、健康、资源和回滚 | 待评审 |  |
