# RETIRED: this directory accepts no runtime environment configuration.
# Build/start/health commands exit with code 78. Use the repository-root .env
# and the single cybernaut-app service; old data is read only by approved
# repository-root migration commands.
