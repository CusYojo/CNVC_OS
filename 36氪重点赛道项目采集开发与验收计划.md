# 【已废弃】36氪重点赛道项目采集开发与验收计划

> 本文档的“三赛道限定”已于 2026-09-01 被全行业采集需求替代，不再作为开发和验收依据。
> 当前有效文档：`36氪全行业项目采集开发与验收计划.md`。

> 文档版本：V1.0
> 编制日期：2026-09-01
> 适用项目：`/Users/hyw/Desktop/sbl_jedi`
> 当前状态：源码开发完成，待隔离数据库、真实来源和运行期验收
> 数据范围：工商成立时间不早于 2025-01-01 的人工智能、具身智能、半导体项目
> 核心策略：全量采集进入候选库，每日定量释放 1 条进入共享线索池
> 实施边界：源码、迁移文件、配置、运维接口和测试已经实现；数据库迁移、生产任务启用及连续运行验收尚未执行。

## 1. 项目目标

本期新增一条面向 36氪项目库的项目采集链路，完整发现自 2025 年起成立、属于人工智能、具身智能或半导体赛道的项目，同时保证共享线索池每天持续出现真实新增项目，而不是首日一次性导入全部存量。

目标结果：

1. 36氪符合条件的项目先完整进入内部候选库，不因每日入池配额而漏采。
2. 候选项目经过成立时间、赛道、注销状态和主体重复校验后，才具备入池资格。
3. 共享线索池默认每天只新增 1 条符合条件的 36氪项目。
4. 当天发现的新项目优先入池；当天没有新项目时，从合格候选库存中补足。
5. 已入池项目在 36氪发生信息变化时更新原线索，不重复计为“新增企业”。
6. 采集、候选、入池、补全、评分和失败重试均可审计、可监控、可暂停。
7. 保留现有历史 `lead_reserve` 数据和正式业务记录，不通过删除旧数据实现切换。

## 2. 已确认的产品决策

### 2.1 时间口径

- 时间下限固定为工商成立时间 `>= 2025-01-01`。
- 该下限不是 36氪文章发布时间、融资时间、项目收录时间或本系统采集时间。
- 2024 年成立、2025 年融资的企业不纳入本期采集范围。
- 后续年份自动纳入，不把年份写死为仅 2025、2026。
- 工商成立时间无法确认的项目进入待核验状态，不直接进入共享线索池。

成立时间取值优先级：

1. 36氪项目详情工商信息中的成立时间；
2. 原始详情中的 `business.estiblishTime`；
3. 可明确转换为工商成立日期的 `setupDate`；
4. 以上均无有效值时标记为 `founded_at_pending`。

### 2.2 赛道口径

本期只纳入：

- 人工智能；
- 具身智能；
- 半导体。

允许一个项目同时具有多个目标赛道标签，但只能形成一个候选项目和一条共享线索。

36氪的“前沿技术”“先进制造”“智能硬件”等分类只是候选召回条件，不能直接等同于目标赛道。最终是否入池必须结合项目名称、一句话介绍、完整项目简介、36氪分类和标签进行二次判定。

### 2.3 “每天有新增”的定义

“每日新增”是指当天首次从候选库正式释放并创建到共享线索池的企业，不包括：

- 同一企业的字段更新；
- 同一企业的新融资事件；
- 重复采集产生的新原始事件；
- 已有线索重新补全或重新评分；
- 为满足数量而复制、改名或重新插入同一企业。

当合格候选库存大于 0 时，每个自然日默认释放 1 条。库存为 0 时必须如实记录“当日无可释放新增”，不得制造重复数据。

### 2.4 业务流转边界

36氪采集结果进入的是“共享线索池”，不是直接写入正式项目池：

```text
36氪项目库
  → 原始采集事件
  → 36氪候选库
  → 范围判定与主体匹配
  → 每日释放
  → 共享线索池 leads
  → 数据补全与评分
  → 人工领取
  → 普通项目 projects.classification='normal'
```

采集任务不得绕过人工领取流程直接创建正式项目。

## 3. 当前实现与数据基线

### 3.1 当前实现

当前项目已经具备：

- `lead_pipeline_raw_events` 不可变原始事件与幂等键；
- Pipeline 状态迁移、决策、证据、人工复核和主体匹配；
- `commitRadarLeadPipelineReady()` 正式线索创建/合并能力；
- 共享线索补全、快照、评分、失败恢复和审计链；
- `runtime_jobs`、`runtime_job_runs` 多实例任务租约与执行历史；
- 36氪历史详情投影 `project36KrLeadDetail()`；
- 历史 `lead_reserve` 每日定量入池任务。

当前不足：

- `lead_reserve` 来自一次性历史 CSV，不是 36氪项目库实时同步；
- 当前 Radar 的 36氪来源是融资快报/新闻，不是完整项目库；
- 历史储备按记录来源键直接插入，未完整复用跨来源主体匹配；
- 历史储备没有真实 `firstSeenAt`、`lastSeenAt` 和来源版本状态；
- 页面“最新入池”可能表示旧库存今天导入，而不是 36氪今天新增。

### 3.2 2026-09-01 只读调研快照

以下数据只用于容量估算，不作为长期固定验收数量：

| 指标 | 调研值 |
|---|---:|
| 36氪宽口径候选 | 310 |
| 详情规则初筛后符合范围 | 约 200 |
| 其中 2025 年成立 | 143 |
| 其中 2026 年成立 | 57 |
| 已存在于历史 `lead_reserve` | 86 |
| 不存在于历史 `lead_reserve` | 114 |
| 已按稳定 36氪项目 ID 进入共享线索池 | 0 |

调研发现 36氪公开页面支持“最近更新”“最新收录”排序，但没有公开精确的项目收录时间字段。因此：

- `foundedAt` 只能用于成立时间门槛和成立月份分析；
- `firstSeenAt` 必须从本系统首次观察该项目时开始记录；
- 不能用企业成立月份冒充 36氪月度新增量；
- 上线后需要连续保存每日快照，满 30 天后才能计算真实月度来源流入。

### 3.3 容量结论

按当前约 200 条初始合格库存测算：

| 每日释放量 | 不考虑未来新增的可持续时间 | 结论 |
|---|---:|---|
| 1 条/日 | 约 200 天 | 本期默认 |
| 2 条/日 | 约 100 天 | 需 30 天流量数据后再评审 |
| 3 条/日 | 约 67 天 | 不建议首期使用 |
| 50 条/日 | 约 4 天 | 不适用于重点赛道候选库 |

## 4. 本期范围

### 4.1 本期包含

1. 36氪项目列表和项目详情只读采集适配器。
2. 2025 年起成立项目的全量回溯采集。
3. 人工智能、具身智能、半导体宽召回和严格入池判定。
4. 不可变原始事件、候选当前投影和内容版本哈希。
5. 稳定 36氪项目 ID 去重与跨来源主体匹配。
6. 候选状态、排除原因、待核验原因和人工复核入口所需后端契约。
7. 独立的采集任务与每日释放任务。
8. 每天 1 条的动态释放、赛道平衡和库存预警。
9. 已入池项目的信息更新，不重复创建线索。
10. 任务监控、失败重试、限流、结构漂移告警和审计。
11. 单元测试、隔离数据库集成测试、真实来源只读验收、HTTP/UI 验收和运行期观察。

### 4.2 本期不包含

- 采集 2025 年以前成立的企业；
- 采集目标三赛道以外的项目；
- 直接把采集结果写入正式项目池；
- 删除现有 1 万条历史储备数据；
- 自动删除已转为普通项目或重点项目的业务记录；
- 用融资日期、文章日期替代工商成立日期；
- 用字段更新冒充每日新增企业；
- 绕过登录、验证码、反爬安全提示或访问控制；
- 未经预览和隔离环境验收直接向业务数据库执行批量写入；
- 首期同时接入新的第三方工商、联系人或社交媒体来源；
- 为了凑足每日数量而放宽成立时间或赛道规则。

## 5. 目标架构

### 5.1 采集与释放解耦

必须将“发现并保存候选”和“正式进入共享线索池”设计成两个任务、两个开关：

```text
任务 A：36kr-project-sync
  → 全量/增量读取 36氪
  → 保存原始事件
  → 更新候选投影
  → 范围判定
  → 不创建正式线索

任务 B：36kr-project-daily-admission
  → 每日选择 1 条 eligible 候选
  → 复用主体匹配
  → 创建/合并正式线索
  → 触发补全与评分
```

两个任务必须能够单独启停。关闭每日释放时，采集仍可积累候选库存；关闭采集时，每日释放可以继续消费已有合格库存。

### 5.2 建议新增候选投影

建议新增通用来源候选投影表 `lead_source_candidates`，36氪作为首个 `source_type='36kr_project'` 的使用方。最终表名和迁移编号在实现时按当前 schema 与已应用迁移号确认，不覆盖已有迁移。

建议字段：

| 字段 | 用途 |
|---|---|
| `id` | 内部候选 ID |
| `source_type` | 固定为 `36kr_project` |
| `source_project_id` | 36氪稳定项目 ID |
| `source_project_id_hash` | 查询和唯一约束使用 |
| `canonical_url` | 36氪项目详情地址 |
| `project_name` | 项目名称当前投影 |
| `company_name` | 工商主体当前投影 |
| `founded_at` | 规范化工商成立日期 |
| `source_industries` | 36氪原始分类 |
| `sector_labels` | AI/具身智能/半导体多标签 |
| `scope_status` | 范围判定状态 |
| `scope_reasons` | 纳入、排除或待复核依据 |
| `first_seen_at` | 本系统首次发现时间 |
| `last_seen_at` | 最近仍能从来源读取的时间 |
| `fetched_at` | 最近采集时间 |
| `content_hash` | 当前详情内容哈希 |
| `raw_event_id` | 最近原始事件 ID |
| `admission_status` | 未释放/处理中/已入池/待复核/失败 |
| `admitted_at` | 正式入池时间 |
| `admitted_lead_id` | 对应正式线索 ID |
| `attempts` | 处理次数 |
| `last_error` | 最近错误摘要 |
| `created_at/updated_at` | 候选投影维护时间 |

约束：

- `UNIQUE(source_type, source_project_id_hash)`；
- 同一项目详情内容不变时不创建重复原始事件；
- 同一项目内容变化时创建新的不可变原始事件，但更新同一候选投影；
- `admitted_lead_id` 只能绑定一条正式线索；
- 候选投影可以重建，原始事件不可覆盖。

### 5.3 候选状态机

建议 `scope_status`：

- `pending`：已发现，尚未完成判定；
- `eligible`：成立时间和赛道均符合，可进入每日释放队列；
- `founded_at_pending`：成立时间无法确认；
- `industry_pending_review`：赛道存在歧义；
- `founded_before_2025`：工商成立时间早于范围下限；
- `industry_not_matched`：不属于三个目标赛道；
- `registration_excluded`：已注销等不允许进入线索池的主体；
- `source_unavailable`：详情暂时不可读取；
- `manual_excluded`：经授权人工确认排除。

建议 `admission_status`：

- `not_ready`；
- `ready`；
- `processing`；
- `admitted`；
- `review`；
- `failed`。

状态改变必须记录原因、操作主体和时间，不允许只覆盖最终状态而丢失历史。

## 6. 来源采集设计

### 6.1 全量回溯

首次回溯按照以下维度分页：

```text
成立年份：2025、2026、后续年份动态生成
  × 36氪候选行业分类
  × 完整页码
  × 必要时的地区/融资状态切片
```

候选行业至少覆盖：

- 企业服务；
- 产业升级；
- 前沿技术；
- 先进制造；
- 通信/半导体；
- 物联网/硬件；
- 工具软件；
- 智能硬件。

全量完成条件：

- 每个查询组合从第 1 页遍历到来源声明的最后一页；
- 已读取条数与来源返回总数可核对；
- 相同项目 ID 跨行业、跨切片出现时只保留一个候选；
- 失败页可重试和断点续跑；
- 预览输出包含查询组合、页数、来源总数、唯一项目数和失败数。

### 6.2 增量同步

首期建议：

- 每 6 小时读取“最新收录”；
- 每 6 小时读取“最近更新”；
- 新发现项目立即拉取详情并进行范围判定；
- 最近 90 天发现的项目每日复查；
- 其他已纳入项目每周复查；
- 待核验项目每月重试；
- 已入池项目发生变化时更新原线索，不占用每日新增配额。

### 6.3 请求控制

建议默认配置：

```dotenv
KR36_PROJECT_SYNC_ENABLED=false
KR36_PROJECT_ADMISSION_ENABLED=false
KR36_PROJECT_MIN_FOUNDED_YEAR=2025
KR36_PROJECT_SYNC_INTERVAL_SECONDS=21600
KR36_PROJECT_DAILY_QUOTA=1
KR36_PROJECT_ADMISSION_HOUR=9
KR36_PROJECT_ADMISSION_MINUTE=0
KR36_PROJECT_DETAIL_CONCURRENCY=3
KR36_PROJECT_REQUEST_MIN_DELAY_MS=350
KR36_PROJECT_MAX_RETRIES=3
KR36_PROJECT_RECHECK_RECENT_DAYS=90
KR36_PROJECT_SAFETY_STOCK_DAYS=45
```

实现要求：

- 默认关闭，完成验收后再单独启用；
- 对超时、5xx、429 采用有上限的指数退避；
- 403、验证码或响应结构变化立即停止当前来源批次并告警；
- 不记录 Cookie、Token、密码或完整敏感响应头；
- 不使用浏览器绕过来源访问控制；
- 来源适配器与业务 Pipeline 解耦，接口变化不应破坏现有共享线索服务。

## 7. 赛道识别规则

### 7.1 宽召回

候选项目只要命中相关 36氪行业分类，即进入详情抓取阶段。宽召回阶段不直接决定入池。

### 7.2 严格判定

判定输入：

- 项目名称；
- 一句话介绍；
- 完整项目简介；
- 36氪行业分类；
- 36氪标签；
- 结构化产品和业务描述。

首版规则词表示例：

| 赛道 | 强信号示例 |
|---|---|
| 人工智能 | 人工智能、大模型、智能体、AIGC、AGI、机器学习、深度学习、多模态、计算机视觉、自然语言、模型训练、模型推理 |
| 具身智能 | 具身智能、人形机器人、机器人本体、VLA、灵巧手、机械臂、运动控制、伺服、机器人大小脑、外骨骼 |
| 半导体 | 半导体、芯片、集成电路、晶圆、EDA、封装、光刻、掩膜、探针台、功率器件、SoC、GPU、NPU、FPGA、MCU、ASIC、IP 核 |

规则要求：

- “智能”“科技”“自动化”等泛词不能单独作为 AI 入池依据；
- 新闻、股东、投资人或应用场景中偶然出现关键词，不能直接判定主体属于该赛道；
- “通信/半导体”分类中的纯通信项目不得自动归为半导体；
- 一个项目可输出多个目标赛道标签；
- 规则版本必须保存到判定记录，便于重跑和审计；
- 低置信度和冲突项目进入人工复核，不自动放行。

### 7.3 成立时间硬门槛

无论赛道置信度多高，只要权威工商成立日期早于 2025-01-01，必须排除。列表年份和详情工商日期冲突时，以详情工商日期为准并记录冲突证据。

## 8. 去重与更新规则

### 8.1 来源内去重

稳定来源键：

```text
36kr:project:<sourceProjectId>
```

同一来源项目 ID：

- 首次发现：创建候选；
- 内容不变：仅更新 `last_seen_at/fetched_at`；
- 内容变化：创建新原始事件并更新候选投影；
- 已入池：合并更新原正式线索，不创建第二条。

### 8.2 跨来源主体匹配

每日释放必须复用现有正式主体匹配链，匹配依据包括：

- 工商公司全称；
- 统一信用代码（如已有可靠证据）；
- 规范化项目名称；
- 官方网站；
- 已有来源 URL 和来源键；
- 人工确认的别名和实体映射。

出现多条同名正式线索时进入 Pipeline review，不能随机选择或直接创建新线索。

### 8.3 来源数据保护

- 36氪原始字段保留来源标签和原始链接；
- AI 补全为空时不得覆盖 36氪已有值；
- AI 与来源冲突时保留冲突，不静默覆盖；
- 人工已确认字段不得被自动采集无条件替换；
- 融资轮次按来源融资 ID 或“轮次+时间”合并；
- 团队和股东按稳定身份或规范化姓名合并。

## 9. 每日释放策略

### 9.1 默认配额

- 默认每个自然日释放 1 条；
- 配额只计算首次创建的正式线索；
- 已有线索更新不消耗配额；
- 当天任务重试不得重复消耗配额；
- 多实例运行时只能有一个任务实例取得当天释放租约。

### 9.2 候选选择优先级

1. 当天首次发现且符合范围的新项目；
2. 最近 7 天首次发现且尚未入池的项目；
3. 合格库存中等待时间最长的项目；
4. 在同等条件下优先补足本周赛道结构。

建议周度赛道目标：

- 人工智能：4 条；
- 具身智能：2 条；
- 半导体：1 条。

赛道库存不足时允许回退到其他目标赛道，但必须记录实际分布，不得选取范围外项目补数。

### 9.3 库存预警

| 合格未入池库存 | 状态 | 动作 |
|---|---|---|
| `>= 120` | 充足 | 保持 1 条/日；提高配额需单独评审 |
| `60-119` | 正常 | 固定 1 条/日 |
| `30-59` | 预警 | 保持 1 条/日，检查来源覆盖和分类漏召回 |
| `< 30` | 高风险 | 禁止提高配额，发出耗尽预警 |
| `0` | 耗尽 | 当日不制造新增，继续采集并明确记录无可释放候选 |

库存可维持天数：

```text
inventoryDays = eligibleNotAdmitted / dailyAdmissionQuota
```

### 9.4 30 天流量校准

上线后连续记录：

- `sourceDiscovered`：来源首次发现数；
- `eligibleDiscovered`：通过范围判定数；
- `updatedExisting`：已有候选或线索更新数；
- `admittedNew`：正式新增线索数；
- `duplicateMatched`：匹配已有主体数；
- `pendingReview`：待人工复核数；
- `eligibleInventory`：合格库存；
- `inventoryDays`：可维持天数。

满 30 天后才能评估真实月度来源流入，并决定是否将每日配额从 1 调整为 2。调整配额属于配置变更，需要保留评审和审计记录。

## 10. 开发工作分解

### 阶段 0：契约与预览工具

目标：不写业务数据库，先证明来源可读、分页完整、字段可解析。

工作项：

1. 定义 36氪列表、详情、分页和错误响应 DTO。
2. 建立脱敏的列表/详情测试 Fixture。
3. 实现来源适配器和响应结构校验。
4. 实现全量回溯预览命令，仅输出统计和 JSONL 报告。
5. 输出宽候选数、唯一项目数、详情成功数、成立时间分布、赛道分布和失败页。
6. 验证公开接口的访问规则、限流和停止条件。

交付物：

- 36氪来源适配器；
- DTO 和解析器；
- Fixture 单元测试；
- `preview` 命令；
- 预览报告样例；
- 外部接口风险说明。

### 阶段 1：原始事件与候选投影

目标：在隔离数据库中安全保存全量候选，但不创建正式线索。

工作项：

1. 增加候选投影表和必要索引。
2. 使用 `lead_pipeline_raw_events` 保存不可变原始事件。
3. 建立稳定来源 ID、内容哈希和幂等键。
4. 实现首次发现、内容不变、内容变化三种写入路径。
5. 实现范围状态、排除原因和规则版本记录。
6. 实现断点续跑和失败页重试。
7. 增加只读库存统计查询。

交付物：

- 新迁移文件；
- Schema 定义；
- 候选 Repository/Service；
- 全量同步服务；
- 候选统计接口或运维命令；
- 隔离 MySQL 集成测试。

### 阶段 2：分类、主体匹配与人工复核

目标：确保只有成立时间和赛道均符合的唯一主体进入待释放队列。

工作项：

1. 实现成立时间规范化和硬门槛。
2. 实现三赛道多标签分类器和规则版本。
3. 实现低置信度、列表/详情时间冲突和行业冲突复核。
4. 接入注销主体排除规则。
5. 复用现有实体匹配、实体映射和 Pipeline review。
6. 保存判定证据和候选匹配结果。

交付物：

- 范围判定服务；
- 三赛道分类器；
- 判定证据契约；
- 人工复核状态；
- 分类和去重测试集。

### 阶段 3：每日释放任务

目标：每天最多创建 1 条真实新线索，并保证重试和多实例幂等。

工作项：

1. 增加 `36kr-project-daily-admission` Runtime Job。
2. 使用任务租约和候选行锁保证单实例释放。
3. 实现当日新项目优先、库存补足和周度赛道平衡。
4. 通过 `commitRadarLeadPipelineReady()` 创建或合并正式线索。
5. 处理实体歧义、注销主体、重复主体和任务重试。
6. 成功后绑定 `admitted_lead_id` 并触发补全/评分。
7. 记录每日配额使用情况和库存快照。

交付物：

- 每日释放服务；
- Runtime Job 注册；
- 配置项及 `.env.example` 说明；
- 任务可靠性和幂等测试；
- 审计与指标。

### 阶段 4：已入池项目更新

目标：36氪信息变化更新原线索，不重复新增。

工作项：

1. 对比内容哈希和字段级差异。
2. 复用来源保护合并策略更新原线索。
3. 新融资、团队、工商和项目简介变化保留来源证据。
4. 更新与每日新增计数完全分开。
5. 冲突字段进入复核，不覆盖人工确认值。

交付物：

- 字段差异与合并服务；
- 更新审计；
- 来源保护回归测试；
- 新增/更新分离指标。

### 阶段 5：监控、页面和运营验收

目标：运营方能确认当天是否采集、是否释放、库存还能维持多久。

最小展示内容：

- 最近采集成功时间；
- 最近释放成功时间；
- 当日来源发现数；
- 当日合格新增数；
- 当日正式入池数；
- 当前合格库存；
- 预计可维持天数；
- 三赛道库存分布；
- 待复核数；
- 最近失败和错误类型。

如首期不增加管理页面，上述信息至少通过受权限保护的运维接口和结构化日志提供。

## 11. 建议排期

| 里程碑 | 工作内容 | 建议周期 | 完成标志 |
|---|---|---:|---|
| M0 | 契约、Fixture、只读预览 | 2-3 个开发日 | 可重复输出来源统计，不写数据库 |
| M1 | 候选表、原始事件、全量同步 | 3-4 个开发日 | 隔离库完整保存候选且幂等 |
| M2 | 时间/赛道判定、主体匹配 | 2-3 个开发日 | 排除、复核、合格状态可解释 |
| M3 | 每日释放、配额、库存监控 | 2-3 个开发日 | 隔离库每天最多新增 1 条 |
| M4 | 来源更新、HTTP/UI、回归 | 2-3 个开发日 | 更新不重复，页面口径正确 |
| M5 | 小流量试运行 | 至少 7 个自然日 | 每日任务稳定、无重复、可回滚 |
| M6 | 月度流量校准 | 30 个自然日观察 | 得到真实月度首次发现量 |

开发时间不包含来源接口重大变更、业务规则重新定义或生产数据库授权等待。

### 11.1 建议代码影响清单

以下是实施时的预期触点，不代表本次已经修改：

| 路径/模块 | 计划变更 |
|---|---|
| `server/src/db/schema.ts` | 增加候选投影和索引定义 |
| `server/drizzle/<next>_*.sql` | 使用下一个未占用迁移号新增候选表/约束 |
| `server/src/services/leadPipelineEventService.ts` | 复用原始事件、幂等和状态迁移，不降低现有不可变约束 |
| `server/src/services/leadReserveProjection.ts` | 提取可复用的 36氪详情规范化投影，保持历史来源兼容 |
| `server/src/services/aiSummaryService.ts` | 复用 `commitRadarLeadPipelineReady()` 的主体创建/合并边界 |
| `server/src/services/runtimeJobScheduler.ts` | 注册采集与每日释放两个独立 Runtime Job |
| `server/src/services/kr36ProjectSourceService.ts` | 建议新增：来源列表/详情适配、限流、重试和契约校验 |
| `server/src/services/kr36ProjectCandidateService.ts` | 建议新增：候选投影、范围判定和库存查询 |
| `server/src/services/kr36ProjectAdmissionService.ts` | 建议新增：每日选取、主体匹配、事务入池和配额幂等 |
| `server/src/routes/meta.ts` 或独立运维路由 | 受权限保护的预览、库存和任务状态接口 |
| `.env.example` | 增加默认关闭的采集/释放配置说明 |
| `package.json` | 增加 preview、accept 和 observe 脚本 |
| `server/tests/` | 增加来源契约、分类、幂等和来源保护单元测试 |
| `server/src/scripts/` | 增加隔离 MySQL、HTTP、Runtime Job 和供给观察验收脚本 |

实施约束：

- 不把 36氪项目库采集塞入现有融资快报解析分支；
- 不修改 `lead_reserve` 的历史语义来冒充实时候选库；
- 不复制一套新的主体匹配、补全或评分实现；
- 新增服务应依赖现有 Pipeline 和 Runtime Job 基础设施；
- 如果现有函数不能安全复用，先提取通用服务并补回归测试，再接入新来源。

## 12. 测试计划

### 12.1 纯单元测试

不得连接业务数据库，覆盖：

- 列表和详情正常解析；
- 字段缺失和 `null`；
- 页面总数、总页数和空页；
- 成立时间 `2024-12-31` 被排除；
- 成立时间 `2025-01-01` 被纳入；
- 时间戳、中文年月和标准日期格式；
- 列表年份与详情工商日期冲突；
- 三赛道单标签和多标签；
- 泛词“智能”不能单独命中 AI；
- 纯通信项目不能因“通信/半导体”分类进入半导体；
- 项目简介提及机器人但主体业务无关时进入复核或排除；
- 内容字段排序变化不应产生无意义哈希漂移；
- 来源 ID 和幂等键稳定；
- 36氪原始值不被空 AI 字段覆盖。

### 12.2 来源契约测试

使用固定脱敏 Fixture 验证：

- 列表 DTO；
- 详情 DTO；
- 错误 DTO；
- 响应结构漂移时失败关闭；
- 未识别字段允许保存在原始事件，但不能误写正式字段；
- 错误页面和验证码页面不得当作正常 JSON 处理。

### 12.3 隔离 MySQL 集成测试

必须使用明确的隔离测试数据库，覆盖：

1. 首次采集创建原始事件和候选投影。
2. 同一项目重复采集不增加候选。
3. 内容不变不增加原始事件版本。
4. 内容变化增加不可变事件并更新候选投影。
5. 同一项目跨行业查询只生成一个候选。
6. 合格候选每日只释放一条。
7. 任务失败回滚后可以重试。
8. 两个并发任务只能有一个成功消耗当日配额。
9. 已入池项目更新不生成第二条正式线索。
10. 主体匹配歧义进入 review，不创建重复线索。
11. 注销企业被排除且不触发补全/评分。
12. 候选入池后正确绑定 `admitted_lead_id`。

### 12.4 真实来源只读验收

该层只验证来源可读和字段可解析，不写业务数据库：

- 2025、2026 和当前年份筛选可执行；
- 相关行业分页能够遍历到结束；
- 项目详情能读取项目 ID、项目名、简介、行业和工商成立时间；
- 至少各抽样 AI、具身智能、半导体 5 条；
- 来源限流、超时和结构异常能被识别；
- 不登录、不提交表单、不绕过验证码。

### 12.5 HTTP 验收

在隔离运行环境验证：

- 管理/运维身份可读取候选和库存统计；
- 普通用户不能读取内部错误和运维信息；
- 采集预览不会创建正式线索；
- 手工触发采集和释放分别受独立权限保护；
- 重复请求具备幂等键或任务互斥；
- 错误响应不泄露凭据、内部连接串或完整来源响应头；
- `/leads` 返回的新线索带有正确 36氪渠道、成立时间和来源链接。

### 12.6 页面验收

使用真实登录态和真实 HTTP 服务验证：

- 新释放项目出现在共享线索池，而不是正式项目池；
- 成立时间显示不早于 2025-01-01；
- 行业显示目标赛道标签；
- 36氪来源链接打开正确项目；
- 页面“最新入池”使用正式入池时间，不冒充来源更新时间；
- 已有项目来源变化不会在列表产生第二条线索；
- 用户领取后进入普通项目并保留来源和评分信息；
- 候选库内部状态不暴露给无权限用户。

### 12.7 运行期验收

至少连续观察 7 个自然日：

- 每日任务按上海时区执行一次；
- 合格库存大于 0 时每天新增恰好 1 条；
- 没有任何来源 ID 重复线索；
- 任务重启或多实例竞争不会重复释放；
- 采集失败不影响现有 API 和其他 Runtime Job；
- 库存、失败、待复核和可维持天数每天产生快照；
- 补全与评分队列没有因新增任务发生失控堆积；
- 来源出现 403、429 或结构漂移时任务停止并告警。

## 13. 验收清单

### 13.1 数据范围验收

| 编号 | 场景 | 预期结果 |
|---|---|---|
| DATA-01 | 工商成立日期为 2024-12-31 | 排除，原因 `founded_before_2025` |
| DATA-02 | 工商成立日期为 2025-01-01 | 通过时间门槛 |
| DATA-03 | 2024 年成立但 2025 年融资 | 排除，不按融资日期放行 |
| DATA-04 | 列表显示 2025、详情工商显示 2024 | 排除并保存冲突证据 |
| DATA-05 | 成立时间缺失 | 进入 `founded_at_pending` |
| DATA-06 | AI 项目命中多个强信号 | 标记 AI |
| DATA-07 | 具身智能芯片项目 | 同时标记具身智能、半导体，可选 AI |
| DATA-08 | 名称带“智能”但无 AI 业务证据 | 不自动纳入 AI |
| DATA-09 | 纯通信设备项目 | 不因大类名称自动归为半导体 |
| DATA-10 | 已注销企业 | 排除且不触发补全/评分 |

### 13.2 分页与完整性验收

| 编号 | 场景 | 预期结果 |
|---|---|---|
| COLLECT-01 | 单一查询多页 | 从第 1 页遍历到最后一页，无页码缺口 |
| COLLECT-02 | 项目跨分类重复出现 | 唯一项目 ID 数正确，候选仅一条 |
| COLLECT-03 | 中间页失败 | 记录失败游标，重试后继续，不从头重复写入 |
| COLLECT-04 | 来源总数变化 | 本次快照记录开始/结束总数和差异 |
| COLLECT-05 | 响应结构变化 | 当前批次失败关闭并告警，不写错误字段 |
| COLLECT-06 | 全量任务重跑 | 候选数量不增长，内容不变事件不重复 |

### 13.3 每日释放验收

| 编号 | 场景 | 预期结果 |
|---|---|---|
| ADMIT-01 | 当天有首次发现合格项目 | 优先释放其中一条 |
| ADMIT-02 | 当天无首次发现但有库存 | 从合格库存释放一条 |
| ADMIT-03 | 当天任务重复执行 | 第二次不再创建新线索 |
| ADMIT-04 | 两实例同时执行 | 仅一个实例取得租约并释放 |
| ADMIT-05 | 候选匹配已有线索 | 更新/绑定已有线索，不创建重复记录 |
| ADMIT-06 | 匹配多条同名线索 | 进入人工复核，当日不随机放行 |
| ADMIT-07 | 合格库存为 0 | 入池数为 0，记录耗尽状态，不造假 |
| ADMIT-08 | 入池事务中途失败 | 候选和正式线索状态一致回滚，可安全重试 |
| ADMIT-09 | 已入池项目详情变化 | 更新原线索，`admittedNew` 不增加 |
| ADMIT-10 | 成功入池 | 触发既有补全和评分，保留来源证据 |

### 13.4 数据质量验收

| 指标 | 门槛 |
|---|---:|
| 来源项目 ID 完整率 | 100% |
| 合格入池项目成立时间有效率 | 100% |
| 合格入池项目成立时间早于 2025 的数量 | 0 |
| 来源项目 ID 重复候选数 | 0 |
| 来源项目 ID 重复正式线索数 | 0 |
| 多实例单日超配额数 | 0 |
| 空 AI 字段覆盖来源有效字段数 | 0 |
| 随机行业样本判定准确率 | `>= 95%` |
| 主体匹配歧义被自动错误放行数 | 0 |

行业抽样要求：每个赛道随机抽查最多 30 条；不足 30 条时抽查全部。多标签项目可同时计入多个赛道样本，但主体只计一条候选。

### 13.5 供给连续性验收

当前约 200 条只是调研快照，不得写成固定断言。验收应检查：

- 当前预览结果可由查询组合、唯一 ID 和排除原因完整重算；
- 全量采集后候选库存与预览唯一合格数一致；
- 启用每日释放前，正式线索新增数为 0；
- 合格库存大于 0 的连续 7 天，每天新增恰好 1 条；
- 库存天数按真实合格未入池数量计算；
- 30 天后能够输出真实 `firstSeenAt` 月度流入，不使用成立月份替代。

## 14. 建议测试与验收命令

实现后应增加专用脚本，命名建议：

```text
kr36:projects:preview
kr36:projects:sync
kr36:projects:admit:preview
kr36:projects:admit
accept:kr36-project-contract
accept:kr36-project-classification
accept:kr36-project-mysql
accept:kr36-project-http
accept:kr36-project-runtime
observe:kr36-project-supply
```

基础门禁继续使用现有项目命令：

```bash
npm run check:types
npm run test:server
npm run check:platform
npm run accept:lead-pipeline-events
npm run accept:lead-pipeline-audit
npm run accept:lead-entity-matches
npm run accept:lead-pool-ui
npm run accept:runtime-job-reliability
npm run accept:runtime-job-leader
npm run build
```

执行口径：

- `check:types`、Fixture 单元测试和静态验收属于纯测试；
- 连接隔离 MySQL 的测试属于数据库集成验收；
- 真实 36氪请求属于外部来源只读验收；
- 登录态页面检查属于 HTTP/浏览器验收；
- 连续 7 天调度观察属于运行期验收；
- 上述证据不得互相替代或重复汇总为同一类通过项。

## 15. 发布步骤

### 15.1 发布前

1. 确认迁移编号未被占用且未覆盖已应用迁移。
2. 在隔离数据库执行迁移和集成验收。
3. 运行全量 `preview`，保存唯一 ID、分类、排除和失败报告。
4. 确认初始配置中采集和释放开关均为关闭。
5. 通过类型、服务端测试、Pipeline、Runtime Job 和构建门禁。
6. 准备候选表、原始事件和正式线索的只读核对 SQL。
7. 准备停用开关和构建回滚方案。

### 15.2 小流量启用

1. 先启用采集、保持每日释放关闭。
2. 仅采一个年份、一个行业切片作为 canary。
3. 核对原始事件、候选、哈希、排除原因和重复数。
4. 扩展到 2025 年至今的全部候选行业。
5. 核对全量候选和预览结果。
6. 开启每日释放预览，确认当天选择目标。
7. 经授权后开启每日释放，每天 1 条。

### 15.3 运行观察

至少连续 7 天记录：

- 任务运行和租约；
- 每日来源发现；
- 每日合格候选；
- 每日正式新增；
- 重复和复核；
- 补全/评分队列；
- 来源错误；
- 库存及可维持天数。

第 30 天输出月度供给报告，再评审每日配额。

## 16. 回滚方案

### 16.1 配置回滚

优先关闭：

```dotenv
KR36_PROJECT_ADMISSION_ENABLED=false
KR36_PROJECT_SYNC_ENABLED=false
```

每日释放和来源采集必须能够分别关闭。

### 16.2 数据回滚

- 不删除不可变原始事件；
- 不删除候选投影，保留排查证据；
- 已入池线索不得因任务停用自动删除；
- 错误创建的正式线索必须通过现有授权软删除/合并流程处理；
- 不通过回滚迁移破坏已绑定的 `admitted_lead_id`；
- 需要重建候选投影时，从原始事件重建并核对哈希。

### 16.3 构建回滚

遵循项目现有候选构建、激活和回滚流程。构建回滚只能证明代码版本恢复，仍需分别核对任务开关、Runtime Job、数据库状态和页面行为。

## 17. 风险与缓解措施

| 风险 | 影响 | 缓解措施 |
|---|---|---|
| 36氪接口未公开、字段变化 | 采集中断或字段错位 | 来源适配器、契约测试、结构漂移失败关闭 |
| 来源限流/403 | 批次失败 | 低并发、请求间隔、指数退避、立即告警 |
| 成立时间口径冲突 | 误纳入旧企业 | 详情工商日期硬门槛、冲突进入复核 |
| 36氪分类过宽 | 行业误判 | 宽召回、严格二次判定、人工抽样 |
| 同一企业多个项目页 | 重复线索 | 来源 ID 去重后再做公司主体匹配 |
| 与 Radar/BP 数据重合 | 跨来源重复 | 复用正式实体匹配和人工 review |
| 全量采集直接灌入业务池 | 首日耗尽库存 | 采集与每日释放双任务、双开关 |
| 每日任务并发 | 单日新增超过配额 | Runtime Job 租约、候选锁、日配额幂等键 |
| 补全/评分积压 | AI 队列失控 | 每日仅 1 条、监控队列、独立暂停释放 |
| 月度新增量不可直接获得 | 配额决策失真 | 记录 `firstSeenAt`，运行 30 天后校准 |
| 库存最终耗尽 | 无法保证每日新增 | 45 天安全库存预警，不制造重复项目 |

## 18. 监控指标

### 18.1 来源采集

- `kr36_project_sync_runs_total`；
- `kr36_project_sync_last_success_at`；
- `kr36_project_pages_fetched_total`；
- `kr36_project_details_fetched_total`；
- `kr36_project_source_errors_total{type}`；
- `kr36_project_contract_drift_total`；
- `kr36_project_rate_limit_total`。

### 18.2 候选质量

- `kr36_project_candidates_total{scope_status}`；
- `kr36_project_candidates_total{sector}`；
- `kr36_project_duplicates_total{match_type}`；
- `kr36_project_pending_review_total{reason}`；
- `kr36_project_content_updates_total`。

### 18.3 每日释放

- `kr36_project_admission_runs_total{status}`；
- `kr36_project_admitted_new_total`；
- `kr36_project_updated_existing_total`；
- `kr36_project_daily_quota_used`；
- `kr36_project_eligible_inventory`；
- `kr36_project_inventory_days`；
- `kr36_project_last_admitted_at`。

告警建议：

- 采集连续 2 个周期失败；
- 发现来源契约漂移；
- 单日新增超过配置配额；
- 来源项目 ID 重复正式线索；
- 库存低于 45 天；
- 补全或评分队列超过既有容量阈值；
- 每日释放任务在合格库存大于 0 时未成功运行。

## 19. 完成定义

只有同时满足以下条件，功能才可标记为完成：

1. 开发范围内源码、迁移、配置、脚本和测试已提交评审。
2. 类型检查、纯测试、来源契约测试和构建全部通过。
3. 隔离 MySQL 集成测试通过，未使用业务数据库制造验收数据。
4. 全量预览可重算候选、排除、复核和合格数量。
5. 真实来源只读抽样通过，没有绕过访问控制。
6. 采集开启、释放关闭时，正式线索新增数为 0。
7. 释放开启后连续 7 天每天最多且预期为 1 条，无重复线索。
8. 已有项目更新不会增加每日新增计数。
9. 登录态共享线索池页面显示正确来源、成立时间和赛道。
10. 正式项目池未被采集任务直接写入。
11. 任务、库存、错误和审计证据完整。
12. 配置停用和构建回滚经过验证。
13. 未声称 7 天观察已经获得 30 天月度供给结论。

## 20. 待评审问题

开发开始前需确认：

1. 每日新增按自然日执行，还是仅工作日执行；本文默认自然日。
2. 海外项目是否纳入；本文默认只要符合成立时间和赛道即纳入，不额外按地区排除。
3. 赛道周度比例是否采用 AI 4、具身智能 2、半导体 1。
4. 候选人工复核是否需要首期管理页面，还是先提供受权限保护的运维接口。
5. 合格库存低于 30 条时，是继续每天 1 条，还是改为工作日释放。
6. 30 天观察后提高每日配额的审批人和审计方式。

在上述问题未确认前，默认采用最保守行为：不扩大数据范围、不提高每日配额、不把待核验项目自动放入共享线索池。

## 21. 实施结果与运行手册（2026-09-01）

### 21.1 已完成源码

- 新增 `lead_source_candidates` 候选投影和 `0091_add_lead_source_candidates.sql`。
- 新增36氪列表/详情适配器，兼容当前 `data.projectList / data.page.totalCount` 与历史详情接口。
- 新增成立时间、三赛道和注销主体范围判定，规则版本为 `kr36-focus-v1`。
- 新增全量回填、增量同步、不可变原始事件、来源 ID 幂等和已入池项目原记录更新。
- 新增每日默认 1 条准入、当天配额占位、多实例互斥、超时处理中恢复和周度赛道轮换。
- 新增 `kr36-project-sync` 与 `kr36-project-daily-admission` 两套默认关闭的 Runtime Job。
- 新增系统管理员运维接口、只读预览、候选库存报告、来源保护和隔离 MySQL 测试门禁。
- 历史 `lead-reserve-daily-intake` 未删除、未改配置；只有新链路灰度通过后才停用。

### 21.2 运行命令

```bash
# 纯测试与构建
npm run accept:kr36-projects
npm run check:types
npm run build

# 真实来源只读预览，不写数据库
npm run preview:kr36-projects -- --pages-per-year=1

# 全量只读预览，JSONL 输出路径由操作人显式指定
npm run preview:kr36-projects -- --backfill --output=.runtime/kr36-preview.jsonl

# 迁移后只读查看库存
npm run report:kr36-supply
```

系统管理员运维接口：

- `GET /api/operations/kr36`：库存和任务状态；
- `GET /api/operations/kr36/preview?pages=1`：真实来源只读预览；
- `PATCH /api/operations/kr36/jobs/:id`：独立启停任务并写审计；
- `POST /api/operations/kr36/jobs/:id/run`：排队立即执行并写审计。

### 21.3 灰度与切换

1. 仅在隔离数据库运行迁移和 MySQL 集成测试；数据库名必须含 `test`、`tests` 或 `acceptance`，并显式设置 `ALLOW_MYSQL_INTEGRATION_TESTS=1`。
2. 真实来源预览必须先恢复成功。403、HTML、验证码或结构漂移都属于硬阻断，不能使用搜索结果页静默补数。
3. 先启用采集、保持准入关闭。首次可临时使用 `KR36_PROJECT_SYNC_MODE=backfill`，完成后必须改回 `incremental`。
4. 核对候选库存后再开启每日准入，并保持 `KR36_PROJECT_DAILY_QUOTA=1`。
5. 连续观察至少 3 天完成技术灰度，按原计划连续 7 天完成运行期验收。
6. 确认无重复、每天不超配额、已入池更新不占配额后，再将 `DAILY_INTAKE_ENABLED=false`。

### 21.4 当前验收状态

- 纯测试、类型检查、数据库边界检查和生产构建已通过。
- 隔离 MySQL 用例已实现但因当前没有获授权的隔离库而安全跳过。
- 2026-09-01 真实来源只读预览返回 403，36氪浏览器页面同时出现 `Failed to fetch`；来源恢复前不得开启采集或准入。
- 未执行业务数据库迁移、HTTP 登录态验收、页面验收、服务重启、生产任务启用和连续 7 天观察。

### 21.5 回滚

- 分别关闭 `KR36_PROJECT_SYNC_ENABLED` 和 `KR36_PROJECT_ADMISSION_ENABLED`。
- 不删除候选、不可变原始事件或已绑定正式线索。
- 错误正式线索只通过现有授权的软删除/合并流程处理。
- 旧 50 条任务仅用于受控切换与回滚，不能与新准入任务同时释放数据。
