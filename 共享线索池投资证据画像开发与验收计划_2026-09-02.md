# 共享线索池投资证据画像开发与验收计划

> 文档版本：V1.2
>
> 编制日期：2026-09-02
>
> 适用项目：`/Users/hyw/Desktop/sbl_jedi`
>
> 适用分支：当前 `main` 工作树
>
> 当前状态：源码迭代与隔离源码浏览器验收已完成；按客户 2026-09-03 最新确认，列表七列为“线索主体、行业 / 产品路线、机构 / 高校背景、融资进展、最新估值、最新动态、更新时间”；顶部筛选保持旧版“线索类型、行业、阶段、地区、渠道、更新时间”六项，不展示新增投资画像筛选；大客户验证和数据状态仅从列表移除，详情页与服务端画像继续保留
>
> 本文性质：开发与验收计划；当前源码完成情况见同目录《共享线索池投资证据画像迭代实现报告_2026-09-02.md》，本文不代表数据已回补、本地版本已生效或生产已发布

## 1. 背景与目标

客户希望共享线索池优先展示能够直接支持投资筛选的信息：

- 具体行业和产业链位置；
- 产品路线、技术路线和产业化阶段；
- 过往大机构投资；
- 高校、科研院所和成果转化背景；
- 融资历史、最新轮次和融资金额；
- 最新估值及其口径；
- 经过证据验证的大客户及商业验证阶段。

本次迭代同时调整列表信息架构：列表不再展示“一句话摘要”和“核心信息”，长篇摘要、工商基础信息和长推荐理由下沉详情页；列表保留精简最新动态和独立更新时间，大客户验证与数据状态则仅从列表移除，其详情页和服务端画像能力继续保留。顶部筛选项和交互保持旧版不变。

目标结果：

1. 每条可见线索能够形成一份可追溯的“投资证据画像”。
2. 列表每一行能够回答“做什么、走什么路线、谁投过、来自哪里、融到什么程度、估值多少、最近发生了什么、数据何时更新”；大客户验证和完整数据状态在详情页核对。
3. 行业、产品路线、投资机构、高校、融资、估值和客户事实全部绑定主体、时间、来源、证据和验证状态。
4. 顶部筛选保持旧版六项；投资画像高级查询仅作为服务端预留能力，不在本期列表顶部展示或暗中应用。
5. 数据完整度与项目评级分开；信息缺失不等于项目差，宣传表述也不能冒充已验证事实。
6. 新投影可从事实快照重建，不把列表 JSON、Zustand 或浏览器状态变成业务事实源。

## 2. 已确认的产品决策

### 2.1 目标列表列

目标表头固定为七列：

| 顺序 | 列名 | 主要内容 |
|---:|---|---|
| 1 | 线索主体 | 项目/品牌名称、工商主体、地区、主体类型、线索评级 |
| 2 | 行业 / 产品路线 | 细分行业、产业链环节、核心产品、技术路线、产业化阶段 |
| 3 | 机构 / 高校背景 | 大机构历史投资、高校/科研院所、人物关系、成果转化关系 |
| 4 | 融资进展 | 最新已完成轮次、融资日期、本轮金额、累计融资 |
| 5 | 最新估值 | 投前/投后估值、币种、对应轮次、估值日期 |
| 6 | 最新动态 | 最新事件的精简时间线，不展示长摘要或来源原文 |
| 7 | 更新时间 | 数据最近更新时间；缺失时按既定口径回退入池时间 |

### 2.2 从列表移除或下沉的内容

- 删除“一句话摘要”列及行内摘要浮层。
- 删除“核心信息”列；成立时间、法人和注册地址保留在详情页。
- 推荐理由不再占独立列表列；线索评级保留为“线索主体”内的小型状态。
- 最新动态保留为第 6 列，只展示精简动态；完整时间线和原始来源继续保留在详情页。
- 大客户验证和数据状态不再占列表列；这两类能力仅从列表展示移除，不删除详情页、服务端画像、审计或高级查询能力。
- 列表不展示长段 AI 文字、来源原文或超过两行的说明。

### 2.3 空值和状态语义

- 缺失、待核验、待核实、暂未披露等无有效展示值时统一显示 ASCII `-`。
- “未融资”是有效融资状态，不等于缺失；未融资时金额和估值显示 `-`。
- “融资信息未披露”不能生成虚构金额或估值。
- “不适用”用于科研成果等不适用主体，融资、金额和估值统一显示 `-`。
- 冲突事实不得静默选择一个值，列表显示“存在冲突”，详情页提供有权限的证据查看或管理复核入口。

## 3. 编制时实现基线

### 3.1 已有能力

- `lead-enrichment-v3` 已定义 13 类研究专题，包括融资、团队、客户合同、产品、技术知识产权、产业化和交易退出。
- `lead_facts`、`lead_fact_evidence`、`lead_fact_conflicts` 和 `lead_enrichment_snapshots` 已支持事实、来源、冲突和不可变快照。
- `GET /api/leads` 已提供分页列表，并有关键词、行业、地区、阶段、渠道、类型和更新时间筛选基础。
- 当前列表已独立展示融资状态、融资金额和估值。
- 详情页已有主体基础信息、产品与商业化、团队成员、证据与动态等模块。
- 当前列表查询与分页正在单独修复，相关计划见 `共享线索池查询与分页缺陷修复及验收计划_2026-09-02.md`。

### 3.2 当前缺口

1. 详情页联网补全目前只启用基本画像、融资、团队、产品和最新动态五类专题。
2. 客户合同、技术路线、产业化和交易估值虽有事实契约，但未进入当前详情页补全范围。
3. 融资事实缺少统一的轮次日期、领投/跟投、机构类型和机构等级结构。
4. `team.education` 不能完整表达人物—高校—院系/实验室—成果转化关系。
5. 客户专题缺少稳定客户身份、脱敏标签、客户等级和统一验证阶段投影。
6. 列表 DTO 仍围绕摘要、核心信息、推荐理由和动态设计，缺少新的投资画像读模型。
7. 当前 `leads.scoring` 和 `radar_profile` 含历史兼容数据，不能继续作为新投资画像字段的唯一权威来源。

## 4. 范围与边界

### 4.1 本期包含

- 投资证据画像字段字典、关系定义和验证规则；
- 行业/产品路线、机构投资、高校背景、融资估值和客户验证事实契约；
- 事实快照到列表读模型的确定性投影；
- `GET /api/leads` 列表 DTO、筛选、排序和分页扩展；
- 七列列表重构、加载/失败/空值/冲突/科研主体适配；
- 详情页必要的证据落点或跳转，不进行无关页面重构；
- 存量数据覆盖率只读审计、回补 preview、灰度和幂等验证；
- 单元、契约、服务端、隔离 MySQL、HTTP、浏览器和真实数据库只读验收；
- 候选构建、可选本地激活、发布和回滚计划。

### 4.2 本期不包含

- 未经确认修改 V3 投资评分权重或评级阈值；
- 把数据完整度直接合并进项目投资评级；
- 未经授权对业务数据库执行迁移、批量回补或重处理；
- 未经授权启动、停止、重启服务或激活候选构建；
- 未经授权发布生产、提交或推送代码；
- 以 AI 主观判断“大机构”“高校成果转化”或“大客户等级”；
- 为通过验收放宽会话、CSRF、Origin、访问权限或证据门禁；
- 重构当前查询分页修复范围之外的无关业务。

### 4.3 与当前未提交工作的协作边界

当前工作树中 `SourcingPage.tsx`、`useAppStore.ts`、`aiSummaryService.ts`、`meta.ts`、`leadPoolUiAcceptance.ts` 及查询契约/测试存在未提交修改。实施时必须：

1. 先读取并保留这些改动；
2. 将本计划的新字段和筛选增量合并到当前查询分页契约；
3. 不覆盖分页、URL 状态、地区、行业和错误恢复修复；
4. 分别验收“查询分页正确”和“投资画像正确”；
5. 不使用 `git add -A`，提交时逐文件确认。

## 5. 业务数据定义

### 5.1 行业与产品路线

行业和路线不得合并成一个自由文本字段。

| 字段 | 含义 | 示例 | 规则 |
|---|---|---|---|
| `industry.level1` | 一级行业 | 高端制造 | 使用批准的标准枚举 |
| `industry.level2` | 二级行业 | 半导体设备 | 必须可映射到一级行业 |
| `industry.segment` | 细分赛道 | 薄膜沉积设备 | 不接受仅有“科技”等宽泛词 |
| `industry.chain_position` | 产业链环节 | 上游设备 | 使用批准枚举或字典 |
| `product.name` | 核心产品名称 | PEALD 设备 | 同一产品使用稳定 `instanceKey` |
| `product.route` | 产品路线 | 原子层沉积设备路线 | 与技术路线区分 |
| `technology.route` | 技术实现路线 | 等离子增强原子层沉积 | 必须绑定产品或项目主体 |
| `production.stage` | 产业化阶段 | 中试/小批量/量产 | 规划和已实现状态分开 |
| `product.use_case` | 主要应用场景 | 先进制程薄膜沉积 | 不从行业名称直接推断 |

展示优先级：细分赛道 > 二级行业 > 一级行业；产品名称 > 产品路线 > 技术路线；缺失项不使用模型生成的营销词补齐。

### 5.2 大机构投资

“大机构”是受控业务分类，不是模型评价。

每个投资事件至少包含：

- 标准机构名称和别名；
- 所属融资轮次与融资事件 `instanceKey`；
- 领投、跟投、战略投资或未披露角色；
- 投资日期；
- 机构类型：市场化 VC/PE、国资基金、产业资本、上市公司、其他；
- 客户批准的机构等级或“大机构”标志；
- 来源、引用、验证状态和冲突状态。

建议建立受控机构字典，包含标准名称、别名、类型、等级、生效状态和修改审计。AI 只能提出机构归一化候选，不能自行把机构升级为“大机构”。

### 5.3 高校与科研背景

高校背景按关系建模，至少区分：

- 学历/校友；
- 当前教师或研究人员；
- 历史任职；
- 实验室/课题组；
- 联合研发；
- 高校成果转化；
- 专利许可或转让；
- 孵化平台或校办企业。

关系至少保存人物、机构、院系/实验室、关系类型、起止时间、对应项目/公司、来源和验证状态。仅出现学校名称时，不得推断具体人物与学校的对应关系；“毕业于某高校”不得等同于“高校成果转化项目”。

### 5.4 融资进展与估值

同一轮融资的轮次、日期、金额、币种、投资方和估值必须使用同一稳定 `instanceKey` 绑定。

必须区分：

- 已完成融资；
- 正在交割；
- 融资计划/募资目标；
- 未融资；
- 轮次或金额未披露。

估值至少保存：

- 投前或投后口径；
- 金额与币种；
- 对应轮次；
- 估值日期；
- 已完成融资估值、企业计划估值或第三方估算；
- 来源和验证状态。

列表只展示最新的已验证有效估值。企业计划估值和媒体估算不得冒充已完成轮次估值；如果业务决定允许展示，必须明确标注口径。

### 5.5 大客户验证

客户重要性与验证阶段分开。

客户重要性建议由受控客户字典维护：

- A：客户确认的头部企业、世界 500 强、央国企龙头或行业龙头；
- B：细分行业重要客户；
- C：普通客户或尚未完成等级判断。

验证阶段采用单向等级：

| 等级 | 阶段 | 最低证据要求 |
|---:|---|---|
| L0 | 提及/宣传案例 | 只能标记“来源提及”，不算已验证客户 |
| L1 | 接触/意向 | 明确意向、交流或合作洽谈依据 |
| L2 | 送样/测试/PoC/试点 | 测试、送样、试点或 PoC 原文/材料 |
| L3 | 框架协议/正式合同 | 合同、采购或正式合作依据 |
| L4 | 订单/交付/验收 | 订单、交付或验收依据 |
| L5 | 回款/复购 | 回款、复购或持续收入依据 |

“战略合作”“联合实验室”“研究伙伴”不能直接升级为付费客户。服务端画像保留最高阶段、代表客户和聚合数量，并在详情页展示与核对；本期不在线索池列表占用独立列。

保密客户需要保存脱敏标签、稳定内部标识、访问权限和证据来源；列表不展示客户画像，列表 API、详情页和服务端仍不得泄露受限客户真实名称。

### 5.6 数据状态

服务端画像和详情页数据状态至少包含：

- 六类画像维度中已验证维度数量；
- `verified`、`unverified`、`conflicted`、`missing`、`not_applicable` 状态；
- 当前冲突数量；
- 最新事实或快照时间；
- 当前画像 schema 版本和来源快照 ID/hash；
- 投影是否过期。

数据完整度只描述“我们掌握了多少可信信息”，不得直接改变线索评级。

## 6. 技术方案

### 6.1 总体链路

```text
原始来源/企业材料
  → 线索主体确认
  → 专题研究与受控抓取
  → 事实契约校验
  → lead_facts + lead_fact_evidence + conflicts
  → lead-enrichment-v3 不可变快照
  → 确定性投资画像投影
  → GET /api/leads 列表 DTO / 筛选 / 排序
  → 七列列表与详情证据落点
```

### 6.2 版本策略

- 保持 `lead-enrichment-v3` 不可变快照语义，新增事实键进入其受控专题契约，不另造并行事实源。
- 列表读模型使用独立的 `lead-investment-profile-v1` schema，并同时绑定快照 ID/hash 与机构/客户字典 hash。
- 联网研究 Prompt 单独升级版本，历史结果保留原版本和来源。
- 新投影绑定准确的冻结快照 ID/hash；`review` 快照中的冲突进入画像状态，不伪装成 `ready`；快照变化后投影标记过期并幂等重建。
- 本期若不调整 V3 评分事实集合，`lead-rating-v3` 可继续绑定原评分适配器；如新快照导致评分输入变化，必须单独评审重评影响。

### 6.3 事实层

优先扩展现有 `TOPIC_CONTRACTS`，不把新字段直接写入自由文本摘要。

建议新增或细化的事实键：

- 行业：`industry.level1`、`industry.level2`、`industry.segment`、`industry.chain_position`；
- 产品：`product.route`、`product.form`、`product.stage`；
- 融资：`financing.date`、`financing.lead_investor`、`financing.investor_role`；
- 高校：`team.institution`、`team.institution_relation`、`team.department_lab`、`team.institution_period`；
- 客户：`customer.name`、`customer.anonymized_label`、`customer.validation_stage`、`customer.confidentiality`；
- 估值继续复用并补齐 `transaction.valuation`、`transaction.pre_money`、`transaction.post_money`、`transaction.currency`，与融资事件共享 `instanceKey`。

现有 `customer.intent/trial/pilot/framework_agreement/formal`、合同、订单、交付和回款事实继续作为客户阶段的确定性依据，不重复存储另一个不一致事实源。

### 6.4 列表读模型

`lead_facts` 是权威事实层；列表需要一个可重建、快照绑定的轻量读模型，避免每行 N+1 查询和对大 JSON 全表扫描。

建议新增 `lead_investment_profile_projection`（最终表名按迁移规范确定）：

| 字段组 | 建议内容 |
|---|---|
| 身份 | `lead_id`、`schema_version`、`snapshot_id`、`snapshot_hash` |
| 行业 | 标准一级/二级/细分行业、产业链位置、筛选标签 |
| 产品 | 最多两个核心产品、路线、技术路线、产业化阶段 |
| 机构 | 已验证大机构摘要、机构类型、机构数量 |
| 高校 | 已验证高校摘要、关系类型、成果转化标志 |
| 融资 | 最新已完成轮次、日期、金额、币种、累计轮次数/金额 |
| 估值 | 最新估值、口径、币种、日期、对应轮次 |
| 客户 | 最高验证阶段、代表客户、A级客户数、已验证客户数 |
| 状态 | 覆盖数、冲突数、验证状态、事实更新时间、投影更新时间 |

多值筛选可使用配套维度表或经过评审的 JSON 标签字段；轮次、估值、客户阶段、更新时间等常用排序/范围筛选必须有可用索引。不得以在分页后对当前 20 条记录内存过滤的方式实现全库筛选。

读模型要求：

- 完全由当前 verified 事实、冲突状态、字典和快照确定性生成；
- 可按单条、批次和全量重建；
- 同一快照与同一字典 hash 重复执行结果一致；
- 不保存来源原文和受限材料；
- 画像投影失败不覆盖上一份有效投影；
- 投影可删除后从事实层恢复。

### 6.5 字典与受控分类

实施前检查现有 `dictionary_groups/dictionary_items` 是否满足以下需求；满足则复用，不满足才新增专用表：

- 标准行业层级和别名；
- 产业链位置；
- 投资机构标准名、别名、类型和等级；
- 高校/科研院所标准名和别名；
- 客户标准名、别名和客户等级；
- 客户验证阶段；
- 产业化阶段。

字典调整需要权限、版本、生效状态和审计；不得把名单硬编码在 Prompt 或前端组件。

### 6.6 API 和查询契约

扩展 `GET /api/leads` 的轻量列表返回，不让列表下载完整事实或证据正文。

建议 DTO：

```ts
type LeadInvestmentProfileSummary = {
  schemaVersion: string
  snapshotId?: string
  industry: {
    level1?: string
    level2?: string
    segment?: string
    chainPosition?: string
  }
  products: Array<{
    name: string
    productRoute?: string
    technologyRoute?: string
    productionStage?: string
  }>
  institutions: Array<{
    name: string
    round?: string
    role?: 'lead' | 'follow' | 'strategic' | 'undisclosed'
    tier?: string
  }>
  academicLinks: Array<{
    institution: string
    relationType: string
    person?: string
    commercialization?: boolean
  }>
  financing: {
    status: string
    latestRound?: string
    latestRoundDate?: string
    latestAmount?: string
    cumulativeAmount?: string
    completedRoundCount?: number
  }
  valuation: {
    value?: string
    type?: 'pre_money' | 'post_money' | 'planned' | 'estimated'
    currency?: string
    date?: string
    round?: string
  }
  customers: {
    highestStage?: 'L0' | 'L1' | 'L2' | 'L3' | 'L4' | 'L5'
    verifiedCount: number
    tierACount: number
    representatives: Array<{ name: string; tier?: string; stage: string; anonymized: boolean }>
  }
  dataStatus: {
    verifiedDimensions: number
    applicableDimensions: number
    conflictCount: number
    status: 'verified' | 'partial' | 'conflicted' | 'missing' | 'not_applicable'
    updatedAt?: string
  }
}
```

服务端预留的可选查询参数（本期不在列表顶部展示）：

- `industryLevel1`、`industryLevel2`、`industrySegment`；
- `productRoute`、`productionStage`；
- `institution`、`institutionType`、`hasMajorInstitution`；
- `academicInstitution`、`academicRelation`、`hasCommercializationLink`；
- `latestRound`、`fundingDateFrom`、`fundingDateTo`；
- `valuationMin`、`valuationMax`、`valuationCurrency`、`valuationType`；
- `customerTier`、`customerStageMin`、`hasVerifiedCustomer`；
- `profileStatus`、`hasConflict`。

查询要求：

- 所有枚举、日期、金额和布尔参数由服务端契约严格校验；
- 非法值返回 400，不静默退化为全量查询；
- 多条件按 AND 组合，同一多选条件内部按明确的 OR/AND 产品语义；
- 计数和列表读取同一筛选语义；
- 分页和 URL 状态继续遵守现有查询分页计划；
- 金额范围先规范币种和数值口径，不直接比较展示字符串；
- 响应不得包含来源原文、受限客户真名、内部材料路径或审计私有字段。
- 页面只发送旧版六项可见筛选；URL 中的高级画像参数不得成为不可见的页面筛选状态。

### 6.7 前端列表

目标文件包括但不限于：

- `src/pages/SourcingPage.tsx`；
- `src/styles.css`；
- `src/types/index.ts`；
- `src/store/useAppStore.ts`；
- `src/lib/leadPresentation.ts`；
- `src/lib/leadPoolFilters.ts`。

实现要求：

1. 删除 `FastSummaryText` 及只为一句话摘要服务的列表浮层代码和样式；若其他页面仍使用则只移除当前列表引用。
2. 删除当前“核心信息”和“推荐理由/信号”独立单元格；恢复精简“最新动态”列。
3. 评级移入线索主体列，不展示长推荐理由。
4. 行业/产品路线最多显示两项产品，超出显示 `+N`。
5. 机构/高校背景分别显示一至两条，保留轮次/关系类型，不展示长履历。
6. 融资与估值分列，所有值带口径或日期；无值显示 `-`。
7. 第 6 列显示最新动态的精简时间线，不把来源原文或长摘要放回列表。
8. 第 7 列独立显示更新时间，优先数据更新时间，缺失时回退入池时间；大客户验证和完整数据状态仅保留在详情页与服务端画像。
9. 行点击、键盘 Enter/Space、查看详情按钮和现有权限行为保持不变。
10. 桌面端优先无横向溢出；不足宽度时使用受控横向滚动，不压缩成不可读文本。
11. 移动端/窄屏按信息优先级折叠，不把字段顺序改成与桌面语义不一致。

### 6.8 详情页证据落点

列表中的结构化值必须能在详情页找到来源或明确状态。本期最低要求：

- 行业/产品路线进入“产品与商业化”或新增紧邻模块；
- 机构投资、融资和估值进入资本时间线；
- 高校背景进入团队关系区；
- 大客户验证进入客户验证区；
- 相关公开来源继续新窗口打开并使用 `rel="noreferrer"`；
- 普通用户只读取已验证最小投影；冲突裁决和完整审计继续要求管理权限；
- 不把 13 个后台专题直接做成 13 个前台区块。

## 7. 分阶段开发计划

### I0：产品口径冻结和基线审计

交付：字段字典、机构/客户/高校分类规则、现有覆盖率报告、性能基线和确认记录。

任务：

- `IP-I0-001` 确认七列表头、列顺序、显示上限和响应式规则。
- `IP-I0-002` 确认行业分类体系及产业链层级。
- `IP-I0-003` 确认“大机构”名单、类型和等级维护责任人。
- `IP-I0-004` 确认高校关系类型和“成果转化”最低证据。
- `IP-I0-005` 确认融资完成、计划融资和估值口径。
- `IP-I0-006` 确认大客户等级、L0-L5 阶段和保密展示规则。
- `IP-I0-007` 只读统计现有线索六类画像覆盖、冲突、来源和时效。
- `IP-I0-008` 记录当前列表 API、DOM、宽度和加载性能基线。

退出条件：所有关键枚举和判定责任已确认；未确认项保持 fail-closed，不由开发人员或模型自行决定。

### I1：事实契约和验证规则

交付：新 schema/Prompt 版本、事实键、实例键、验证器、契约测试和金标样本。

任务：

- `IP-I1-001` 扩展行业、产品路线、融资事件、机构、高校和客户事实键。
- `IP-I1-002` 定义融资轮次共享 `instanceKey` 和估值关联规则。
- `IP-I1-003` 定义人物—高校和客户—合同/订单/交付/回款实例键。
- `IP-I1-004` 增加机构/客户字典归一化，模型输出只作为候选。
- `IP-I1-005` 增加主体、时间、金额、币种、期间、关系和生命周期门禁。
- `IP-I1-006` 增加保密客户投影和访问安全测试。
- `IP-I1-007` 扩展目标专题执行集合，避免无关 13 专题全部消耗预算。
- `IP-I1-008` 建立企业、科研项目、团队三类金标样本。

退出条件：错误主体、错误轮次、宣传客户、计划融资和估值混淆均能被契约拒绝或进入冲突。

### I2：快照和投资画像读模型

交付：迁移、投影服务、重建脚本、幂等和回滚测试。

任务：

- `IP-I2-001` 设计并评审投影表及索引，不在应用启动时自动 DDL。
- `IP-I2-002` 新增下一顺序迁移；迁移编号以实施时仓库现状为准。
- `IP-I2-003` 实现快照到投影的纯函数和服务。
- `IP-I2-004` 实现单条/批次重建、旧投影保留和过期标识。
- `IP-I2-005` 实现机构、客户、高校字典变更后的受控重投影。
- `IP-I2-006` 建立 preview/report/rollback 或可重建恢复机制。
- `IP-I2-007` 验证同一快照重复执行结果不变。

退出条件：投影可删除重建、无 N+1、不会覆盖事实层、不会泄露受限数据。

### I3：列表 API、查询能力和排序

交付：轻量 DTO、严格查询契约、分页查询、索引证明和 API 测试。

任务：

- `IP-I3-001` 扩展 `Lead` 列表类型或拆分专用 `LeadListItem`。
- `IP-I3-002` 将投资画像摘要接入 `listLeads()` 的页查询。
- `IP-I3-003` 扩展 `leadPoolQueryContract.ts` 和前端安全解析常量，但不改变顶部筛选 UI。
- `IP-I3-004` 服务端保留行业、产品路线、机构、高校、轮次、估值和客户查询能力，当前页面不发送这些高级参数。
- `IP-I3-005` 实现明确的排序白名单；禁止任意字段拼接 SQL。
- `IP-I3-006` 保持关键词、分页、URL 和错误恢复的现有修复语义。
- `IP-I3-007` 增加查询计划/索引审计，禁止每行单独查询事实。
- `IP-I3-008` 验证列表响应不含完整 facts/evidence/scoring 大 JSON。

退出条件：旧版六项页面筛选、分页元数据和非法参数处理正确；服务端预留查询能力不泄露到顶部 UI。

### I4：七列列表和详情证据落点

交付：七列列表 UI、旧版六项筛选面板、详情必要模块、空态/错误态和浏览器验收。

任务：

- `IP-I4-001` 替换表头和七列网格。
- `IP-I4-002` 移除一句话摘要和核心信息列表渲染。
- `IP-I4-003` 将评级移入线索主体，移除长推荐理由，保留精简最新动态列。
- `IP-I4-004` 实现行业/产品、机构/高校、融资、估值、最新动态和更新时间单元格；列表不渲染大客户验证和数据状态。
- `IP-I4-005` 保持旧版“线索类型、行业、阶段、地区、渠道、更新时间”六项筛选及 URL 同步，不增加投资画像筛选控件。
- `IP-I4-006` 实现缺失、未融资、不适用、冲突、过期和加载状态。
- `IP-I4-007` 补齐详情页资本、高校和客户证据落点。
- `IP-I4-008` 验证键盘、焦点、链接新窗口和响应式布局。

退出条件：真实 DOM 恰好显示“线索主体、行业 / 产品路线、机构 / 高校背景、融资进展、最新估值、最新动态、更新时间”七列，不出现大客户验证和数据状态列；两者在详情和服务端画像中仍可用；顶部真实 DOM 恰好保留旧版六项筛选。

### I5：存量覆盖与灰度回补

交付：只读审计、回补预览、小样本灰度、批次报告和幂等证明。

任务：

- `IP-I5-001` 重新查询可见线索及各维度 never/ready/stale/conflicted 状态。
- `IP-I5-002` 先对 5—10 条多来源样本执行不写库金标验证。
- `IP-I5-003` 在确认隔离库执行写入型链路验收。
- `IP-I5-004` 生成业务库回补 preview，包括目标、来源、预计调用、缺口和风险。
- `IP-I5-004A` 对待处理最新快照输出“原始事实 → 已验证事实 → 已绑定证据 → 已验证且具备 HTTP(S) 证据”的可用性漏斗，并按行业产品、机构、高校、融资、估值、客户六维统计线索覆盖。
- `IP-I5-005` 经单独授权后执行小批 canary 写回。
- `IP-I5-006` 验证事实、证据、快照、投影、API 和真实详情页全链路。
- `IP-I5-007` 经再次确认后有界并发回补；额度耗尽时停止并保留可恢复结果。
- `IP-I5-008` 回补重跑返回 `changed=0` 或明确列出新增来源差异。

退出条件：金标样本覆盖计划中的关键场景，六维缺口均有明确处理结论，canary 精度和证据完整性达标，失败可恢复，不把原始事实、来源约束补全或单维投影误称为完整互联网研究。

### I6：构建、激活和发布

交付：测试报告、候选构建、可选本地激活证据、发布和回滚记录。

任务：

- `IP-I6-001` 完成目标测试、类型检查、服务端测试和平台门禁。
- `IP-I6-002` 生成候选构建并记录 build ID/静态资源。
- `IP-I6-003` 经授权后确认旧进程归属、停止、激活候选并启动统一服务。
- `IP-I6-004` 验证 `/api/health`、`/api/health/components`、列表 API 和真实 DOM。
- `IP-I6-005` 生产发布需单独授权，并核对在线资源与数据库 schema。
- `IP-I6-006` 失败时回滚 Web/Server 构建和投影迁移影响。

退出条件：源码、测试、本地生效和生产发布分别有独立证据，不混写为“已完成”。

## 8. 验收标准

以下复选框只有取得对应运行态、数据或浏览器证据后才勾选；源码和静态验收的阶段性结果单独记录在迭代实现报告中，避免把“测试通过”误写成“功能已生效”。

> 2026-09-02 运行态更新：已按专项测试、真实 MySQL 只读审计、授权 canary/批次回填凭证和陈斌账号真实 DOM 重新核验。当前勾选 25/66，未勾选 41/66。数据库最新口径为 804 条快照：391 条已有当前投影（8 条部分验证、383 条画像缺失），413 条尚未生成投影；当前没有完整验证画像。`npm run accept:lead-investment-profile` 为 110/110 通过，`npm run accept:lead-investment-profile:runtime` 通过，服务健康为 `ready`。
>
> 未勾选项不是一律“源码未实现”：其中行业/机构/客户受控字典尚未取得业务签字；机构、高校、融资、估值和客户维度缺少可评估的真实 verified 样本；普通用户权限、特定 canary 的 DOM 全链路、隔离 MySQL 写入验收、真实失败恢复、新快照替换/删除重建及配额耗尽场景尚未实际执行；API 的全筛选集合、N+1、融资样本查询计划和正式 I0 性能/体积基线也尚未闭环。

### 8.1 产品与列表结构

- [x] `IP-UI-001` 表头恰好为“线索主体、行业 / 产品路线、机构 / 高校背景、融资进展、最新估值、最新动态、更新时间”；列表不显示大客户验证和数据状态列，但详情页与服务端画像仍保留两者。
- [x] `IP-UI-002` 列表源码和 DOM 均不存在“一句话摘要”列及其摘要浮层。
- [x] `IP-UI-003` 列表源码和 DOM 均不存在“核心信息”列。
- [x] `IP-UI-004` 推荐理由不再占独立列表列，线索评级保留在线索主体列，最新动态保留为独立精简列。
- [x] `IP-UI-005` 每个新列最多显示约定条数，超出以 `+N` 表示，不出现大段换行文字。
- [x] `IP-UI-006` 行点击、详情按钮、Enter/Space 打开详情行为保持一致。
- [x] `IP-UI-007` 10/20/50 条分页、加载、空结果、错误和重试状态正常。
- [x] `IP-UI-008` 桌面主验收宽度无非预期横向溢出；窄屏存在可用降级方案。
- [ ] `IP-UI-009` 普通用户看不到管理复核入口或受限客户真名。
- [x] `IP-UI-010` 所有公开外链新窗口打开并包含 `rel="noreferrer"`。
- [x] `IP-UI-011` 顶部筛选真实 DOM 恰好为“线索类型、行业、阶段、地区、渠道、更新时间”，不显示“更多投资筛选”或画像高级筛选项。

### 8.2 行业和产品路线

- [ ] `IP-IND-001` 行业一级、二级、细分赛道和产业链位置使用批准字典。
- [ ] `IP-IND-002` 产品路线和技术路线分别存储、分别展示。
- [ ] `IP-IND-003` 同一产品的名称、路线和产业化阶段使用同一实例键。
- [ ] `IP-IND-004` 规划产品不得显示为已发布或已量产产品。
- [x] `IP-IND-005` 列表显示的每个行业/路线值能在详情事实或来源中核对。
- [ ] `IP-IND-006` 服务端行业/路线可选查询结果集合等于画像投影集合；该能力不进入本期顶部筛选 UI。

### 8.3 机构和高校背景

- [ ] `IP-BKG-001` “大机构”仅由受控机构字典判定，模型不能自行升级。
- [ ] `IP-BKG-002` 机构投资绑定具体轮次和投资角色；未披露角色不显示为领投。
- [ ] `IP-BKG-003` 高校关系绑定具体人物或项目主体、关系类型和来源。
- [ ] `IP-BKG-004` 学历/校友、当前任职、实验室和成果转化不会相互混淆。
- [ ] `IP-BKG-005` 只有学校列表但无逐人对应时，不强行建立人物—学校关系。
- [ ] `IP-BKG-006` 服务端机构、高校和成果转化可选查询使用标准身份和别名归一化；该能力不进入本期顶部筛选 UI。

### 8.4 融资和估值

- [ ] `IP-CAP-001` 融资轮次、日期、金额、币种、投资方和估值绑定同一融资事件。
- [ ] `IP-CAP-002` 最新轮次按已完成融资日期确定，不按数组顺序或报道访问时间猜测。
- [ ] `IP-CAP-003` 计划融资、募资目标和已完成融资严格区分。
- [ ] `IP-CAP-004` 融资金额、估值、市场规模、合同金额和收入不能互相冒充。
- [ ] `IP-CAP-005` 投前、投后、计划和第三方估算估值口径明确。
- [ ] `IP-CAP-006` “未融资”显示为融资状态，金额和估值显示 `-`。
- [ ] `IP-CAP-007` 未披露、待核验、不适用等无值状态不渲染成伪金额或伪估值。
- [ ] `IP-CAP-008` 服务端估值范围可选查询基于规范数值和币种，不比较展示字符串；该能力不进入本期顶部筛选 UI。

### 8.5 大客户验证

- [ ] `IP-CUS-001` 客户标准身份、别名、客户等级和保密状态由受控字典/审核维护。
- [ ] `IP-CUS-002` L0—L5 阶段严格按证据门槛派生，不能由模型直接提高等级。
- [ ] `IP-CUS-003` 战略合作、联合实验室和研究伙伴不会自动成为正式客户。
- [ ] `IP-CUS-004` 正式客户至少有合同、采购、订单、交付、验收或回款依据。
- [ ] `IP-CUS-005` L4 必须有订单/交付/验收依据，L5 必须有回款/复购依据。
- [ ] `IP-CUS-006` 服务端画像与详情页按约定数量展示代表客户及 `+N`，聚合计数准确；线索池列表不展示客户画像。
- [ ] `IP-CUS-007` 保密客户只返回脱敏标签，网络响应和 DOM 不包含受限真名。
- [ ] `IP-CUS-008` 服务端客户等级和验证阶段可选查询与画像投影集合一致；该能力不进入本期顶部筛选 UI。

### 8.6 证据、冲突和投影

- [x] `IP-EVD-001` 每个 verified 展示值至少绑定一个可追溯事实和证据。
- [x] `IP-EVD-002` 搜索命中但原文引用、主体或口径校验失败时不得进入 verified 投影。
- [ ] `IP-EVD-003` 同一事实实例出现不同值时进入冲突，不静默覆盖。
- [ ] `IP-EVD-004` E3 单独支持的重大融资、估值、合同、订单或回款事实不得标为 verified。
- [x] `IP-EVD-005` 投影绑定冻结快照 ID/hash/schema version，并保留 ready/review 状态语义。
- [x] `IP-EVD-006` 同一快照与同一字典 hash 重复投影结果一致，二次执行不新增重复记录。
- [ ] `IP-EVD-007` 新快照产生后旧投影过期并成功替换；失败时保留上一份有效投影。
- [x] `IP-EVD-008` 数据完整度与线索评级分开保存和展示。
- [ ] `IP-EVD-009` 删除投影后可以从事实/快照完整重建。

### 8.7 API、查询和性能

- [x] `IP-API-001` 列表 DTO 返回投资画像摘要，不返回完整 facts/evidence/scoring 大对象。
- [x] `IP-API-002` 新筛选参数具有严格枚举、日期、金额和布尔校验。
- [x] `IP-API-003` 非法参数返回 400，不静默返回全量数据。
- [x] `IP-API-004` 多条件组合、分页 total/list/page/totalPages 语义一致。
- [x] `IP-API-005` 旧版六项筛选的 URL 前进/后退、详情返回和直接粘贴状态一致。
- [ ] `IP-API-006` 服务端高级查询与画像投影做 ID 集合对账，差异为 0；不要求在本期顶部 UI 暴露这些条件。
- [ ] `IP-API-007` 列表查询无每行事实 N+1；数据库查询数与 pageSize 无线性增长。
- [ ] `IP-API-008` 新列表 API 的 P95 不超过 I0 同规模基线的 120%；如基线已不达标，实施前另行确认绝对预算。
- [ ] `IP-API-009` 常用筛选和排序具有索引/查询计划证据，无无边界大 JSON 全表扫描。
- [ ] `IP-API-010` 20 条列表响应大小不高于 I0 基线的 120%，且不包含受限字段。

### 8.8 数据回补和真实样本

- [x] `IP-DATA-001` 回补前生成 preview，包含总数、never/stale/conflicted/ready、专题、来源、预计调用和风险。
- [x] `IP-DATA-001A` preview 按事实键和六维输出 raw/verified/evidence-bound/HTTP-eligible 漏斗；存在原始数据但无可投影事实时明确失败关闭，不直接进入 canary 写回。
- [x] `IP-DATA-002` 不写库金标样本覆盖企业、科研项目、未融资、历史多轮融资、估值冲突、高校成果转化和保密客户。
- [ ] `IP-DATA-003` 写入型验收只在确认隔离 MySQL 数据库执行并受安全开关保护。
- [x] `IP-DATA-004` 业务库 canary 写入获得单独授权，并限定明确 lead IDs/batch ID。
- [ ] `IP-DATA-005` canary 验证原始来源→事实→证据→快照→投影→API→真实 DOM 全链路。
- [ ] `IP-DATA-006` 回补失败保留批次、模型、Prompt、结果、失败、调用和恢复依据。
- [x] `IP-DATA-007` 幂等重跑为 `changed=0`，或能解释新增来源导致的确定性差异。
- [ ] `IP-DATA-008` 无可用额度时停止并报告，不擅自换模型或无限重试。

## 9. 测试与证据矩阵

| 层级 | 建议命令/方式 | 证明内容 | 写入边界 |
|---|---|---|---|
| 纯函数单元测试 | `node --env-file-if-exists=.env --import tsx --test <目标测试>` | 字段归一化、实例键、阶段派生、显示语义 | 不写业务库 |
| 契约测试 | `server/tests/leadEnrichmentContract.test.ts` 及新增测试 | 事实键、证据、主体、金额、时间、客户阶段 | 不写业务库 |
| 类型检查 | `npm run check:types` | 前后端 DTO 和实现一致 | 不写业务库 |
| UI 源码验收 | `npm run accept:lead-pool-ui` | 新表头、旧列移除、空值和结构契约 | 不写业务库 |
| 投资画像源码总验收 | `npm run accept:lead-investment-profile` | 画像归并、迁移契约、查询、组件渲染、UI 与补全链 | 不写业务库 |
| 投资画像运行态只读验收 | `npm run accept:lead-investment-profile:runtime` | 快照/字典 hash、覆盖率、保密、索引计划、HTTP DTO 与参数 | 要求已执行 0092，不写业务数据 |
| HTTP 验收 | `npm run accept:lead-pool-http` 扩展 | 认证、查询参数、分页、响应投影 | 默认只读/fixture |
| 服务端全测 | `npm run test:server` | 服务、路由、权限和回归 | 运行前核对 `.env` 与写入守卫 |
| 隔离 MySQL | 新增专用 acceptance | 迁移、事实、投影、索引、幂等和回滚 | 仅隔离库并显式开关 |
| 平台门禁 | `npm run check:platform` | 类型、数据库边界、单服务和迁移清单 | 按现有脚本边界 |
| 构建 | `npm run build` | 候选 Web/Server 产物 | 可能只生成候选；先核对 4100/锁 |
| 真实库审计 | 新增 `audit:lead-investment-profile` | 覆盖、冲突、集合一致性、查询计划 | 只读 |
| 浏览器验收 | 已登录普通用户和管理员 | 真实 DOM、布局、权限、外链和交互 | 不写业务数据 |
| 本地激活 | `activate:build:if-present` + `bash start.sh` | 运行版本、健康、静态资源 | 需单独授权 |

建议新增：

- `server/tests/leadInvestmentProfileContract.test.ts`；
- `server/tests/leadInvestmentProfileProjection.test.ts`；
- `server/tests/leadInvestmentProfileQuery.test.ts`；
- `server/src/scripts/leadInvestmentProfileAcceptance.ts`；
- `server/src/scripts/leadInvestmentProfileMysqlAcceptance.ts`；
- `server/src/scripts/leadInvestmentProfileAudit.ts`；
- `server/src/scripts/backfillLeadInvestmentProfile.ts`；
- 对应 `package.json` scripts，默认 preview/只读，apply 使用显式参数和安全守卫。

## 10. 发布、激活与回滚

### 10.1 发布前

- 记录当前分支、HEAD、工作树状态和未提交文件；
- 完成直接测试、类型检查、服务端测试、平台门禁和构建；
- 验证迁移向前兼容，旧服务读取新 schema 不崩溃；
- 记录候选 build ID、迁移清单、投影批次和回滚点；
- 对公共静态资源核对 Nginx 可读权限；
- 不因构建成功自动声明本地或生产已生效。

### 10.2 本地激活（需授权）

1. 确认 4100 监听和 PID 属于当前项目；
2. 停止确认的项目进程，不使用宽泛进程匹配；
3. 激活已有候选；
4. 运行单服务启动前检查；
5. `bash start.sh` 启动统一 Node 服务；
6. 验证 `/api/health`、`/api/health/components`、实际静态资源和七列 DOM；
7. 使用真实样本验证列表→详情证据；
8. 失败时恢复旧构建指针并重启验证。

### 10.3 数据回滚

- 事实层不物理删除历史证据；错误事实使用版本、`rejected`、`superseded` 或冲突裁决处理；
- 投影是可重建读模型，可按 snapshot/batch 回退或重建上一份有效投影；
- 字典变更保留版本和审计，可恢复上一生效版本后重新投影；
- 回补脚本保留批次 ID、目标 lead IDs、前后快照、错误和恢复状态；
- 不使用删除业务事实或清空整表作为常规回滚方式。

## 11. 交付物

- 本开发与验收计划；
- 《共享线索池投资画像受控字典与产品口径确认表_2026-09-02.md》；
- 产品字段字典和受控分类确认记录；
- 事实契约、验证器和版本说明；
- 迁移和投影服务；
- API DTO、查询契约、筛选和索引；
- 七列列表和详情证据落点；
- 单元、契约、服务端、MySQL、HTTP、UI 和浏览器测试；
- 只读覆盖率审计与回补 preview；
- canary/全量批次报告（仅在授权执行后）；
- 构建、激活、健康、静态资源和真实 DOM 证据；
- 回滚记录和未完成项清单。

## 12. 评审前必须确认的问题

| 决策 ID | 问题 | 推荐方案 | 未确认时处理 |
|---|---|---|---|
| `IP-DEC-001` | 大机构如何定义 | 客户维护标准机构字典、类型和等级 | 不显示“大机构”标签，只显示已验证投资方 |
| `IP-DEC-002` | 行业分类体系 | 内部投资赛道库为主，保留外部分类映射 | 只展示现有行业，不自动升级细分赛道 |
| `IP-DEC-003` | 高校背景范围 | 学历、任职、实验室、成果转化分别展示 | 只展示有明确关系类型的事实 |
| `IP-DEC-004` | 大客户定义 | 客户等级与 L0-L5 验证阶段二维展示 | 不以公司规模替代验证阶段 |
| `IP-DEC-005` | 保密客户展示 | 脱敏标签 + 权限控制，不向普通列表返回真名 | 默认只显示“某头部客户” |
| `IP-DEC-006` | 估值展示口径 | 默认只展示已完成融资的最新投前/投后估值 | 计划或估算估值显示 `-` |
| `IP-DEC-007` | 是否改评分 | 本期不改评分，仅新增画像和筛选 | 不触发评级规则变更 |
| `IP-DEC-008` | 最新动态是否保留列表 | 已确认保留精简列，完整时间线留在详情 | 按 2026-09-03 最新口径执行 |

## 13. 完成定义

只有同时满足以下条件，才能把本迭代标记为“已完成”：

1. 产品口径和受控字典已确认；
2. P0 事实、证据、投影、API 和 UI 验收全部通过；
3. 列表显示集合与筛选集合对账差异为 0；
4. 真实企业、科研项目、未融资、多轮融资、估值冲突、高校成果转化和保密客户样本通过；
5. 未出现受限客户、证据正文、内部路径或密钥泄露；
6. 直接测试、类型检查、服务端测试、平台门禁和构建结果明确；
7. 若要求本地生效，已完成监听、健康、资源和真实 DOM 验收；
8. 若要求生产发布，已完成生产迁移、在线资源、健康和真实业务样本验收；
9. 源码、测试、本地激活、数据回补和生产发布分别有证据；
10. 未完成、跳过、环境缺失和需授权事项全部显式记录。
