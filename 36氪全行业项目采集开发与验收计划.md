# 36氪全行业项目采集开发与验收计划

> 文档版本：V2.0
> 编制日期：2026-09-01
> 适用项目：`/Users/hyw/Desktop/sbl_jedi`
> 数据范围：36氪项目库全部行业，工商成立时间不早于 2025-01-01
> 核心策略：全行业完整进入候选库，每日按配额释放到共享线索池
> 当前边界：源码已按全行业口径调整；业务库迁移、历史回填、任务启用、服务重启和生产验收均未执行。

## 1. 目标与口径

采集范围由“人工智能、具身智能、半导体”调整为“36氪项目库全部行业”。行业不再作为排除条件，仍保留以下硬门槛：

1. 工商成立时间必须不早于 `2025-01-01`。
2. 成立时间缺失的项目进入 `founded_at_pending`，不直接进入共享线索池。
3. 注销、吊销等不符合现有工商准入规则的主体不得进入共享线索池。
4. 同一 36氪项目 ID 只形成一个候选；跨来源主体仍复用现有实体匹配和合并规则。
5. 采集结果先进入候选库，不直接创建正式项目，也不绕过人工领取流程。
6. AI、具身智能、半导体标签继续保留，用于统计和检索，不再决定是否准入。

## 2. 只读来源基线

2026-09-01 对36氪真实列表接口进行只读对比：

| 成立年份 | 原重点行业筛选 | 全行业筛选 | 增量 |
|---|---:|---:|---:|
| 2025 | 230 | 386 | 156 |
| 2026 | 80 | 139 | 59 |
| 合计 | 310 | 525 | 215 |

以上是列表总量，不等于最终可准入数量。全量详情抓取后仍需按成立日期、工商状态和主体去重重新计算合格库存。

## 3. 目标流程

```text
36氪全部行业项目（2025年至今）
  → 列表全量/增量发现
  → 详情采集与不可变原始事件
  → lead_source_candidates 候选投影
  → 成立时间与工商状态判定
  → 每日配额释放
  → 共享线索池 leads
  → 补全、评分、人工领取
  → 正式项目
```

采集和释放继续由两个独立任务负责：

- `kr36-project-sync`：发现并更新全部行业候选，不直接写入共享线索池。
- `kr36-project-daily-admission`：从合格候选中按每日配额创建或合并共享线索。

## 4. 开发计划

### 阶段一：全行业来源适配

- 默认向36氪列表接口传递空的行业筛选集合，覆盖全部行业。
- 保留显式 `tradeIds` 参数，便于故障诊断和抽样，但定时同步不得默认使用重点行业集合。
- 继续按成立年份分页，从 2025 年遍历到当前年份。
- 全量回填使用完整分页；增量同步继续读取“最近更新”和“最新收录”切片并去重。

交付物：来源适配器、分页契约测试、真实来源只读预览。

### 阶段二：全行业范围判定

- 规则版本升级为 `kr36-all-industries-v2`。
- 删除“未命中三赛道即排除”的逻辑。
- 保留重点赛道标签，未命中标签的项目以空数组保存。
- 仅由成立日期和工商准入状态决定 `eligible`。
- 兼容历史 `sector_unmatched` 数据，但新规则不再产生该状态。

交付物：分类器、规则版本、单元测试、候选范围原因。

### 阶段三：候选库与历史回填

- 应用 `0091_add_lead_source_candidates.sql` 前，先在隔离数据库执行迁移和回滚验证。
- 以 `source_type + source_project_id_hash` 保证同一36氪项目幂等。
- 首次执行 `backfill`，完整抓取 2025 年至今全部行业。
- 保存 `first_seen_at`、`last_seen_at`、详情哈希和原始事件，禁止用成立日期冒充系统首次发现日期。
- 对规则版本较旧的候选重新分类，使原 `sector_unmatched` 项目按全行业规则进入合格库存。

交付物：迁移验证、全量回填报告、重复与失败清单、规则升级重分类结果。

### 阶段四：每日释放

- 默认每日释放量为 `KR36_PROJECT_DAILY_QUOTA=10`，可通过配置调整。
- 从全部合格候选中按上海日期生成稳定随机顺序后抽取；同一天重跑顺序一致，跨天重新洗牌。
- 不按候选首次发现时间排序，历史库存与当天新增均有机会被抽中。
- 不再按 AI、具身智能、半导体轮换或优先，避免其他行业长期饥饿。
- 当日配额按已入池和处理中记录共同占位，保证任务重试不会超额。
- 库存为0时如实记录，不制造重复项目。

交付物：全行业准入查询、幂等测试、并发锁测试、每日库存报告。

### 阶段五：灰度切换

1. 保持两个新任务默认关闭。
2. 在隔离环境完成迁移和小范围回填。
3. 启用 `kr36-project-sync`，按6小时间隔采集并观察成功率和候选库存。
4. 启用 `kr36-project-daily-admission`，每日09:10处理10个合格候选。
5. 正式切换时关闭旧的 `lead-reserve-daily-intake` 每日50条任务；旧数据和任务定义仅用于显式回滚。
6. 保留旧数据和任务定义作为回滚依据，不删除历史储备。

## 5. 验收标准

### 5.1 来源与范围

- [ ] 默认列表请求的 `tradeIdList` 为空，真实接口返回全行业结果。
- [ ] 2025和2026列表总量不低于本次只读基线；若来源实时变化，记录验收时点和差异原因。
- [ ] 每个年份、排序和分页均能遍历至结束，无固定页数截断。
- [ ] 2025年以前成立的项目不得进入 `eligible`。
- [ ] 2025年及以后成立、工商状态合格的非重点行业项目能够进入 `eligible`。
- [ ] 成立时间缺失进入 `founded_at_pending`；注销、吊销主体进入 `registration_ineligible`。
- [ ] AI、具身智能、半导体标签存在时正确保留，缺失时不影响全行业准入。

### 5.2 数据完整性与幂等

- [ ] 同一36氪项目跨行业、跨排序、跨分页只生成一个候选。
- [ ] 重复执行全量和增量同步不增加重复候选。
- [ ] 详情变化更新原候选和已入池线索，不重复创建“新增企业”。
- [ ] 原始事件、内容哈希、候选当前投影和入池线索可相互追溯。
- [ ] 原规则的 `sector_unmatched` 候选完成重分类，不因历史状态永久遗漏。

### 5.3 每日释放

- [ ] 全行业合格候选均有机会进入每日队列，不存在三赛道硬过滤。
- [ ] 每日从全部合格候选中稳定随机抽取；同日重跑不漂移，跨日顺序会变化。
- [ ] 抽取不按候选新旧排序，历史库存和当天新增均不存在固定优先级。
- [ ] 默认每日最多处理10个合格候选；重复运行、任务重启和多实例并发均不超额。
- [ ] 已存在的同主体线索执行合并或更新，不制造重复企业。
- [ ] 每日释放后正确触发既有补全、评分和审计链。

### 5.4 运行与监控

- [ ] 能查看总候选、合格、待核验、待释放、处理中、已入池和失败数量。
- [ ] 库存报告包含无重点标签候选数量，不能只展示三个重点赛道。
- [ ] 来源403、429、超时、响应结构变化和部分详情失败均有明确状态及可重试策略。
- [ ] 两个任务可独立启停，启停和立即执行均要求系统管理员并写审计。
- [ ] 新链路连续运行至少7天后，再决定是否关闭旧每日50条任务。

## 6. 验收命令与证据

以下命令只代表对应层级的证据，不能相互替代：

```bash
# 单元与契约测试
node --env-file-if-exists=.env --import tsx --test \
  server/tests/kr36ProjectClassifier.test.ts \
  server/tests/kr36ProjectSourceService.test.ts \
  server/tests/kr36ProjectSyncService.test.ts \
  server/tests/kr36RuntimeJobDefinitions.test.ts

# 类型检查
npm run check:types

# 真实来源只读预览，不写数据库
npm run preview:kr36-projects -- --pages-per-year=1

# 迁移后只读库存报告
npm run report:kr36-supply
```

最终上线验收还必须提供：迁移记录、首次全量回填统计、任务运行记录、真实数据库候选样本、登录态共享线索页面、连续7天每日新增及去重证据。

## 7. 当前完成度

已完成源码层调整：

- 默认列表召回改为全行业。
- 非重点行业不再被 `sector_unmatched` 排除。
- 规则版本升级为 `kr36-all-industries-v2`。
- 每日释放取消三赛道轮换与新旧优先级，改为全行业按日期稳定随机抽取。
- 库存统计增加无重点标签候选计数。
- 更新相关单元测试、README和示例配置。

尚未完成运行层工作：

- 未迁移业务数据库。
- 未执行全行业完整回填。
- 未启用定时同步或每日释放任务。
- 未启动或重启本地/生产服务。
- 未执行真实数据库、HTTP、登录态页面及连续7天验收。



线索池：
挖掘具体行业和产品路线
过往大机构的投资
高校背景
融资、估值、轮次
验证的大客户
