# 尽调渲染测试材料

三个 JSON 文件来自 `aiDueDiligenceSkillRuntimeService.test.ts` 的虚构测试项目，经原报告归一化函数生成；不含业务数据。来源使用 example.test。

该材料用于验证真实 DOCX/PDF/页面产物及诊断记录。正文较短，不代表合格的尽调报告，也不是收益基线；独立质量检查失败必须保留，不能因文件生成成功而认定技能评测通过。
