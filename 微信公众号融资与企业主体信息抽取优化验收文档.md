# 微信公众号融资与企业主体信息抽取优化验收文档

> 版本：V1.3
> 日期：2026-08-26
> 项目：SBL Jedi 共享线索库 / 专属项目
> 状态：P0 验收通过；P1 安全可评分线索已完成 Codex 重处理，原始候选实体治理待后续执行
> 对应方案：`微信公众号融资与企业主体信息抽取优化开发方案.md`
> 文档性质：本文件定义验收口径并记录 2026-08-26 的实际执行结果。

## 0. 当前验收结论

**当前结论：有条件通过。**

原阻断项复测结果：

- [x] P0：正文融资轮次、金额、领投方和原文证据已正确抽取并展示。
- [x] P0：`5 亿元人民币` 不再被占位结果覆盖，证据状态独立展示。
- [x] P0：领投方仅保留 `经纬创投`，不含“经纬观点”等污染文本。
- [x] P0：微信公众号链接已归位为文章来源，公司官网独立显示。
- [x] P0：品牌名、工商全称、工商字段和字段级来源已分离展示。
- [x] P0：地区已由工商强证据纠正为广东，人名/团队名不再产生宁夏结论。
- [x] P0：已转专属项目仍可补全并按占位值保护规则同步。

仅剩 P1：跨来源转载合并为单一融资事件、全历史微信公众号小批次回填及持续质量指标。未对业务库扩大批量写入范围。

只有本文件中所有 P0 项通过，且没有数据安全和发布阻断项，才允许签署“验收通过”。

### V1.2 运行链路复测结论

- [x] 微信公众号定时、重试、机构账号、手工触发和历史导入入口在候选写入后均投递同一持久化 Radar 同步交接。
- [x] 同步交接为 `syncOnly`，不会为了处理微信候选而重复抓取公共源。
- [x] “上一轮同步仍在运行”按良性并发折叠，不再使运行任务进入死信。
- [x] 历史良性并发死信可在启动时自动恢复；组件健康对真实死信失败关闭。
- [x] Radar AI 审核跨运行重试不再发生决策幂等冲突。
- [x] 实际重放运行 `91fd3e0e-697a-4614-a593-122d3e73cbe8` 状态为 `succeeded`，采集结果明确为 `wechat_candidate_handoff`，运行任务 `deadLetter=0`。
- [x] 诺因智能融资事实与企业主体字段已从业务库读取复核，不仅验证了纯函数夹具。

### V1.3 Codex 历史重提取与重评分验收结论

本次只使用本机 Codex CLI 的 `gpt-5.6-sol`，没有调用项目网关模型。四路并发处理安全可评分的正式微信公众号线索，失败批次经收紧 schema 和空事实容错后定向补跑。

- [x] 最终处理 `101/101` 条，四个最终分片剩余清单均为 `0`，未处理失败为 `0`。
- [x] 数据库中的模型标记均为 `gpt-5.6-sol`，共 `101` 条；没有混入项目网关模型结果。
- [x] 共写入 `62` 条 Codex 源内融资事实；轮次、金额、来源 URL 和连续原文引文硬校验错误为 `0`。
- [x] 等级分布：`D 37`、`C- 54`、`C+ 3`、`B- 3`、`B 2`、`A- 2`。
- [x] 评级状态：无法评级 `37`、参考评级 `24`、正式评级 `40`；无法评级记录的顶层历史分残留为 `0`。
- [x] 主体分类：company `90`、event `8`、project `2`、team `1`；非企业内容不再沿用历史企业高分。
- [x] 已处理记录的评分任务未完成数为 `0`，待执行/重试/运行中的网关丰富主题为 `0`，不会被旧网关任务覆盖。
- [x] 服务 `GET /api/health` 返回 `200 / ready`。
- [x] 诺因智能已写入：`天使 ++ 轮融资`、`单笔金额 5 亿元人民币`、`经纬创投`领投、完整微信原文引文；评分为 `C- / 27.1`，信息覆盖率 `70%`。

本批边界：仅对已经形成正式线索且通过主体/评分安全门禁的 101 条记录自动写回。尚未形成正式线索的原始候选、一个线索聚合多篇文章以及“未命名项目”实体污染，不自动创建或拆分实体，须先完成候选到实体的一对一治理后再进入同一 Codex 流程。

## 1. 验收目标

验证系统能把微信公众号正文中的融资和企业主体信息转换为：

1. 可追溯的结构化事实；
2. 与事实分离的核验状态；
3. 安全、幂等的线索与专属项目投影；
4. 不误导用户的页面展示；
5. 可预览、可审计、可回滚的历史修复结果。

## 2. 验收环境与安全要求

### 2.1 环境分层

| 层级 | 用途 | 写入要求 |
|---|---|---|
| 单元测试 | 规则、解析器和展示选择器 | 不连接业务数据库 |
| 集成测试 | 候选 → 事实 → 线索 → 项目全链路 | 只允许专用测试数据库 |
| 预发布/本地服务 | 页面和 API 验收 | 使用隔离数据或明确测试记录 |
| 生产回填 | 诺因智能及历史数据修复 | 必须先预览、审批批次、保留审计和恢复点 |

### 2.2 P0 安全门禁

- [x] 写入型集成测试创建、迁移并销毁随机专用 acceptance 数据库。
- [x] 回填命令默认不写入，必须显式指定 `--apply`。
- [x] 回填前输出目标记录、项目投影、字段差异和证据。
- [x] 人工编辑或高优先级来源字段不会被自动覆盖。
- [x] 字段变更保留事件 ID、事实幂等键、抽取器版本、项目旧/新值、批次 ID 和快照。
- [x] 重复运行不会创建重复融资记录或重复有效变更。
- [x] 批次回滚只恢复当前值仍等于该批次写入值的投影字段；后续人工变更会进入冲突，不删除原始文章和证据。

任一安全门禁失败，整体验收直接不通过。

## 3. 金标样本

### 3.1 G-01：诺因智能微信公众号文章

| 项目 | 期望值 |
|---|---|
| 文章来源 | 原微信公众号文章 URL |
| 正文抓取 | 成功；可回放库内原文 |
| 融资轮次 | `天使++轮` |
| 原始轮次 | `天使 ++ 轮` |
| 融资金额 | `5 亿元人民币`，规范显示可为 `5 亿元` |
| 币种 | `CNY` |
| 领投方 | `经纬创投` |
| 证据片段 | 包含“近期完成天使 ++ 轮融资，单笔金额 5 亿元人民币，本轮融资由经纬创投领投” |
| 证据状态 | 至少为“原文已标注，待交叉核验”；不得仅显示“待核验”而隐藏事实 |
| 公司官网 | 不得使用微信公众号文章 URL |
| 主体名称 | `诺因智能` |
| 工商全称 | 有可靠证据后可为 `深圳诺因智能有限公司`；无可靠证据时显示待核验 |
| 工商成立日期 | 与品牌/团队叙述成立时间分开保存 |
| 地区 | 不得因“银川团队”或“李银川”输出宁夏；应由工商/官网等更强证据确定 |
| 专属项目 | 在不覆盖人工高优先级字段的前提下安全同步融资事实 |

固定对象：

- 候选键：`wechat_api:78f0f36abf46521148717d4c235d633b`
- 线索 ID：`daf2a843-797c-44da-a13b-3eec6a4dd5b7`
- 项目 ID：`6bb3ac47-192f-4964-9c71-90a854588aa6`

### 3.2 扩展金标集

| 编号 | 场景 | 关键期望 |
|---|---|---|
| G-02 | `天使++轮` 无空格 | 标准化后仍为 `天使++轮` |
| G-03 | `天使 ＋＋ 轮` 全角符号 | 标准化为 `天使++轮`，保留原文证据 |
| G-04 | “市场规模 5 亿元” | 不得识别为企业已完成融资 |
| G-05 | “拟融资 5 亿元” | 识别为募资目标，不得写入已完成融资 |
| G-06 | “估值 5 亿元” | 识别为估值，不得写入融资金额 |
| G-07 | 历史轮次与本轮同时出现 | 分成两条有时间/语义关系的事实，不互相覆盖 |
| G-08 | “经纬观点”栏目 | 投资方仅保留合法机构名，不含栏目标题和句式残片 |
| G-09 | 品牌简称无公司后缀 | 可进入实体解析，不因缺少“有限公司”直接跳过 |
| G-10 | 人名“李银川” | 不产生宁夏/银川地区结论 |
| G-11 | 微信文章带公司介绍 | 文章仍是来源页，不自动成为官网 |
| G-12 | 已转专属项目 | 继续补全；同步遵循字段优先级和人工保护 |
| G-13 | 文章成立时间与工商日期不同 | 分语义保留并展示冲突，不互相覆盖 |
| G-14 | 同一文章重复回放 | 不新增重复融资事实或审计噪声 |
| G-15 | 多篇转载同一融资 | 合并为同一融资事件，多来源作为证据集合 |

## 4. 功能验收

### 4.1 FETCH：抓取与回放

#### FETCH-01 正文已抓取样本可回放（P0）

- 前置：库内已有 G-01 正文。
- 操作：不访问外网，直接从原始记录回放抽取。
- 期望：生成与在线抓取相同的融资候选和证据。
- [x] 通过

#### FETCH-02 抓取状态与事实抽取状态分离（P0）

- 期望：可分别看到“抓取成功/失败”和“事实抽取成功/失败/无候选”。
- 期望：抓取成功但抽取失败时有明确错误，不得伪装为“无融资”。
- [x] 通过

#### FETCH-03 重试幂等（P1）

- 操作：对同一候选连续执行三次抓取或回放。
- 期望：原始记录、事实和证据不重复；版本变化时只产生可追踪的新抽取版本。
- [x] 通过

### 4.2 NORM：文本归一化

#### NORM-01 轮次空格归一化（P0）

输入至少覆盖：

```text
天使 ++ 轮
天使++轮
天使 ＋＋ 轮
天使+ +轮
```

期望：在语义等价时统一输出 `天使++轮`，同时保留原始文本。

- [x] 通过

#### NORM-02 金额与币种归一化（P0）

- 输入：`5 亿元人民币`、`人民币5亿元`、`RMB 5亿`。
- 期望：币种为 CNY，显示值一致，原文值可追溯。
- [x] 通过

#### NORM-03 证据偏移不丢失（P1）

- 期望：归一化后仍能在原始正文中准确定位并高亮证据片段。
- [ ] 通过

### 4.3 FUND：融资事实抽取

#### FUND-01 G-01 融资事实完整抽取（P0）

- [x] 轮次为 `天使++轮`。
- [x] 金额为 `5 亿元人民币`或等价规范值。
- [x] 币种为 CNY。
- [x] 领投方仅为 `经纬创投`。
- [x] 原文证据完整绑定。
- [x] 不含“经纬观点”“融资由经纬创投”“轮融资”等污染项。

任一子项失败，本用例失败。

#### FUND-02 事实与核验状态分离（P0）

- 前置：只有微信公众号单一原文证据。
- 期望：保留金额和轮次，同时标注“单源/待交叉核验”。
- 禁止：把金额值替换为“融资金额待核验”。
- [x] 通过

#### FUND-03 来源无关抽取（P0）

- 操作：把同一正文分别标记为微信公众号、投资机构官网和普通媒体来源。
- 期望：候选事实内容一致；只允许证据等级不同。
- [x] 通过

#### FUND-04 非融资金额排除（P0）

- 使用 G-04、G-05、G-06。
- 期望：市场规模、募资目标和估值不进入已完成融资金额。
- [x] 通过

#### FUND-05 多轮融资与转载去重（P1）

- 使用 G-07、G-15。
- 期望：历史轮次不覆盖本轮；同一事件多来源合并证据，不重复生成事件。
- [ ] 部分通过：多轮保留和同文回放幂等已覆盖；跨来源转载合并为单一事件仍待 P1 实施。

### 4.4 ENTITY：企业主体与工商信息

#### ENTITY-01 品牌简称可进入实体解析（P0）

- 输入：`诺因智能`。
- 期望：不因名称缺少法定后缀而跳过；输出主体候选和证据状态。
- [x] 通过

#### ENTITY-02 工商全称证据门禁（P0）

- 期望：只有可靠工商、官方或可交叉验证证据时才写入 `legalName`。
- 期望：未确认时页面显示“工商全称待核验”，不把品牌名伪装为公司全称。
- [x] 通过

#### ENTITY-03 成立时间语义分离（P0）

- 输入：文章、官网、工商来源给出不同日期。
- 期望：品牌/团队成立叙述进入 `claimedFoundedAt`，工商日期进入 `registeredAt`。
- 期望：显示来源差异或冲突，不静默覆盖。
- [x] 通过

#### ENTITY-04 官网所有权校验（P0）

- 期望：公司官网具有域名所有权或官方关联证据。
- 期望：微信公众号、媒体和搜索结果 URL 不得进入官网字段。
- [x] 通过

#### ENTITY-05 已转项目仍可补全（P0）

- 前置：线索状态为“已转专属项目”。
- 期望：主体和融资补全任务仍处理该线索，不因状态过滤跳过。
- [x] 通过

### 4.5 REGION：地区解析

#### REGION-01 人名和团队名消歧（P0）

- 输入：`创始人李银川`、`银川团队`。
- 期望：不输出宁夏或银川地区事实。
- [x] 通过

#### REGION-02 强证据纠正历史弱证据（P0）

- 前置：历史画像地区为“宁夏”，置信度中或低；新工商/官网证据指向深圳。
- 期望：形成字段级纠正或冲突，不继续把历史值无条件置顶。
- 期望：保留旧值及纠正原因的审计记录。
- [x] 通过

#### REGION-03 注册地与经营地分离（P1）

- 期望：注册地址、总部、办公地可分别保存和展示，不强行压成单一地区。
- [ ] 待 P1：本次已分离注册地址和业务地区，但尚未新增总部、办公地多值结构。

### 4.6 UI：页面展示

#### UI-01 融资事实优先于占位值（P0）

- 前置：`fundingRounds[0].amount=5 亿元`，数据质量状态为待交叉核验。
- 期望：页面显示 `5 亿元` 和核验状态。
- 禁止：仅显示“融资金额待核验”。
- [x] 通过

#### UI-02 官网与文章来源分离（P0）

- 期望：微信公众号链接出现在“文章来源/信息来源”。
- 期望：没有已验证官网时显示“公司官网待核验”。
- [x] 通过

#### UI-03 证据可查看（P0）

- 期望：融资轮次、金额、领投方、工商全称和地区均可查看来源类型、URL 和证据摘要。
- [x] 通过

#### UI-04 冲突不被隐藏（P1）

- 期望：不同来源日期或地区不一致时，页面显示“存在冲突”和各自来源，不用一个占位值隐藏全部事实。
- [ ] 通过

#### UI-05 线索页与项目页一致（P0）

- 期望：两页对同一事实使用一致的金额、轮次和证据状态；允许项目页保留人工高优先级值，但必须说明差异。
- [x] 通过

### 4.7 SYNC：线索与专属项目投影

#### SYNC-01 空值和占位值可安全补齐（P0）

- 期望：项目为空或仍为系统占位值时，可从已验证事实补齐轮次和金额。
- [x] 通过

#### SYNC-02 人工值保护（P0）

- 前置：项目字段已被人工修改并有审计记录。
- 期望：系统不自动覆盖，只生成建议更新或冲突项。
- [x] 通过

#### SYNC-03 同步审计（P0）

- 期望：记录事实 ID、来源线索、旧值、新值、同步决策、批次和时间。
- [x] 通过

#### SYNC-04 重复同步幂等（P0）

- 操作：同一事实同步三次。
- 期望：项目结果不变，不新增重复融资轮次，不产生无意义重复审计。
- [x] 通过

## 5. 历史回填验收

### 5.1 BACKFILL-01 诺因智能定向预览（P0）

预览至少输出：

| 字段 | 当前值 | 建议值 | 决策 | 证据 |
|---|---|---|---|---|
| 融资轮次 | 待核验 | 天使++轮 | 更新占位值 | 微信正文原句 |
| 融资金额 | 页面待核验/底层 5 亿元 | 5 亿元人民币 | 保留事实并修正展示 | 微信正文原句 |
| 领投方 | 污染数组 | 经纬创投 | 清洗与去重 | 微信正文原句 |
| 公司官网 | 微信文章 URL | 空或已验证官网 | 移出官网字段 | URL 类型校验 |
| 工商全称 | 空 | 候选或可靠工商全称 | 按证据级别决定 | 工商/官方证据 |
| 地区 | 宁夏 | 深圳/广东或冲突待核验 | 纠正弱证据 | 工商/官网证据 |

- [x] 预览不写入数据库。
- [x] 所有差异都有证据和覆盖理由。
- [x] 人工字段保护决策可见。

#### 5.2 BACKFILL-02 定向应用（P0）

- 前置：BACKFILL-01 预览经过人工确认。
- 操作：对固定线索/项目执行单记录 `apply`。
- [x] 只修改预览中获准字段。
- [x] 原始正文和旧值审计仍可查询。
- [x] 线索页和项目页结果符合 G-01。
- [x] 回填批次可通过事件 ID、批次 ID 和快照定位，并已演练条件式恢复。

#### 5.3 BACKFILL-03 重复执行（P0）

- 操作：对已完成记录再次预览和应用。
- 期望：有效数据差异为零；融资记录和证据不重复。
- [x] 通过

#### 5.4 BACKFILL-04 批量回填（P1）

- [ ] 支持来源、日期、数量和游标范围。
- [ ] 每批有成功、跳过、冲突、失败统计。
- [ ] 单条失败不破坏已提交记录和检查点。
- [ ] 支持从检查点恢复。
- [ ] 冲突记录进入人工复核队列。

## 6. 接口与数据契约验收

### 6.1 API 返回

- [x] 融资事实返回规范值、`roundRaw/amountRaw/currency`、来源、证据状态和抽取器版本。
- [x] 工商主体返回品牌名与法定全称两个字段。
- [x] 官网与文章来源使用不同字段。
- [x] 日期按品牌叙述/工商日期分字段返回，地区冲突继续使用结构化冲突契约。
- [x] 新字段均向前兼容，旧客户端仍可读取原基础字段。

### 6.2 数据库约束

- [x] 同一融资事实包含基于轮次、金额、日期、来源与证据指纹生成的幂等键。
- [x] 候选键、线索 ID、事实幂等键、来源 URL 和项目 ID 可关联原文、事实与投影。
- [x] 事实来源、字段来源、同步审计和人工字段保护在同步后保留。
- [x] 占位文本不作为真实金额、轮次或官网写入事实字段。

## 7. 自动化测试验收

### 7.1 必过测试

```bash
npm run check:types
npm run test:server
npm run build
```

计划新增的专项门禁：

```bash
npm run test:wechat-lead-extraction
npm run accept:wechat-lead-extraction
```

验收记录：

| 命令 | 结果 | 时间 | 证据路径/日志 |
|---|---|---|---|
| `npm run check:types` | 通过 | 2026-08-26 | TypeScript 前后端零错误 |
| `npm run test:server` | 通过 | 2026-08-26 | 342 项：338 通过、4 项按专用数据库开关跳过、0 失败 |
| `npm run build` | 通过 | 2026-08-26 | 最终候选构建及资源版本见第 9 节 |
| `npm run test:wechat-lead-extraction` | 通过 | 2026-08-26 | 34/34 通过 |
| `npm run accept:wechat-lead-extraction` | 通过 | 2026-08-26 | G-01 轮次、原始值、金额、币种、领投方和来源契约通过 |
| `npm run accept:lead-enrichment:mysql` | 通过 | 2026-08-26 | 随机 acceptance 数据库创建、迁移、写入验收后销毁，26 项检查通过 |
| 页面 E2E/人工验收 | 通过 | 2026-08-26 | 已登录真实浏览器检查线索页和项目页，控制台 0 error；隔离验收账号已清理，截图见 `.runtime/evidence` |

### 7.2 覆盖要求

- [ ] G-01 至 G-14 已覆盖；G-15 跨来源转载集合合并待 P1。
- [x] 核心规则有正例和反例。
- [x] 抓取状态、抽取状态、证据、主体、同步和展示阶段可分别诊断。
- [x] 专项测试使用固定原文夹具，不依赖微信公众号页面当前是否可访问。

## 8. 安全与鲁棒性验收

- [x] 外部 URL 访问经过协议、域名/IP 和重定向校验，SSRF 正反例测试通过。
- [x] 文章提示词作为不可信文本，不能改变工具边界；提示注入测试通过。
- [x] 模型 URL、工商字段和融资事实须通过证据绑定与格式门禁。
- [x] 失败或超时不会把已有正确事实清空为“待核验”。
- [x] 验收日志未输出密钥、Cookie 或完整敏感响应头。
- [x] 批量任务有限量、游标、重试上限、单条失败隔离和条件式回滚。
- [x] 单个来源失败不阻断其他记录提交。

任一高危安全项失败，整体验收直接不通过。

## 9. 部署与实际服务验收

### 9.1 发布前

- [x] 数据结构向前兼容；迁移 0048 在随机专用数据库完成创建、迁移和销毁验收。
- [x] 抽取入口按 Radar/微信公众号路径接入，项目同步仅补空或占位值，补全工作器保留独立开关。
- [x] 构建候选、激活版本和上一版本回滚指针均已验证。
- [x] 诺因智能写入前已执行定向差异预览。

### 9.2 发布后

- [x] 最新前端和服务端构建成功。
- [x] 新候选构建已被激活，不只是生成在候选目录。
- [x] 服务已重启，`/api/health` 与组件健康检查正常。
- [x] 浏览器实际加载最终构建资源，控制台无错误。
- [x] 固定隔离文章夹具正确进入事实链路；专用 MySQL 集成验收通过。
- [x] 诺因智能线索页和项目页通过真实浏览器检查。
- [x] 观察窗口内无持续抽取异常、重复写入或同步循环。

部署验收不能仅凭“进程正在运行”或某一项测试为绿判定通过。

## 10. 诺因智能最终页面验收清单

### 10.1 融资信息

- [x] 页面显示 `天使++轮`。
- [x] 页面显示 `5 亿元人民币`或等价规范值。
- [x] 页面显示领投方 `经纬创投`。
- [x] 不出现污染投资方文本。
- [x] 页面显示原文已标注/待交叉核验等证据状态。
- [x] 可打开融资事实对应的微信公众号来源。

### 10.2 企业信息

- [x] 品牌名和工商全称分别显示。
- [x] 工商全称只有证据绑定后才显示；无证据路径仍显示待核验。
- [x] 微信文章不再显示为公司官网。
- [x] 已验证官网独立显示，工商字段逐项提供来源入口。
- [x] 工商成立日期 `2025-06-23` 与品牌成立叙述 `2025-07` 分开显示。
- [x] 法定代表人、注册资本、信用代码等遵守无证据不填门禁；当前值均附直接来源。

### 10.3 地区与项目同步

- [x] 不再因人名或“银川团队”显示宁夏。
- [x] 广东地区结论绑定深圳工商注册地址来源。
- [x] 专属项目显示 `天使++轮`、`5 亿元`，与线索事实一致。
- [x] 人工维护字段未被未经授权覆盖。

## 11. 验收证据包

验收完成后应归档：

- 修复前后的数据库字段级快照。
- G-01 原文夹具及事实抽取 JSON。
- 规则/模型版本和证据定位结果。
- 所有自动化测试日志。
- 专用测试数据库集成测试报告。
- 诺因智能线索页、企业信息页、项目页截图。
- 回填预览、应用、重复执行和回滚演练记录。
- 构建、激活、重启、健康检查及实际资源版本记录。
- 未通过项、已知限制和后续处理人。

本次已归档的可复核证据：

- 批次回填及恢复快照：`.runtime/evidence/wechat-lead-backfill-final-contract-20260826.json`、`.runtime/evidence/wechat-lead-backfill-final-replay-20260826.json`、`.runtime/evidence/wechat-lead-backfill-qa-final-20260826-0155.json`。
- 线索页截图：`.runtime/evidence/knowin-lead-final-20260826.png`（已登录隔离管理员视角，截图后验收账号和会话已清理）。
- 专属项目页截图：`.runtime/evidence/knowin-project-final-20260826.png`（同一隔离验收会话）。
- 最终激活版本：`build-20260826T024425407Z-1586-608304b9`；服务资源：`assets/index-BOacPy75.js`、`assets/index-B4BTwa2g.css`。
- 微信候选同步重放：运行 `91fd3e0e-697a-4614-a593-122d3e73cbe8`，`succeeded`；`collection.reason=wechat_candidate_handoff`，`candidateWrites=1`，`mysql-runtime-jobs.deadLetter=0`。
- 诺因智能业务库复核：线索 `daf2a843-797c-44da-a13b-3eec6a4dd5b7` 的融资轮次、金额、领投方及证据完整；工商主体为深圳诺因智能有限公司，注册地址在深圳，业务地区为广东，字段级证据已落库。
- 定向事实事件：`f41f302ad29cf245d7c492f025c25b6cd8a19bb078d018a094b7a99c2c851d6d`；重复应用返回 `unchanged/replayed=true`。
- 公司证据事件：`b64abb1f5abc7e697c4d8fa9487ce80be44c22350aae5c1ad6cc6cca35586d23`；重复应用返回 `unchanged/replayed=true`。

P1 后续负责人建议：数据平台负责人。完成期限为正式启动全历史批次前；具体日历日期由业务负责人排期。启动批次前必须先实现 G-15 跨来源事件聚合，并保持默认预览。

## 12. 最终签署

| 角色 | 姓名 | 结论 | 日期 | 备注 |
|---|---|---|---|---|
| 开发 | Codex 执行记录 | P0 通过 | 2026-08-26 | 代码、定向回填、构建与实际服务已验证 |
| 测试 | Codex 自动化与浏览器验收 | P0 通过 | 2026-08-26 | 业务/数据/发布负责人仍需按组织流程签署 |
| 产品/业务 |  | 待签署 |  |  |
| 数据负责人 |  | 待签署 |  |  |
| 发布负责人 |  | 待签署 |  |  |

最终结论只能选择：

- **通过**：全部 P0 通过，无安全与发布阻断项，证据包完整。
- **有条件通过**：仅剩不影响数据正确性和主流程的 P1/P2 项，并明确负责人和完成日期。
- **不通过**：任一 P0、数据安全或实际部署验证失败。

当前最终结论：**有条件通过（全部 P0 通过；仅剩 G-15、全历史批量回填和持续指标等 P1 项）**。
