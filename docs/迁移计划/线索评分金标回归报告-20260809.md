# 线索评分金标回归报告（2026-08-09）

## 1. 结论

`lead-scoring-gold-v1` 已使用真实 `gpt-5.6-sol` 和生产同款 Claude Agent SDK 执行。项目七维与论文五维共 4 条技术金标全部进入预设分数区间，高质量样本均显著高于对应低质量样本；质量、成本、耗时、Turn 和零工具阈值全部通过。

本报告证明当前项目/论文评分技术回归门禁通过，不代表业务负责人已批准全部生产金样。研究、初筛和补全 Profile 已由独立金标覆盖；跨 Profile 的全局日预算、限流和熔断也已由 MySQL 持久化运行门禁覆盖。网关没有回传输入 Token，完整用量观测仍未关闭。

## 2. 可复验证据

| 项目 | 值 |
| --- | --- |
| 金标版本 | `lead-scoring-gold-v1` |
| 金标文件 | `server/assets/lead-scoring-gold-v1.json` |
| SHA-256 | `0f6e3d8ab5665fdbafd250c5286c5e38757d2b9c5ffa9fd3428707c31bfd1103` |
| Agent Profile | `lead-scoring-agent-v1` |
| 输出 Schema | `lead-scoring-output-v1` |
| 工具契约 | `no-tools-v1` |
| 项目评分标准 | `1.0`，7 个维度 |
| 论文评分标准 | `paper-v1`，5 个维度 |
| 模型 | `gpt-5.6-sol` |
| 执行命令 | `npm run accept:lead-scoring-gold` |
| 发布接入 | `deploy.sh` 在生产编译、数据库初始化后且服务重启前强制执行 |

金标文件内容由验收脚本中的固定 SHA-256 锁定；修改样本必须显式变更版本及门禁代码，不能无痕改写历史基准。

## 3. 实测结果

| 用例 | 预设区间 | 实际总分 | 结果 |
| --- | ---: | ---: | --- |
| 项目高质量：已验证工业视觉项目 | 60–90 | 76 | 通过 |
| 项目低质量：概念期泛化平台 | 0–35 | 5 | 通过 |
| 论文高质量：有基准、实体测试和付费试点 | 55–88 | 78 | 通过 |
| 论文低质量：无方法、实验及团队的观点文章 | 0–35 | 4 | 通过 |

| 指标 | 发布阈值 | 实测 | 结论 |
| --- | ---: | ---: | --- |
| 分数区间命中率 | 100% | 100%（4/4） | 通过 |
| 高低质量排序命中率 | 100% | 100%（2/2） | 通过 |
| 平均绝对中点偏差 | ≤20 分 | 8.375 分 | 通过 |
| 项目高低分差 | ≥20 分 | 71 分 | 通过 |
| 论文高低分差 | ≥20 分 | 74 分 | 通过 |
| 平均成本 | ≤0.15 美元/条 | 约 0.05004 美元/条 | 通过 |
| 平均模型耗时 | ≤60 秒/条 | 约 27.56 秒/条 | 通过 |
| 最大 Turn | ≤2 | 2 | 通过 |
| SDK 工具调用 | 0 | 0 | 通过 |

最终复跑平均输出 Token 为 2,001.5，输入 Token 为 0；成本字段非零且可用，但 Token 用量并不完整，因此不能用本报告关闭 `MIG-0541/LEAD-023` 的完整 Token 门禁。

## 4. 发布边界

静态门禁验证金标同时具备项目/论文两条工作流及高/低质量样本，验收脚本必须锁定 SHA、调用真实评分 Agent、计算区间准确率、排序、偏差、成本和用量完整性，并且 `package.json` 与 `deploy.sh` 必须保留真实模型入口。

当前可据此关闭 `LEAD-024/GATE-M5-04` 的技术发布标准；人工复核流程已有独立 10 项真实 MySQL 验收。以下事项继续保持未完成：

1. 研究、初筛、补全三个 Profile 的金标与负例回归已由 `lead-workflow-gold-v1` 补齐；仍需持续扩充业务金样。
2. 实体重复率指标和历史 42 组精确同名主体的治理。
3. 输入 Token 完整观测，以及运行门禁触发后的生产通知渠道和责任人。
4. 业务负责人对生产金样、区间和误差容忍度的最终签字。
