# 线索研究、初筛与补全金标回归报告（2026-08-09）

## 1. 结论

`lead-workflow-gold-v1` 已使用真实 `gpt-5.6-sol` 和生产同款 Claude Agent SDK 执行。研究、初筛和增量补全三个 Profile 共 7 条技术金标全部通过，契约准确率与 `sourceId`–原文引文绑定准确率均为 100%；质量、成本、时延、Turn 和零工具阈值全部通过。

本轮同时修复了宿主证据校验缺口：此前只验证引文出现在整份输入中，现在当宿主提供结构化来源时，还必须验证引文属于模型声明的 `sourceId`。把来源 A 的原文错误挂到来源 B 会失败关闭。

## 2. 可复验证据

| 项目 | 值 |
| --- | --- |
| 金标版本 | `lead-workflow-gold-v1` |
| 金标文件 | `server/assets/lead-workflow-gold-v1.json` |
| SHA-256 | `18048cae785e0722e2553982358f8cc4b287906e05fa577528fb3ed5cdbdca2a` |
| 研究契约 | `lead-research-agent-v1` / `lead-research-output-v1` |
| 初筛契约 | `lead-screening-agent-v2` / `lead-screening-output-v1` |
| 补全契约 | `lead-enrichment-agent-v1` / `lead-enrichment-output-v1` |
| 宿主工具版本 | `lead-research-host-tools-v1` |
| 模型 | `gpt-5.6-sol` |
| 执行命令 | `npm run accept:lead-workflow-gold` |
| 发布接入 | `deploy.sh` 在生产编译、数据库初始化后且服务重启前强制执行 |

金标内容由验收脚本中的固定 SHA-256 锁定；任何样本变化必须显式更新版本和门禁，不能原地修改历史基准。

## 3. 用例与结果

| Profile | 场景 | 核心预期 | 结果 |
| --- | --- | --- | --- |
| Research | 单一高可靠来源 | 提取事实、保留缺口、来源绑定 | 通过 |
| Research | 官网总部与登记住所不一致 | 同时保留两项事实并建立冲突 | 通过 |
| Screening | 客户采购且完成验收 | 有证据 `accept` | 通过 |
| Screening | 投后估值 4 亿/8 亿冲突 | `review`，不得猜测 | 通过 |
| Screening | 匿名论坛单方声称亿元营收 | 弱来源关键结论必须 `review` | 通过 |
| Enrichment | 业务地区为空 | `set_if_empty` 白名单补丁 | 通过 |
| Enrichment | 已有北京、登记来源为上海 | 零覆盖补丁，进入 `conflicts` | 通过 |

| 指标 | 发布阈值 | 实测 | 结论 |
| --- | ---: | ---: | --- |
| 契约准确率 | 100% | 100%（7/7） | 通过 |
| 来源绑定准确率 | 100% | 100%（7/7） | 通过 |
| 平均成本 | ≤0.10 美元/条 | 约 0.00854 美元/条 | 通过 |
| 平均模型耗时 | ≤60 秒/条 | 约 10.13 秒/条 | 通过 |
| 最大 Turn | ≤2 | 2 | 通过 |
| SDK 工具调用 | 0 | 0 | 通过 |
| 单用例最大尝试 | ≤2 | 1 | 通过 |
| 重试率 | ≤15% | 0% | 通过 |

平均输出 Token 为 341.71，输入 Token 仍由网关返回 0。成本字段非零且可用，但完整 Token 用量不可观测，因此本报告不能关闭 `MIG-0541/LEAD-023`。

## 4. 发布边界与剩余事项

主体、项目/论文评分、研究、初筛和补全五类 Profile 现均具备不可变真实模型回归门禁，可关闭 `MIG-0543`。以下事项继续单独跟踪：

1. 输入 Token 完整观测和主体批次成本的单条精确归因。
2. 全局运行门禁触发后的生产通知渠道、责任人和升级路径。
3. 更多弱来源、来源时效性和来源撤回场景的业务金样扩充。
4. 历史重复主体及缺失原始资产的人工治理。
