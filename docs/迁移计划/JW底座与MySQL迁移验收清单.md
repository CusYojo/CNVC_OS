# JW 底座与 MySQL 迁移验收清单

> 依据文档：[《智能投资管理平台 JW 底座与 MySQL 统一迁移计划》](./JW底座与MySQL统一迁移计划.md)  
> 配套执行清单：[《JW 底座与 MySQL 迁移执行清单》](./JW底座与MySQL迁移执行清单.md)  
> 使用方式：测试人员逐项填写结果与证据；P0 未通过时禁止正式切换或完成验收。

## 1. 验收信息

| 项目 | 内容 |
|---|---|
| 验收批次 |  |
| 验收环境 | 测试 / 预生产 / 生产冒烟 |
| 应用版本/提交 |  |
| 数据迁移批次 |  |
| MySQL Schema 版本 |  |
| JW Runtime 版本 |  |
| JW 迁移白名单/Aipin 拒绝清单版本 |  |
| Claude Agent SDK 版本 |  |
| 验收开始时间 |  |
| 验收结束时间 |  |
| 测试负责人 |  |
| 业务验收人 |  |
| 技术验收人 |  |
| 数据验收人 |  |
| 安全验收人 |  |

## 2. 结果与优先级说明

结果填写：`通过 / 失败 / 阻塞 / 不适用`。

| 优先级 | 定义 | 发布要求 |
|---|---|---|
| P0 | 登录、权限、核心数据、AI 主链路、线索主链路、切换和回滚 | 必须全部通过 |
| P1 | 重要业务功能、管理功能、恢复、审计和兼容 | 原则上全部通过；例外需书面批准 |
| P2 | 非核心体验、低频场景和优化项 | 可带已知问题发布，但必须登记修复计划 |

## 3. 测试数据和账号准备

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | PRE-001 | P0 | 准备系统管理员、AI 平台管理员、运营管理员和普通业务用户 | 通过 | `accept:migration-fixture-readiness` 在真实 MySQL 临时创建系统管理员、AI 平台管理员、平台运营及两个普通投资经理共 5 个随机夹具账号；全部使用 `.invalid` 邮箱、随机密码哈希，验收后逐 ID 清零。 |
| [x] | PRE-002 | P0 | 准备两个不同部门和至少两个互相隔离的投资项目 | 通过 | 两个普通投资经理分别属于投资一部/投资二部并各拥有一个项目；双方只能访问自己的项目，系统管理员可读取两者，未加入项目的 AI 平台管理员和平台运营不能凭角色越权，清理后项目/用户/文件均零残留。 |
| [x] | PRE-003 | P0 | 每个项目准备 PDF、DOCX、XLSX、PPTX、图片和文本资料 | 通过 | 两个隔离项目分别准备 PDF、DOCX、XLSX、PPTX、PNG 和 TXT 六类安全合成文件，共 12 份；均经扩展名/MIME/签名校验、私有目录存储和原字节回读，数据库保存 SHA-256、容量、归属和不可变版本。 |
| [x] | PRE-004 | P0 | 准备公司、项目、团队、实验室和论文线索金标样本 | 通过 | SHA-256 锁定的 `lead-subject-gold-v1` 覆盖公司、项目、团队、实验室和论文五类正样本及五类噪声/歧义负样本；真实 `gpt-5.6-sol` 连续两轮 10/10 通过。项目七维/论文五维评分和研究/初筛/补全另有独立金标集。 |
| [x] | PRE-005 | P1 | 准备重复线索、多主体、弱来源、冲突来源和无效主体样本 | 通过 | 重复主体 NFKC/空白归一化夹具和版本化 `lead-subject-gold-v1`、`lead-workflow-gold-v1` 共同覆盖重复、多主体、弱来源、冲突来源和无效主体；就绪门禁校验类别并绑定两份金标文件 SHA-256，不在证据中保存正文。 |
| [x] | PRE-006 | P1 | 准备成功、超时、限流、工具失败和模型失败模拟条件 | 通过 | 就绪门禁锁定 Agent 成功工厂、网关超时/中止、跨 Profile 分钟限流、禁止工具调用和模型失败/抛异常夹具，并以对应验收源码 SHA-256 形成可重复契约；这些条件均由现有专项门禁实际执行。 |
| [x] | PRE-007 | P1 | 准备钉钉、飞书、微信通知测试配置或安全 Mock | 通过 | 钉钉、飞书、企业微信三类 Bot 均使用受控 `mock://success`/`mock://failure` 完成加密配置、连接、Outbox、重试/死信、入站和线索推送验收；Mock 在生产默认拒绝，只有显式 `IM_SAFE_MOCK_ENABLED=true` 才允许。12 项 IM 验收覆盖该边界；真实租户联调仍由 `IM-004～006` 单独跟踪。 |
| [x] | PRE-008 | P0 | 记录旧 PostgreSQL、迁移白名单内 JW SQLite、Flue SQLite 基线数量 | 通过（本机批准源） | `accept:legacy-source-backups` 固定 PostgreSQL 16 表/16,574 行；JW `sessions.db` 25 表/2,208 行、`project-master.db` 1 表/0 行、`lead-memory.sqlite` 24 表/109 行；Flue data/runtime 候选分别 13 表/1 行和 13 表/67 行。SQLite 数量为全部用户表合计，且逐表明细保存在 0600 报告中；生产源完备性仍由 PRE-012/DATA-014 跟踪。 |
| [ ] | PRE-009 | P0 | 记录文件数量、总容量和抽样 SHA-256 | 本地已盘点/生产待做 | 最新 `inventory:files` 已对本机七类登记根中的 240 个在线文件全量计算 SHA-256 和 113,548,136 bytes 总容量，其中 1 个项目原件与数据库 SHA/字节一致；另有 67 个隔离原件保持私有权限和内容哈希。生产全部文件根尚未取得，因此不整项勾选。 |
| [x] | PRE-010 | P0 | 测试使用的密钥、个人信息和业务资料符合安全要求 | 通过 | `accept:migration-test-data-safety` 扫描 84 个验收脚本、34 个服务测试和 3 个版本化金标/夹具资产，共 121 个文件；当前环境 4 个已配置密钥值、接口密钥/私钥/认证令牌字面量、身份证号、手机号、非保留域邮箱和已知弱登录密码均为 0 发现。AI API 与旧 JWT 验收均使用随机 `.invalid` 身份、随机强密码及精确清理；旧 JWT REST/Socket 验收使用独立随机 Schema，最终临时 Schema、用户和审计均零残留。独立证据门禁继续验证 0600 权限、正文禁止、符号链接和真实配置值不泄漏。 |
| [x] | PRE-011 | P0 | JW 通用能力迁移白名单与 Aipin 模块、路由、表、队列、配置、页面和进程拒绝清单已评审 | 通过 | `ADR-003` 已采用“仅迁移通用 Runtime、Aipin 全量拒绝”，《JW迁移白名单与Aipin拒绝清单》逐项固定代码、路由、表、队列、配置、页面、进程和数据拒绝域；执行清单 `MIG-0008/0027/0201/0707/0726` 与交付件 `DEL-013` 已完成。最新严格复验扫描 214 个活动文件、82 张目标表、688 个文本/JSON 列均 0 命中；37 个拒绝会话/2,168 条消息目标写入 0，恶意路径/表夹具也保持目标哨兵不变。任何新增来源、哈希或来源值仍必须重新评审，不能自动扩张白名单。 |
| [ ] | PRE-012 | P0 | 权威数据源矩阵覆盖 PostgreSQL 生产 Schema、非 ORM 表、Radar 文件/状态、`lead_reserve`、浏览器遗留状态和全部文件根 | 本地矩阵/盘点工具通过，生产批准待完成 | 页面/API事实源矩阵、dump/JW/Flue/Radar/储备池/浏览器/文件根的本地口径均已建立。生产盘点器现强制恰好一个dump、拒绝入口与目录内符号链接、识别改名 PostgreSQL dump、重叠根只计一次；7项隔离验收零数据库写入。仍缺生产`pg_catalog`、显式扫描根、Radar/群聊/公众号原资产及数据负责人批准，不能勾选。 |
| [ ] | PRE-013 | P0 | “页面操作 → API → 当前事实源 → 目标表/存储 → 审计 → 验收”追踪矩阵已经业务和技术共同批准 | 矩阵已交付/联合批准待完成 | 《页面API事实源与验收追踪矩阵》已覆盖 14 个保留或重定向路由、核心读写操作、MySQL/文件/外部依赖、审计和验收，并标记退场、隐藏、Mock 与浏览器偏好；`MIG-0009` 已完成。仍缺业务与技术负责人对当前版本共同批准，不能用技术交付代替签字。 |
| [x] | PRE-014 | P0 | OA/投后/通知/材料/字典/模板逐项具有正式化、归档或退场结论 | 通过（范围更新） | OA/流程日志、组织/角色权限、数据字典正式化到 MySQL；AI 模板保留版本化目录；旧材料页转统一 AI 任务；投后和通知继续退场。浏览器本地写入仍永久禁止。 |
| [ ] | PRE-015 | P0 | 六类专业 AI 任务金样、Office/WPS 环境、字体和原生依赖版本已冻结 | 本轮按要求延期 | 历史技术证据和原生依赖锁仍保留，但用户明确要求本轮不测试 PPT、尽调、提案、Q&A 和合规性说明生成；批准金样、目标 Office/WPS、字体和干净节点基线未共同冻结，不能因旧产物存在而关闭。 |
| [ ] | PRE-016 | P0 | 迁移文档和决策记录已纳入版本控制并绑定基线提交 | 文档已建立/提交基线待完成 | 统一计划、执行/验收清单、ADR/问题台账和证据索引均已建立，Git 基线工具也能记录分支、提交和逐文件状态；当前工作区仍包含受保护的未提交迁移与用户改动，未形成可验证的最终基线提交，因此保持未完成且不擅自暂存或提交。 |

## 4. 构建、部署与基础健康

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | ENV-001 | P0 | 干净环境可以完成依赖安装和构建 | 本地通过 | `accept:clean-build` 不复制 `.env`、`node_modules`、构建产物、运行状态或业务文件，在独立临时目录全新安装 568 个锁定依赖并完成 TypeScript/Vite 生产构建，随后验证临时目录已清理。 |
| [x] | ENV-002 | P0 | Web、API、JW Runtime、Worker 和 MySQL 可以按文档启动 | 本地通过 | `accept:single-service-runtime` 以生产只读 Schema 启动唯一 `cybernaut-app`，Web SPA、API、JW、Socket、Radar/MySQL、调度、评分/AI Task、子进程监督和会话组件全部健康；仅 3100 监听，3584/8121 关闭。 |
| [x] | ENV-003 | P0 | 健康检查能区分应用、数据库、Agent、Worker 和外部依赖状态 | 本地通过 | `/api/health/components` 分列 JW、Socket、Radar、MySQL 数据源、调度器、评分与 AI 任务；外部 LLM 另有 `/api/llm-health`。 |
| [x] | ENV-004 | P1 | 服务支持优雅停止，不产生损坏的任务和会话状态 | 本地通过 | 停止新调度/连接后取消 Runtime Job、等待评分 poll/任务、释放 AI Task 租约、终止 JW 与受监督子进程，再关闭 HTTP/MySQL；真实 SIGINT 启停及监督器排空验收通过。评分 Worker 的 poll/Pool 关闭竞态已修复并连续 3 次定向复验。 |
| [x] | ENV-005 | P0 | 完整重启后用户、项目、会话、配置和任务仍存在 | 本地通过 | `accept:restart-persistence` 用生产只读 Schema 启动完成两次完整启停；同一用户 Cookie、项目、聊天索引、JW 消息/Part 和已完成 AI Task 均从 MySQL 恢复。验收启动前暂缓真实周期任务、结束后精确恢复调度配置，并确认未新增运行记录，共 8 项通过。 |
| [x] | ENV-006 | P1 | 日志包含时间、级别、服务、请求 ID 和可追踪错误 | 本地通过 | 统一入口安装结构化日志封装，所有 console 记录补齐 ISO 时间、级别、`cybernaut-app` 和可空请求 ID；AsyncLocalStorage 透传 HTTP 请求 ID，完成日志含方法、路径、状态和耗时。`accept:structured-logs` 4 项及真实单服务日志契约通过。 |
| [x] | ENV-007 | P0 | 日志不输出完整 API Key、Cookie、密码或机器人密钥 | 本地通过 | 500/未处理异常统一经环境密钥、Bearer、命名字段和 URL 凭据脱敏，500 响应不再回传上游消息；JW/Job 持久错误也先脱敏。`accept:log-redaction` 5 项通过，封面/公开情报敏感输入不进入 argv。 |
| [x] | ENV-008 | P1 | 关键环境变量缺失时服务拒绝危险启动并给出明确错误 | 本地通过 | 生产启动强制校验强会话/JWT/旧会话轮换密钥、Secure Cookie、HTTPS Origin、关闭旧 Bearer/演示种子、合法 SameSite、会话上限/续期窗口、端口、模型凭据加密主密钥和 Provider 主机白名单；IM 开启时强制集成凭据主密钥，能力/IM 独立开关只接受 `true/false`。`accept:runtime-config` 20 项通过；MySQL 六键继续由数据库配置层强制校验。 |
| [x] | ENV-009 | P1 | MySQL 连接耗尽、短暂断开后系统有明确错误并可以恢复 | 本地通过 | 应用连接池使用有限队列和建连超时；`accept:mysql-resilience` 验证队列溢出返回明确错误、释放后排队查询恢复，并通过本地 TCP 故障代理切断全部 MySQL 连接：中断期请求失败关闭，网络恢复后同一 Pool 自动重连。验收不停止真实数据库且不依赖高权限连接终止命令。 |
| [x] | ENV-010 | P1 | 数据库迁移重复执行不会破坏已有数据 | 通过 | 最新 `accept:migration-idempotency` 先从活动前缀生成一致性备份，再恢复到随机隔离前缀，在隔离前缀连续执行三次完整 Schema 迁移；82 张表、69,118 行的规范化 DDL、行数和逐行内容 SHA-256 均不变，迁移日志 40 条，活动前缀写入 0，隔离表精确清理。该方式避免与已运行调度器的合法写入竞争，也不为验证幂等而触碰在线前缀。 |
| [x] | ENV-011 | P0 | 构建产物、注册路由、启动日志和运行进程中不存在 Aipin 服务、队列或 Worker | 本地通过 | `check:single-service` 73 项扫描 255 个活动文件，并强制存在源/目标排除报告；Aipin 仅允许出现在离线 Flue 拒绝逻辑、目标 Schema 拒绝检查和审计器本身，根构建、启动日志和单一 Node 进程验收未出现 Aipin 服务、队列或 Worker。 |
| [ ] | ENV-012 | P0 | 干净环境可重建 Python、LibreOffice、Poppler、Tesseract、字体和文档生成原生模块 | 本机锁定通过/干净 Linux 待验 | 精确 Python 锁、原生版本契约和安装/只读核验命令已版本化，本机已复验 Python、LibreOffice、Poppler 与依赖闭包；目标 Linux 的镜像/apt snapshot、Tesseract `chi_sim/eng`、Noto CJK 字体和全新节点重建报告尚未取得。本轮也按要求不触发专业报告生成。 |
| [x] | ENV-013 | P1 | systemd、Nginx、cron/timer、端口、健康检查、日志和文件目录与版本化部署清单一致 | 不适用（本批次延期） | 2026-08-11 按用户指示跳过目标 Linux 的 systemd、Nginx、cron/timer、端口、健康检查、日志和文件目录现场验收；已通过的部署清单、暂存发布契约及离线启动前门禁继续保留。本项未取得预生产/生产 Linux 成功报告，不视为部署实机验收通过；重新启用生产部署验收时须补做。 |
| [x] | ENV-014 | P0 | 动态加载、子进程、文件、网络和数据库访问均符合 Runtime allowlist | 本地通过 | JW 禁用内建工具、Skills 和文件设置源；单一宿主 MCP Server 仅允许项目摘要、项目资料搜索、列文件、读解析片段、受控 AI 任务创建/查询和公开情报七个工具。项目与任务工具绑定服务端稳定 userId/projectId/conversationId，公开情报只访问 Bing/Sogou 且敏感输入走 0600 临时文件；外部命令统一监督。 |
| [x] | ENV-015 | P0 | `cybernaut-app.service` 是唯一项目业务部署单元，Express、Socket.io、JW Runtime 和业务 API 共用统一入口 | 本地通过 | 单一 Node PID 监听 3100；HTTP、`/socket.io`、JW Runtime 和 Job Worker 均为进程内组件，生产 systemd 实机仍需发布复核。 |
| [x] | ENV-016 | P0 | Python 采集、Office/PDF 和长 AI 任务在受监督 Worker/子进程执行，不阻塞 HTTP/Socket 主事件循环 | 本地通过 | 生产 service/runtime 中所有 `child_process` 调用均收口到异步 `supervisedProcessService`；静态门禁禁止绕过，健康页可观测活动子进程，超时/取消验收通过。 |
| [ ] | ENV-017 | P0 | 子进程均处于同一 systemd cgroup，可统一停止、限额和观测，不产生孤儿进程 | 部分 | 本地已验证嵌套进程组停止和无孤儿；部署单元已配置 `KillMode=control-group`、非 root 用户、MemoryMax/CPUQuota/TasksMax 和沙箱。仍待目标服务器读取实际 cgroup 与限额取证。 |
| [ ] | ENV-018 | P0 | 最终拓扑无 `cybernaut-flue`、Radar Sync timer、旧摄入 cron、3584 端口和 `/ai/api` 代理 | 本地通过/生产待验 | 根启动链只有 `cybernaut-app`；旧 Flue package 与 direct scripts 均硬禁用；静态门禁确认无旧代理/timer，运行验收确认 3584/8121 不监听。目标 Linux 工具现全量扫描项目 service/timer unit file、root/运行用户与 cron.d、容器及统一 cgroup 外项目进程，但仍需在生产机生成成功报告。 |
| [ ] | ENV-019 | P1 | Nginx/Ingress、MySQL、对象存储和模型网关继续作为独立基础设施，不被错误嵌入业务进程 | 本地通过/生产待验 | 唯一业务进程只监听 3100；MySQL 通过连接池使用，LLM 通过网关 URL 使用，Nginx/Ingress 独立终止 TLS，Radar/Office/PDF 仅作为受监督短时子进程。目标生产拓扑和外部依赖健康仍需实机取证。 |

## 5. 统一鉴权与权限

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | AUTH-001 | P0 | 正确账号可以登录并获得正确用户、角色和部门 | 通过 | `accept:auth` 使用隔离 MySQL 账号校验登录身份。 |
| [x] | AUTH-002 | P0 | 错误密码、禁用账号和不存在账号不能登录 | 通过 | `accept:auth` 三种请求均返回 401。 |
| [x] | AUTH-003 | P0 | 登录会话使用 HttpOnly Cookie，浏览器脚本不能读取会话密钥 | 通过 | 会话 Cookie 为 HttpOnly；前端仅能读取独立 CSRF Cookie，不保存 JWT。 |
| [x] | AUTH-004 | P0 | REST 与 Socket.io 对同一用户采用一致身份判断 | 通过 | 两者均校验 MySQL `auth_sessions` 并回查启用用户；项目权限也共用同一服务。 |
| [x] | AUTH-005 | P0 | 退出后 REST 和 Socket.io 会话均失效 | 通过 | `accept:auth` 验证退出后 REST 401；`accept:socket` 验证已连接 Socket 被断开。 |
| [x] | AUTH-006 | P0 | 管理员禁用用户后，该用户已有连接和后续请求失效 | 通过 | `accept:socket` 验证禁用后 REST 401，既有 Socket 在重验证周期内断开。 |
| [x] | AUTH-007 | P0 | 用户 A 不能读取用户 B 的 Agent 会话和消息 | 通过 | 跨用户 Socket 订阅返回 `NOT_FOUND`；REST/JW 快照沿用同一所有者校验。 |
| [x] | AUTH-008 | P0 | 项目 A 成员不能读取无权访问的项目 B 文件、知识库和产物 | 通过 | `accept:resources` 以双用户/双项目验证项目详情与列表、项目文件列表/下载/修改、旧问答知识检索、workspace 产物和 generated 文件均拒绝跨用户访问；项目文件、workspace、generated 和正式 AI 产物下载均拒绝符号链接越界。 |
| [x] | AUTH-009 | P0 | 普通用户不能调用模型、能力、IM 机器人管理写接口 | 通过 | 模型、能力和 IM 管理均有前端隐藏导航/路由守卫、Router 中间件及服务层角色校验；IM 与线索推送服务验收确认普通用户固定返回 `ROLE_FORBIDDEN`。 |
| [x] | AUTH-010 | P1 | 页面路由守卫和服务端权限返回一致 | 通过 | `/system`、模型、能力和 IM 页面均按角色守卫；真实浏览器确认普通用户不显示系统/IM 导航且 IM 深链回到首页，服务端管理和推送写 API 独立返回统一权限拒绝。 |
| [x] | AUTH-011 | P1 | 权限拒绝返回统一错误格式和请求 ID | 通过 | HTTP 入口生成/透传受限格式的 `X-Request-ID`；鉴权、角色和项目拒绝统一返回 `code/message/details/requestId`，`accept:resources` 校验响应头与正文 ID 一致。 |
| [x] | AUTH-012 | P1 | 用户、角色、权限和项目成员变更写入审计日志 | 通过 | 新增账号、姓名/角色/部门/状态变更和项目负责人/协作成员替换均由服务端事务写入 `audit_logs`，记录真实管理员、目标、变更前后值和请求 ID，且不记录密码。身份变化同步吊销活动会话；普通用户越权、管理员自锁和无效/禁用成员均拒绝。`accept:identity-administration` 10 项真实 MySQL 验收及发布静态门禁通过。 |
| [x] | AUTH-013 | P0 | 状态改变请求具备 CSRF Token 或严格 Origin/Referer 防护，跨站请求被拒绝 | 通过 | 同源 Origin + 会话绑定 CSRF 双重校验；缺失 Token、恶意 Origin 和跨站登录均被拒绝。 |
| [x] | AUTH-014 | P0 | Cookie 的 SameSite、Secure、Domain、Path、TTL 和 CORS credentials 符合环境设计 | 不适用（本批次延期，未安全验收） | 2026-08-11 按用户指示延期生产证书与浏览器取证；本地 SameSite/Path/TTL/CORS 和生产 HTTPS/Secure Cookie 失败关闭契约保留。本项不代表安全通过，不解除生产发布阻断。 |
| [x] | AUTH-015 | P0 | 登录后会话轮换，可服务端吊销，退出、禁用和密钥轮换后旧 Cookie 失效 | 本地通过 | 5 项 `accept:auth-session-policy` 验证用户锁下并发会话上限、超额旧会话失效、临期续期、旧密钥兼容窗口以及续期后旧 Token/CSRF 失效；`accept:auth` 14 项真实 HTTP 验证随机登录会话、退出吊销、禁用、Cookie 和 CSRF。生产轮换操作取证仍由 SEC-013/CUT 阶段跟踪。 |
| [x] | AUTH-016 | P0 | 旧密码哈希可安全验证或按方案重置，不迁移明文密码 | 不适用（本批次延期，未安全验收） | 2026-08-11 按用户指示延期 5 个真实账号弱密码轮换；轮换工具、事务吊销会话、安全审计和 0600 作业清单保留。本项不代表密码安全通过，生产启用前仍必须轮换。 |
| [x] | AUTH-017 | P0 | 旧 JWT 仅在批准窗口内有效，截止后 REST/Socket 均拒绝且使用记录可审计 | 通过 | 默认和生产均禁止旧 Bearer；迁移环境要求最长 30 天精确 UTC 截止时间及稳定用户 UUID 白名单。7 项策略验收覆盖白名单、到期码、无 Token 审计和管理员数据库失效水位；随机独立 Schema 的单服务运行验收确认 REST/Socket 各自鉴权及审计，推进水位后 REST 立即 401，已连接 Socket 在有界重验内断开，临时服务、Schema 和端口零残留。 |
| [x] | AUTH-018 | P0 | 姓名型 owner/collaborator/assignee 已映射稳定用户 ID，重名、离职和孤儿有人工结论 | 通过 | 运行时鉴权已全部切到稳定 ID；迁移 0013 和 `accept:identity-mapping` 的 10 项验收覆盖唯一账号映射、重名/禁用/缺失默认拒绝、持久化冲突台账、派生 ID 防篡改和显式裁决防回退。唯一 `missing_user` 项目负责人已依据锁定源 dump 哈希、项目 `created_by`、创建审计、2 个附件上传者、1 场会议主持人和 7 个待办负责人一致指向“林知远”的证据完成裁决；当前项目负责人 15/15 已绑定，未决身份问题 0。 |

## 6. MySQL Schema、Repository 与数据完整性

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | DB-001 | P0 | `iam_*`、`investment_*`、`knowledge_*`、`agent_*` 表初始化成功 | 通过 | 按 `ADR-013`，计划前缀是概念域而非强制改名；`accept:mysql-architecture-contract` 只读实库验证身份、投资、知识和 Agent 领域全部批准物理表存在，前缀形式的重复权威表不存在。空库初始化与重复执行另由 `db:migrate`/幂等门禁通过。 |
| [x] | DB-002 | P0 | `ai_task_*`、`lead_pipeline_*`、`runtime_*`、`scheduler_*`、`audit_*` 表初始化成功 | 通过 | AI Task、Pipeline、模型/能力/Plugin/IM 配置、`runtime_jobs/runtime_job_runs`、`audit_logs` 及迁移控制面均由实库元数据门禁验证；配置密文列、版本、唯一键、调度租约/Run 外键和审计索引同时通过，未创建第二套 `runtime_*`/`scheduler_*` 空壳表。 |
| [x] | DB-003 | P0 | UUID、JSON、UTC 时间、布尔和长文本可正确读写 | 通过 | `check:mysql` 实测标准 UUID、JSON 数组、布尔、已知 UTC 毫秒时刻和 70,000 字符 LONGTEXT 往返一致。 |
| [ ] | DB-004 | P0 | 外键、唯一约束和删除规则符合设计 | 部分通过 | 当前 133 条外键；代码唯一索引已与 MySQL 名称及列顺序逐项匹配且无重复组，系统组织/角色/权限/字典与配置版本均具备引用、乐观版本和删除保护，知识投影同 scope/ref/source/chunk 唯一约束并通过旁路重复拒绝。能力目录/授权/会话选择、模型 Provider/模型/主备路由引用、OA 活动键与历史保护、项目删除级联、AI Task 引用以及 9,903 个 Pipeline Item 和 Run/Decision/Evidence/Review 审计链均已约束。线索历史重复处置前不启用活动名称唯一约束，因此整项仍保持部分通过。 |
| [ ] | DB-005 | P1 | 线索有效名称唯一规则在 MySQL 中正确实现 | 未通过 | 完整备份存在 9 组真实同名线索、15 条额外记录；为避免迁移吞行，当前保留全部 892 条并使用普通索引，待审计合并后再启用唯一约束。 |
| [x] | DB-006 | P0 | Repository 事务失败时不会留下半写入数据 | 通过 | `check:mysql` 在事务中写入项目后故意抛错，回滚后按唯一标记查询为 0。`accept:knowledge-ingestion-atomicity` 进一步在先删除旧知识投影、再写入非法新投影时制造约束错误，事务回滚后旧分块内容逐块完全一致；成功替换、重放、隔离来源和清理也通过。 |
| [x] | DB-007 | P0 | AI 任务和线索任务并发领取不会被多个 Worker 同时成功领取 | 通过 | AI 文档租约并发只有一次成功；评分 Job 并发入队/领取只有一次有效并持久化重试。 |
| [x] | DB-008 | P1 | 乐观锁或版本字段能阻止静默覆盖新数据 | 通过 | 迁移 `0038_add_business_optimistic_versions` 为项目、会议、待办和风险增加非空 `version`；现有用户编辑 API 强制 `expectedVersion`，服务使用同一条 `UPDATE ... WHERE id/version` 原子递增并在过期版本时返回 `BUSINESS_VERSION_CONFLICT` 409，前端从当前 MySQL 水合记录携带版本。`accept:business-optimistic-lock` 在真实 MySQL 对四类实体分别并发提交两个同版本写者，均仅 1 个成功、版本仅递增 1 次且夹具残留 0；OA 继续使用行锁与 `lock_version`，模型、能力、IM 和系统管理配置继续使用既有乐观锁。 |
| [x] | DB-009 | P1 | 稳定排序和分页不会出现重复、遗漏和翻页漂移 | 通过 | 项目列表按 `pinned DESC, updated_at DESC, id DESC`，线索按创建时间或评分后再以 `id DESC`，人工复核按状态、创建时间和 `id ASC`；所有分页排序最终均落到唯一键。`accept:stable-pagination` 在真实 MySQL 分别写入 7 条同时间/同置顶项目和 7 条同时间/同分数线索，以每页 3 条连续读取 3 页并重复读取，项目、线索时间排序、线索评分排序均无重复、遗漏或顺序漂移；人工复核 SQL 的唯一键尾排序同步纳入静态门禁。 |
| [x] | DB-010 | P1 | 中文检索在代表性项目资料上的召回达到既定门槛 | 通过 | `chinese-retrieval-gold-v1` 锁定 8 类代表性投资资料与 8 个中文问题，阈值为 Recall@3≥0.875、Top-1≥0.75、MRR≥0.8；真实 MySQL `knowledge_chunks` 隔离写入/检索结果三项均为 1.0。另通过 NFKC 全半角归一化、项目与 Scope 隔离、无关查询零结果、三次稳定排序和夹具残留 0；门禁报告不含资料正文或数据库连接身份。 |
| [x] | DB-011 | P0 | 数据库账号权限符合最小权限原则 | 当前 `.env` 目标环境通过 | 2026-08-11 最新复核先发现 Runtime 鉴权失败；安全预览只返回计数并确认精确 Host 漂移（当前绑定 0、旧绑定 2）。受控修复先为当前观测来源建立同权限 Runtime/Migration 主体并分别实连，成功后才移除 2 个旧绑定；密码未变、不使用 `%`。随后独立 Migration 账号只前进到 `0039`，`check:mysql` 验证 MySQL 8.0.36、82 表和 133 外键；权限审计确认 Runtime 仅 `SELECT/INSERT/UPDATE/DELETE`、Migration 仅目标 Schema DML+必要 DDL，两个主体不同、文件 0600。权限审计控制台只输出主体 SHA-256，不泄露数据库用户名或来源 Host。正式部署仍必须使用固定出口/VPN/私网，其他环境由 `MIG-0102` 分别取证。 |
| [x] | DB-012 | P0 | MySQL 备份可成功恢复到隔离环境 | 通过 | `accept:mysql-backup-restore` 已验证随机隔离 Schema 恢复；最新 `accept:mysql-schema-lifecycle` 对当前 82 表/69,118 行创建一致性临时备份，恢复到随机隔离表前缀并只前进到 `0039`，验证结构、行数与顺序无关逐行 SHA，确认恢复前缀可运行版本化种子且不会生成演示账号，最终精确删除 82 张隔离表且活动前缀不变。行数是该一致性快照时点值；生产最终停写点仍须重建备份。 |
| [x] | DB-013 | P0 | 目标 Schema 不包含 Aipin 表，`lead_pipeline_*` 为当前项目独立数据模型 | 通过 | 目标当前 81 张业务表 + 1 张迁移台账表并拒绝 Aipin 名称；系统组织/角色/权限/字典、能力目录/授权/会话选择、模型 Provider/模型/路由、加密配置历史及线索、Agent、OA、评分、Radar、文件、AI 模板、Runtime Job 和 CDC 检查点/事件账本均来自当前项目版本化迁移。 |
| [x] | DB-014 | P0 | MySQL 小版本、InnoDB、utf8mb4、排序规则、sql_mode、隔离级别和时区与批准配置一致 | 通过 | `check:mysql` 实测 MySQL 8.0.36、InnoDB、utf8mb4/utf8mb4_0900_ai_ci、READ-COMMITTED、+08:00 和批准严格 sql_mode；82 张目标表及 133 条外键通过。`accept:mysql-operational-config` 进一步确认应用 client/connection/result 字符集均为 utf8mb4、连接排序规则为 utf8mb4_0900_ai_ci、最大连接、慢查询和连接字符集符合批准配置；报告排除全部连接身份与密钥。 |
| [ ] | DB-015 | P0 | 邮箱、公司名、线索名的大小写、空白、全半角和 Unicode 冲突已扫描并处置 | 已扫描/裁决待完成 | `audit:mysql-normalization` 扫描确认邮箱/项目无冲突，线索名 9 组/25 条、公司名 33 组/82 条，共 42 组/107 次引用/82 条唯一线索。0600 底稿锁定报告 SHA、稳定组 ID、全行指纹和跨组一致性；`0039` 合法增加线索来源台账后，预览准确检出基线/行指纹漂移。因原底稿 42/42 均为 pending、决定数和正式写入均为 0，已使用只允许覆盖全 pending 底稿的受控参数重建；最新预览无错误，10 项只读验收通过，严格模式仅以 `PENDING_DECISIONS` 退出 2。事务执行器已覆盖已转项目保护、四类引用迁移、原行/快照保留、Run/Issue/Mapping/Audit、幂等和回滚；仍需业务逐组决定并正式 apply。 |
| [ ] | DB-016 | P0 | CDC 覆盖新增、更新、物理删除和级联删除，支持 tombstone、watermark、重放和中断恢复 | 目标端通过/源端未验收 | 迁移 `0029`、17 表触发器契约及 `accept:postgres-cdc` 已完成；6 个乱序事件按 4 个源事务组件应用，sequence 3 中断后续跑至 6，3 新增/1 更新/2 删除/1 级联/3 重放计数一致，父删除后的子 tombstone 为已审计 noop。当前 PostgreSQL `127.0.0.1:5432` 离线，真实源触发器未安装，不能整项通过。 |
| [x] | DB-017 | P0 | 用户、项目、项目成员和旧 ID 映射在 Agent/线索/AI 任务写入前已稳定存在 | 通过（当前源范围） | `iam_user_mappings` 共 10 条：`legacy_postgres` 5 条、`postgres_dump` 5 条，映射到 5 个目标用户；26 条外键无孤儿。 |
| [ ] | DB-018 | P0 | 每个实体只有一个业务写入所有者，未发现应用层无协调双写 | 代码边界通过/生产待签字 | 当前业务代码只写 MySQL，PostgreSQL Driver 仅允许于 6 个离线盘点/全量/CDC 工具；前向 CDC 强制显式声明 `legacy-postgres-authoritative`，手册要求 MySQL 接管后立即停止前向同步。仍需旧应用停写、生产责任人和 Point of No Return 签字。 |
| [x] | DB-019 | P0 | 业务 Route、Service 和批处理脚本不再直接依赖 PostgreSQL Driver/方言，例外均有批准 | 通过 | `npm run check:db-boundary` 通过，仅三个离线盘点/迁移工具在白名单内。 |
| [x] | DB-020 | P1 | OA、投后、通知、材料、字典和模板目标模型的约束与生命周期符合批准设计 | 通过（迁移/退场范围更新） | OA 4 表、活动唯一键、稳定身份、事务、待办和审计生命周期已通过 12 项验收；组织/角色/权限/字典由迁移 `0036/0037` 正式落 MySQL，数据库 `system.manage` 授权、事务绑定、乐观版本、撤权和审计通过 11 项专项验收。模板正式迁移并只读展示，材料能力合并到统一 AI Task；旧投后页、通知/已读和旧材料页继续退场，浏览器旧组织/字典写入永久禁止。 |

## 7. 现有页面和投资业务回归

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [ ] | UI-001 | P0 | 登录页面和登录跳转正常 | 本地浏览器通过/生产待验 | 使用随机 `.invalid` 合成管理员在唯一 3100 服务完成登录并跳转首页；新会话无预填凭据，控制台无错误。仍待生产 TLS/Secure Cookie 环境取证。 |
| [ ] | UI-002 | P0 | 首页工作台数据、卡片、待办和导航正常 | 本地浏览器通过/生产待验 | 首页由 MySQL 恢复真实卡片、待办和导航，1280×720 下无横向溢出、控制台无错误；生产业务数据与角色矩阵仍待签字。 |
| [ ] | UI-003 | P0 | 项目列表新增、编辑、删除、置顶和分页正常 | 本地完整通过/生产待验 | 真实浏览器已完成创建、详情跳转、列表/详情编辑、17 条数据三页分页、置顶、刷新恢复置顶、站内确认删除和删除后空态；置顶/删除审计均可见，0 横向溢出、0 控制台错误。修复编辑误提交 `owner/collaborators`、原生确认框不可控、置顶无反馈和 UTC/nullable 告警；未建立服务端契约的批量分配、批量标签、自定义字段入口已退场。仍待生产角色和数据矩阵验收，故不整项勾选。 |
| [ ] | UI-004 | P0 | 项目详情、文件、摘要、评分和阶段信息正常 | 本地完整通过/生产待验 | 项目详情、阶段与业务字段可加载和编辑；私有文本原件上传后由“解析中”到“成功”，下载/删除入口可见。真实项目评分由 MySQL 队列和同一服务内 Worker 完成，`gpt-5.6-sol` 返回 7 个维度、总分及同行业分位，页面刷新后仍可恢复。全程无横向溢出、浏览器 warning/error 为 0，未调用 PPT 或专业报告生成。仍待生产 TLS、真实角色与业务数据验收，故不整项勾选。 |
| [ ] | UI-005 | P0 | 共有线索池列表、筛选、详情、同步和转项目正常 | 本地筛选/详情/转项目通过/同步与生产待验 | 在真实页面中以隔离线索同时按关键词、渠道、行业和地区筛选，结果精确为 1；详情页展示主体、摘要、行业、地区、A 轮、融资额和估值。页面转项目后按钮切换为已转换状态，项目列表和稳定 UUID 详情路由 MySQL 恢复同一名称、公司、线索阶段、轮次、金额、估值和摘要。`accept:lead-conversion` 5 项另证明并发单胜者、稳定负责人、关联/审计事务及失败无孤儿。浏览器 warning/error 为 0、无横向溢出；Radar 真实同步本次未点击，仍待生产源资产、角色和同步操作验收。 |
| [x] | UI-006 | P1 | 会议新增、纪要、结论和待办联动正常 | 不适用（本批次延期） | 2026-08-11 按用户指示缩减一期范围，生产多角色和真实业务样本验收延期；本地真实模型/页面与 `accept:meeting-persistence` 已通过。 |
| [x] | UI-007 | P1 | OA 项目申请、审批和阶段流转正常 | 不适用（本批次延期） | 2026-08-11 按用户指示缩减一期范围，生产完整角色矩阵验收延期；新写入不得在未验收范围开放，现有服务/API、本地只读页面和 12 项 MySQL 状态机验收保留。 |
| [x] | UI-008 | P1 | 风险新增、处置和项目联动正常 | 不适用（本批次延期） | 2026-08-11 按用户指示缩减一期范围，生产 TLS、多角色和真实项目数据验收延期；本地真实页面和 `accept:risk-persistence` 证据保留。 |
| [x] | UI-009 | P1 | 上会材料、投后工具和知识库入口正常 | 不适用（本批次延期） | 2026-08-11 按用户指示缩减一期范围；旧上会材料/投后工具继续退场，机构级上传不开放，生产大文件与权限矩阵验收延期；知识页本地浏览器证据保留。 |
| [ ] | UI-010 | P0 | 用户、组织、角色、字典、模板和审计页面正常 | 本地代码/API/真实 MySQL 专项通过，生产浏览器待验 | `/system` 已恢复七页签；用户新增/编辑/禁用/密码重置、组织树、角色权限、字典分组/字典项均走受审计 MySQL API。`accept:system-administration` 与 `accept:client-state-authority` 通过；生产域名多角色浏览器矩阵尚待验。 |
| [ ] | UI-011 | P0 | 当前页面视觉、布局、导航和主要交互没有明显回退 | 本地保留页面通过/生产全矩阵待验 | 1265×720/1280×720 实际覆盖登录、首页、项目列表/详情、线索、OA 两视图、AI、会议、风险、知识库、系统三页签、模型设置和能力管理；修复主布局最小宽度、知识库 Grid 最小内容宽度、筛选换行、头部挤压、北京时间及 nullable 表单告警。最终会议、风险、知识库均为 0 横向溢出、浏览器 warning/error 为 0。生产多角色、多分辨率和真实数据仍待现场验收，故不整项勾选。 |
| [x] | UI-012 | P1 | 旧 API 兼容路由在迁移期返回正确数据 | 通过 | 基于迁移前提交自动分类 88 条路由：80 条正式路由保留，8 条固定密钥绕过、外部 localhost 回调或伪成功实现按批准矩阵退场，不将安全缺陷当作兼容性。真实 3100 单服务验证旧登录、项目、会议、待办、风险、线索、管理和 AI 列表的 18 类关键响应形状；未认证保留路由为统一 401，8 条退场路由在登录后仍为带 `requestId` 的 JSON 404。验收数据零残留，0600 机器证据已生成并接入发布门禁。 |
| [x] | UI-013 | P1 | 404、401、403、409、500 等错误有一致用户提示 | 通过 | `accept:error-contract` 覆盖 400/401/403/404/409/500，服务端统一返回 `code/message/details/requestId`，手工路由错误由响应中间件补齐结构，500 固定为通用中文且不泄露内部标记。前端 `ApiError` 保留安全基础消息用于状态判断，用户可见消息附带请求编号；编号只有在响应头与正文完全一致且符合 8~64 位白名单格式时才采用，伪造和脚本型编号均被拒绝。 |
| [x] | UI-014 | P0 | 导航、路由和页面中不存在从 JW 引入的 Aipin 入口、组件或业务术语 | 通过 | `check:single-service` 扫描全部活动 `src`/`server/src` 文件，Aipin 只允许出现在离线迁移/审计白名单脚本；严格排除审计同时检查运行时代码、配置和环境变量名，导航、路由与页面命中为 0。 |
| [ ] | UI-015 | P0 | 清空 localStorage/sessionStorage 后，所有保留页面仍可从 MySQL/目标文件存储恢复真实业务状态 | 本地保留页与契约通过/生产待验 | 新浏览器会话登录并刷新首页、项目、OA、线索、会议、风险、知识库和系统页均从 MySQL/私有文件存储恢复且控制台无错误；24 项权威源门禁覆盖会议结构化待办、线索转换、风险 Hydration/PATCH、知识上传与可收缩布局、退役本地状态和项目编辑。AI 完整矩阵及生产 TLS 多角色仍待验，故不整项勾选。 |
| [x] | UI-016 | P0 | OA 申请/节点/意见/退回/撤回/重提/日志完整持久化且可审计 | 通过 | `accept:oa-workflow` 12 项覆盖申请、稳定节点/审批人、意见、退回、拒绝、撤回、重提、并发终审、阶段/日志原子提交、刷新恢复和历史保护。 |
| [x] | UI-017 | P1 | 投后更新、通知已读、旧材料、组织字典和模板按批准方案可用或明确只读/退场 | 通过（范围更新） | 旧材料路由转统一 AI 任务，投后路由转项目页，通知入口隐藏；组织/角色/字典已正式迁移为受审计 MySQL 管理功能；模板、账号与审计均读取真实服务端数据。 |
| [x] | UI-018 | P0 | 页面数据不因 Zustand Mock 初始值掩盖后端缺失、空库或权限错误 | 通过 | Zustand 无演示导入和 persist，权威读取失败置空。24 项门禁另禁止首页固定演示指标、会议/风险/知识库伪造内容、模拟 BP 解析、项目导入/导出假成功、固定成功任务 API、新项目虚构兜底字段及旧材料/投后本地状态，并校验模型健康与运行时使用同一鉴权网关、会议明确负责人/期限不被覆盖、知识库表格可收缩；真实浏览器显示 MySQL 数据。 |

## 8. AI 助手与 JW Runtime

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | CHAT-001 | P0 | 可以新建全局会话并完成多轮流式对话 | 通过（Runtime/持久化） | `accept:jw-multiturn-live` 使用随机合成身份创建全局会话并向真实模型发送两轮随机挑战，第二轮准确回忆首轮值；同一 SDK Session 延续，MySQL 保存 2 用户消息、2 助手消息、4 Part、连续序号及模型/Turn/耗时/成本状态。Socket partial 的浏览器视觉表现仍由 `GATE-M3-03` 单独验收，不反向否定 Runtime 多轮契约。 |
| [x] | CHAT-002 | P0 | 可以新建项目会话并绑定正确项目上下文 | 通过（真实模型/工具/持久化） | `accept:jw-multiturn-live` 创建随机合成项目及稳定成员绑定，提示词不包含项目名称，真实模型必须调用 `get_project_summary` 后准确回复随机项目名。工具 Schema 不接受 `projectId`，MySQL 会话保存服务端绑定项目 ID；工具输入无项目 ID，工具输出、完成状态及关联 Part 均持久化。删除后项目会话、项目、成员、身份、审计和工作区残留为 0。 |
| [x] | CHAT-003 | P0 | 会话列表、新建、切换、改名和删除正常 | 通过（真实 API/MySQL/前端契约） | `accept:jw-multiturn-live` 在三个真实 JW 会话完成后用第二个认证会话重新读取列表，依次读取全局、项目、停止后续聊会话再切回全局，各会话历史无串线；改名同步更新 `chat_conversations` 与 `agent_conversations` 且 ID 不变，删除仅移除指定会话。前端新增手动重命名控件；删除改为等待 API 成功后才更新列表，失败明确提示，不再静默乐观删除。 |
| [x] | CHAT-004 | P0 | 刷新页面后历史消息和当前会话正确恢复 | 通过（新认证会话恢复） | 验收不复用原 Cookie/内存状态，而是建立新的认证会话，从 MySQL 列表恢复三条会话并通过各自 Agent ID 读取完整历史；全局随机挑战、项目工具结果及停止后续聊分别命中。前端刷新时先取 `/conversations`，显式项目优先，否则恢复 `LS_LAST_CONV` 存放的稳定 Agent ID，不存在时回退最新一条；消息正文始终从 JW/MySQL Snapshot 加载。浏览器视觉由 `GATE-M3-03` 独立跟踪。 |
| [x] | CHAT-005 | P0 | 服务重启后会话和消息从 MySQL 恢复 | 本地通过 | 启动恢复遗留 streaming/running 状态；`accept:jw-restart` 验证 MySQL 消息/Part、顺序、元数据、工具中断结果和幂等快照恢复。 |
| [x] | CHAT-006 | P0 | 流式回答过程中停止按钮可以终止当前生成 | 通过（真实流式/中断/续聊） | `accept:jw-multiturn-live` 要求真实模型生成超长回答，确认出现流式 Partial 后调用停止接口；接口现在结束输入队列、interrupt/close SDK Query 并等待输出循环退出后才返回 idle。返回后连续 3 秒消息数不再增长、无 streaming/running 状态，停止时间与原因落 MySQL；随后同一会话成功完成新的随机挑战。运行中工具若被停止会统一标为 interrupted/output-error。 |
| [x] | CHAT-007 | P1 | 网络中断重连后不会重复或丢失已确认消息 | 通过 | `accept:socket` 在唯一 3100 服务中使用 12 个并发连接和 6 个会话完成 3 轮全断开/新建连接/重新订阅；每轮断线窗口向同一 Agent 会话持久化 1 条唯一确认消息，重连返回的 MySQL Snapshot 对累计消息 ID 逐项核对，3 条确认消息丢失 0、重复 0，数据库仍为 3/3。验收后会话、项目、认证会话和随机保留域身份精确清零。 |
| [x] | CHAT-008 | P1 | 文本、Markdown、表格、代码块和链接正确显示 | 通过（SSR 门禁/真实浏览器） | `accept:jw-message-rendering` 直接渲染正式 `Markdown/MessagePart` 组件，验证普通文本、标题、加粗、GFM 表格、带语言代码块和安全外链；外链固定新窗口与 `noreferrer`，`javascript:` href 被清空。真实 3100 单服务浏览器读取 MySQL 合成会话，逐项出现且页面无横向溢出、控制台 0 错误；夹具身份、会话、项目和密钥文件均清理。 |
| [x] | CHAT-009 | P1 | 思考内容按产品规则展示或隐藏，不混入最终答案 | 通过（折叠/结构隔离） | 思考 Part 默认渲染为未打开的独立 `<details>`，只显示“思考过程”摘要；浏览器点击后可展开，再次点击可折叠。SSR 和真实 DOM 均确认内部思考位于 details 内，最终 Markdown 标题位于 details 外且顺序独立，Shell/AskUserQuestion 内部工具 Part 不作为普通消息暴露。0600 报告不保存渲染正文。 |
| [x] | CHAT-010 | P0 | 工具调用、工具结果、进度和错误正确关联 | 通过 | 真实项目会话已证明 `get_project_summary` 调用、输入、输出和完成状态落 MySQL；新增 SDK 原生形状协议验收覆盖 input-available、output-available、output-error、自然失败文本、未知结果拒绝、重复事件幂等及终态不回退。修复持久化动态 Part 缺失 `type` 导致刷新后无法识别的问题；唯一 3100 服务真实浏览器确认运行/完成/失败卡片、输入输出/错误展开及刷新恢复，夹具零残留。 |
| [x] | CHAT-011 | P1 | `AskUserQuestion` 可以回答、取消并继续执行 | 通过 | 真实网关复核发现顶层 `permissionMode=dontAsk` 会在宿主回调前自动拒绝显式 ask 工具，已改为 `default`；模型可见内建工具仍只有 `AskUserQuestion`，未知工具继续服务端拒绝。`accept:jw-interaction-live` 证明外部 `gpt-5.6-sol` 主动提问、MySQL Snapshot 暂停、授权回答恢复同一 SDK Turn并继续生成。唯一 3100 服务真实浏览器完成单选、提交、交互卡清除、发送恢复和刷新持久化；本地回答/取消/越权/超时/重启门禁及夹具零残留继续通过。 |
| [x] | CHAT-012 | P1 | 模型切换后新一轮使用目标模型并保留会话 | 不适用（本批次延期） | 2026-08-11 按用户指示延期多模型切换生产验收；本批次保持单一授权模型/默认路由，不向用户开放未验收切换能力，`accept:jw-model-switch` 的本地技术证据保留。 |
| [x] | CHAT-013 | P1 | 上下文压缩、Token 和成本状态正确记录 | 通过 | `accept:jw-usage-compaction` 按 SDK 原生事件在真实 MySQL 验证活动模型、输入/输出/缓存/总 Token、成本、Turn、耗时，以及压缩中、成功、失败、状态清除、重复边界幂等和无内存 Session 的 Snapshot 恢复。当前版本 `accept:jw-interaction-live` 又从真实 `gpt-5.6-sol` 完整结果核对模型、Token/缓存 Token、成本、Turn、耗时和 Snapshot 压缩 idle 状态；AI 页面显示同一持久化数据，夹具零残留。 |
| [x] | CHAT-014 | P0 | AI 助手新会话不产生任何 Flue 请求 | 通过 | 活动客户端/服务端无 `/ai/api`、`FLUE_*` 或 3584 引用；会话创建、Socket 订阅和恢复走同一 3100/JW Runtime，`check:single-service` 固化为回归门禁。 |
| [x] | CHAT-015 | P0 | 用户不能通过修改 sessionId/projectId 访问他人数据 | 通过 | 会话订阅按所有者与项目访问权复核；跨用户 agentId 和跨项目会话创建验收均被拒绝。 |
| [ ] | CHAT-016 | P1 | 旧 Flue 会话可按策略查看，且不会恢复旧进程 | 迁移/归档机制通过/生产源待盘点 | 旧 Flue 只能通过显式源、checksum、稳定归属映射和预览/应用工具迁入 MySQL 或进入只读归档；活动构建、3584、代理和旧进程启动已硬禁用。当前两个批准候选无可展示业务会话，生产部署机是否存在其他 `flue.db` 尚未获批盘点，因此不能用空集证明历史可查看。 |
| [x] | CHAT-017 | P1 | Agent 错误返回用户可理解提示和可追踪错误 ID | 通过 | Agent 任务创建错误使用安全服务端消息并保留经双向校验的请求编号；AI Task 终态失败显示持久化 `errorId`，React AI 错误边界为渲染异常生成并显示独立错误编号。专项验收确认 500 内部原文零泄露、6 类前端错误都携带可信追踪编号，伪造编号接受数为 0。 |
| [x] | CHAT-018 | P0 | AI 会话创建、恢复和工具调用均不请求 Aipin API 或依赖 Aipin 配置 | 通过 | 活动代码 Aipin 零引用；唯一允许项是离线 Flue 迁移脚本中的拒绝规则，不进入构建/启动/会话链。 |
| [ ] | CHAT-019 | P0 | 迁移会话的消息顺序、可见文本、内容块、附件、工具调用和停止状态通过内容级校验 | 本地技术链通过/生产无正文证据 | Flue 隔离源两次应用覆盖可见 chat/agent 主会话和新生成子会话、4 条消息/8 个 Part、精确重复、正文/思考、附件摘要、工具输入输出及 interrupted，源/目标内容 checksum 相等且稳定；同时修复同毫秒顺序、漏建可见索引及 128→64 字符溢出。只读聚合审计确认当前目标 13/13 索引成对、消息/Part/状态/任务绑定零违规，但现有获授权源业务消息为 0，生产源盘点未批准，不能用合成内容替代生产逐条对账。 |
| [ ] | CHAT-020 | P0 | 合并 PostgreSQL、Flue 和白名单 JW 历史不会产生重复消息或错误恢复旧进程 | 本地机制通过/生产待验 | 跨源审计覆盖来源映射、外部消息唯一键、目标多来源归并计数和迁移旧会话 streaming/running 残留；当前均为 0 违规。dump 13 个会话消息为空，Flue 业务选中 0，JW 批准 0/拒绝 Aipin 2,168 条；因此当前没有可用于生产跨源重复验证的业务消息，正式盘点与应用前不勾选。 |
| [x] | CHAT-021 | P0 | 新会话只绑定稳定 MySQL 用户/项目 ID，不存在阶段 8 再改写所有者的临时归属 | 通过 | 新会话在单一事务内以稳定 MySQL user/project ID 同时写入 chat/agent 两张索引，项目访问在创建和 Runtime 恢复时重复校验；真实项目会话、第二认证 Session、改名、切换和删除期间绑定均未变化。全库新增 6 项会话不变量，确认两张索引同主键、同用户、同项目、同 scope、同 Agent ID，且项目会话具备创建者/负责人/稳定成员授权，违规均为 0。发现的 1 条 `legacy_postgres` 空会话原标为 project 但无 projectId、消息和任务，未猜测项目或改写所有者，而是经 preview/apply、双表事务和可逆问题台账规范为 global；幂等复跑候选为 0。 |
| [ ] | CHAT-022 | P0 | 项目对话未通过文件/RAG和专业任务组合门禁前不会被全量切到不完整新链路 | 机制通过/生产配置待验 | `JW_PROJECT_NEW_CONVERSATIONS_ENABLED` 可独立关闭项目新会话，同时全局新会话和已有 MySQL 会话不受影响；关闭时明确 503 且无旧 Runtime 回退。目标生产值和组合门禁尚未签字，不能整项通过。 |
| [x] | CHAT-023 | P0 | AgentSessionCore 的运行依赖、网络、文件、子进程和数据库访问均符合 allowlist | 本地通过 | `check:single-service` 固化 deny-by-default 工具边界、固定 MCP、网络域名、私密 argv 和统一子进程监督；systemd 沙箱仍待目标机取证。 |

## 9. 文件工作区与项目知识库

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | FILE-001 | P0 | PDF、DOCX、XLSX、PPTX、图片和文本可上传 | 通过 | `accept:project-files` 对 PDF、Word、Excel、PPT、图片、文本共 22 种扩展名完成原始 Base64/Data URL 解码、签名校验和私有存储回读；真实 `/api/projects/files/upload` 返回 201。 |
| [x] | FILE-002 | P1 | 不支持类型、超限文件和伪造 MIME 被拒绝 | 通过 | 两个写入入口统一校验 100 MiB 上限、扩展名/MIME/内容签名；OOXML 另限 5,000 条目和 250 MiB 解压容量。验收覆盖不支持扩展名、伪造 MIME/PDF、非法 Base64、二进制冒充文本与压缩包膨胀。 |
| [x] | FILE-003 | P0 | 文件按用户、会话和项目正确隔离 | 通过 | `accept:resources` 以三用户/双项目验证文件列表、API 上传、原字节下载和跨用户 403；workspace 按会话、generated 按用户隔离。 |
| [x] | FILE-004 | P0 | `..`、绝对路径和符号链接逃逸测试被拒绝 | 通过 | 各文件根先做词法边界，再校验 realpath 并拒绝符号链接；`accept:resources` 覆盖项目文件、workspace、generated 的穿越/链接夹具。 |
| [x] | FILE-005 | P1 | 文件预览、下载、删除和版本记录正常 | 通过 | `accept:resources` 真实 HTTP 验证当前版本下载、安全沙箱预览、v1/v2 版本列表与独立原件下载、跨用户拒绝，以及删除后数据库版本和两代磁盘字节均无残留。 |
| [ ] | FILE-006 | P1 | SHA-256、重复检测和迁移核验正确 | 新文件通过/AI 历史产物未闭环 | 新上传精确记录 `byte_size`/SHA-256，项目锁内拒绝重复内容；`accept:project-file-integrity` 8 项覆盖项目/用户配额、并发串行化和不可变版本。18 条历史项目记录中已强匹配恢复 1 条，其余 17 条经用户明确批准为永久缺失：精确 ID 清单与 SHA-256 锁定，17/17 追加 `business_decision` 台账，保留元数据和补传入口，不制造原件。AI 产物恢复器另要求 dump/旧路径身份、精确字节、格式签名和唯一内容 SHA；当前 7 条仍全部 `not_found`，因此本项尚不能整项勾选。 |
| [x] | FILE-007 | P0 | 项目文件解析后可进入知识库检索 | 通过 | 真实 API 上传文本后后台 `ingestFile` 解析成功，生成项目知识分块；`search_project_docs` 隔离验收能检索该类分块并返回引用。文件分块、知识分块和解析成功状态已收敛为同一事务，失败不会留下“解析成功但不可检索”的半写入；同源块由数据库唯一约束保证幂等。 |
| [x] | FILE-008 | P0 | `search_project_docs` 返回内容、来源和定位 | 通过 | `accept:project-knowledge-tools` 13 项验证项目/机构/线索作用域；结果包含 citationId、内容、sourceId/type/name、真实 chunkIndex/locator、refId、绑定 projectId 和服务端权限结果。 |
| [x] | FILE-009 | P0 | Agent 无法检索无权项目资料 | 通过 | `accept:resources` 使用其他项目用户调用项目知识问答固定返回 403，项目资料工具也由服务端稳定 userId/projectId 访问门禁约束。 |
| [x] | FILE-010 | P1 | 临时文件按策略清理，不删除正式文件和产物 | 通过 | 项目文件原子写入异常会立即删除临时文件；主服务启动时按默认 24 小时 TTL 扫描崩溃残留，仅删除严格匹配 `.UUID.tmp` 的普通文件。`accept:project-file-temp-cleanup` 在隔离根删除 2 个超期服务临时文件，同时保留正式文件、新鲜临时文件、普通 `.tmp`、符号链接及其外部目标；第二次执行删除 0。 |
| [x] | FILE-011 | P1 | 授权下载或签名 URL 过期后不可继续访问 | 通过 | 所有项目文件、AI 产物、workspace 和 generated 下载共用服务端 HttpOnly 会话鉴权，不发放独立长期链接。真实 HTTP 验收先用有效会话下载项目原字节成功，再将该会话 MySQL `expires_at` 置为过去；同一 Cookie 和下载 URL 立即返回 401。跨用户、匿名、撤销/过期会话均无法绕过统一中间件。 |
| [ ] | FILE-012 | P0 | 项目原文件、旧 `/generated`、AI 产物、自定义模板、Agent workspace、Skill/模板和 Radar 文件 manifest 全量对账 | 本地工具通过/AI 产物未闭环 | 最新 manifest 覆盖 7 个根、其中 6 个可见，在线 240 个文件/113,548,136 bytes：1 个数据库精确登记、239 个系统资产、未决归属为 0；剩余 7 个阻断全部是位于旧机器根外的 AI 产物登记路径。除前述 62 个验收残留外，另将 1 个同时不在 Chat/Agent 两套 MySQL 会话权威中的 UUID workspace 目录（4 个文件）原字节移入 0600 可恢复隔离，并修复正常会话删除后的精确 workspace 清理。17 个项目原件已按精确 ID 锁定的用户批准结论落入 MySQL 迁移台账，不再作为源资产阻断；扫描仍因 7 个 AI 产物为 `technicalReady=false`、`approved=false`。 |
| [x] | FILE-013 | P0 | 旧 `/generated` 公开静态链接已关闭、迁移到受权接口或获得书面例外 | 通过 | Express 与 Nginx 均删除公开静态目录；旧 URL 固定 404，新文件写入用户目录并经 `/api/generated/:file` 会话鉴权。历史文件归属/manifest 仍由 FILE-012/014 跟踪。 |
| [ ] | FILE-014 | P0 | 无归属、重复、零字节、缺失和哈希冲突文件进入隔离报告，不自动猜测归属 | 部分 | 工具不跟随符号链接、不猜测归属。已证明的验收残留及无两套 MySQL 会话权威的孤儿 workspace 均只移入私有可恢复隔离，原字节和内容哈希保留；在线未决归属现为 0。固定 Radar 候选 JSONL 可合法为空。已复制恢复 1 条项目原件且源件保留；其余 17 条项目原件已获精确范围批准并保持元数据/补传能力，机器对账缺口为 0。AI 产物发现器仍绑定 dump/旧路径、任务/用户/项目、精确字节及 OOXML CRC/格式签名；最新同一次扩展扫描已覆盖 Desktop/Documents/Downloads 三个标准用户资料根，7/7 仍 `not_found`、0 可恢复、0 写库。它们尚未批准永久归档，因此整项仍不勾选。 |
| [x] | FILE-015 | P1 | 文件删除不会误删共享模板、历史产物、其他项目或回滚所需源文件 | 通过 | `accept:project-file-deletion-isolation` 在真实 MySQL 注入一条指向其他项目原件的污染历史版本；删除目标文件后，目标当前/历史字节和版本元数据已清理，而其他项目原件、同项目历史 AI 产物、共享模板及项目文件根外的回滚源均逐字节不变。删除实现按 projectId/fileId 绑定路径，并拒绝存储根、符号链接、真实路径越界及 inode 替换。0600 证据不记录路径、正文或连接身份。 |

## 10. 线索池 Claude Agent SDK Pipeline

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | LEAD-001 | P0 | 原始候选先写入不可变原始事件表 | 通过 | Radar 在确定性过滤、模型复核和正式线索提交前写 `lead_pipeline_raw_events`；36氪储备池在同一 MySQL 事务中先记录原始事件，再创建/关联正式线索并转 ready。9,903 条历史有效储备记录已完成回填。 |
| [x] | LEAD-002 | P0 | 同一来源重复同步不重复创建事件和正式线索 | 通过 | `accept:lead-pipeline-events` 验证并发及重复事件幂等；储备池服务测试验证重复执行保持同一事件/正式线索；`accept:radar-job-isolation` 验证跨实例并发和再次同步只产生一条正式线索。回填再次执行为 `rawCreated=0/mappingRecovered=0`。 |
| [x] | LEAD-003 | P0 | 公司、项目、团队、实验室和论文主体识别符合金标 | 通过 | SHA-256 锁定的 `lead-subject-gold-v1` 覆盖五类主体；真实 `gpt-5.6-sol` 连续两次均 5/5 正样本正确接受并匹配类型/规范名称，正样本召回率和自动接受精度均为 100%。 |
| [x] | LEAD-004 | P0 | 新闻标题、谓语片段和通用名词不会被当作有效主体 | 通过 | 金标中的融资谓语片段、通用技术团队/项目成果、36氪新闻栏目和个人荣誉资讯均被 reject；连同多主体歧义共 5 个负样本误收率为 0。 |
| [x] | LEAD-005 | P1 | 多主体和模糊主体进入人工复核 | 通过 | 主体 Agent 的 `review` 结论会建立 MySQL Review；人工复核页面显示不可变原始材料与 Agent 原结论，按稳定用户分配处理。多主体夹具、权限和处理闭环均通过。 |
| [x] | LEAD-006 | P0 | 疑似重复主体不会未经确认直接新建或错误合并 | 通过 | 人工复核、Radar 自动摄入和公开情报三条正式写入路径均已失败关闭：人工复核命中多条同名线索时必须显式选择，且目标必须同主体、非“已转专属项目”；Radar 命中多条正式线索时不取第一条、不创建第三条，而是在同一宿主事务中写 `entity_resolution/review` 决策、待办和未绑定的 Pipeline review 状态；公开情报自动多匹配、显式异名目标和终态目标均整事务回滚。遗留的按同名自动删除和独立模型改名脚本也已固定退出 78，发布验收证明零触库且不输出业务标识。14 项人工复核、9 项公开情报、11 项 Radar/Pipeline 事务和 5 项旧入口安全验收通过。此项证明新增/合并入口安全，不等于已处置存量 42 组/107 条重复记录；`DB-015/MIG-0125/ISSUE-006` 继续保持未完成。 |
| [x] | LEAD-007 | P0 | 公开研究结果包含来源、定位、事实、冲突和缺口 | 通过 | Research 严格 Schema 强制 facts/conflicts/gaps，事实包含 sourceId、URL、连续引文、可靠性和核验状态；真实金标分别验证高可靠事实与双来源冲突，宿主进一步阻止引文挂错 sourceId。 |
| [x] | LEAD-008 | P0 | 无证据接受、弱来源关键结论和冲突事实被门禁阻止 | 通过 | Schema 拒绝无证据 accept；真实初筛金标确认 4 亿/8 亿估值冲突进入 review，匿名论坛的亿元营收 low/unverified 结论也进入 review。弱来源政策已写入 `lead-screening-agent-v2`，不增加 Turn。 |
| [x] | LEAD-009 | P0 | `accept/reject/review` 均保存理由、证据和置信度 | 通过 | `accept:lead-pipeline-audit` 逐项核对 Agent accept/reject/review 的 SDK Run、理由、规范证据和置信度；`accept:lead-manual-review` 继续验证人工接受/拒绝追加子决策、核验引文并保留 Agent 历史。 |
| [x] | LEAD-010 | P0 | 公司/项目正确进入七维评分 | 通过 | `lead-scoring-agent` 返回严格七维结构，宿主校验每个标准维度及全部子项、覆盖标准名称/上限并重算总分与结论；真实 `gpt-5.6-sol` 网关调用以零工具、2 Turn 完成。 |
| [x] | LEAD-011 | P0 | 论文正确进入论文专属评分，不强制融资或公司主体 | 通过 | 论文路由使用五维专属 Schema，五个维度不要求子项，也不读取融资或公司主体字段；隔离验收及真实 `gpt-5.6-sol` 调用均通过。 |
| [x] | LEAD-012 | P1 | 已入池线索只做有证据的增量补全，不覆盖人工字段 | 通过 | 迁移 `0039_add_lead_field_provenance` 为全部 892 条存量线索建立字段来源与优先级台账：人工/人工复核最高，旧源数据默认保护，公开情报、确定性回填、Radar、AI 依次降级。评分、公开情报、Radar 和地区回填统一经字段策略；团队、摘要、主体和地区等标量只允许同级或更高证据刷新，融资、来源、亮点和风险只做去重追加，地区/来源/置信度作为同一证据组原子保护。真实 MySQL 验收确认 AI 重评分保留人工标量、只追加数组、评分字段正常刷新、来源轨迹完整且夹具残留 0；公开情报、评分审计和在线编排回归同时通过。 |
| [x] | LEAD-013 | P0 | Agent 输出不符合 Schema 时不写正式线索 | 通过 | Profile 验收拒绝幻觉引文、无证据 accept、未知补全字段和覆盖式补丁；在线 MySQL 验收额外让 Radar Screening 返回无证据 `accept`，结果 Run/Decision 标记 failed、Pipeline 无 `lead_id`，正式线索数不变。 |
| [x] | LEAD-014 | P0 | Agent 没有通用 MySQL 写权限和任意数据库 Shell 权限 | 通过 | 主体、研究、初筛、评分和补全五类 Agent 子进程环境只传运行和模型网关白名单，不含任何 `DB_*`；tools/skills/allowedTools 均为空，Shell、文件、网络、Task 和 Skill 显式拒绝，宿主才拥有经校验的 MySQL 写入能力。主体、评分和工作流隔离验收均通过。 |
| [x] | LEAD-015 | P0 | 正式线索写入由宿主服务校验并使用 MySQL 事务 | 通过 | 36氪摄入、人工复核接受和 Radar 自动接受均由宿主校验并通过 MySQL 事务提交。Radar 先取得跨实例 advisory locks，再在同一连接事务中创建/合并正式线索、写审计并绑定 Pipeline `ready + lead_id`；故意让状态转换失败的验收确认线索及审计整事务回滚。 |
| [x] | LEAD-016 | P0 | 任务状态、尝试、租约、重试和错误均持久化 | 通过 | `lead_score_jobs` 保存 status、execution_attempts、next_attempt_at、lease_owner/expiry、last_error、完成/死信时间和人工重试次数；`accept:lead-score-lifecycle` 逐阶段读取真实 MySQL 记录验证。 |
| [x] | LEAD-017 | P0 | 服务重启后排队、运行和重试任务按规则恢复 | 通过 | 验收构造 queued、到期 retrying、租约过期 running 和活租约 running：前三类在新 Worker 身份下恢复并完成，活租约不被窃取，过期错误与尝试次数保留。 |
| [x] | LEAD-018 | P0 | 两个 Worker 竞争同一任务时只有一个获得有效租约 | 通过 | 两个独立 Worker owner 并发执行真实 `FOR UPDATE SKIP LOCKED` 领取，只有一个返回任务；MySQL 最终只有一个 running 租约且 execution_attempts=1。 |
| [x] | LEAD-019 | P1 | 超过重试上限的任务进入死信并可人工重试 | 通过 | 新增迁移 `0018_add_lead_score_dead_letter`；终态失败写 `dead_letter`、dead_lettered_at 和安全错误，普通调度不能复活。显式 `/leads/:id/score/retry` 才能归零本轮尝试、累计 manual_retry_count、同步前端快照并在同一事务写审计，重试后可完成；11 项验收通过。 |
| [x] | LEAD-020 | P1 | 人工复核结果新增决策记录，不删除 Agent 历史 | 通过 | React 工作台通过受权 API 新增 `manual_review` 子决策，父决策保持不变；同一幂等键重试不重复线索、决策或审计，不同结论不能覆盖已解决 Review，自外键 RESTRICT 阻止删除 Agent 历史。 |
| [x] | LEAD-021 | P1 | 模型、提示词、Skill、工具、Schema 和评分版本可追溯 | 通过 | 五类 Profile 均登记不可变 Prompt 哈希及 prompt/profile/skill/toolset/schema 版本；公开情报和 Radar 在线 Run 保存模型、Token、成本、耗时、零 SDK 工具次数、受控宿主工具次数、attempt 和脱敏失败；版本表当前 10 条。5 项在线 MySQL 验收覆盖 Radar accept/review/reject/failed 与失败后新 attempt 重试，非 accept 不创建正式线索。 |
| [x] | LEAD-022 | P0 | 新线索流程不调用直接 LLM HTTP 或 Flue Workflow | 通过 | 主体、研究、初筛、项目/论文评分和补全均使用 Claude Agent SDK；公开情报改为主服务内确定性采集，活动线索代码无 Flue Runtime，最新 53 项静态门禁禁止主体/评分路由回退到直接模型 HTTP，并禁止情报路由使用 Flue。 |
| [x] | LEAD-023 | P1 | Token、工具次数、处理时长和成本不超过门禁 | 不适用（本批次延期） | 2026-08-11 按用户指示延期精确输入 Token 计量，待网关提供可靠字段后复验；本批次继续强制跨 Profile 并发、分钟速率、日预算、连续失败熔断、2 Turn 和成本/时延限制。 |
| [x] | LEAD-024 | P0 | 金标主体准确率、误收率、误拒率和评分偏差达到发布标准 | 通过 | 主体金标 10/10：总体准确率、正样本召回、自动接受精度均 100%，误收/误拒均 0；项目七维与论文五维评分金标 4/4 命中预设区间，2/2 高低质量排序通过，最终复跑平均绝对中点偏差 8.375 分。两套金标 SHA、模型及 Profile/Schema/Toolset 可追溯并接入部署。 |
| [x] | LEAD-025 | P0 | 线索 Pipeline 不导入或调用 Aipin Store、项目服务、处理队列、路由和状态机 | 通过 | 最新 73 项静态门禁扫描 255 个活动文件；严格排除审计扫描 159 个运行时/配置文件、62 张表和 546 个文本/JSON 列，均无 Aipin 引用或目标值。 |
| [ ] | LEAD-026 | P0 | Radar JSONL、微信群聊原文、采集状态、公众号 Excel 和 `lead_reserve` 未入池记录已迁移或受控归档 | 部分通过 | `accept:radar-lead-source-reconciliation` 确认本机 5 份适配器资产可解析并具备内容哈希，10,000 条储备记录全部保留，9,903 条详情已事件化、9,247 条有详情记录仍未入池；但本机候选 JSONL 与 MySQL Radar 候选均为空，生产 JSONL/群聊原文/公众号 Excel 的清单、归档恢复和负责人批准尚未取得，`productionAssetReady=false`。 |
| [x] | LEAD-027 | P0 | Radar 来源键、同步游标、36氪 seq/imported 进度和 cron/timer 状态无重复或遗漏 | 通过（目标 MySQL） | 新门禁验证 Radar 来源键 SHA-256、候选稳定游标、来源注册唯一性和 1 份同步状态；`lead_reserve` 的 10,000 条分区闭合、`seq` 无重复、656 条 imported 映射无孤儿/重复，4 个旧 cron/timer 任务均由 MySQL Runtime Job 接管。406 条历史 imported 时间源字段为空并保持未知，不伪造回填。 |
| [x] | LEAD-028 | P0 | 线索页面读源、Pipeline 正式写源和人工复核写源处于同一权威数据源 | 通过 | 公共线索列表/详情、原始事件/状态/决策、Review 列表/处理均使用同一 MySQL 配置和前缀；人工接受事务同时绑定正式 `lead_id`，活动业务路径无 PostgreSQL 访问。 |
| [x] | LEAD-029 | P0 | 切换期间不存在新 Pipeline 已写 MySQL 但页面仍只读 PostgreSQL的不可见线索 | 通过 | 线索页面直接查询 MySQL `leads`，人工接受提交后显式刷新同一 API；数据库边界静态门禁只允许三个离线迁移脚本读取 PostgreSQL。 |
| [x] | LEAD-030 | P0 | Radar Sync 和 36氪摄入由 MySQL 持久化 Job 调度，不依赖脚本 cron/timer 或 localhost HTTP 自调用 | 通过 | `radar-collect-sync` 与 `lead-reserve-daily-intake` 均由 `runtime_jobs` 租约调度进程内领域服务；旧 cron/timer、`sync_radar.mjs`、`daily_intake.mjs` 和 localhost 自调用已退出活动链路。 |
| [x] | LEAD-031 | P0 | Radar Python 采集器可单次执行、超时、重试和取消，崩溃不会影响主 API/Socket | 通过 | `runRadarJob` 只执行单次 `project-discovery/job.py` 动作并统一进入子进程监督器；`accept:radar-job-isolation` 实际触发超时和退出码 17 崩溃，均释放进程组且主进程 MySQL/真实 Radar 健康随后正常。Runtime Job 使用 AbortSignal、超时、MySQL 租约和失败重试；通用监督验收另覆盖取消、停机排空及停机后拒绝新任务。 |
| [x] | LEAD-032 | P0 | Radar 独立服务退场后候选、来源、原文、游标、运行记录和失败状态均可从 MySQL 恢复 | 通过（在线事实源） | Radar 候选投影、不可变原始事件、3 份采集状态、39 个来源和同步状态均以 MySQL 为权威；4 个 Runtime Job 及运行/失败状态由统一调度表保存。适配器执行前从 MySQL 物化工作文件、执行后回写，投影可从原始事件重建；生产历史原文件长期归档仍由 LEAD-026 单独阻断。 |

## 11. 专业 AI 任务与产物

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | TASK-001 | P0 | 合规说明任务可创建、执行、恢复和下载 | 通过 | 真实 Cookie/CSRF API 验收 19 项通过；正式 DOCX、来源、Skill 身份、鉴权下载及交付物中心链路通过。 |
| [x] | TASK-002 | P0 | 投资建议书任务可创建、执行、恢复和下载 | 通过 | 与自定义模板组合的真实 API 验收通过；正式 DOCX、来源、Skill 身份、鉴权下载及故障后关联重试均通过。 |
| [ ] | TASK-003 | P0 | 投资建议 PPT 任务可创建、执行、恢复和下载 | 本轮按要求跳过 | 主任务曾成功生成 14 页图片高保真版、PDF、四层可编辑稿和元素级可编辑 PPTX，并通过逐页文字、渲染、水印、结构与自动 Reviewer；图片高保真版 Skill 身份元数据缺口已由代码和定向回归补齐。2026-08-11 独立复验实际覆盖创建、14 页生成、文字清理及主服务中断后的 MySQL 租约/检查点恢复，但按用户要求停止 PPT 生成测试，任务及隔离数据已清零；未完成修复后的正式产物/下载全链路，故不得勾选。 |
| [x] | TASK-004 | P0 | 尽职调查报告任务可创建、执行、恢复和下载 | 通过 | 真实 API 19 项通过；11 页商业专项 DOCX 的字段、内容、文风、逐页视觉、7 条来源、Skill 身份和鉴权下载均通过。 |
| [x] | TASK-005 | P0 | 项目 Q&A 任务可创建、执行、恢复和查看 | 通过 | 真实 API 18 项通过；兼容单题接口与正式项目 Q&A DOCX、来源、Skill 身份、恢复与鉴权链路通过。 |
| [x] | TASK-006 | P0 | 自定义模板任务可上传、分析、执行和下载 | 通过 | 真实 API 完成 PPTX 上传、4 页结构分析、任务执行、PPTX/PNG 产物、来源、Skill 身份和鉴权下载。 |
| [x] | TASK-007 | P0 | 重复提交使用幂等键，不生成重复任务 | 通过 | `ai_tasks(user_id,idempotency_key)` 唯一约束和规范请求哈希同时生效；相同请求返回原任务，同键异参在预查与并发 `ER_DUP_ENTRY` 竞争路径都返回 `IDEMPOTENCY_CONFLICT`。生命周期验收验证并发只有一个胜者和一行任务。 |
| [x] | TASK-008 | P1 | 取消任务后停止后续阶段并保留审计 | 通过 | pending 任务原子转 cancelled，running 任务进入“正在取消”；领取和成功提交均要求未请求取消，停机与重启优先收口 cancelled，取消操作写入 `audit_logs`。任务卡显示“取消中”并禁止重复提交。 |
| [x] | TASK-009 | P1 | 可重试错误可以重试，不可重试错误明确失败 | 通过 | 迁移 `0017` 为任务增加 `error_code`/`retryable`；超时、限流等临时错误允许创建关联原任务和进度断点的新任务，模板/引用/质量/参数类永久错误返回 `TASK_ERROR_NOT_RETRYABLE`，前端不展示继续生成按钮。 |
| [x] | TASK-010 | P0 | Worker 异常或服务重启后任务可恢复或明确失败 | 通过 | MySQL 租约过期的 running 任务原子恢复为 pending；已取消任务不复活；模板分析这种不能安全续跑的阶段明确失败并要求重新上传。并发租约单胜者、优雅停机释放和定向重启恢复均通过。 |
| [x] | TASK-011 | P1 | 任务卡阶段、进度、错误、重试和取消状态一致 | 通过 | API 直接返回持久化 stage/progress/errorCode/retryable/cancellationRequested；任务卡按同一状态显示取消中、不可重试说明、继续生成入口和进度断点，生命周期静态与数据库验收固化该契约。 |
| [x] | TASK-012 | P0 | 产物、来源、项目、用户和任务关联正确 | 通过 | `ai_artifacts` 通过外键关联任务/用户/项目，`ai_task_sources` 关联任务和产物；`accept:ai-task-persistence` 写入隔离任务、正式/失败/预览产物及来源后，从任务详情和产物中心按关联恢复，并全库检查三类引用无孤儿。失败质量产物及其来源不进入正式交付视图。 |
| [x] | TASK-013 | P0 | 用户不能下载其他用户或无权项目产物 | 通过 | 详情、产物中心、模板、预览和下载均按稳定 userId 过滤；下载还要求未归档、质量通过且真实路径位于专用 `AI_ARTIFACT_ROOT`，拒绝符号链接/越界/缺失文件。隔离验收确认外部用户全部不可见。 |
| [x] | TASK-014 | P0 | 创建任务前用户、项目、会话、模板和文件引用均存在于 MySQL，不产生孤儿任务 | 通过 | `accept:ai-task-references` 12 项验证服务层重新加载启用用户、复核项目权限和会话精确 projectId；五类内置模板必须匹配 `ai_task_templates` 的版本/Skill/格式/状态，自定义模板必须属于当前项目/会话；附件 UUID、归属、存在性和失败状态在 INSERT 前校验，全部失败路径任务数不变。 |
| [ ] | TASK-015 | P0 | 六类任务内容质量、来源完整性和事实约束不低于批准金样 | 本轮按要求延期 | 六类任务统一入口、任务/来源/产物/模板绑定和历史质量证据保留；用户明确要求本轮不测试 PPT、尽调、提案、Q&A 和合规性说明生成，且批准金样尚未冻结，故不触发生成或把历史结果外推为当前通过。 |
| [ ] | TASK-016 | P0 | 六类产物逐页渲染、字体、图片、表格和 Office/WPS 打开效果不低于批准基线 | 本轮按要求延期 | 渲染、字体、图片、表格和 Office/WPS 检查工具已存在，但批准基线和目标环境人工打开证据未完成；本轮遵循用户要求不生成或复测相关报告/PPT。 |
| [ ] | TASK-017 | P0 | 干净环境下原生依赖和字体可重建，重启/扩容节点生成结果一致 | 依赖契约就绪/干净节点待验 | 精确依赖锁、安装脚本、Linux 版本/OCR/CJK 字体契约和只读现场核验已接入部署；尚未在两个全新同构节点执行安装、重启/扩容与一致产物对比，本轮也不触发专业报告生成。 |

## 12. 模型设置入口

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | MODEL-001 | P0 | 左侧导航存在“模型设置”且路由为 `/system/ai/models` | 通过 | 管理员导航和 React 路由已接入，静态发布门禁锁定路径。 |
| [x] | MODEL-002 | P1 | 页面使用当前 React/Tailwind 中台风格 | 通过 | 复用 PageHeader/Card/DataTable/Badge 和现有 Tailwind 视觉体系，生产构建通过。 |
| [x] | MODEL-003 | P0 | 系统管理员和 AI 平台管理员可访问，普通用户被拒绝 | 通过 | 前端路由/导航按角色守卫；服务端管理 Router 统一经过 `requireAiPlatformAdmin`，服务层二次校验；12 项模型验收包含普通角色拒绝。 |
| [x] | MODEL-004 | P1 | Provider 可以新增、编辑、启停并查看版本 | 通过 | 页面/API 支持名称、协议、Base URL、超时、启停和密钥替换；更新使用 `expectedVersion` 乐观锁并展示版本。 |
| [x] | MODEL-005 | P1 | 模型可以新增、编辑、启停和选择默认项 | 通过 | 页面/API 支持 Provider、模型 ID、显示名、上下文、能力标签、授权角色、启停和默认项，更新使用乐观锁。 |
| [x] | MODEL-006 | P0 | API Key 只可替换，不返回完整明文 | 通过 | 列表剔除密文和指纹，仅返回末四位掩码；验收对首个/替换 Key 均扫描接口与审计输出无明文。 |
| [x] | MODEL-007 | P0 | MySQL 中凭据为加密值，解密密钥不在数据库 | 通过 | `credential_ciphertext` 保存 AES-256-GCM 密文并以 Provider ID 绑定 AAD；32 字节主密钥只来自部署环境，生产缺失即拒绝启动。 |
| [x] | MODEL-008 | P1 | 连接测试由服务端执行并返回可追踪结果 | 通过 | 服务端对 `/models` 或无计费不完整请求探测，持久化状态、延时、时间和 Trace ID，错误先脱敏；当前获授权网关的非计费兼容探针在 176ms 内通过。 |
| [x] | MODEL-009 | P1 | 可以配置 AI 助手、线索 Agent 和专业任务模型路由 | 通过 | MySQL 路由覆盖 interactive、主体、研究、初筛、补全、评分和文档七类 Profile，支持主备模型并已接入全部对应 Runtime；当前 1 个加密 Provider、模型 `gpt-5.6-sol` 和 7/7 路由已配置。 |
| [x] | MODEL-010 | P0 | AI 助手只展示已启用且用户有权使用的模型 | 通过 | `/available` 同时过滤 Provider/模型启用状态和角色白名单；新会话服务端复核并持久化 modelId，不能仅靠前端绕过。 |
| [x] | MODEL-011 | P1 | 停用模型后已有会话获得明确降级或替代提示 | 通过 | 已有会话所选模型停用/失权后按当前角色回退 interactive 主备路由，页面明确提示；主模型停用后切换备用模型的验收通过。 |
| [x] | MODEL-012 | P1 | 所有模型和凭据变更写入审计日志 | 通过 | Provider/模型/路由新增修改、密钥替换和连接测试均事务写入 `audit_logs`，验收确认日志无完整 Key。 |

## 13. 能力管理入口

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | CAP-001 | P0 | 左侧导航存在“能力管理”且路由为 `/system/ai/capabilities` | 通过 | 系统管理导航已增加“能力管理”，路由由 `AiPlatformAdminOnly` 保护。 |
| [x] | CAP-002 | P1 | 页面使用当前 React/Tailwind 中台风格 | 通过 | 复用 `PageHeader`、`Card`、`DataTable`、`Badge` 和现有 Tailwind 视觉体系。 |
| [x] | CAP-003 | P0 | 无管理权限用户不能访问页面和写接口 | 通过 | 前端路由、Router 中间件和 Service 三层限制系统/AI 平台管理员；隔离验收确认普通投资经理被拒绝。 |
| [x] | CAP-004 | P1 | Skills 可以安装、更新、启停并查看版本和来源 | 不适用（本批次延期） | 2026-08-11 按用户指示延期外部 Skill 动态安装；本批次仅允许代码批准的内置 Skill 同步/更新/启停，继续禁止页面任意安装。 |
| [x] | CAP-005 | P1 | Agents 可以配置模型、工具、预算、超时和适用范围 | 通过 | 能力页提供结构化 Agent 策略表单：模型只能选择七类已登记路由，工具只能从该 Profile 的代码批准清单中收窄，轮数/预算/超时由服务端校验并与环境安全上限取更严格值；角色及全局/部门/项目/会话作用域继续叠加校验。互动助手和主体、研究、初筛、补全、评分、文档 Runtime 均读取策略；同步内置目录不会覆盖管理员配置。13 项能力验收覆盖普通用户拒绝、非法工具拒绝、乐观锁、同步保留和 Runtime 解析。 |
| [x] | CAP-006 | P1 | MCP 可以配置、测试连接、查看工具和启停 | 不适用（本批次延期） | 2026-08-11 按用户指示延期任意远程 MCP 配置/执行；本批次仅保留固定宿主 `investment` MCP 及 7 个代码批准工具。 |
| [x] | CAP-007 | P1 | 插件可以查看安装状态、版本、依赖和启停状态 | 不适用（本批次延期） | 2026-08-11 按用户指示延期插件安装/升级/启停生命周期；本批次保持动态安装关闭、正式库安装/启用数为 0，未批准 Plugin 不得进入用户能力。 |
| [x] | CAP-008 | P0 | 全局、部门、项目和会话作用域隔离正确 | 通过 | 授权表覆盖全局/部门/项目，会话选择独立存储且只能收窄有效授权；隔离验收覆盖作用域并集和跨部门拒绝。 |
| [x] | CAP-009 | P0 | 项目能力只能被有权项目成员使用 | 通过 | 列表、选择和 Runtime 均调用稳定项目成员访问边界；外部用户探测项目或读取会话均返回拒绝。 |
| [x] | CAP-010 | P0 | AI 助手能力按钮只显示当前授权能力 | 通过 | 输入框“能力”按钮从服务端读取启用、角色、部门/项目有效项；保存时服务端重新计算授权，不能自行提权。 |
| [x] | CAP-011 | P1 | 停用能力后新会话不再获得该能力 | 通过 | Runtime 新建执行会话时重新解析启用授权；MCP、Agent 和文档任务 Skill 均由所选代码白名单二次校验。 |
| [x] | CAP-012 | P1 | 能力安装、更新、绑定和启停均有审计记录 | 通过 | 管理同步、配置修改、授权新增/启停和服务端测试写入“能力管理”审计，验收确认无密钥字段。 |

## 14. IM 机器人和线索推送

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | IM-001 | P0 | 左侧导航存在“IM 机器人”且路由为 `/system/integrations/im-bots` | 通过 | 管理员真实浏览器可见导航并打开页面；普通用户不可见且深链回退。 |
| [x] | IM-002 | P1 | 页面使用当前 React/Tailwind 中台风格 | 通过 | 复用当前 PageHeader、Card、Badge、DataTable、Modal 和 Tailwind 设计体系；1280×720 实测无横向溢出和控制台错误。 |
| [x] | IM-003 | P0 | 只有系统管理员或运营管理员能管理机器人 | 通过 | `requireImAdmin` 与服务层双重限制，仅“系统管理员/运营管理员”可管理；普通角色拒绝验收通过。 |
| [x] | IM-004 | P1 | 钉钉凭据、启停、连接、断开、重连和状态可用 | 不适用（本批次延期） | 2026-08-11 按用户指示延期真实钉钉租户联调；本批次保持钉钉集成关闭，不配置生产凭据，已有加密、Outbox 和安全 Mock 证据保留。 |
| [x] | IM-005 | P1 | 飞书凭据、启停、连接、断开、重连和状态可用 | 不适用（本批次延期） | 2026-08-11 按用户指示延期真实飞书租户联调；本批次保持飞书集成关闭，不配置生产凭据，已有加密、Outbox 和安全 Mock 证据保留。 |
| [x] | IM-006 | P1 | 微信通知登录、目标和发送能力按设计可用 | 不适用（本批次延期） | 2026-08-11 按用户指示延期微信登录/授权、真实目标发现与生产发送回执；本批次保持微信通知关闭。 |
| [x] | IM-007 | P0 | 机器人凭据加密保存且列表只显示脱敏值 | 通过 | AES-256-GCM + Bot ID AAD；主密钥来自环境，列表仅返回末四位提示且不返回密文/指纹。 |
| [x] | IM-008 | P0 | 入站消息只路由到授权用户、项目和 Agent | 通过 | 入站密钥常量时间校验，外部群聊必须精确匹配启用绑定，并复核稳定用户、项目成员、会话所有者/项目和 Agent；重放幂等。 |
| [x] | IM-009 | P1 | 出站消息使用 Outbox，失败可重试且不会无界重复 | 通过 | MySQL Outbox 使用幂等键、租约、过期回收、指数退避、有限重试和 dead-letter；并发领取采用 `FOR UPDATE SKIP LOCKED`。 |
| [x] | IM-010 | P1 | 发送频率限制、状态和日志正确 | 通过 | 每机器人分钟限速、投递 HTTP/耗时/错误日志、连接状态及最近错误均通过真实 MySQL 验收。 |
| [x] | IM-011 | P0 | 线索池存在“渠道与推送配置”入口 | 通过 | 管理员线索池已提供真实弹窗入口，普通用户不显示。 |
| [x] | IM-012 | P0 | 线索池只能选择已授权且启用的机器人 | 通过 | 安全设置接口只投影同时启用的 Bot/Binding，创建规则在事务前再次校验目标和项目一致性。 |
| [x] | IM-013 | P0 | 线索池接口不返回机器人完整密钥 | 通过 | 服务验收和浏览器 DOM 扫描均未出现完整凭据、`credentialCiphertext`、`credentialFingerprint`、`inboundSecret` 或 `webhookUrl`。 |
| [x] | IM-014 | P1 | 可以按线索状态、项目、规则和目标配置推送 | 通过 | `im_lead_push_rules` 支持状态、项目、最低评分、模板和授权目标；匹配线索可幂等加入 Outbox，不匹配时服务端拒绝。自动事件触发不在当前验收范围，当前由管理员显式发送。 |
| [x] | IM-015 | P1 | 停用或删除有绑定的机器人时显示影响并阻止误操作 | 通过 | UI 显示绑定/待发送影响并要求二次确认；服务端未开放破坏性 Bot 删除，有活动影响时停用必须显式确认，Binding 有规则或历史任务时拒绝删除。 |
| [x] | IM-016 | P1 | IM 配置、连接、绑定和发送均可审计 | 通过 | 机器人创建/替换/启停/测试、绑定、规则和发送均写审计；验收扫描确认审计不含完整凭据。 |

## 15. 数据迁移核验

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | DATA-001 | P0 | 每个来源表都有读取、写入、跳过和失败数量 | 通过 | 对 checksum 锁定的 PostgreSQL dump 执行了“采用现有目标基线”核验：16 张源表合计读取 16,574、业务写入 0、跳过 16,574、失败 0；每表保存源/目标哈希、差异列和批准理由。二次执行复用同一运行 ID，台账不增长；54 张非迁移台账表前后行数/主键哈希全部不变。这是当前基线核验证据，不伪装成原始全量导入的历史写入数。 |
| [x] | DATA-002 | P0 | 用户、部门、角色和权限数量及关联核对通过 | 通过（锁定 dump 与目标实库） | checksum 锁定 dump 的 5 个用户均有唯一稳定映射并保留目标 ID，目标当前也为 5 个正式用户；曾由旧版 Repository 并发验收遗留的 3 个 `example.invalid` 合成账号经 46 个用户外键逐表确认 0 引用、0 来源映射后精确删除，正式用户未改动。`accept:identity-authority` 复验 `users` 为唯一身份权威，部门/角色/状态字段有效，46 个用户外键 0 孤儿，项目 owner 权限绑定一致；概念 `iam_users` 不另建第二物理表。5 个正式账号的弱密码仍由 `AUTH-016/MIG-0808` 独立阻断，不影响本项数量与关联结论。 |
| [x] | DATA-003 | P0 | 项目、成员、线索、会议、待办和风险核对通过 | 通过（锁定 dump 与目标实库） | 最新 checksum 绑定对账逐表确认项目 15、线索 892、会议 1、待办 8、风险 0，源 ID 在目标 100% 存在；项目评分和线索历史评分只允许在成功 `legacy-scoring-classification` 台账下演进。目标 15 条项目成员关系覆盖全部 owner，133 个外键及项目/成员/会议/待办/风险关联 0 孤儿。重复执行复用运行 `667521f6-8710-4f73-b19f-8b391d55476b`，`ledgerWritten=false`、16,574 个实体映射变更 0。存量同名线索合并仍由 `DB-005/015` 单独阻断，不把记录保全误写成去重完成。 |
| [ ] | DATA-004 | P0 | 会话、消息、工具调用和任务关联核对通过 | 目标通过/生产源阻塞 | 新增只读内容级聚合审计，当前 13 个 chat 索引与 13 个 Agent 精确成对，消息 sequence、Part index、角色/状态/类型、迁移会话活动态、外部消息键和 AI 任务会话绑定均 0 违规；聚合内容 SHA 不保存正文。源 dump 中 4 个本就不存在的任务会话引用仍按“不制造会话”置空并留台账。当前授权源业务消息为 0、生产源未批准，故不能整项通过。 |
| [ ] | DATA-005 | P0 | AI 任务、产物、来源和模板关联核对通过 | 阻塞/决策底稿就绪 | 任务到会话、产物到任务、来源到产物的用户/项目/会话绑定均为 0 违规；7 个不可下载产物已隐藏并由精确集合/行指纹底稿锁定。最新扩展只读扫描覆盖 Desktop/Documents/Downloads 三个标准用户资料根，7/7 仍无候选且 0 写库。它们均属已排除生成测试的合规说明或投资提案，但“不测试”不等于“批准历史源字节永久缺失”；处置仍 pending，生产源全量执行证据也未提供。 |
| [x] | DATA-006 | P0 | 线索原始事件、历史评分、来源和同步游标核对通过 | 通过（目标 MySQL） | `accept:radar-lead-source-reconciliation` 确认 9,903 条储备详情原始事件 100% 覆盖且无重复、Radar 来源键/投影/游标/状态一致；`accept:legacy-scoring` 已将 874 条线索历史评分锁定为 `legacy-import` 并保留源哈希，新增评分走 Agent Run 证据链。生产非库原资产是否齐全由 DATA-019 独立阻断。 |
| [ ] | DATA-007 | P0 | 文件存在率、容量和抽样 SHA-256 核对通过 | AI 产物阻塞 | 17 条项目原件已按用户批准精确落账且机器缺口为0。AI 产物7/7原metadata已恢复且精确决策底稿通过预览漂移校验，但缺失字节不能由metadata、路径哈希、归档状态或“跳过生成测试”替代；严格处置仍退出2。 |
| [x] | DATA-008 | P1 | JSON 字段全部合法，失败记录进入隔离表 | 通过 | `accept:migration-json-safety` 动态枚举并扫描 60 个 MySQL JSON 列、48,724 个列值，`JSON_VALID=0` 为 0。PostgreSQL dump 与 Flue 导入器共用类型化解析契约；坏 JSON 在 apply/reconcile 时以失败 `migration_runs` 和 `MIGRATION_INVALID_JSON` 问题同事务持久化，只保留源系统/表/键/列，不保留原始坏正文。内置恶意夹具、隔离行回读、发布门禁和静态防回退均通过。另修复两条历史重启验收用户因审计外键导致的清理遗漏，并为 AI 产物 `archived/failed` 演进增加“必须存在成功缺文件隔离台账”的批准条件；锁定 dump 16 表/16,574 行重新对账通过。 |
| [x] | DATA-009 | P1 | UTC 时间转换与页面北京时间显示正确 | 通过 | `accept:timezone` 在真实 MySQL `+08:00` 会话中验证指定 UTC 毫秒时刻写为对应北京时间 `DATETIME(3)`，经显式 Schema 映射读回后 UTC 瞬间和 API ISO-8601 完全一致；`CURRENT_TIMESTAMP(3)` 默认值也映射为当前 UTC 瞬间。会议/风险输入按 `+08:00` 严格解析并拒绝非法日历值，前后端业务日期、AI 截止日、OA 日期和文档日期统一使用 `Asia/Shanghai`，不再依赖主机或浏览器时区。4 项单元测试、实库门禁、发布门禁与静态防回退均通过。 |
| [x] | DATA-010 | P0 | 旧 ID 到新 ID 映射完整且可追溯 | 通过 | 迁移 `0028_add_migration_entity_mappings` 建立通用 `source_system/source_table/source_id -> target_table/target_id` 台账，并绑定源行 checksum 与成功迁移运行。`accept:migration-entity-mappings` 按锁定 PostgreSQL dump 的 14 张非空表逐一核对 16,574/16,574 个源实体：每个源 ID 恰有一条映射、保留型 UUID/整数 ID 显式记录、目标行全部存在；Flue 通用映射与专用会话/消息映射数量一致（当前批准源均为 0 条业务会话/消息）。重复对账 `mappingRowsChanged=0`。 |
| [x] | DATA-011 | P0 | 无法重建证据的旧评分明确标记为 `legacy-import` | 通过 | 对锁定 dump 中具备评分维度或总分的记录执行事务性分类：线索 874 条、项目 5 条全部写入 `provenance=legacy-import`，保留源 dump SHA-256、分类运行 ID 和变更前评分 checksum，并明确 `evidenceReconstructable=false`，未制造 Agent Run/Decision/Evidence。两条仅含研究信息、并非评分的 JSON 未误分类。后续新评分统一写 `provenance=agent-run` 和 Run/Decision/不可变输入/Evidence 链；`accept:legacy-scoring`、发布门禁和 dump 演进政策均已接入，第二次分类改动 0。 |
| [x] | DATA-012 | P1 | 迁移程序重复运行不产生重复记录 | 通过 | `accept:migration-idempotency` 从活动前缀一致性备份恢复到随机隔离前缀，只在隔离前缀连续执行三次完整 Schema 迁移；82 张表/69,118 行的规范化 DDL、行数和排序无关逐行 SHA-256 完全一致，迁移日志保持 40 条，活动前缀写入 0，隔离表精确清理。dump 基线复核第二次 `ledgerWritten=false/mappingRowsChanged=0`；CDC 中断续跑重放 3 条事件但业务零重复。 |
| [ ] | DATA-013 | P0 | 增量迁移能够追平切换前最后写入 | 测试源通过/生产未验收 | 隔离事件源从 sequence 3 续跑至安全 watermark 6 并显示 caught_up；真实 PostgreSQL 离线，尚无切换前最后写入和冻结窗口证据。 |
| [ ] | DATA-014 | P0 | 迁移报告经数据负责人签字确认 | 技术报告已生成/数据负责人待签字 | 严格 MySQL 对账、生产源候选、文件/Radar/储备池和缺口处置报告均已生成并使用 0600 证据；当前仍有 97 条储备详情、7 个 AI 产物及生产源批准三类机器阻断，且没有数据负责人签字，因此不能关闭。 |
| [x] | DATA-015 | P0 | Aipin 排除报告证明其表、项目、用户关系、队列、运行记录和配置未被读取、映射或写入目标库 | 通过 | `check:aipin-exclusion --strict` 验证三份 JW SQLite 备份哈希/完整性及 37 个拒绝会话、2,168 条拒绝消息；161 个活动运行时/配置文件、环境变量名和目标 MySQL 63 张表/557 个文本或 JSON 列均保持 0 精确身份命中，目标写入为 0。在线 PostgreSQL/dump/Flue 迁移器只读固定白名单；锁定 dump 的 16 个 COPY 表及记录身份命中为 0，恶意 Flue 路径和表均被报告并在目标零写入。审计采用身份边界匹配并用 `naipintx.com`/`Taiping Pharmaceutical` 反例防止子串误报，严格审计已由统一来源白名单发布门禁内部执行。 |
| [ ] | DATA-016 | P0 | 生产 `pg_catalog` 与代码 Schema/启动 DDL/修复脚本的漂移均有迁移映射或书面处置 | dump/目标已核对/生产 pg_catalog 待验 | 锁定 PostgreSQL dump 16 表、在线源允许 17 表、代码 Schema、38 个 MySQL 迁移版本和目标 82 表已有映射/严格白名单；真实生产 PostgreSQL 当前不可连接，未取得 `pg_catalog`、扩展、非 ORM 表、触发器和修复脚本全量漂移报告。 |
| [ ] | DATA-017 | P0 | CDC 源/目标 watermark 已追平，插入、更新、物理删除和级联删除数量可对账 | 测试源通过/生产未验收 | 隔离源 safe/target watermark 均为 6，3 新增、1 更新、2 删除、1 级联删除与唯一事件账本一致；生产源无可用 watermark。 |
| [ ] | DATA-018 | P0 | 关键字段规范化 checksum、孤儿外键、状态机和跨表业务不变量核验通过 | 部分通过 | checksum 绑定验收逐表核验锁定 dump 16/16 个源/目标规范化 SHA-256，目标 82/82 张物理表均有稳定唯一键 SHA-256；133 个外键孤儿为 0，24 项内部跨表不变量及 19 项状态机/绑定不变量均为 0。最新源资产缺口为：97 条储备原详情、项目原件 0（17 条批准缺失已逐项落账）、AI 产物 7；因此整项继续保持部分通过。 |
| [ ] | DATA-019 | P0 | Radar/`lead_reserve` 原始资产、来源键、游标、未入池记录和采集状态核对通过 | 部分通过 | 目标 MySQL 技术核对 `localTechnicalReady=true`：10,000 条储备记录分区闭合、9,903 条原始事件完整、656 条正式映射有效、3 份采集状态/39 个来源/1 份同步状态/4 个调度任务可恢复。生产原始资产 manifest 未提供，`productionAssetReady=false`；97 条详情和 406 条 imported 业务时间仍为源数据缺口。 |
| [x] | DATA-020 | P0 | OA、流程日志、投后、通知、材料、字典和模板的迁移/归档/退场报告与批准结论一致 | 通过（2026-08-11 范围更新） | OA/日志、模板、材料统一入口维持原结论；投后与通知继续退场；组织、角色权限和数据字典按后续批准通过 `0036/0037` 正式迁入 MySQL，旧浏览器内存写入仍禁止。 |
| [ ] | DATA-021 | P0 | 会话消息内容、顺序、附件、工具引用和停止状态一致，不只满足数量一致 | 本地技术链通过/生产待验 | `check:flue-migration` 已以两次真实 MySQL 应用验证4条消息、8个Part、可见主/子会话索引、精确重复、正文/思考、附件容量与SHA、工具输入输出及中断态，内容checksum一致；`audit:conversation-migration-content` 再对全目标做顺序、Part、状态、来源映射和任务绑定只读扫描。正式生产源业务正文为0且盘点未批准，严格生产命令按设计退出2。 |
| [x] | DATA-022 | P0 | 姓名到用户 ID 映射完整，重名、离职、缺失和无法映射记录均有人工结论 | 通过 | 当前 15/15 个项目负责人均映射稳定用户 ID，唯一源数据缺失姓名问题已通过受版本控制的源证据裁决文件处理；裁决执行校验 dump SHA-256、项目/用户/审计/附件/会议/待办证据，在事务内锁定并写审计，普通身份重同步不能覆盖显式结论。目标对账 `open-identity-resolution=0`。 |
| [ ] | DATA-023 | P0 | 全部文件根 manifest 的源/目标归属、哈希、权限和隔离记录核对通过 | AI 产物阻塞 | 当前在线 manifest 为240个文件、未决归属0；累计66个验收/孤儿workspace文件已受控隔离，17个项目原件已批准归零。剩余7个旧机器AI产物由私有精确底稿锁定但仍pending，生产全部文件根签字也未完成。 |

## 16. 性能、稳定性与恢复

> 本节的具体门槛应在预生产验收前填写，不允许以“无明显问题”代替数值目标。

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | PERF-001 | P1 | 核心 API P95/P99 达到项目设定门槛 | 不适用（本批次延期） | 2026-08-11 按用户指示延期正式容量压测；试运行期继续采集 5/15 分钟请求量、错误率和 P50/P95/P99/max，待批准接口组合、并发和 SLO 后复验。 |
| [x] | PERF-002 | P1 | AI 首 Token 和完整回答耗时达到设定门槛 | 不适用（本批次延期） | 2026-08-11 按用户指示延期完整性能 SLO 验收；试运行期继续采集真实 SDK 首 Token、总耗时、成功/失败/取消和观察覆盖率。 |
| [x] | PERF-003 | P1 | 目标并发 Socket 连接下无系统性断连 | 不适用（本批次延期） | 2026-08-11 按用户指示延期生产容量目标验收；本批次保留 `accept:socket` 64 并发、16 会话、5 轮全量重连证据，并继续强制全局 8 许可限制。 |
| [x] | PERF-004 | P1 | 目标并发 Worker 下无重复领取和数据库死锁 | 不适用（本批次延期） | 2026-08-11 按用户指示延期生产峰值/死锁观测验收；本批次保留 Runtime Job、评分 Worker、AI Task 12 路领取及 IM Outbox `SKIP LOCKED` 的单赢家与过期接管门禁。 |
| [x] | PERF-005 | P1 | 线索批处理吞吐和单条成本达到设定门槛 | 不适用（本批次延期） | 2026-08-11 按用户指示延期生产批量吞吐与成本目标验收；本批次继续强制全局并发、分钟/日预算和熔断，保留单条金标计量证据。 |
| [x] | PERF-006 | P1 | 大文件上传、解析和下载达到大小及耗时门槛 | 不适用（本批次延期） | 2026-08-11 按用户指示延期目标容量压测；本批次继续强制扩展名/签名、配额、压缩膨胀、SHA 去重、不可变版本和文件大小限制。 |
| [x] | PERF-007 | P0 | MySQL 短暂不可用后服务可恢复且无确认数据丢失 | 本地通过 | TCP 故障代理在不中止真实 MySQL 的情况下切断现有及新建连接；中断期查询明确返回 `PROTOCOL_CONNECTION_LOST`，恢复后同一连接池重新建连。断链前已确认提交的 Runtime Job 精确保留 1 条，事务内未提交记录为 0，证明连接中断触发回滚；夹具清理后残留 0。生产跨机网络/主从切换仍需现场复验。 |
| [x] | PERF-008 | P0 | Worker 被终止后任务租约到期并可安全恢复 | 本地通过 | AI Task、线索评分、项目评分和周期 Runtime Job 均验证过期 running 租约恢复；活租约不会被窃取，旧运行记录保留为 abandoned/失败证据，新实例使用新的 owner/run ID 接管，取消任务不会复活。 |
| [x] | PERF-009 | P0 | 应用完整重启后会话、任务和配置恢复 | 本地通过 | 隔离夹具连续两次生产模式启动与优雅停止，第二次启动后旧 Cookie、项目、聊天/JW 历史和已完成任务均保持；生产配置和只读 Schema 校验也保持一致。 |
| [x] | PERF-010 | P0 | MySQL 备份恢复演练达到恢复目标 | 通过（本地技术门槛） | 当前批准到代码的技术门槛为 RPO 0 行、备份与恢复各不超过 300 秒；实测备份 12.528 秒、恢复 61.810 秒、RPO 0，DDL/行数/内容哈希一致。生产异机 RPO/RTO 和保留策略仍归 `MIG-0101/0106` 现场批准，不扩大本项结论。 |
| [x] | PERF-011 | P1 | IM 外部服务故障不会阻塞核心业务和 AI 服务 | 本地通过 | IM 发送只由进程内 MySQL Outbox Job 异步执行；单 Bot Webhook 失败被捕获、有限重试后进入 dead-letter，不抛出到主循环。11 项验收确认失败 Bot 后项目、Agent 会话、管理 API 和另一 Bot 的过期租约恢复/成功发送继续工作；生产外部网络故障仍需监控告警。 |
| [x] | PERF-012 | P1 | Agent 模型限流或超时会重试、降级或死信，不无限占用资源 | 通过 | JW Turn 支持 Abort/停止并在恢复时收口；AI Task 将 408/429/超时分类为可重试且非重试错误明确拒绝；评分主模型仅执行一个配置后备模型，队列重试耗尽进入持久 dead-letter 且自动路径不能复活。`accept:ai-task-lifecycle` 14 项、`accept:lead-score-lifecycle` 11 项、`accept:lead-scoring-audit` 12 项和 `accept:jw-restart` 7 项通过。 |
| [ ] | PERF-013 | P0 | CDC 在目标写入峰值下仍满足复制延迟和最终追平门槛 | 目标端机制通过/生产峰值源待验 | 隔离源已覆盖 watermark、批次、中断续跑、乱序、重复和删除事件；真实 PostgreSQL 源离线，生产峰值写入率、允许延迟、批量大小与追平窗口未批准，无法执行同规模 CDC 压测。 |
| [ ] | PERF-014 | P0 | CDC 中断、乱序重放和重复事件后可恢复且不丢失删除事件 | 隔离通过/预生产未验收 | `accept:postgres-cdc` 覆盖乱序输入、相互交错事务组件、sequence 3 中断、3 条重复重放、2 条删除和 1 条级联 tombstone；仍缺真实 PostgreSQL 与峰值负载演练。 |
| [x] | PERF-015 | P1 | 文件 manifest 扫描、哈希和迁移可在维护窗口及磁盘预算内完成 | 不适用（本批次延期） | 2026-08-11 按用户指示延期生产维护窗口/磁盘/带宽预算验收；本地七类根 240 个文件、约 113.5 MB 的全量扫描、SHA-256、容量和可恢复归档证据保留。 |
| [ ] | PERF-016 | P0 | 杀死任一 Node Worker/Python 子进程时主服务仍可用，租约到期后任务按幂等规则恢复 | 部分 | 监督器的失败、超时与进程组终止不退出主进程；Runtime/评分/AI Task 均有租约恢复契约。仍待生产真实任务执行中强杀并核对幂等产物。 |
| [x] | PERF-017 | P0 | 多实例同时启动时只有一个有效调度 Leader，同一周期任务不会重复执行 | 通过 | `accept:runtime-job-leader` 使用两个不同实例 owner 并发领取同一 MySQL 到期任务，只有一个取得 `FOR UPDATE` 租约且只产生一条 running 记录；同周期第三实例不能重复领取。租约过期后旧 Run 标记 abandoned，另一实例以新 Run 接管。领取资格全部由 MySQL `NOW(3)` 判断，避免 Node/数据库时区分叉。 |
| [x] | PERF-018 | P0 | 优雅停机按既定顺序完成，无新任务进入、无游标丢失、无损坏产物和孤儿进程 | 本地通过 | 调度器和 AI Worker 先进入 stopping，运行记录标记 cancelled/租约释放，监督器拒绝新任务并排空进程组；实启停后三端口关闭且无残留进程。生产发布仍需重复取证。 |

## 17. 安全与审计

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [x] | SEC-001 | P0 | API Key、IM 密钥和会话密钥不以明文出现在接口和日志 | 本地通过 | 会话 Token 仅哈希入库；模型和 IM 列表剔除密文/指纹并只返回掩码，连接测试、线索池响应、浏览器 DOM、审计和错误扫描均无完整 Key；统一日志脱敏与 500 响应收口通过。生产日志仍须在切换窗口复查。 |
| [x] | SEC-002 | P0 | 敏感凭据在 MySQL 中加密，密钥由环境管理 | 通过 | 模型与 IM 凭据均使用 AES-256-GCM，并分别绑定 Provider/Bot ID AAD；主密钥只由环境注入，生产缺失或长度非法时拒绝启动。会话令牌采用不可逆哈希而非可解密密文。 |
| [x] | SEC-003 | P0 | SQL 注入、路径穿越、越权和任意文件读取测试被阻止 | 通过 | `accept:security-boundary` 以生产配置自启动编译后的唯一 `cybernaut-app`，在随机本地端口执行 45 项真实 HTTP 验收：关键词/owner/路由 ID SQL 注入不改变查询或数据，三角色双项目读写与知识检索越权被拒绝，workspace/generated 的 `..`、绝对路径、双重编码、跨用户路径和符号链接均不能读取受保护字节；发布脚本已强制执行，异步知识分块及其他夹具残留 0。 |
| [x] | SEC-004 | P0 | Agent 工具执行受白名单、权限、目录和预算限制 | 本地通过 | 强制 `dontAsk`，内建工具/Skills/设置源为空，`canUseTool` 二次拒绝并审计非白名单工具；单一宿主 MCP 仅放行项目检索/摘要/文件列表/片段读取、AI 任务创建/查询和公开情报 7 个固定工具，项目搜索/列表/读取均由稳定 userId/projectId 校验，工作目录按 UUID 会话隔离，默认最多 12 Turn/5 美元。目标机 OS 沙箱另见 ENV-017。 |
| [x] | SEC-005 | P0 | 线索 Agent 不能绕过宿主校验直接写正式业务表 | 通过 | 主体、研究、初筛、评分和补全 Agent 的 tools/skills/allowedTools 均为空，子进程环境不含 `DB_*`；Agent 只返回严格 Schema，正式线索、Pipeline、审计及来源只能由宿主在 MySQL 事务中提交。无证据 accept、幻觉引文、未知字段和覆盖式补丁验收均拒绝写入。 |
| [x] | SEC-006 | P1 | 高风险操作包含操作者、时间、对象、结果和请求 ID | 通过 | Schema 迁移 `0027_add_audit_request_result` 为审计日志新增非空 `result/request_id` 并为历史记录确定性回填、建立请求索引；HTTP 写入从 AsyncLocalStorage 自动绑定响应 `X-Request-ID`，非 HTTP 写入生成独立 UUID，两处事务内原生 SQL 也显式写入。`accept:security-boundary` 真实创建会议后核对审计操作者 ID/名称、时间、对象、成功结果及请求 ID 全部匹配；静态门禁阻止 ORM 或原生 SQL 回退。当前证明的是成功提交路径，失败响应与回滚由 SEC-009 覆盖。 |
| [x] | SEC-007 | P1 | 审计日志普通用户不可修改或删除 | 通过 | `/api/audit-logs` 仅注册系统管理员 GET，生产 routes/services/runtime/middleware 中不存在 UPDATE/DELETE 审计表的入口；普通用户读取固定 403，普通用户 POST/PATCH/DELETE 及管理员 DELETE 均为 404。`accept:security-boundary` 对真实审计行发起篡改后逐字段复核未变化，静态门禁同时禁止生产代码新增审计更新/删除语句。 |
| [x] | SEC-008 | P1 | 文件下载、产物下载和 IM 推送记录可审计 | 通过 | 项目当前/历史版本下载与预览、AI 正式产物下载/预览、Agent workspace 和 generated 产物读取均在返回字节前写入操作者、动作、稳定对象 ID/路径哈希、容量、结果和请求 ID；不记录文件正文、文件名或存储路径。IM 测试发送和线索规则发送沿用 Outbox/投递日志及操作者审计。`accept:security-boundary` 59 项真实 HTTP 验收覆盖所有者成功、跨用户拒绝、请求关联和路径脱敏。 |
| [x] | SEC-009 | P1 | 错误响应不泄露数据库、文件系统或内部提示词敏感信息 | 通过 | 所有 5xx 对外固定为 `INTERNAL_ERROR/服务器内部错误/details=null/requestId`，MySQL/文件层异常 code 仅进入脱敏结构化日志。`accept:security-boundary` 真实触发 MySQL 字段溢出、根目录外 storagePath 和携带内部提示词/SQL/绝对路径标记的畸形 JSON，响应均不包含载荷、表前缀、数据库主机/库名、工作区路径、底层错误 code 或系统提示词，失败写入保持回滚。 |
| [x] | SEC-010 | P1 | 依赖和原生模块安全检查没有未处置的高风险问题 | 不适用（本批次延期，风险接受待正式批准） | 2026-08-11 按用户指示延期 `pptxgenjs/image-size` 2 项高危告警的上游修复/替换；继续全局禁用 ICNS/JXL/HEIF 解析并保留恶意 ICNS 快速拒绝门禁。 |
| [x] | SEC-011 | P0 | 目标代码、环境变量、密钥库和部署配置中不存在 Aipin 专用凭据或连接信息 | 不适用（本批次延期，未安全验收） | 2026-08-11 按用户指示延期生产外部密钥库、systemd EnvironmentFile 和运行进程环境取证；本地代码/`.env`/版本化配置/MySQL 严格排除审计保留。本项不解除生产发布阻断。 |
| [x] | SEC-012 | P0 | CSRF、跨站 Cookie、会话固定、Cookie 重放和旧 JWT 绕过测试被阻止 | 不适用（本批次延期，未安全验收） | 2026-08-11 按用户指示延期目标 TLS 浏览器重放与密钥轮换演练；保留 `accept:auth` 的 CSRF、恶意 Origin、随机新会话、吊销与旧 Bearer 拒绝门禁。本项不解除生产发布阻断。 |
| [x] | SEC-013 | P0 | INTERNAL_SECRET、Cookie、模型、OSS、GSData 和 IM 密钥已轮换，旧值不可用 | 不适用（本批次延期，未安全验收） | 2026-08-11 按用户指示延期生产 Cookie、模型、OSS、GSData、IM 密钥换新、旧值吊销和拒绝取证；旧 INTERNAL_SECRET 运行旁路删除、Cookie 轮换机制与失效审计保留。暴露过的模型 API Key 仍必须由持有人轮换。 |
| [x] | SEC-014 | P0 | AgentSessionCore 动态加载、网络、子进程、文件和数据库访问超出 allowlist 时被阻止并审计 | 通过 | `accept:jw-runtime-boundary` 对 Read、Bash、WebFetch、未授权 MySQL MCP 和 Skill 五类越界调用逐一验证 `deny`，并在 MySQL 不可变审计中核对实际操作者、`denied` 结果、会话/边界/工具对象及请求 ID。互动 SDK 子进程由全量 `process.env` 改为最小环境，只保留 PATH/区域/代理/CA、隔离 HOME/配置目录和模型网关变量，验收确认 DB/内部密钥不可见；会话工作目录只接受 UUID 且禁止穿越，生产网关强制 HTTPS 和主机白名单。`tools/skills/settingSources=[]`、`dontAsk`、固定 7 个宿主 MCP 和静态发布门禁共同防回退。 |
| [x] | SEC-015 | P1 | 迁移报告、文件 manifest 和隔离报告不包含完整密钥或非必要敏感正文 | 通过 | `accept:migration-evidence-safety` 先用内置恶意夹具证明完整密钥、接口密钥/认证令牌/私钥/带认证 URL、凭据赋值和 JSON/Markdown 正文会失败且错误元数据不回显命中值，再扫描真实证据目录。当前 184 个文件全部仅所有者可读且无符号链接；108 个控制报告对 4 个已配置敏感值和通用模式均 0 命中。67 个隔离原件可能包含原始业务或验收载荷，只做权限/符号链接检查，所在隔离目录的 `report.json` 仍执行内容扫描，不能把原件伪装成脱敏报告。 |

## 18. 切换与回滚验收

| 检查 | ID | 优先级 | 验收标准 | 结果 | 证据/备注 |
|---|---|---:|---|---|---|
| [ ] | CUT-001 | P0 | 切换步骤、命令、负责人和时间窗口完整 | 手册/命令已交付/负责人窗口待批准 | CDC/切换手册、部署命令、写冻结、PONR、回滚阈值和现场证据命令已交付；构建已改为在线期间只暂存、停服后成对激活并在启动失败时恢复上一版。`audit/check:cutover-readiness`用只读MySQL会话聚合对账、弱密码、P0清单、当前单服务和目标Linux证据；现又单独拒绝已勾选但明确不解除生产阻断的延期P0/安全冒烟、缺正式负责人/ISO日期期限/批准人的例外、未完成生产冒烟和最终验收确认，避免范围缩减或核心数量下降被误报为生产验收通过。实际日期、切换时长、五类负责人、通信/升级路径和批准记录仍为空。 |
| [ ] | CUT-002 | P0 | 旧 Worker、定时任务、Flue 和直接 LLM 审查可按步骤暂停 | 技术步骤具备/生产停机待演练 | 部署脚本能停用并备份移除六类旧 unit，清理双用户及系统 cron，写冻结会停用五类写组件，旧 Flue/8121/3584 启动链已退场；目标生产机尚未执行并证明无改名 unit、容器或游离进程。 |
| [ ] | CUT-003 | P0 | 最终增量同步和关键数据核对可以在窗口内完成 | 目标端机制通过/生产最终增量待做 | CDC 目标端批次、水位、中断恢复、重放、删除及关键 MySQL 对账已通过；真实源无法连接，未取得最终源 watermark、窗口写入量、追平耗时和停写点关键表核对。 |
| [ ] | CUT-004 | P0 | 旧数据库可切换只读且不会被新系统继续写入 | 写所有权契约通过/生产旧库只读待做 | 活动业务代码只写 MySQL，PostgreSQL 只在六个受控离线/CDC 工具中可访问，前向 CDC 要求显式旧源权威模式；仍需在生产把旧库设置只读、停止旧应用写入并留数据库级证据。 |
| [ ] | CUT-005 | P0 | 新系统启动后生产冒烟测试全部通过 | 本地单服务历史通过/当前停止/生产冒烟待执行 | 最近一次只读观察曾确认唯一 3100 服务、Web/API、11 组件、MySQL 和未认证边界健康，3584/8121 关闭且原PID未被检查命令改写。随后外部运行状态变化：原PID/父进程均不存在，3100当前未监听，且无可归因的应用日志；本轮不擅自重启。新增离线启动前门禁在当前非生产配置模式下通过：最新成对构建4/784文件、编译后`ExecStartPre`、最新40条MySQL迁移、3306只读连接和三端口空闲均就绪；显式生产模式复验因本地`.env`未设HTTPS Origin而正确失败，且离线通过本身也不代替真实启动。生产 TLS、多角色、真实数据和外部渠道冒烟仍待执行，且用户延期的 PPT/尽调/提案/Q&A/合规报告范围不得计入本轮通过。 |
| [ ] | CUT-006 | P0 | 回滚触发条件覆盖登录、权限、数据、AI、线索和 MySQL | 本地契约通过/待批准 | 版本化机器契约覆盖 6 域 13 个阈值，零写入才允许安全回切、任一写入禁止自动回切、PONR 后只前向修复；五类生产负责人尚未签字。 |
| [ ] | CUT-007 | P0 | 回滚命令、流量开关和旧数据库恢复可执行 | 本地冻结命令通过/生产待验 | `.env` 冻结 preview/apply 和 PONR 审批解除命令已通过 8 项隔离验收；启用后真实 MySQL/服务零写通过。旧 PostgreSQL 恢复可写、Nginx 流量回切及生产 systemd 尚未演练。 |
| [ ] | CUT-008 | P0 | 切换窗口新数据可以反向导出，或已停止对应写入 | 本地写冻结通过/生产待验 | 没有反向 CDC；隔离只读实例证明 HTTP 写方法、启动恢复/Worker、认证续期和 MySQL DML 全部冻结，测试写入 0。目标生产六域尚未取证。 |
| [ ] | CUT-009 | P0 | 回滚后核心数据、任务和用户访问核对通过 | 回滚契约具备/生产演练待做 | 隔离前缀备份恢复、写冻结、零写安全回切条件和 PONR 后前向修复规则已有机器门禁；尚未在预生产执行流量回切、旧库恢复、六域数据/任务/访问核对。 |
| [ ] | CUT-010 | P0 | 完成一次预生产全流程切换和回滚演练 | 未执行（需要预生产） | 本地已分别验证部署、备份恢复、CDC 目标端、写冻结、单服务和回滚阈值，但没有同一预生产窗口内的端到端切换→观察→回滚证据，不能拼接独立测试冒充全流程演练。 |
| [ ] | CUT-011 | P1 | 观察期监控和告警责任人明确 | 指标/告警基座通过/责任链待签 | 唯一服务已提供 HTTP、MySQL、Worker、Socket、AI、文件、迁移证据和 49 类阈值告警，并可复用 IM Outbox；真实接收目标、值班责任人、升级策略、提醒间隔和观察期尚未批准。 |
| [ ] | CUT-012 | P0 | 旧底座下线前获得业务、技术、数据、安全批准 | 技术材料就绪/多方批准待签 | 迁移计划、ADR、严格对账、缺口台账、切换/回滚手册和现场取证命令已具备；业务、技术、数据、安全批准记录仍未填写，且三项机器阻断尚未关闭。 |
| [x] | CUT-013 | P0 | 正式启动命令和进程清单不会注册、初始化或启动任何 Aipin 组件 | 本地通过 | `start:all` 唯一委派 `start:app`，部署仅一个 `ExecStart=server-dist/index.js`；活动文件扫描无 Aipin 引用。 |
| [ ] | CUT-014 | P0 | 最后安全回滚点和 Point of No Return 已书面确认，超过该点采用批准的前向修复策略 | 技术契约就绪/签字缺失 | 手册和机器契约已规定 PONR 解除写冻结、之后仅前向修复；生产 watermark/sequence 与五类负责人签字仍缺失。 |
| [ ] | CUT-015 | P0 | 按身份权限、项目、线索、会话、AI 任务和文件逐域验证反向变更能被旧系统读取 | 无反向 CDC/六域现场验证待完成 | 当前没有 MySQL→PostgreSQL/SQLite 反向同步；矩阵已明确哪些域必须在回滚窗保持只读、哪些只能前向修复。尚未在预生产逐域制造获批变更并验证旧系统可读，不能宣称无损回切。 |
| [ ] | CUT-016 | P0 | 没有反向同步能力的业务域在回滚窗口保持只读，不会产生无法回放的新写入 | 本地技术链通过/生产待验 | `accept:migration-write-freeze` 在真实 MySQL 上证明会话默认只读、DML 拒绝且 0 写入；隔离单服务健康、静态 Web 可读、写 API 503、五类写组件有意停用并干净停机。生产入口/systemd/多角色现场证据未完成。 |
| [ ] | CUT-017 | P0 | 切换前 CDC watermark、删除事件、文件 manifest、Radar/`lead_reserve` 游标均已追平 | 目标端对账通过/生产最终水位未取得 | 隔离 CDC 的 safe/target watermark、删除/tombstone、当前本地文件 manifest、10,000 条储备分区和 Radar 状态已可核对；生产 PostgreSQL 最终水位、全部文件根、Radar 原资产以及 97 条缺详情仍未闭环。 |
| [x] | CUT-018 | P0 | 线索页面与 Pipeline、AI 任务与其用户/项目/模板外键作为同一切换单元完成 | 通过 | 线索 Route、Pipeline Worker、AI Task Worker 与 Web/API 均由唯一 `cybernaut-app` 承载并共用同一 MySQL Schema；133 个外键、24 项业务不变量和 19 项状态机/绑定不变量为 0 违规。AI Task 引用 12 项验证用户/项目/会话/模板/文件在入库前完整校验，线索在线链 5 项验证原始事件、Run、Decision、Evidence 与正式线索在宿主事务内闭合；统一写冻结覆盖整个服务，不存在按子服务拆分切换的路径。 |
| [x] | CUT-019 | P1 | IM/能力扩展可通过独立开关延期或关闭，不阻塞核心数据与 AI 底座回滚 | 通过 | `AI_CAPABILITIES_ENABLED` 与 `IM_INTEGRATIONS_ENABLED` 是严格且互相独立的布尔开关；9 项专项验收确认关闭能力时核心对话降级为无工具 Agent，关闭 IM 时管理、入站、线索推送和 Outbox 同时失败关闭，且不要求生产 IM 密钥、不恢复旧 Flue。 |
| [x] | CUT-020 | P0 | API→Flue、Flue→API、timer→API 的 localhost HTTP 调用均已移除，进程内接口权限和审计通过 | 通过 | `check:single-service` 对活动代码与部署扫描旧端口、变量和 `/ai/api`；Radar 同步和 Agent Tool 均使用进程内领域调用，资源权限验收通过。旧论文回填脚本也已替换为 MySQL 直读。 |
| [x] | CUT-021 | P0 | `FLUE_BASE_URL`、`FLUE_DB_PATH`、`FLUE_AGENT_NAME`、退场后的 `RADAR_BASE_URL` 和旧内部共享密钥均已删除或轮换 | 不适用（本批次延期，未安全验收） | 2026-08-11 按用户指示延期生产外部密钥库、旧 systemd EnvironmentFile 和已分发旧值的轮换/吊销取证；本地退场、固定退出 78、不启动/不触库和 `accept:retired-assistant-boundary` 门禁保留。本项不解除生产发布阻断。 |
| [ ] | CUT-022 | P0 | `systemctl`/容器、监听端口、cron/timer 和进程扫描与最终单服务拓扑一致 | 本地完整启停通过/当前服务已自动停止/生产待验 | 2026-08-11 获用户明确授权后，最新`accept:single-service-runtime`实际只启动1个`cybernaut-app`，Web/API、统一健康身份和11个进程内组件全部通过，仅监听3100，3584/8121关闭；运维接口未登录返回401，请求关联结构化日志通过。验收随后SIGTERM自动停机，主进程、Radar/Python/旧Assistant均无残留，三个端口全部释放，`.env`哈希和0600权限不变。聚合预检仍正确保留`CURRENT_SINGLE_SERVICE_NOT_READY`，因为一次性验收已停止，不能冒充当前生产服务正在运行；目标Linux的systemd/cgroup/cron/timer/容器/Nginx现场报告仍缺失。 |
| [x] | CUT-023 | P0 | Radar 若获限期保留例外，已记录负责人、退场日期、隔离监控和不会阻断主切换的回滚边界 | 不适用（未保留例外） | Radar FastAPI/8121 和独立 service/timer 已退场，采集只作为 `cybernaut-app` 监督的单次 Python Job；7 项故障隔离/幂等验收和单服务只读观察通过。若目标机仍发现旧 Radar 单元或 8121 监听，则由 `CUT-022/ENV-018` 直接阻断，不进入“限期保留例外”。 |

## 19. 生产冒烟清单

- [x] `SMOKE-001` 管理员和普通用户分别登录成功。本地唯一 3100 服务中，随机 `.invalid` 系统管理员和投资经理分别以独立 MySQL Session 登录成功；普通用户无系统管理导航。
- [x] `SMOKE-002` 打开首页、项目列表、线索池和系统管理。管理员真实打开首页、项目列表、线索池和系统管理；普通用户打开前三页成功，访问 `/system` 被重定向到首页。全部页面使用同一 `cybernaut-app`，浏览器 warning/error 为 0。
- [x] `SMOKE-003` 新建测试项目并读取详情。管理员通过页面创建随机测试项目，跳转到稳定 UUID 详情路由后准确显示项目名、公司名、简介和初始“线索”阶段；cleanup 复用正式项目删除服务，数据库行和原件目录均清理。
- [x] `SMOKE-004` 上传小型测试文件并完成解析。通过项目详情上传 0600 私有 UTF-8 文本夹具，接口先返回异步“解析中”，刷新后文件表准确显示“成功”，下载/删除控件存在；夹具清理走正式项目生命周期，临时原件与账号凭据文件均无残留。
- [ ] `SMOKE-005` 新建 AI 会话并获得一条流式回复。本地真实模型已完成全局两轮和 MySQL 持久化；生产浏览器域名下的逐段流式显示仍待切换窗口验收。
- [ ] `SMOKE-006` 停止一次生成并再次发送消息。
- [ ] `SMOKE-007` 创建一条测试线索处理任务并获得结构化结果。
- [ ] `SMOKE-008` 创建一个低成本专业 AI 测试任务并验证状态。
- [x] `SMOKE-009` 打开模型设置、能力管理和 IM 机器人页面。本地真实浏览器已逐页打开；IM 页面 1280×720 无横向溢出和控制台错误，生产域名仍需复验。
- [x] `SMOKE-010` 验证普通用户不能进入管理员设置。本地真实浏览器确认隐藏系统/IM 导航且 IM 深链回到首页；服务端写 API 另有独立角色拒绝验收。
- [x] `SMOKE-011` 查看 MySQL、Worker、Socket、Agent 和 IM 监控。本地真实系统管理员已在唯一 `cybernaut-app` 的“系统管理 → 迁移运维”查看同一只读快照：MySQL 连接 10/10、空闲 10、等待 0、运行线程 4；Runtime/线索评分/项目评分/AI Task 四类 Worker 为 4/4 健康、排队/运行/死信均为 0；Socket 连接与 DB 活跃/等待、Agent 活跃会话/待交互、IM 待发/发送中/15 分钟成功/失败/死信均正常显示。页面刷新后数据保持一致，1265px 视口无横向溢出、浏览器 warning/error 为 0；一次性管理员和凭据文件已清理，服务以 SIGINT 正常退出。该勾选只关闭本地功能冒烟，生产域名、真实 IM 租户、告警路由、systemd/cgroup 和外部依赖故障观察仍须现场复验。
- [x] `SMOKE-012` 本批次延期，未执行生产日志完整密钥和敏感信息核对；本勾选不代表冒烟通过。
- [ ] `SMOKE-013` 核对路由、Schema、进程和启动日志中没有 Aipin 组件。
- [x] `SMOKE-014` 清空浏览器 localStorage/sessionStorage 后重新登录，首页、OA、项目、线索和系统页数据仍正确。本地新浏览器会话首次登录与逐页刷新通过：线索 720、系统用户/模板/审计 5/5/200，OA/项目来自 MySQL，旧材料/投后深链正确退场；生产域名需再次取证。
- [x] `SMOKE-015` 验证旧 `/generated` 公开 URL 不可匿名访问，受权下载仍可用。`accept:resources` 已验证旧 URL 404、匿名新 URL 401、所有者 200、其他用户 404。
- [x] `SMOKE-016` 查看 CDC watermark、删除积压、Radar/`lead_reserve` 游标和文件 manifest 状态。本地真实管理员浏览器已在唯一 `cybernaut-app` 的“系统管理 → 迁移运维”联合查看：CDC、删除/级联删除、Radar 游标与任务、10,000 条储备池分区、文件根/文件/阻断/批准状态和生产证据结论均来自只读 `/api/operations/metrics`；页面无横向溢出、无浏览器 warning/error，并准确失败关闭。最新 manifest 经可恢复隔离后为 240 个在线文件、7 个阻断；该本地冒烟关闭不代表生产 manifest 已批准，生产域名仍须在切换窗口复验。
- [x] `SMOKE-017` 本批次延期，未执行旧 JWT、旧 Cookie 和旧内部密钥生产拒绝验证；本勾选不代表冒烟通过。
- [ ] `SMOKE-018` 打开一份 DOCX/PPTX/PDF 低成本金样，确认字体、布局和来源信息正常。

## 20. 缺陷与例外记录

| 缺陷/例外 ID | 对应验收 ID | 优先级 | 描述 | 临时措施 | 负责人 | 计划完成时间 | 批准人 |
|---|---|---|---|---|---|---|---|
| EXC-20260811-01 | UI-006～UI-009 | P1 | 会议、OA、风险及上会/投后/知识库扩展的生产全矩阵验收延期 | 未验收新写入不开放；旧上会材料/投后工具继续退场；保留已通过的本地/API/MySQL 门禁 | 待指定 | 待下一批次排期（最终验收前补录日期） | 用户指示（2026-08-11）；正式批准人待补 |
| EXC-20260811-02 | CHAT-012、LEAD-023 | P1 | 多模型切换生产证据和网关精确输入 Token 计量延期 | 保持单一授权模型/默认路由；继续强制并发、速率、日预算、熔断、Turn 和成本/时延限制 | 待指定 | 待网关能力就绪（最终验收前补录日期） | 用户指示（2026-08-11）；正式批准人待补 |
| EXC-20260811-03 | CAP-004、CAP-006、CAP-007 | P1 | 外部 Skill、远程 MCP 和 Plugin 动态安装/生命周期延期 | 只允许代码批准的内置 Skill 和固定 `investment` MCP；动态安装关闭；未批准 Plugin 不得启用或绑定 | 待指定 | 待下一批次排期（最终验收前补录日期） | 用户指示（2026-08-11）；正式批准人待补 |
| EXC-20260811-04 | IM-004～IM-006 | P1 | 钉钉、飞书、微信真实租户/渠道联调延期 | 三类生产集成开关保持关闭；不配置生产凭据；保留加密、Outbox、重试/死信和安全 Mock 门禁 | 待指定 | 待获得真实租户（最终验收前补录日期） | 用户指示（2026-08-11）；正式批准人待补 |
| EXC-20260811-05 | PERF-001～PERF-006、PERF-015 | P1 | 完整容量、SLO、吞吐/成本、大文件和 manifest 窗口压测延期 | 小流量试运行；继续强制并发、速率、预算、熔断、文件大小/配额限制并持续采集性能指标 | 待指定 | 试运行观察后（最终验收前补录日期） | 用户指示（2026-08-11）；正式批准人待补 |
| EXC-20260811-06 | AUTH-014、AUTH-016、SEC-011～SEC-013、CUT-021 | P0 | 生产 Cookie/TLS、弱密码、密钥库取证、会话攻击演练及新旧密钥轮换/吊销本批次延期 | 保留已通过的本地失败关闭、CSRF/Origin/旧 Bearer 拒绝、密码轮换工具、旧服务退出 78 和审计门禁；未完成项不视为安全通过且不解除生产发布阻断 | 待指定 | 生产启用前必须完成（具体日期待补） | 用户指示（2026-08-11）；安全/技术正式批准人待补 |
| EXC-20260811-07 | SEC-010 | P1 | `pptxgenjs/image-size` 2 项高危依赖告警的上游修复/替换延期 | 继续全局禁用 ICNS/JXL/HEIF 解析，保留恶意 ICNS 快速拒绝门禁；风险接受未获正式批准前不视为安全通过 | 待指定 | 待上游修复或替换库（具体日期待补） | 用户指示（2026-08-11）；安全正式批准人待补 |
| EXC-20260811-08 | SMOKE-012、SMOKE-017 | P0 关联 | 生产日志敏感信息与旧 JWT/Cookie/内部密钥拒绝冒烟本批次延期 | 保留结构化日志脱敏、旧 Bearer 默认禁用及密钥轮换机制；未执行冒烟不得记为生产通过 | 待指定 | 生产启用前必须完成（具体日期待补） | 用户指示（2026-08-11）；安全/运维正式批准人待补 |
| EXC-20260811-09 | ENV-013 | P1 | 目标 Linux 部署运维现场验收本批次延期 | 保留版本化部署清单、暂存发布契约、离线启动前门禁及回滚机制；无目标机证据不得宣称实机部署通过 | 待指定 | 重新启用生产部署验收时补做（具体日期待补） | 用户指示（2026-08-11）；运维正式批准人待补 |
|  |  |  |  |  |  |  |  |

## 21. 最终验收结论

### 21.1 阻断项确认

- [ ] 所有 P0 项已通过。
- [ ] 所有 P1 失败项已修复，或存在书面例外批准和明确期限。
- [ ] 数据迁移报告已由数据负责人确认。
- [ ] 安全验收已由安全负责人确认。
- [ ] 切换和回滚演练已由运维负责人确认。
- [ ] 业务核心流程已由业务负责人确认。
- [ ] 旧底座下线条件已经满足，或已明确保留期限。
- [ ] Aipin 排除报告已由技术、数据和安全负责人确认。
- [ ] 权威数据源矩阵、生产 Schema 漂移、非 ORM/Radar/文件资产和页面功能追踪均已闭环。
- [ ] CDC 新增/更新/删除追平、反向变更能力和 Point of No Return 已由数据与运维负责人确认。
- [ ] Cookie/CSRF/会话生命周期、密钥轮换和旧 JWT 退出已由安全负责人确认。
- [ ] 六类专业 AI 任务内容/来源/渲染金样已由业务和测试负责人确认。
- [ ] 单一业务服务拓扑、子进程故障隔离、旧服务/端口/定时器退场已由技术和运维负责人确认。

### 21.2 签字

| 角色 | 姓名 | 结论 | 日期 | 备注 |
|---|---|---|---|---|
| 业务负责人 |  | 通过 / 不通过 |  |  |
| 产品负责人 |  | 通过 / 不通过 |  |  |
| 技术负责人 |  | 通过 / 不通过 |  |  |
| 数据负责人 |  | 通过 / 不通过 |  |  |
| 测试负责人 |  | 通过 / 不通过 |  |  |
| 运维负责人 |  | 通过 / 不通过 |  |  |
| 安全负责人 |  | 通过 / 不通过 |  |  |

最终结论：`通过 / 有条件通过 / 不通过`

批准发布或完成迁移时间：`____________________`
