# Radar 切换后 7 天观察记录

版本：1.0  
观察对象：`project-discovery` 并入 `cybernaut-app.service` 后的生产运行状态  
时区：Asia/Shanghai  
首次观察开始：2026-08-17 16:37  
有效 Day 0：待“Agent 熔断按 profile 隔离”修复部署后重新采样  
最早结束：待新 Day 0 确定后顺延 7 × 24 小时  
状态：原 20:39 候选基线已失效，等待修复部署和重新起算，不具备最终签字条件

## 1. 关闭条件

以下条件必须同时满足，才可关闭 `RADAR-RETIRE-007`：

1. 自 Day 0 重置基线起已连续观察满 7 × 24 小时；
2. 已覆盖至少 3 个完整公众号每日采集周期；
3. 基线后无新增 P0/P1 故障；
4. Radar Runtime Job 无新增死信，关键任务无连续 3 次失败；
5. 候选、原始事件、来源配置和正式线索无不可解释的数量回退、重复或孤儿记录；
6. 旧 3584/8121 端口、旧 systemd/timer 和独立 `project-discovery` 进程始终未恢复；
7. OpenAlex、生产资产四方批准和服务器凭据轮换分别按主验收清单关闭，不得以观察期代替。

## 2. 已失效的 Day 0 候选基线

采样时间：2026-08-17 20:39（Asia/Shanghai）

> 该候选基线在 21:01 出现真实业务死信后失效，不计入 7 天连续稳定窗口。下表仅保留为事故前水位审计证据；不得据此推导 2026-08-24 已满足观察期。

| 指标 | 基线结果 |
|---|---:|
| 主服务健康 | ready |
| 实时健康组件 | 12/12 `ok=true` |
| Radar 候选 | 6,176 |
| Radar 原始事件 | 6,972 |
| Radar 来源注册 | 1,312 |
| 正式线索 | 1,927 |
| 储备池 | 10,000 |
| 项目 | 17 |
| 评分任务 done | 33 |
| 评分任务 dead_letter | 0 |
| 评分任务 queued / running | 0 / 0 |
| Radar Runtime Job | 5 个，最近状态均为 succeeded |
| 3584 / 8121 监听 | 0 / 0 |

### Day 0 事件处置

- 发布包曾遗漏 `server/assets/ai/scoring_standard.json`，已补齐完整 `server/assets` 运行资产并核对哈希。
- 生产遗留 `SCORE_MODEL=zeelin-oai/gpt-5.6-sol` 绕过模型注册表准确 key，导致评分结构化输出不稳定；已修正为 `gpt-5.6-sol`。
- 曾评估放宽 Agent 轮数，但对照试验证明无需放宽；生产能力策略已通过管理服务恢复为版本 22、`maxTurns=2`。
- 正式评分 Gold Set 4/4 通过：区间准确率 100%、排序准确率 100%、平均耗时 39.410 秒、平均成本 0.0553875 美元、实际最大 2 轮、工具调用 0。
- 10 条评分死信通过既有人工重试领域接口重新入队并写入审计，最终全部完成；队列收敛为 31 `done`、0 死信、0 积压。
- 最后一次发布重启曾安全取消正在领取的 `radar-collect-sync`；随后通过受管手工运行入口重跑，2026-08-17 19:00:21 收敛为 `succeeded`，连续失败数 0、租约已释放。最终 5 个 Radar Job 最近状态均为 `succeeded`。
- 19:04 后执行本地集成测试时发现本地 `.env` 指向生产 MySQL，线上 Worker 抢占了两条临时测试队列记录；测试清理已级联删除临时线索和队列行，线上 Worker 于 19:06:14 完成退出。该过程在 24 小时历史遥测中留下非业务测试记录，正式队列未形成持久死信。
- 20:32—20:35 复验时再次触发相同环境隔离问题，3 次临时测试评分进入线上运行时，形成 1 次成功、2 次失败的非业务进程内遥测；临时线索和队列行均已级联删除，20:39 复核所有评分/BP/项目评分/AI Task 队列均无积压或死信，Radar 5 个任务最近均为 `succeeded`、无活动租约。
- 已为 4 个会写 MySQL 的服务端集成测试增加双重门禁：只有 `ALLOW_MYSQL_INTEGRATION_TESTS=1` 且数据库名明确包含 `test`/`acceptance` 时才执行；默认生产连接下跳过。另为候选兼容、Radar 管理、微信群 Push、Runtime Job 可靠性、Radar 任务隔离和 Lead Agent Runtime Guard 6 个写库验收脚本增加 `ALLOW_MYSQL_ACCEPTANCE_WRITES=1` 与隔离库命名双重门禁，生产库下均在首次写入前拒绝。安全复验为 190 项中 186 项通过、4 项按隔离规则跳过、0 失败，TypeScript 检查和生产构建通过。
- 20:39 只读核对：Radar 候选 6,176、原始事件 6,972、来源注册 1,312、正式线索 1,927、储备池 10,000、项目 17；评分任务 33 `done`、0 死信、0 积压。正式线索较 19:07 增加 2 条，评分完成数同步增加 2，属于切换后正常业务处理。
- 当时为避免把上述非业务遥测混入稳定性结论，候选 Day 0 曾顺延到 20:39；后续不得在生产数据库上运行任何写入型测试或验收脚本。该候选基线随后因 21:01 的真实业务死信失效。
- 20:46 使用只读 `npm run observe:radar-cutover` 统一口径复核通过：公网健康 12/12、5 个 Radar Job 最近均成功且无租约/连续失败、业务队列无积压或死信、候选缺失原始事件为 0。机构公众号周期使原始事件由 6,972 增至 6,975，候选、正式线索、储备池和项目数量保持不变，增长可解释且未触发 P0/P1。
- 21:00 全仓 `npm run check:single-service` 通过，扫描 461 个活动文件；已关闭时区格式、JW 交互权限健康状态、公共线索池导入旧基线、86 表 ER 目录、附属域交接、模型/能力 UI 静态契约和 Radar 管理遥测等既有静态阻断。该复核未写生产数据库，不改变 20:39 观察基线。
- 21:01 发现 1 条真实正式线索评分进入 `dead_letter`。其直接错误为“线索 Agent 连续失败熔断中”；前序 5 次失败全部来自 `lead-research-agent`，但生产代码按所有 Agent profile 的全局最近失败计算熔断，错误阻断了 `lead-scoring-agent`。这是基线后的真实业务故障，因此 20:39 候选基线立即失效。
- 根因修复已在本地完成：熔断窗口查询增加 `agent_profile=?`，只允许同一 Agent profile 的连续失败打开对应熔断；全局并发、速率和预算限制保持共享。验收同时增加“跨 profile 失败不得打开无关熔断”和“同 profile 连续失败必须打开对应熔断”两项契约，并为该写库验收增加隔离数据库门禁。
- 生产熔断窗口自然关闭后，21:07 通过既有领域接口对该真实死信执行 1 次审计化人工重试；21:08 完成。21:15 只读快照确认评分队列恢复为 33 `done`、0 死信、0 积压，公网健康 12/12、5 个 Radar Job 最近均成功、候选孤儿为 0。
- 截至 21:15，修复尚未部署到生产，生产仍存在跨 profile 熔断的潜在复发条件。有效 Day 0 只能在修复发布并复核线上代码后重新采样；新的最早结束时间为该采样时刻后 7 × 24 小时。

说明：修复前产生的死信、超时、租约恢复和 stale completion 指标会在 24 小时滚动窗口内继续出现，不删除历史审计和运行记录。下一次有效观察期必须以修复已部署为前提，并以新 Day 0 后是否新增 P0/P1 或业务死信为判定口径。

## 3. 每日观察表

| 日次 | 计划采样时间 | 主服务/组件 | 5 个 Radar Job | 公众号周期 | 新增死信 | P0/P1 | 数据核对 | 结论 |
|---|---|---|---|---|---:|---:|---|---|
| 候选 Day 0（失效） | 08-17 20:39 | 12/12 正常 | 最近均成功 | 已完成 1 个完整周期 | 21:01 新增 1 条 | 0 | 基线后出现业务死信 | 不通过 |
| 修复后 Day 0 | 待部署后采样 | 待采样 | 待采样 | 待核对 | 待采样 | 待采样 | 建立新基线 | 待定 |
| Day 1 | 新 Day 0 + 24h | 待采样 | 待采样 | 待核对 | 待采样 | 待采样 | 待核对 | 待定 |
| Day 2 | 新 Day 0 + 48h | 待采样 | 待采样 | 待核对 | 待采样 | 待采样 | 待核对 | 待定 |
| Day 3 | 新 Day 0 + 72h | 待采样 | 待采样 | 待核对 | 待采样 | 待采样 | 待核对 | 待定 |
| Day 4 | 新 Day 0 + 96h | 待采样 | 待采样 | 待核对 | 待采样 | 待采样 | 待核对 | 待定 |
| Day 5 | 新 Day 0 + 120h | 待采样 | 待采样 | 待核对 | 待采样 | 待采样 | 待核对 | 待定 |
| Day 6 | 新 Day 0 + 144h | 待采样 | 待采样 | 待核对 | 待采样 | 待采样 | 待核对 | 待定 |
| Day 7 | 新 Day 0 + 168h | 待采样 | 待采样 | 至少 3 周期 | 待采样 | 待采样 | 最终核对 | 待定 |

## 4. 每次采样必须记录

- `/api/health` 与 `/api/health/components` 状态；
- 5 个 Radar Runtime Job 的最近状态、连续失败数、下次运行时间和最新错误；
- 评分、BP、项目评分和 AI Task 队列的 queued、running、dead_letter；
- 候选、原始事件、来源注册、正式线索、储备池和项目数量；
- 3584、8121、旧 systemd/timer、旧进程和 `RADAR_BASE_URL` 的零残留结果；
- 当日公众号周期、候选新增量、失败账号和补采队列；
- 新增告警、处置人、处置时间、根因和是否达到 P0/P1。

## 5. 当前仍需外部完成的项目

- 配置真实 `OPENALEX_API_KEY` 并通过实网采集；
- 产品、研发、运维、数据四方以稳定身份批准生产资产 manifest；
- 轮换聊天中曾暴露的服务器登录密码，并优先改为 SSH 公钥登录；
- 使用安全的生产部署权限发布 Agent profile 级熔断修复，并核验线上代码版本；
- 发布后建立新 Day 0，等待新的观察窗口自然结束并完成 Day 1—Day 7 记录。

以上任一项未完成时，不得把主迁移清单状态改为“全部完成”。
