# MySQL 迁移执行记录（2026-08-08）

## 结论

当前 Express 业务运行层已从 PostgreSQL 切换到 MySQL 8.x，使用 `.env` 中的 `DB_HOST`、`DB_PORT`、`DB_DATABASE`、`DB_USERNAME`、`DB_PASSWORD`、`DB_FREFIX`。在线 PostgreSQL 与仓库完整备份已按来源优先级合并到 MySQL，核心业务表完成行数、字段内容哈希和外键审计。

本记录的初始数据基线只表示 MySQL 业务数据阶段完成；后续执行记录已完成 JW Runtime 接入、Radar Job 化和本地单服务合并。生产 Flue 历史应用、原始文件资产、安全整改、目标机拓扑取证和最终切流仍未完成，因此不能把本记录视为全迁移验收通过。

## 数据基线与结果

| 数据域 | MySQL 行数 | 迁移结果 |
|---|---:|---|
| 用户 | 5 | 在线 PostgreSQL 账号属性优先，保留完整备份 UUID 以维持历史外键 |
| 用户身份映射 | 10 | `legacy_postgres` 5 条、`postgres_dump` 5 条 |
| 项目 | 15 | 哈希一致 |
| 项目文件 | 18 | 元数据行哈希一致；原始文件不能由数据库行哈希替代，当前已恢复并核验 1/18，仍缺 17 条 |
| 会议 / 待办 / 风险 | 1 / 8 / 0 | 哈希一致 |
| 线索 | 892 | 哈希一致，未因重复名称丢行 |
| 36氪储备池 | 10,000 | 哈希一致，保留 `seq/imported/imported_at` |
| 审计日志 | 652 | 哈希一致 |
| 会话索引 | 13 | PostgreSQL 部分已迁移；Flue canonical stream 尚未迁移 |
| 文件分块 / 知识分块 | 53 / 4,781 | 哈希一致 |
| AI 任务 / 产物 / 来源 | 5 / 7 / 124 | 哈希一致 |

当前共检查 33 条 MySQL 外键，未发现孤儿记录。Schema 迁移记录共 6 个版本：基线、`lead_reserve`、历史重复线索兼容、用户身份映射、Agent 会话结构、会话多来源映射与迁移审计结构。当前前缀下共 27 张应用表，另有 1 张 Drizzle 迁移台账表。

后续单服务、文件完整性、AI 任务、评分死信、线索不可变事件、Pipeline 审计、项目评分租约、模板分析进度和 Agent 全局运行保护迁移已把当前 Schema 扩展到 52 张应用表、68 条外键和 24 个迁移版本；`project_score_jobs` 取代项目评分路由的进程内 Map，以 MySQL 持久化排队、租约、重试、死信和重启恢复。新增 `ai_template_analysis_progress` 取代 `.runtime` JSON 在线进度文件，按稳定用户/项目/可选 AI Task 隔离，重启时无法续跑的分析明确转失败而非永久显示运行中。`lead_agent_runtime_permits` 统一记录跨 Profile 并发许可、分钟限流、日预算预留/实耗、失败熔断和过期回收。`lead_pipeline_raw_events/items/transitions` 已承载 9,903 条历史有效储备事件、状态投影和 11,215 条追加式状态历史，656 条 imported 记录全部恢复正式线索映射，零孤儿。最新幂等快照覆盖 47,909 行，重复迁移前后 DDL、行数和逐行内容 SHA-256 均不变。上述“哈希一致”只证明数据库行与 Schema 稳定，不代表原始文件存在或内容哈希一致；97 条缺失详情的储备记录及其他原件完成度仍以文件 manifest 和来源归档报告为准。

2026-08-10 最新复核：当前为 61 张应用表加 1 张 Drizzle 台账表、96 条外键、28 个 Schema 迁移版本，最新幂等快照 48,122 行，重复迁移前后保持不变。迁移 `0027_add_audit_request_result` 为审计日志补齐非空结果和请求 ID、历史确定性回填及请求索引；真实 HTTP 高风险写入已验证操作者、时间、对象、结果和响应请求 ID 一致，原生 SQL 写入也纳入静态防回退门禁。新增迁移证据安全门禁扫描 30 个证据文件及其中 27 个 JSON/Markdown 报告，当前配置密钥、通用凭据格式、非必要消息/文件正文、非仅所有者可读文件和符号链接命中均为 0；压缩逻辑备份与隔离原件只按受限原始证据管理，不虚称正文脱敏。互动 Agent SDK 已改为最小环境，不再继承 DB/内部密钥；文件、子进程、网络、数据库和动态加载五类越界访问均拒绝并写 `denied` 审计，工作目录和模型网关另有路径及 HTTPS/主机白名单。新增 JSON 迁移安全门禁扫描 60 个目标 JSON 列/48,724 个列值，非法值为 0；PostgreSQL dump 与 Flue 坏 JSON 以失败运行和 `MIGRATION_INVALID_JSON` 问题同事务隔离且不复制原始正文。时间契约已补充真实 MySQL 验收：现有 `+08:00 DATETIME(3)` 墙钟经显式 Schema 映射保持同一 UTC 瞬间，API 输出 UTC ISO、页面固定按 `Asia/Shanghai` 展示，指定毫秒时刻和数据库默认值均通过。回归还清理了 2 个仅关联验收审计/会话的历史重启夹具用户，并把 AI 产物 `archived/failed` 演进严格绑定到成功的缺文件隔离台账，锁定 dump 16 表/16,574 行重新对账通过。Provider、模型、七类 Profile 路由及能力目录、作用域授权和会话选择均以 MySQL 为权威源；当前 1 个加密 Provider、1 个启用模型 `gpt-5.6-sol` 和 7/7 路由已配置，非计费连接探针通过。当前目标库已从一致性逻辑备份恢复到隔离 Schema，62 张表的 DDL、行数和内容哈希一致。PostgreSQL dump 的 16 张源表已在 REPEATABLE READ 下完成现有基线采用核验，读取 16,574、业务写入 0、跳过 16,574、失败 0；Flue 候选源及 JW 排除证据已收口。唯一未决项目负责人已由源 dump 哈希及跨项目审计/附件/会议/待办证据完成稳定 ID 裁决，当前未决身份问题为 0。17 个项目文件和 7 个 AI 产物缺少源字节，其中 7 个不可下载产物已从在线列表隐藏；24/24 缺件均有成功运行及问题台账，但这只关闭在线死链接和误展示风险，不代表原件恢复。已清理 17 条无存活项目引用、名称和正文均为资源验收标记的历史知识分块，并修复异步入库后的验收清理。完整迁移仍由生产源资产签字、97 条原详情及 17+7 个源文件缺口阻断。

2026-08-10 DATA-010/011 后续复核（替代上段阶段数字）：迁移 `0028_add_migration_entity_mappings` 后为 62 张应用表加 1 张 Drizzle 台账表、97 条外键和 29 个 Schema 迁移版本，最新幂等快照为 64,700 行。锁定 PostgreSQL dump 的 16 张表/16,574 个源实体均已有 checksum 与运行绑定的目标 ID 映射，逐条目标存在性通过；重复 dump 对账未新增运行台账且 `mappingRowsChanged=0`。874 条线索和 5 条项目历史评分已标记 `legacy-import`，保留源 SHA-256、分类运行和原评分 checksum，明确证据不可重建；未来评分必须保存 `agent-run` 的 Run/Decision/不可变输入/Evidence 链。迁移证据安全扫描现覆盖 31 个文件及 28 个报告，JSON 门禁扫描 60 列/48,733 个值。154/154 服务测试、生产构建、75 项静态边界/258 个活动文件、MySQL 冒烟、目标完整性和两轮幂等验收通过。严格全源复核仍按预期退出 3，阻塞项仍为生产源资产签字、97 条原详情、17 个项目原件和 7 个 AI 产物源文件，而非本次 ID 或评分迁移。

最终回归补充：项目评分输入状态机门禁执行后新增 1 条不可变 Prompt 版本基线，最终两轮幂等快照为 63 表/64,701 行/29 个迁移版本；前述 64,700 为该门禁执行前快照。

2026-08-10 MIG-0722 复核：新增独立标量与约束迁移验收，61 个应用闭合枚举契约全部通过，其中 21 个按 checksum 锁定 PostgreSQL dump 与目标 MySQL 逐值对账；15 个布尔列均为 `tinyint(1)` 且只含 0/1/NULL，141 个 `varchar(36)` UUID 列物理类型与值格式正确，29 个代码唯一索引与 MySQL 名称、列顺序完全一致且无重复组。JSON 门禁最新扫描 60 列/48,734 个值，迁移证据安全覆盖 32 个文件/29 个报告，静态边界为 76 项/259 个活动文件。该验收已加入发布流程；线索有效名称唯一约束仍需先处置 9 组历史重名，由 `DB-005/MIG-0125` 保持未完成。数据库仍为 63 表/64,701 行/29 个迁移版本，完整迁移仍受生产源签字、97 条原详情和 17+7 个源文件缺口阻断。

2026-08-10 DB-009 复核：项目列表补齐 `id DESC` 唯一键尾排序；线索创建时间/评分分页和人工复核分页也均确认以唯一 ID 收尾。真实 MySQL 门禁使用项目与线索各 7 条主排序值完全相同的夹具，以每页 3 条跨 3 页读取并重复整组读取，项目、线索时间、线索评分三种分页均无重复、遗漏或顺序漂移，夹具随后清理。发布流程与静态边界已接入，当前静态检查为 77 项/260 个活动文件。

2026-08-10 MIG-0707 复核：在线 PostgreSQL 17 张表、dump 16 张表及 Flue Canonical 表只读固定来源白名单；新增门禁绑定成功基线对账的 dump SHA-256，确认实际 16 个 COPY 表全部获批且排除身份表/记录命中为 0。恶意 Flue 路径与表夹具分别产生 `AIPIN_SOURCE_REJECTED` 和 `AIPIN_TABLE_REJECTED`，目标迁移台账及 Agent 表哨兵零变化；严格排除审计复核 161 个活动文件、63 张目标表、557 个文本/JSON 列、37 个拒绝会话和 2,168 条拒绝消息，目标身份值与写入均为 0。夹具报告已汇总到专用 0600 证据并从 Flue 生产来源目录清理，`flueReconciliationReady` 恢复为 true。证据安全现覆盖 33 个文件/30 个报告，静态边界为 77 项/261 个活动文件；最新幂等快照为 63 表/64,702 行/29 个版本，新增 1 行是统一服务运行时成功的 `auth-session-cleanup` 正式调度历史。严格全源复核仍按预期退出 3，阻塞项未变。

2026-08-10 MIG-0709 复核：新增 checksum 绑定的业务不变量发布门禁。锁定 dump 的成功基线 Run 覆盖 16/16 个表，逐表源/目标规范化 SHA-256 和 verified 状态有效；目标 63/63 张表均有稳定唯一键 SHA-256，97 个外键孤儿为 0。排除仅属于外部源资产完整性的 97 条原详情、17 个项目原件和 7 个 AI 产物后，19 项目标跨表不变量全部 0 违规；Pipeline、线索/项目评分、AI Task、Runtime、人工复核与 OA 的 12 项租约、当前投影、终态时间和 active key 状态机规则同样全部 0。154/154 服务测试、生产构建、78 项静态边界/262 个活动文件和 63 表/64,702 行/29 版本两轮幂等验收通过；证据安全覆盖 34 个文件/31 个报告。目标内部一致性已证明，但 97+17+7 个源资产缺口仍继续阻断 DATA-018 和全量迁移就绪。

2026-08-10 MIG-0705/MIG-0701 复核：在隔离空 MySQL Schema 做真实全量应用时发现历史 dump 的 `audit_logs` 缺少后加非空 `request_id`，修复为在线 PostgreSQL 与 dump 共用确定性演进规则 `request_id=legacy-<源ID>`、`result=success`，与 `0027` 历史回填一致。新增显式 `migrate:dump:checkpointed`，使用按来源 SHA-256 派生的 MySQL advisory lock，逐表业务提交和 `migration_runs.report` 检查点同事务持久化；失败后同一 Run ID/attempt 续跑并重新验证已完成表。隔离门禁验证 16 表/16,574 行 dry-run 零写、坏 JSON 默认全事务零残留、13/7 批次、7 表后中断与 9 表零泄漏、attempt 2 续完 16/16，以及重复应用的业务计数/哈希/映射数量稳定。目标库基线更新后第二次 `ledgerWritten=false/mappingRowsChanged=0`；最终 154/154 测试、生产构建、79 项静态边界/264 个活动文件和 63 表/64,703 行/29 版本幂等验收通过，证据安全覆盖 35 个文件/32 个报告。严格全源复核仍因生产源签字及 97+17+7 个源资产缺口退出 3；42 组精确重复线索仍须业务裁决。

2026-08-10 PostgreSQL CDC 阶段复核：迁移 `0029_add_postgres_cdc` 将目标扩展为 65 表/98 外键/30 个 Schema 版本，新增按 source instance 唯一的检查点和按 source sequence 唯一的事件账本。源端安装器为 17 张白名单表生成插入、更新、删除触发器，删除保存完整 tombstone、操作者、批次与级联标记；应用器用 PostgreSQL safe xmin 阻止长事务 sequence 越水位，并按交错事务组件原子提交到 MySQL。隔离演练在 sequence 3 中断后续跑到 6，3 个重放事件未重复业务写入，3 新增/1 更新/2 删除/1 级联计数准确；父删除后的子 tombstone 保留为 noop 事件。前向应用强制声明旧 PostgreSQL 唯一写入所有者，切换手册明确无反向 CDC 时的 Point of No Return。最新回归为 154/154、生产构建、80 项静态边界/269 个活动文件、65 表/64,704 行两轮幂等、65/65 表 checksum、98 外键零孤儿、23 项内部业务不变量、12 项状态机不变量、61 个 JSON 列/48,738 个值、17 个布尔列、144 个 UUID 列、31 个唯一索引；证据安全为 36 文件/33 报告。真实 PostgreSQL 当前拒绝连接，故源端捕获、生产 watermark/延迟和最终追平尚未验收，增量迁移相关 P0 项仍保持未完成。

2026-08-10 FILE-015 复核：项目文件删除不再信任数据库中的任意根内相对路径，而是逐项绑定 projectId/fileId，并拒绝存储根、符号链接、realpath 越界和校验后 inode 替换。真实 MySQL 隔离验收故意给目标文件插入指向其他项目原件的污染版本路径；删除目标后其当前/历史字节及版本元数据清理，其他项目原件、同项目历史 AI 产物、共享模板和根外回滚源均逐字节不变。发布门禁、0600 无正文证据和静态防回退已接入；静态边界为 90 项/281 个活动文件，证据安全为 56 个文件/52 个报告。

2026-08-10 迁移测试夹具复核：`accept:migration-fixture-readiness` 在真实 MySQL 完成 5 个角色、2 个部门、2 个隔离项目和每项目 6 类文件/共 12 份原字节回读，验证普通用户双向隔离、AI/运营管理员不因角色获得项目权限、系统管理员可审计。重复/多主体/弱来源/冲突/无效主体以及成功/超时/限流/工具失败/模型失败各 5 类契约均有版本和哈希；使用随机账号、`.invalid` 邮箱、随机密码及合成文件，证据不保存正文、身份或连接信息。验收后项目、文件、版本和用户夹具零残留，`PRE-001/002/003/005/006` 与 `MIG-0025` 关闭。

2026-08-10 PRE-010 复核：AI Task API 验收不再登录固定演示账号或使用已知弱密码，改为每次创建随机 `.invalid` 管理员/普通用户和强密码；每条成功 INSERT 后立即登记精确 ID，finally 在清理会话/项目后删除用户并查询确认零残留。新增测试数据安全发布门禁扫描 78 个验收脚本、34 个服务测试和 3 个金标/夹具 JSON，共 115 文件；真实配置密钥值、凭据/私钥、手机号、身份证号、非保留域邮箱和弱登录密码 0 发现，内容清单 SHA-256 固定。证据安全最终覆盖 60 文件/56 报告。

2026-08-10 错误契约复核：前端不再丢弃服务端请求编号。`ApiError` 保留基础安全消息和独立 requestId，只有响应头/正文编号相同且满足长度/字符白名单时才生成带编号的用户消息，伪造不一致和脚本型编号均拒绝；自定义 Agent 技能错误也保留可信编号，AI Task/渲染异常继续显示专用错误 ID。`accept:error-contract` 对 400/401/403/404/409/500 的统一形状、500 原文脱敏、手工路由补全、前端双向校验和错误 ID 展示全部通过。首次完整构建发现验收脚本跨服务端 `rootDir` 引用前端模块，现已将纯错误契约抽到 `server/src/contracts/apiErrorContract.ts` 并由前端复用、重导出，服务端 TypeScript 边界和生产构建均通过。清理失败构建生成的 2 个前端 JS/SourceMap 副产物后，静态边界 90 项/285 文件，154/154 服务测试通过，测试源安全 116 文件零发现。

2026-08-10 Socket 压力复核：`accept:socket` 不再选择、禁用或恢复现有业务用户，全部使用随机 `.invalid` 合成身份并清理认证会话、Agent 会话、项目及自身审计日志。64 个并发连接、16 个独立会话和 5 轮全量重连的首轮压力暴露共享 MySQL Pool 队列溢出会影响订阅、广播和评分 Worker；现以 `SOCKET_DB_CONCURRENCY=8` 为握手、订阅、广播及周期重认证提供公平全局背压，并禁止广播错误日志输出查询参数或身份/会话标识。最终完成 385 次连接，握手/订阅瞬时失败 0、64 路广播全达、跨用户接受 0、合成身份残留 0；生产构建、154/154 服务测试和 90 项/285 文件静态边界通过，证据安全覆盖 64 文件/60 报告。生产网络条件、持续时长和容量目标仍由 `PERF-003` 保持待批准。

2026-08-10 JW 真实多轮复核：随机 `.invalid` 全局会话经唯一 3100/JW Runtime 向真实 `gpt-5.6-sol` 完成两轮随机挑战，第二轮正确回忆首轮且复用相同 SDK Session。MySQL 保存 2 条用户消息、2 条助手消息、4 个消息 Part、连续序号和活动模型/Turn/耗时/成本状态；工具调用、SQLite/Flue/3584 Runtime 引用及 3584/8121 监听均为 0。会话删除已从单纯 interrupt 加固为结束队列、interrupt/close SDK Query、等待输出循环后再删 MySQL，测试身份、认证会话、对话、审计和工作区零残留。`GATE-M2-01/GATE-M3-01/CHAT-001` 关闭；生产构建、154/154 服务测试、90 项/286 文件静态边界、117 个测试源零发现和 66 文件/62 报告证据安全通过。生产浏览器 partial 流式视觉、Token/上下文压缩和项目工具多轮仍分别由 `GATE-M3-03/CHAT-013/CHAT-002` 跟踪。

2026-08-10 JW 真实项目上下文复核：同一安全验收新增随机合成项目、稳定成员授权和项目会话；提示词不携带随机项目名，真实 `gpt-5.6-sol` 调用一次宿主 `get_project_summary` 后准确回复。工具输入不含 `projectId`，项目范围只取服务端绑定的 MySQL 会话；工具输出、complete 状态和动态 Part 关联落库。会话、项目、成员、身份、审计和工作区验收后均为零残留，`CHAT-002` 关闭。工具进度/失败、停止/交互及文件/RAG/专业任务组合门禁仍待完成，`CHAT-010/GATE-M3-04/MIG-0317` 保持未勾选。

2026-08-10 JW 真实停止复核：旧停止接口暴露“interrupt 已发出但输出循环尚未收敛即返回 idle”的迟到写入竞态。现以 Runtime 停止态屏蔽尾部输出，结束队列、interrupt/close Query 并等待输出循环退出；运行中工具统一转 interrupted/output-error，最后持久化 `user_stop`。真实超长回答出现 Partial 后停止，接口返回后 3 秒无新消息且无 active 状态，同一会话可继续下一轮；`CHAT-006` 关闭。工具自然失败/进度和交互请求仍未通过，`GATE-M3-04` 保持未勾选。

2026-08-10 JW 会话生命周期与刷新复核：第二个认证 Session 从 MySQL 恢复全局、项目和停止续聊三条会话及其不同历史，交替切换再返回全局无串线；改名同步更新 chat/agent 两张索引且稳定 ID 不变，删除只移除目标会话。前端增加手动改名，删除不再静默乐观更新，API 失败会保留列表并提示。`CHAT-003/004` 关闭；浏览器视觉仍由 `GATE-M3-03` 跟踪。

2026-08-10 会话稳定身份复核：全库新增 chat/agent 同 user/project/scope/Agent ID 及项目稳定授权不变量。发现 1 条 `legacy_postgres` 空会话声明 project 但无 projectId，消息/任务均为 0；未猜测项目，使用 preview/apply 工具按固定校验和同步规范两张索引为 global，并在迁移运行/问题台账保留可逆证据。应用双表各 1 行，幂等复跑候选 0；18 项状态机/绑定不变量全部零违规，`CHAT-021` 关闭。

2026-08-10 重复主体写入边界复核：关闭 `LEAD-006`。人工复核只有在目标名称与复核主体一致且线索未转专属项目时才允许显式绑定；多条同名记录未选择目标时整事务回滚。公开情报拒绝自动多匹配、异名显式目标和终态目标。Radar 命中多条记录时不再取第一条，正式写入先回滚，再在同一连接事务中形成实体冲突决策、待办及未绑定的 review 状态。对应 14+9+11 项真实 MySQL 事务验收和 90 项静态边界通过；历史 42 组/107 条重复记录保持原样，继续由 `DB-015/MIG-0125/ISSUE-006` 跟踪，未执行未经批准的自动合并。

2026-08-10 MySQL 正式账号分离复核：当前 `.env` 文件权限由 0644 收紧为 0600，并在真实目标 MySQL 生成强凭据、精确绑定当前应用来源的 Runtime/Migration 两个独立账号。Runtime 仅有目标 Schema `SELECT/INSERT/UPDATE/DELETE`，Migration 仅增加 `CREATE/ALTER/DROP/INDEX/REFERENCES`，两者无全局或授权权限。换号前后完成 Migration DDL、Runtime DML、Runtime DDL 拒绝、权限审计和可恢复回滚文件验证；密码不打印、不写证据，主体仅保存 SHA-256。换号还修复了 Runtime 路径在 Schema 已就绪时仍隐式执行 Drizzle DDL 的问题，现为 Runtime 只读核对、Migration 显式应用。最终 67 表/104 外键/64,735 行/33 条迁移日志幂等，154/154 服务测试、生产构建和单 `cybernaut-app`/10 组件实启通过；证据安全为 77 文件/68 报告。这关闭当前目标的 `DB-011`，但其他正式环境和 root-owned 生产迁移凭据仍由 `MIG-0102/ISSUE-008` 跟踪。

2026-08-10 身份与项目成员审计复核：补齐原先缺失的用户写接口和项目成员稳定 ID 管理接口。服务端事务重新确认操作人仍为启用的系统管理员，创建账号执行 14 位以上复杂密码策略；用户姓名、角色、部门或状态变化会吊销活动会话并触发实时失效，禁止当前管理员禁用或移除自身角色。项目成员替换同时维护 `projects.owner_user_id/collaborators` 与 `project_members`，拒绝普通成员绕过专用接口修改负责人/协作者。创建用户、更新身份和更新项目成员均写入真实管理员、变更前后目标及请求 ID，不保存密码。真实 MySQL 的 `accept:identity-administration` 10 项通过且随机夹具零残留，运行账号无需 DDL；`AUTH-012` 关闭，154/154 服务测试、生产类型检查和 92 项/298 文件静态边界通过。

2026-08-10 单服务浏览器回归复核：在唯一 3100 服务用随机 `.invalid` 身份和项目实际覆盖登录、首页、项目列表/详情、线索、OA、AI、系统、模型与能力设置。修复项目编辑越过专用成员接口、布局横向溢出、UTC 原始时间和 nullable 输入告警；项目创建、列表/详情编辑、17 条项目三页分页及系统三个 MySQL 页签通过，已覆盖页面均为 0 横向溢出、0 控制台 error。删除仅验证到原生确认框，置顶、文件、评分及生产角色矩阵继续作为待验项。验收后合成数据与临时凭据零残留；154/154 测试、生产构建、20 项前端权威源、10 项身份管理、122 个测试源安全、92 项静态边界/299 个活动文件、目标结构完整性和单进程/10 组件实启通过，退出后 3100 无监听。该证据证明前端可由一个业务服务提供，不替代 PostgreSQL 生产源、资产补源、去重、弱密码轮换、TLS/systemd/监控和正式切流验收。

2026-08-10 项目列表写操作浏览器复核：置顶改为优先排序并提供可见标识，刷新后 MySQL 状态保持；删除改用站内确认框并等待服务端成功，不再依赖原生对话框。随机合成项目真实完成置顶、刷新、删除、空态与审计查询，0 控制台 error、0 横向溢出；无正式后端契约的批量分配、批量标签和自定义字段入口已删除。前端权威源门禁禁止这些假操作回流；验收后项目、成员、账号、会话、审计和临时凭据残留均为 0。该结果只关闭本地 `UI-003` 操作缺口，生产角色矩阵仍待现场验收。

2026-08-10 旧 API 兼容与退场复核：以迁移前提交 `0a43c7d2d7eca0c55630a77c1ae29b02cf0d367d` 自动提取 88 条路由，80 条正式路由逐条保留，8 条固定密钥绕过、Flue localhost 回调或伪解析/伪任务/伪上传接口按批准矩阵退场。真实 3100 单服务完成旧登录、项目与 18 类关键列表响应形状验收；未认证保留路由统一 401，输入合法会话/CSRF/Origin 后 8 条退场路由仍统一返回带 `requestId` 的 JSON 404，不会落到 HTML 或 SPA fallback。0600 证据不含凭据、身份或业务正文，验收数据零残留；`MIG-0731/UI-012` 关闭并接入发布门禁。

2026-08-10 MySQL 账号来源漂移复核：开发网络出口变更导致原精确 Host 绑定的 Runtime/Migration 账号返回 1045，4 个 MySQL 测试因此失败。新增安全 Host 轮换命令：预览证明当前绑定 0、失效绑定 2；应用时使用 0600 DBA 恢复凭据为新观测来源建立相同 Runtime DML/Migration DDL 最小权限，两类凭据实际连接成功后才删除 2 个失效主体。未放宽为 `%`，凭据未变更或输出，证据不保存 Host 原值。修复后 154/154 测试、MySQL 冒烟/账号审计、目标完整性与唯一 3100/10 组件服务实启通过。生产必须使用固定出口、VPN 或私网，不依赖动态开发机出口。

2026-08-10 五类文件迁移抽样复核：用受版本控制的真实 PDF、DOCX、PPTX、XLSX 和 PNG 各 1 份复用上传签名/安全边界后做格式级打开。PDF 页和文本层、DOCX 正文、PPTX 幻灯片和可编辑文本、XLSX 工作表/非空单元格、PNG 像素尺寸均可读。首轮的 XLSX 不被 ExcelJS 兼容，加入与正式 RAG 同源的 SheetJS 回退后通过，该差异不被静默忽略。`0600` 证据排除路径、文件名和正文；`MIG-0724` 关闭。17+7 个缺失生产原件及全量 manifest 仍由 `MIG-0723/FILE-012/014` 保持阻断。

2026-08-10 JW SQLite 白名单迁移复核：新增 `migrate:jw-sqlite:preview/apply` 和 `accept:jw-sqlite-migration`，将批准的 `sessions.db` 绑定到固定 SHA-256、必需表、完整性和显式来源集合。当前评审结论仍是通用 JW 会话/消息白名单为 0；37 个拒绝会话和 2,168 条所属消息仅做聚合分类，不读取消息正文。真实 MySQL 预演及两次应用确认 Agent 会话、消息和两类来源映射均零变化，只保存 1 条幂等成功 Run 与 1 条聚合排除 Issue；严格排除审计通过，`MIG-0703` 关闭。生产源是否还有其他 SQLite/Flue/文件根仍未签字，不影响该实现验收，也不能据此通过最终全源切流。

2026-08-10 JW 新会话灰度边界复核：按 `ADR-012` 不恢复 Flue 流量开关，新增全局/项目两个独立 JW 新会话准入变量。真实 MySQL 验收确认关闭范围明确 503 且 chat/agent 零创建，已有会话在关闭新建后仍可读取和追加；两域可分别开启，非法布尔值失败关闭，策略无备用 Runtime，活动创建路径无退役 Runtime import 或代理。合成用户、项目和会话全部清理，`MIG-0314` 关闭。项目组合门禁和目标生产配置尚未签字，`MIG-0317/GATE-M3-05/CHAT-022` 继续保持未通过。

2026-08-10 JW 网络重连消息完整性复核：增强 `accept:socket`，在唯一 3100 服务中以 12 个并发连接、6 个项目会话执行 3 轮全断开和重新订阅。每轮断线窗口向同一 Agent 会话写入 1 条唯一确认消息，重连的 MySQL Snapshot 对累计 ID 逐项核对；最终 3/3 持久化，丢失 0、重复 0。结合既有新认证 Session 刷新恢复和完整服务重启恢复证据，`CHAT-007/GATE-M3-02` 关闭；生产 Socket 在线率与告警仍由 `OPS-002` 跟踪，不把功能验收外推为监控已完成。

2026-08-10 Flue 内容级迁移复核：增强隔离迁移源和报告后发现合并后的全量消息曾按 `created_at,id` 重排，同一毫秒记录会受随机 UUID 影响；现改为 `created_at,sequence,id`。连续两次真实 MySQL 应用保持 1 个会话、3 条消息、7 个 Part、1/3 条来源映射和稳定内容 checksum；逐项验证 user/assistant 0/1/2 顺序、可见文本、reasoning/text/attachment/tool_call/tool_result Part、附件容量/SHA-256、工具输入输出及 interrupted 状态。0600 报告只保存计数和结论；临时默认来源报告汇总后精确删除，正式 `flueReconciliationReady` 为 true。`MIG-0316` 关闭；生产源未就位，`CHAT-019/020/MIG-0715` 保持未通过。

2026-08-10 JW 模型用量与上下文压缩复核：Runtime 现在按 SDK 原生初始化、结果、状态和压缩边界事件，把活动模型、输入/输出/缓存/总 Token、成本、Turn、耗时与压缩状态写入 MySQL；重复压缩 UUID 不重复计数，状态结束可清除瞬时 compacting，失败信息先脱敏。真实 MySQL 验收覆盖失败后恢复、自动/手动两个边界、重复重放、负 Token 拒绝、无内存 Session Snapshot 恢复和测试数据零残留；前端显示 Token、成本与压缩状态，发布/静态门禁已接入，`MIG-0307` 关闭。增强后的真实模型门禁本次因外部 `gpt-5.6-sol` 第一轮 180 秒未完成而失败，单服务和 MySQL 无异常且合成数据已清理；因此 `CHAT-013` 仍为部分通过，需在稳定生产出口重跑实际响应 Token 明细。

2026-08-10 JW 交互请求复核：`AskUserQuestion` 现作为唯一可见的 Claude Code 内建工具，并以显式 `permissions.ask` 规则进入宿主权限回调；它不在 `allowedTools` 自动放行清单中。问题先持久化到 MySQL 会话 Snapshot，所属用户可回答单选、多选、自由输入或取消；越权、过期、非法回答失败关闭，Abort、超时、服务停止/重启均释放等待，回答正文不写会话元数据。React Hook、回答 API 和问题卡片已接入；真实 MySQL 专项验收覆盖应答恢复、取消继续、清理与零残留，发布/静态门禁通过，`MIG-0308` 关闭。真实外部模型主动提问、浏览器作答后继续生成尚未取证，`CHAT-011/GATE-M3-04` 保持未关闭。

2026-08-10 JW 消息渲染复核：真实 MySQL 浏览器夹具和正式 SSR 组件门禁共同验证标题、普通/加粗文本、GFM 表格、语言代码块和 HTTPS 链接；危险 `javascript:` href 被中和，外链固定新窗口与 `noreferrer`。Reasoning 默认折叠在独立 details，展开/折叠可操作且最终答案始终在其结构之外；Shell 与 AskUserQuestion 内部工具 Part 不作为普通消息显示。唯一 3100 服务页面无横向溢出、控制台 0 错误，夹具及 secret 文件零残留，`CHAT-008/009` 关闭；生产多角色/多分辨率视觉矩阵仍由 `GATE-M3-03` 跟踪。

2026-08-10 JW 会话模型切换复核：AI 会话顶部新增当前模型选择器；服务端把模型切换与消息提交按会话串行，忙碌或等待交互时拒绝。切换校验启用模型和角色范围，关闭旧 SDK Query 但保留 `sdkSessionId`、MySQL 消息/Part/会话索引；更新 `modelId` 并清除旧活动模型，下一轮重新读取选择创建 Runtime。真实 MySQL 隔离验收覆盖两个模型、越权/无效/忙碌拒绝、历史保留、同值幂等、默认回退和夹具零残留；发布/静态门禁通过。外部网关切换后新一轮 `init/result` 尚未重跑，`CHAT-012` 保持部分通过。

2026-08-10 JW 工具生命周期复核：修复持久化动态工具 payload 缺少 `type=dynamic-tool` 导致刷新后前端归一化失败的问题，并阻止重复 SDK tool_use 将 complete/error/interrupted 终态退回 running。真实 MySQL 专项验收按 SDK 原生协议形状覆盖 input-available、output-available、output-error、自然失败文本、重复事件幂等、未知结果零孤儿及无内存 Runtime 的 Snapshot 恢复；输入、输出和错误始终关联同一消息，0600 证据不保存正文，随机夹具零残留。唯一 3100 服务浏览器确认运行/完成/失败三态、详情展开和刷新恢复，无横向溢出；临时凭据及数据库记录全部清理，`CHAT-010` 关闭。真实外部模型 AskUserQuestion 后续生成仍由 `GATE-M3-04` 跟踪。

2026-08-10 JW 真实交互链复核：真实网关确认模型生成了 `AskUserQuestion`，但顶层 `permissionMode=dontAsk` 在宿主 `canUseTool` 前将其自动拒绝；改为 `default` 后，显式 ask 规则正确暂停到服务端，模型可见内建工具仍仅此一项，未知工具失败关闭。新增真实模型门禁证明问题进入 MySQL Snapshot、授权回答恢复同一 SDK Turn、模型继续生成、工具终态落库和夹具零残留。唯一 3100 服务浏览器完成单选、提交、交互卡清除、发送恢复及刷新持久化，无横向溢出；停机后身份、项目、会话、消息、认证、工作区和 secret 文件全部清理。`CHAT-011/GATE-M3-04` 关闭，发布流程已纳入真实外部交互门禁。

2026-08-10 JW 当前版本用量复核：真实 `gpt-5.6-sol` 交互最终结果同时验证活动模型、输入/输出/缓存/总 Token、成本、Turn、耗时和 Snapshot 压缩 idle 状态；结合既有原生压缩事件、重复边界幂等、失败恢复及无内存 Session 验收，`CHAT-013` 关闭。0600 证据不含模型正文或凭据，测试身份、会话、消息和工作区零残留。

## 合并优先级

1. 在线 PostgreSQL 非空数据优先于完整备份中的同一账号属性。
2. 用户按邮箱识别同一主体；目标 UUID 保留完整备份版本，避免破坏项目、任务和产物外键。
3. 在线 PostgreSQL 为空而完整备份非空的数据域保留备份数据，不以空表覆盖。
4. 新 MySQL 专属表没有旧源时保持空表，不生成演示数据。

## 已执行门禁

- `npm run db:migrate`
- `npm run inventory:postgres`
- `npm run migrate:data:preview` / `npm run migrate:data`
- `npm run migrate:dump:preview` / `npm run migrate:dump`
- `npm run check:mysql`
- `npm run check:db-boundary`
- `npm run check`
- `npm run build`
- 生产构建启动及 `/api/health` 检查

## 已知未关闭风险

1. 完整备份包含 9 组真实同名线索、15 条额外记录。为保证迁移不吞行，当前保留全部 892 条并暂不启用有效名称唯一约束；需在保留审计证据的前提下完成业务合并后再加约束。
2. 服务启动时业务地区回填扫描 892 条并更新 731 条，这是目标 Schema 新字段的预期派生写入，但应在正式切换报告中与原始迁移哈希分开记录。
3. 初次启动恢复发现的 1 个评分任务曾因外部模型请求失败进入延迟重试，后续人工重试已完成为 `done`；生产切流仍需用目标网关复验新任务、重试和熔断链路。
4. `pg` 仍作为离线盘点/迁移工具依赖存在；业务 Route、Service、批处理和生产启动路径已由静态门禁禁止使用。
5. MySQL 当前按 `+08:00` 会话时区保存 `DATETIME(3)`，与本次历史数据迁移保持一致；服务端 Schema 已显式把该墙钟值转换为同一 UTC 瞬间，API 输出 UTC ISO、页面固定显示 `Asia/Shanghai`，指定毫秒时刻和数据库默认值均已通过真实 MySQL 验收。若未来要求把物理列统一改存 UTC，仍需单独的数据转换窗口，不能只改连接参数。
6. Flue SQLite→MySQL 迁移器已实现并通过隔离冒烟，但目标部署机的实际 Flue 历史库仍未取得。JW Runtime、Radar MySQL 状态权威源和本地旧端口退场已完成；Radar JSON/XLSX 仍作为每次任务前由 MySQL 重建、任务后回灌的 Python 适配器工作介质，尚缺原始文件归档和生产恢复演练。浏览器遗留状态与生产端口/systemd 退场仍待现场取证。
7. 主项目依赖审计报告 16 个漏洞，Agent 子项目报告 15 个漏洞；未自动执行可能产生破坏性升级的 `npm audit fix --force`。
8. 开发机公网出口曾变更并使 MySQL 精确 Host 账号失效；已有不放宽权限的安全轮换工具，但正式环境仍必须使用固定出口、VPN 或私网从根源消除漂移。

2026-08-11 项目原件缺失处置复核：用户明确确认剩余 17 个项目原件不存在并批准忽略。版本化决定清单以精确文件 ID 集合及 SHA-256 `a32ac70dbbd87954ff53a4dbfb30fdd7a68c4c915e73eb355df63e5b19556ec3` 锁定范围；事务内写入 1 条成功 `project-file-missing-disposition` Run 和 17 条逐项 `PROJECT_FILE_SOURCE_FILE_MISSING_APPROVED` 台账，幂等复跑新增 0。处置不修改项目文件元数据、不制造原件字节、不删除业务记录，并保留未来补传能力。业务不变量现报告 97 条储备详情、0 条未处置项目原件和 7 个 AI 产物源文件；17 条不再阻断，但其他生产源、文件根、7 个 AI 产物及正式签字仍使 `fullMigrationReady=false`。
