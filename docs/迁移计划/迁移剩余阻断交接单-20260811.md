# 迁移剩余阻断交接单（2026-08-11）

## 当前结论

- 目标代码、MySQL Schema 和启动入口已经收敛为 **1 个项目业务服务**：`cybernaut-app.service` / `npm run start:app`，只监听 `127.0.0.1:3100`。
- MySQL、Nginx/Ingress 和启用 AI 时的模型网关是外部基础设施，不计入项目业务服务数量，也不会由应用自动启动。
- 当前 3100、3584、8121 均无监听；因此可以确认旧 3584/8121 常驻服务已退场，但不能把停机状态写成“当前单服务已运行”。未经明确授权，不启动 3100。
- 当前目标库为 MySQL 8.0.36，81 张业务表加 1 张内部迁移台账表、133 个外键、40 个迁移版本、69,118 行；目标结构完整，但 `fullMigrationReady=false`。
- 严格切换聚合门禁当前有 11 个阻断码；核心清单 297 项中完成 232 项、未完成 65 项，其中 P0 60 项、P1 5 项。另有 27 项按本批次范围延期，9 组例外均待正式负责人、期限和批准人；6 个 P0 安全延期和 2 个已勾选但未执行的安全冒烟合计 8 项，仍明确阻断生产。生产冒烟另有 6 项未完成，最终验收确认 13 项均未签署。
- 固定演示/弱口令账号生成器已永久退役，不再生成账号，也不得恢复。现存 5 个正式账号的密码轮换是独立安全事项，不能通过恢复生成器或跳过审计处理。
- 17 个缺失项目原件已经依据用户的精确批准落账，不再是阻断项。
- 按用户要求，本轮不执行 PPT、尽调、提案、Q&A 和合规性说明报告生成测试；相关金样、逐页渲染和原生环境验收保持延期，不以伪造结果关闭。

## 十一个严格切换阻断

| 阻断码 | 当前证据 | 所需外部输入或授权 | 输入取得后的安全入口 | 主要关联项 |
|---|---|---|---|---|
| `CURRENT_SINGLE_SERVICE_NOT_READY` | 2026-08-11 获授权的最新完整启停验收已证明1个Node业务进程承载Web/API/JW/Socket/Worker/调度、11个组件健康且仅监听3100；验收按设计自动停机，当前三个端口再次无监听 | 正式切换时允许目标服务持续运行并执行只读观察；一次性本机验收不能代替当前生产运行态 | 启动前 `npm run check:single-service-prestart`；目标已运行后使用 `npm run accept:single-service-runtime:observe`，并在Linux执行现场取证 | `ENV-017/018`、`CUT-005/022`、`MIG-0916/0929` |
| `INVARIANT_AI_ARTIFACT_SOURCE_FILE_MISSING` | 7 个旧 AI 产物只有归属/任务/元数据，可信源字节仍缺失并保持隐藏归档态 | 数据负责人提供可信源文件，或在 0600 私有底稿中批准 `approved-permanent-archive`；“不测试报告生成”不能代替历史处置批准 | `npm run validate:source-gap-dispositions:strict`，再依次执行 `npm run apply:source-gap-dispositions:preview` 和 `npm run apply:source-gap-dispositions` | `FILE-006/012/014`、`DATA-005/007/018/023`、`MIG-0407/0723` |
| `INVARIANT_LEAD_RESERVE_MISSING_DETAIL` | 97 条储备记录保留原行、源键和问题台账，标记 `source_missing`，未导入也未猜测补全 | 数据负责人补回可信 `detail_json`，或在同一 0600 私有底稿中批准 `approved-permanent-quarantine` | 与上一项共用严格验证、preview 和 apply；正式应用前必须同时核对两个完整集合及行指纹 | `LEAD-026`、`DATA-018/019`、`MIG-0507` |
| `P0_ACCEPTANCE_ITEMS_PENDING` | 60 个 P0 仍未完成；这是聚合阻断，不是单独的数据错误，包含本表其他阻断、生产验收和用户明确延期项 | 业务、数据、安全、运维和测试按责任域提供批准、环境与现场证据 | 每个责任域完成后运行 `npm run check:migration-checklist-status`；全部关闭前 `npm run check:cutover-readiness` 应继续退出 2 | 全部 60 个待完成 P0；重点见下节 |
| `DEFERRED_PRODUCTION_BLOCKERS_REMAIN` | 6 个 P0 安全项和 `SMOKE-012/017` 两个已勾选但未执行的安全冒烟，合计 8 项；结果明确为“未安全验收”“不代表冒烟通过”或“生产启用前必须完成” | 安全/技术负责人完成生产 TLS/Cookie、现存弱密码、日志敏感信息、旧凭据拒绝、密钥库/运行环境、会话攻击、密钥轮换和旧共享密钥吊销取证 | 完成真实验收后把对应权威清单行改为通过证据；仅补例外批准不能清除此阻断 | `AUTH-014/016`、`SEC-011/012/013`、`CUT-021`、`SMOKE-012/017` |
| `UNAPPROVED_ACCEPTANCE_EXCEPTIONS_REMAIN` | 9 组例外仍使用待指定负责人、无 ISO 日期期限或正式批准人待补 | 为每组例外补稳定负责人、明确 `YYYY-MM-DD` 截止日期和正式批准身份 | 修改权威清单例外表后运行 `npm run check:migration-checklist-status`；聚合器只读取数量，不输出身份或例外正文 | `EXC-20260811-01～09`、最终 P1/安全/运维签字 |
| `PRODUCTION_SMOKE_ITEMS_PENDING` | 生产冒烟仍有 6 项未完成；它们不属于 297 个核心表格项，旧聚合器不会由 `pendingP0` 自动覆盖 | 在批准的生产/预生产窗口执行非排除范围的真实冒烟；被用户排除的专业报告/PPT链路保持延期，不在本轮强行执行 | 更新权威清单后运行 `npm run check:migration-checklist-status` 和严格切换门禁 | `SMOKE-005/006/007/008/013/018` |
| `FINAL_ACCEPTANCE_CONFIRMATIONS_PENDING` | 最终验收 13 个汇总结论全部未勾选，包含数据、安全、运维、业务、PONR 和单服务拓扑签字 | 对应责任人依据底层证据逐项确认；不得因核心行数量下降自动代签 | 更新权威清单最终确认区后复跑清单及切换门禁 | 最终验收结论 13 项 |
| `PRODUCTION_SOURCE_INVENTORY_NOT_APPROVED` | 本机候选和备份已哈希锁定，但没有目标生产机的完整源根批准报告 | 数据/运维负责人提供全部生产扫描根、唯一 PostgreSQL dump、所有 Flue 源文件及稳定审批身份 | 在生产机运行 `npm run inventory:production-sources -- --environment production --scan-root <根> --postgres-dump <文件> [--flue-source <文件> ...] --approve --approved-by <稳定身份>`，发现未声明源或符号链接会失败关闭 | `PRE-009/012`、`DB-018`、`DATA-004/014/016`、`MIG-0702/0708/0715/0716/0717`、`GATE-M0-06` |
| `TARGET_LINUX_DEPLOYMENT_EVIDENCE_NOT_READY` | 静态取证契约 11 项通过；macOS 本机不能替代目标 Linux，且没有 24 小时内的成功现场报告 | 预生产和生产 Linux 访问权限；唯一 systemd 单元、Nginx/TLS、cron/timer、容器、cgroup 和环境权限须可完整只读检查 | 部署并健康后在每台目标机运行 `npm run capture:target-single-service-evidence`；聚合门禁只接受 24 小时内 `live-target-linux` 成功报告 | `ENV-012/013/017/018`、`CUT-005/010/022`、`MIG-0929`、`GATE-M9-04` |
| `WEAK_PASSWORD_ACCOUNTS_REMAIN` | 目标/迁移/弱口令账号为 5/5/5，明文或无效哈希为 0；0600 私有清单就绪，数据库写入 0 | 5 个账号负责人分别在隐藏终端输入各自新密码；不得在聊天、argv、环境变量、日志或文档中提供密码 | 从 `.runtime/migration-decisions/weak-password-rotation-roster.json` 读取逐账号命令，运行 `npm run rotate:user-password -- --email <账号>`；随后运行 `npm run check:weak-password-rotation-roster` 和 `npm run audit:password-hashes` | `AUTH-016`、`MIG-0808`、`GATE-M8-05` |

## 未单列为七个阻断码、但仍阻止全量通过的事项

1. **42 组历史重复线索**：当前共 42 组、107 次记录引用、82 条唯一线索，0600 私有底稿全部为 `pending`。需要业务负责人逐组选择 `merge`、`keep-separate` 或 `exception` 并填写理由、审批身份和时间；先运行 `npm run validate:lead-duplicate-dispositions:strict` 和 `npm run apply:lead-duplicate-dispositions:preview`，确认无冲突后才可正式 apply。不得自动选择主记录或物理删除记录。关联 `DB-005/015`、`MIG-0125/0524`、`ISSUE-006`。
2. **生产密钥轮换/吊销**：生产 Cookie、模型、OSS、GSData、IM 及历史共享密钥仍需由持有人换新并证明旧值不可用；此前在对话中暴露过的模型 API Key 必须在对应网关侧轮换，不能只修改本地文件。即使这些行按本批次范围延期，新的 `DEFERRED_PRODUCTION_BLOCKERS_REMAIN` 也会继续阻止生产切换。关联 `SEC-011/013`、`CUT-021`。
3. **专业报告测试延期**：`PRE-015`、`TASK-003/015/016/017` 及对应生产冒烟/最终签字保持未完成。这是用户批准的本轮测试范围排除，不是功能已经通过，也不影响 7 个旧 AI 产物必须单独处置的结论。
4. **真实批准插件与外部渠道已退出本批次范围**：动态 Skill、任意远程 MCP、Plugin 生命周期和钉钉/飞书/微信真实联调均已按本批次范围裁决为延期/不适用，保持关闭且不配置生产凭据；不得把安全 Mock 或零安装状态描述为完整外部验收。它们不再计入本批次待完成 P1，但未来重新开放时仍需恢复专项验收。
5. **非 P0 性能目标已退出本批次范围**：API/AI/Socket/Worker/批处理/大文件和文件扫描的生产容量门槛已按本批次范围延期；已存在的本地机制证据保留，但不得外推为生产容量承诺。CDC 恢复和子进程故障隔离等 P0 性能项仍在清单中。

## 最小继续输入清单

要继续从当前状态推进，至少需要按实际可提供范围给出以下输入；无需在聊天中提供任何密码或密钥：

1. 是否允许在本机启动一次唯一 3100 服务做当前构建运行验收，或指定可操作的预生产环境。
2. 目标 Linux 的部署/取证执行权限，或由运维执行后回传脱敏 `live-report.json`。
3. 生产源根、唯一 PostgreSQL dump、Flue 源清单及数据/运维负责人批准。
4. 5 个正式账号由各自负责人完成隐藏输入密码轮换。
5. 业务负责人完成 42 组重复线索私有底稿裁决。
6. 数据负责人分别批准或补回 97 条储备详情和 7 个 AI 产物源字节。
7. 安全负责人完成生产密钥轮换、旧值吊销和拒绝旧请求取证。

上述任一输入到位后，只推进对应责任域并复跑只读聚合；未取得其他输入时，不改写其结论。

## 本地可执行性终审

2026-08-11 最终只读审计把当前 65 个核心未完成项按互斥口径完整分类，集合无重复、无未知 ID、无遗漏：

| 类别 | 数量 | 当前边界 |
|---|---:|---|
| 用户明确排除的专业报告/PPT生成或相关环境验收 | 5 | 本轮不执行；不得用假结果勾选 |
| 需要业务裁决、数据负责人处置或可信源文件的项目 | 12 | 42组重复线索、97+7源缺口及其关联约束/文件/数据项；工具已就绪但决定均为pending |
| 需要生产/预生产环境、启动授权、正式负责人/签字、生产源、密钥或账号持有人操作 | 48 | 本机继续执行不能产生所需范围的证据 |

非核心部分另有 6 个未完成生产冒烟和 13 个最终确认；8 个已勾选延期项仍明确阻断生产，9 组例外均未正式批准。三份 0600 私有底稿复核仍为重复线索 42/42 pending、源缺口 2/2 领域 pending、弱密码 5/5 未轮换，数据库写入均为0。当前没有剩余的未授权本地实现动作可以真实关闭这些项目；继续需要上节任一外部输入。

## 复核命令

完成任一责任域后依次执行：

```bash
npm run check:migration-checklist-status
npm run audit:cutover-readiness
npm run check:cutover-readiness
```

前两项用于刷新清单和脱敏聚合报告；最后一项在仍有任何阻断时退出 2 是正确行为。全过程继续排除 PPT、尽调、提案、Q&A 和合规性说明报告生成测试。
