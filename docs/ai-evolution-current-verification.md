# 自进化当前验证范围

> 当前状态以 `ai-evolution-change-manifest.json` 和 `docs/ai-evolution-acceptance-matrix.md` 为准。本文件后续段落保留各阶段当时的历史记录，其中出现的“最新为 0107”等结论已经被后续迁移替代；当前源码最新迁移为 `0110_add_ai_evolution_requested_rollback`。

本记录补充历史完成审计，不将局部测试视为原设计文档 AT-01 至 AT-18 全部通过。当前工作仍在隔离工作树，业务数据库未迁移，业务服务未重启或发布。

## 本次集中回归

2026-09-08 本地发布准备适配器：prepareLocalEvolutionRelease 调用既有 build-platform 纯暂存入口，从当前 live 目录冻结旧运行身份、从暂存候选读取新运行身份，并在前后复核授权、候选指针与文件身份；该阶段不激活、不停服务。实际临时目录暂存、含糊命令输出拒绝及发布包恢复共 3 PASS、0 FAIL。随后 buildReleaseAcceptance 在临时目录执行真实监听阻止激活、候选摘要篡改拒绝、停服状态激活、回退和失败版本保留，返回 ok，productionFilesChanged=0、processMutation=false。Windows 不具备 POSIX chmod/stat 权限位语义，脚本改为核对服务账号的实际目录/文件访问，验收对回退指针核对当前账号读写；POSIX 路径仍检查 Web 搜索/读取权限和指针 0600。此验收只操作临时发布目录，没有停启业务服务。

2026-09-08 发布运行身份绑定：EvolutionReleaseReceipt 现在必须包含候选运行身份，以及存在旧版本时对应的旧运行身份；字段包括基线提交、补丁摘要、依赖锁摘要、服务入口摘要和 Web 入口摘要。协调器在领取批准或执行部署操作前统一解析，MySQL claim/read/complete 共用相同校验；代码候选还核对候选身份与已验收 manifest。恢复适配器只使用 receipt 中的持久化身份，不再接受调用方另传预期身份。发布协调、恢复与本地适配器测试 13 PASS、0 FAIL，后端类型检查通过。旧格式的未完成领取记录缺少身份时会保持恢复失败并要求人工核对，不猜测当前磁盘版本；隔离 MySQL 因 Docker 当前不可用，新增持久化字段的数据库路径尚未重跑。

2026-09-08 发布恢复扫描器补充：候选仓库新增有界读取已领取且仍处于 activating 的发布批准，按批准 ID 分页；createEvolutionReleaseRecoveryWorker 串行处理记录、合并重叠 tick、跳过单条暂时失败以继续本页，并在下一轮重新扫描失败项，停止时等待在途恢复。恢复调度及恢复流程测试合计 5 PASS、0 FAIL，后端类型检查通过。当前只完成扫描和调度核心，尚未在业务主服务启动；正式方案要求它运行在可停启业务服务的独立发布进程中，避免业务服务停掉自己后无法完成激活与恢复。未选择或接入真实进程控制前，不将此项视为 AT-09/AT-17 完成。

2026-09-08 进度断线恢复补充：AiEvolutionRuns 对网络错误与服务端暂时故障保留最后确认记录，并明确标注未刷新、暂禁操作；权限或对象访问错误仍清空记录。Edge 390px 夹具依次模拟正常进度、网络失败、重新连接后的完成状态和 403 权限撤销，验证记录保留及提示、操作禁用、自动更新和撤销后清空，浏览器检查通过且截图已查看；前端类型检查通过。普通聊天经验快照已接入，原生 skill-creator 的历史版本追踪仍须单独验证，本次 UI 修复不代替该项。

2026-09-08 重评刷新重试修复：浏览器测试加入首次请求失败后的 page.reload，原随机键在刷新后变化，断言先失败。组件改为对候选 ID、候选摘要、环境和新基线计算稳定 SHA-256 幂等键；同一重评刷新后复用，绑定变化自然使用不同键。原浏览器脚本再次运行通过，覆盖刷新后重新加载方案、同键重试和列表仅刷新一次。该修复不依赖浏览器保存提案状态，成功仍以服务端响应为准；数据库组合仍待恢复。

2026-09-08 真实 Git 重评验证：提取生产调用的 readEvolutionReevaluationBase，在临时 Git 仓库创建两次提交，验证分支推进后返回新 SHA、脏工作区拒绝、开发仓库缺少目标提交拒绝、非法 ref 拒绝。函数结束前再次读取目标 ref，检查期间变化返回冲突。该测试与规格测试共 7 PASS、0 FAIL；不修改项目仓库的提交或工作区。Docker 引擎仍不可连接，Git/HTTP/MySQL 整体组合尚未验证。

2026-09-08 重评页面入口：代码候选支持查询授权环境、核对新旧提交、创建重评提案并刷新会话提案列表；相同候选/环境/新基线的失败重试复用幂等键。Edge 390px 宽度夹具使用真实组件及项目 styles.css、模拟 API，验证完整交互、一次失败后的同键重试、成功仅刷新一次，以及基线展示无水平溢出；两张截图已人工查看，前端类型检查通过。该浏览器证据不覆盖实际 Git/HTTP/MySQL 组合，相关环境验证仍待 Docker 恢复。

2026-09-08 新基线重评入口：新增 reevaluation-plan / reevaluation 接口，宿主解析目标 Git 分支为固定提交，要求干净目标工作区及开发仓库包含该提交，并复核仓库、环境和来源授权。创建新提案保留原需求、范围、预算和验收条件，只改变固定基线；不复制旧评估或批准，不自动执行模型。规格与发布策略测试 7 PASS、0 FAIL，后端类型检查通过。新增路由已加入真实会话/CSRF/Origin 夹具，但本轮运行失败于隔离 MySQL 127.0.0.1:43318 ECONNREFUSED；随后 docker ps 报 Docker 引擎管道不可用，故该集成检查未完成。新基线真实 Git/HTTP 组合和重新执行完整门禁、前端入口仍待验收，不将 AT-16 标记完成。

2026-09-08 本地恢复适配器补充：createLocalEvolutionRecoveryAdapter 读取实际 dist/server-dist 文件身份，核对健康接口身份，并在停服前后验证 build-rollback.json 与旧构建备份；仅调用宿主提供的停服、既有回退脚本和启动接口，拒绝错误 receipt、错误指针、非候选文件及取消信号。临时文件测试验证候选/旧版本识别、健康身份不匹配、错误指针阻止停服、取消阻止停服及恢复后的旧文件身份；与恢复流程测试合计 4 PASS、0 FAIL。停启与回退命令仍为测试注入，未运行真实服务控制，独立发布进程方案等待用户选择，不能据此宣布 AT-17 完成。

2026-09-08 恢复宿主流程接线：recoverAiEvolutionRelease 将真实候选/来源及环境授权、实际目录会话锁、已领取记录读取、适配器核验和数据库完成记录串联；只接受 approvalId 与环境字段，适配器由可信宿主注入。完成记录的重复恢复不调用部署适配器。runEvolutionReleaseRecovery 在外部操作前后复核权限和锁，检查期间撤销或失锁不记录成功。恢复组合、协调器、健康检查共 12 PASS、0 FAIL，后端类型检查通过。尚未注册实际部署适配器或启动恢复调度；当前测试使用模拟部署状态，不能解释为真实发布或真实进程故障演练完成。

2026-09-08 发布互斥组件补充：withEvolutionReleaseLock 使用实际目录路径的摘要作为 MySQL 会话锁名称；固定连接持锁，竞争请求立即拒绝，并提供执行权复核和失效信号；未知连接状态销毁连接，避免带锁连接回到池。隔离 aiEvolutionReleaseLockMysql.test.ts 验证同一实际路径互斥、不同目录并行、异常释放后重取、释放底层锁后执行权复核拒绝且不返回成功，1 PASS、0 SKIP。组件要求同一目标的所有发布者使用同一协调库，尚未接入实际部署适配器；执行中的外部命令是否能及时终止仍需适配器和故障演练证明，不能用会话锁测试替代该证明。

2026-09-08 已领取发布记录读取补充：MySqlAiEvolutionCandidateRepository.readClaimedRelease 从批准、候选、运行和 release_claimed 事件读取绑定的 receipt；要求真实操作者与运行所有者、目标环境、消费状态和候选摘要一致，记录唯一且状态可恢复。隔离 aiEvolutionMysql.test.ts 覆盖未领取拒绝、外部用户拒绝、错误环境拒绝、事件摘要篡改拒绝、已领取批准过期后仍可核对及完成后状态读取，1 PASS、0 SKIP；原并发领取与幂等完成同时通过。该夹具运行前确认无活动进化 run 后清理固定隔离库中的旧合成进化记录，不触碰业务库。恢复调度、目标串行锁和实际部署适配仍未接线。

2026-09-08 发布恢复组件补充：recoverEvolutionRelease 接收宿主读取的已领取发布记录，核对实际版本及健康状态；健康候选只补记状态，不重复领取或激活；不健康候选仅在旧版本可确认时回退，并再次核对；未知或核对期间变化的目标保持未确认；补记数据库失败不会触发回退。发布协调器与健康检查测试共 9 PASS、0 FAIL。当前仍缺目标级串行锁、已领取记录的恢复调度与实际部署适配器接线，此组件测试不代表真实重启恢复、AT-09 或 AT-17 验收通过。

2026-09-08 最新回归：aiEvolution/aiExperience 与上传模板快照测试合计 142 项，130 PASS、12 SKIP、0 FAIL；跳过项需要显式启用隔离 MySQL、Docker 或构建夹具，未计为通过。后端类型检查通过。另外单独启用隔离 MySQL、真实 HTTP 及项目范围夹具运行 aiEvolutionSkillVersionMysql.test.ts，1 PASS、0 SKIP：新增 compliance_statement / custom_template_document 实际内置包的首次冻结、只读读取、重试包哈希一致和错误任务类型拒绝；原项目消费者权限撤销、恢复、试用、并发激活与回退断言同时通过。任务行由测试夹具创建，评估仍为合成数据，未调用真实报告模型，不能视为端到端报告交付验收。

数据库边界脚本、生产发布门禁契约脚本和迁移清单状态脚本已运行。前两者返回 ok；迁移清单脚本的 ok 仅表示清单结构检查通过，其输出仍有 65 个核心待办、8 个阻止生产的延期项、9 个未批准例外，不能解释为可发布。此检查不读取业务库，不能证明业务库已应用迁移或共享原工作树状态一致。单服务边界脚本依赖 `/bin/bash`，本轮未执行该脚本。

2026-09-08 发布任务持久化补充：源码迁移 0107 新增 ai_evolution_release_jobs，按操作者和幂等键唯一，并将一个 release 批准唯一绑定到一个任务；保存状态、receipt、尝试次数和带 fencing token 的租约。POST /candidates/:id/release 在重新核对候选归属、仓库及环境授权后只入队并返回 202，不在业务服务进程中执行停服或文件切换。入队事务锁定批准、候选和运行，检查代码候选、批准用途、环境、期限和消费状态，仅首次请求写 release_queued 事件及审计。静态迁移契约测试 1 PASS、0 FAIL，前后端及服务端类型检查通过。隔离工作树迁移 journal 当前共 108 项，最新为 0107；0107 只存在于源码，未应用到业务数据库，Docker 不可用期间也尚未在固定隔离 MySQL 中执行。独立发布领取、租约续期、实际激活及任务终态同步仍待完成。

2026-09-08 独立发布进程源码补充：新增 start:ai-evolution-release-publisher 入口，启动时先扫描已领取但未完成的发布，再轮询 release jobs。准备阶段从已批准产物中识别并复核构建路径索引，物化到临时目录，调用纯暂存构建并持久化包含新旧运行身份的 receipt；租约按 fencing token 续期，准备失败最多三次且保存受限错误摘要。dispatch 在目标级 MySQL 锁内重复检查用户、候选、仓库、实际 Git 提交、干净工作区和目标目录，再消费一次性批准。消费与任务状态写入之间崩溃时，相同 receipt 可幂等恢复。激活适配器只使用可信配置中的固定 argv 停启命令以及项目固定 build-platform --activate/--rollback，停服前核对暂存指针和文件身份，切换或回退后总会尝试恢复服务，再用实际文件和健康接口身份决定成功、失败或回退。恢复完成同时同步候选与 release job 终态。针对性测试 18 PASS、0 FAIL，类型检查通过；这些测试使用临时文件及注入的生命周期命令，尚未在服务器服务管理器上执行真实停启和故障演练，因此不能把 AT-17 标记完成。

publisher 健康判定已从单次探测改为：先确认目标文件身份，再在默认 120 秒内等待 `/api/health` 返回同一部署身份和 ready；期间实际文件变化或租约取消会立即结束，避免 systemd 刚启动时的短暂 starting 被误判为回退条件。新增 systemd、发布目标及生命周期配置示例和只读 preflight；preflight 要求 0107 全部列、请求/批准唯一索引及租约索引存在，并核对两个配置的目标集合。示例契约与 preflight 单元测试 2 PASS、0 FAIL，健康及适配器回归 12 PASS、0 FAIL。示例未安装到服务器，也没有取得服务器 root/systemd 权限。

2026-09-08 正式发布页面补充：代码候选页面新增目标环境选择、一次性发布批准、二次确认入队和发布状态刷新；公开状态响应不返回批准 ID、receipt、租约持有者或 fencing token。发布按钮只在候选已验收时出现，入队后显示 queued/preparing/prepared/activating/succeeded/failed/rolled_back 及受限错误信息。Edge 390px 真实组件夹具验证明确批准、首次入队 503 后同一幂等键重试、queued 状态、候选只刷新一次且无水平溢出；截图 release-mobile.png 已人工查看，布局和状态文案可读。前后端类型检查通过；该浏览器夹具模拟 API，真实浏览器到服务器数据库与独立 publisher 的组合验收尚未完成。

- 自进化及经验测试：138 项，127 PASS、11 SKIPPED、0 FAIL。日志位于 `.runtime/evolution-ui/current-regression.log`。显式环境测试未包含在该次通过数量内。
- 新增真实 Express HTTP 鉴权测试：9 个候选读取/登记/试用/审批/回退/产物端点，在没有登录会话时均返回 401/AUTH_REQUIRED。使用实际 requireAuth 与 aiEvolutionRouter，监听随机本机端口，结束后关闭。此结果只证明未登录边界，不证明已登录用户的 CSRF、Origin 或跨项目权限。
- 隔离 MySQL 版本测试已覆盖登记、审批消费、试用、回退、并发幂等、基线包冻结和重试继承；单连接池下授权回调查询数据库不再因只读事务保留连接而阻塞。候选评估为合成输入，不证明真实技能质量改善。
- Q&A、尽调、投资提案的原技能及冻结技能执行验收通过；错配技能包在清理任务目录前拒绝。PPT 原技能及冻结技能验收通过。上述执行验收使用模拟模型响应，仅确认文件传递和执行协议。
- 技能包支持单文件 8 MiB、总包 16 MiB，包含 PPT 技能已有约 6.8 MB 示例文件。保存和登记前验证重建能力，任务使用快照中的正文、参考资料和运行文件。
- 版本登记、试用预览、审批启动、同键重试、回退、刷新状态及撤权清除已做浏览器模拟 API 验证。正式生效与回退另有下述真实浏览器、HTTP 与隔离数据库的组合证据。

## 尚未证明完成的关键范围

- 不同项目之间的完整业务任务隔离及完整产品页面组合验收仍未完成。个人和项目范围的来源撤销、审批、试用切换与回退已有下述真实 HTTP 证据。
- 完整任务从聊天创建到报告交付的真实数据库与模型联调；当前直接报告分支已传入冻结包，但不能据此把 AT-12 标为 PASS。
- 上传模板与合规性说明均已接入技能快照，但完整生成验收仍未完成。普通聊天的技能策略仍需核验。当前 `usesDirect*` 为按任务类型固定返回，投资提案、PPT、Q&A、尽调的旧生成分支不应再重复计为实际入口缺口。
- 真实模型、真实可见/隐藏样本、内容事实与数值、语义检查、视觉检查以及可测量质量改善。
- 代码发布适配、合并基线变化后重评、故障演练。试用到期调度及正式生效接口已有下述隔离验证，业务服务尚未启用。
- 完整自然语言入口和经验输出遵守验证，以及 AT-01 至 AT-18 的逐项实际证据。

后续完成判断仍以用户原始设计文档为准，不能用本文件缩小目标。

## 正式生效验证补充

已实现正式生效方案、独立批准和幂等执行接口，使用 `skill-promotion` 摘要绑定原试用的作用域、版本、回退版本与 revision。事务锁定并检查未到期的绑定，消费批准，清除期限并保留回退版本，记录 promotion 历史。隔离 MySQL 与真实 HTTP 分别覆盖三次并发正式生效只提交一次，以及正式生效后手动回退。真实 HTTP 夹具启用 `EVOLUTION_SKILL_HTTP_FLOW_TEST=true` 与 `EVOLUTION_SKILL_HTTP_PROMOTION_TEST=true`，候选及评估仍为合成输入。

新增三个正式生效接口已纳入真实登录会话测试：无会话返回 401，缺少 CSRF 或跨来源请求返回 403，合法会话的非法输入返回 400。该测试使用 `EVOLUTION_ISOLATED_HTTP_TEST=true`，结果 1 PASS、0 FAIL。权限测试总计覆盖 12 个未登录路由和 8 个写路由的 CSRF/Origin 边界。

页面支持查看正式生效方案、明确批准、同键重试及成功后刷新状态；浏览器模拟 API 验证确认重试不重复批准，正式生效后保留回退入口。此证据不代替真实浏览器到后端的组合验收。

随后使用 `EVOLUTION_SKILL_BROWSER_TEST=true`（同时启用隔离 MySQL 和 HTTP flow 标志）完成真实浏览器组合验证：Edge 加载实际状态组件，使用真实登录/CSRF Cookie，调用实际 Express 路由与隔离 MySQL，点击查看方案、批准正式生效、刷新页面、回退。数据库历史断言为 trial revision 1、promotion revision 2、rollback revision 3；1 PASS、0 FAIL。证据图保留在临时运行目录 `evolution-version-mysql-qfI0rX/real-browser-promotion-rollback.png`。组件由测试 HTML 承载且没有产品样式，不能据此证明完整产品页面布局或聊天状态验收。

## 任务加载权限与执行边界

2026-09-08 上传模板技能接入补充：worker 在解析上传模板前冻结 generate-document-from-template 包，再将校验后的 LoadedAiSkill 交给模板解析服务；有快照时不加载当前磁盘技能。创建重试任务使用只读快照读取接口，沿用原任务技能版本，并保留原项目、会话、能力与来源权限检查。模板解析器拒绝技能名称不匹配。新增实际内置技能包加载测试覆盖指令/引用/摘要，以及错误技能和篡改内容拒绝。相关回归 9 PASS、0 FAIL，后端类型检查通过。此证据不含真实上传任务从创建到文档交付的模型联调；下段记录为技能接入前的文件完整性补充。

2026-09-08 上传模板完整性补充：resolveAiCustomTemplateForTask 在原有归属/会话/托管路径检查后，核对实际文件字节与登记 SHA-256；不一致返回 CUSTOM_TEMPLATE_CONTENT_CHANGED。报告 worker 从再次核验的同一 buffer 创建任务临时副本并在 finally 清理，避免后续源文件变化影响执行。aiUploadedTemplateSnapshot.test.ts 验证替换源文件后副本仍保持原字节、后续加载及物化拒绝不匹配内容、无效哈希拒绝和清理，1 PASS、0 FAIL。上传模板背后的通用技能仍需接入版本快照，此补充不代表 AT-12 或完整上传模板任务验收完成。

2026-09-08 合规入口补充：任务加载及重试预检现已包含 compliance_statement。经哈希校验的包物化到独立临时目录，模板主引用、附加引用与规则引用只允许指向包内文件；章节解析及生成共用重定向后的模板，finally 清理目录。临时路径不进入长期 blueprint 缓存。aiEvolutionFrozenTemplate.test.ts 使用实际内置 DOCX 验证字节一致、实际章节解析摘要一致、越界引用拒绝及清理，1 PASS、0 FAIL。该测试不调用模型，不证明完整报告生成或版式质量验收完成。下段为接入前的分支审查记录。

2026-09-08 源码分支复核：合规性说明走 `composeComplianceStatement`，其 blueprint 在生成前从 `generate-investment-compliance-note/assets/compliance-layout-authority.docx` 读取版式和章节树。只冻结 LoadedAiSkill.instructions 不足以完成该入口：还必须让 blueprint 及后续渲染读取同一不可变包中的二进制模板，并记录包版本。上传模板独立走 resolveAiCustomTemplateForTask，需要保持上传文件权限及模板版本绑定，不能直接套用内置技能路径。下一步先完成合规入口的模板包物化与引用重定向，再接入其任务快照；不通过将合规入口改成其他报告类型来规避原业务流程。

项目范围通过真实项目访问服务与指定技能/项目的发布授权共同授权。`EVOLUTION_SKILL_PROJECT_HTTP_TEST=true` 的隔离 HTTP 夹具已验证项目试用、正式生效、回退以及撤销项目归属或发布授权后的拒绝。测试权限表按当前 Drizzle 模型生成列定义，不是业务迁移，也不等同于全套数据库约束验收。

项目消费者夹具建立另一个真实用户、项目成员记录和报告任务，调用实际 `freezeTaskAiEvolutionSkills`：加载发布者共享版本；无法读取发布者候选管理数据；撤销成员资格后恢复任务被拒绝；恢复成员资格后快照不变；修改能力工具列表后返回 `EVOLUTION_SKILL_PERMISSIONS_CHANGED`；实际 retry_of_task_id 关系的新任务继承原快照。此结果覆盖任务加载与恢复，未运行真实报告模型。

报告任务加载快照后复核当前工具权限、配置和能力依赖哈希，配置变化或技能停用拒绝加载旧快照；针对性策略测试与服务端类型检查已通过。当前直接报告执行器通过模型消息检查点观察取消状态，尚未证明能立即终止已启动的工具调用，不把加载阶段的权限复核等同于运行中强制撤销。真实模型效果验收尚待明确模型与调用预算，未执行付费模型调用。

## 到期调度验证细节

主服务启动/关闭流程已接入技能到期 worker，功能开关启用时每 30 秒扫描一页（最多 100 条），失败条目不阻塞后续页。到期回退在锁定绑定后重新检查期限与 revision，校验基线产物，原子更新版本指针、清空试用期限并记录 `expiry` 历史及 `skill_trial_expired` 审计。已有任务快照不改写。启动期间收到关闭信号时不再创建 worker。

`aiEvolutionSkillExpiry.test.ts` 与隔离模式 `aiEvolutionSkillVersionMysql.test.ts` 合计 3 PASS、0 FAIL、0 SKIPPED；包括真实数据库到期查询、提前触发无变更、产物缺失保留状态、并发幂等，以及启动 worker 后恢复基线。调度集成夹具仅处理自己创建的绑定，因为隔离库保留的其他测试运行使用不同临时产物根目录。服务端类型检查通过。未启动或重启业务服务，不将这些结果视为实际部署验收。

## 已登录 HTTP 补充验证

`aiEvolutionHttpAuth.test.ts` 在隔离库按已有迁移定义创建用户与会话测试表，通过实际 `createAuthSession` 建立会话，挂载真实鉴权中间件和自进化路由。五个写端点分别验证缺失 CSRF 返回 CSRF_INVALID、跨源加正确 CSRF 返回 ORIGIN_FORBIDDEN、正确会话和 CSRF 到达输入校验返回 INVALID_ARGUMENT；已登录读取不存在候选返回 404；停用测试账号后原会话返回 AUTH_DISABLED。测试结束清理自己的账号与会话，不涉及业务账号或真实密钥。

## 个人技能试用真实 HTTP 链路

2026-09-07，在 `127.0.0.1:43318/evolution_isolated_test` 上启用 `EVOLUTION_ISOLATED_MYSQL_TEST=true` 和 `EVOLUTION_SKILL_HTTP_FLOW_TEST=true`，执行 `node --import tsx --test server/tests/aiEvolutionSkillVersionMysql.test.ts`：1 PASS、0 FAIL、0 SKIPPED，退出码 0。

该模式通过 `fixtures/evolutionAuthorizedHttp.ts` 使用真实登录会话、CSRF、路由、服务和 MySQL，验证基线及候选包登记、重复登记、候选批准、试用方案、发布审批、三次同键并发启动、状态读取、三次同键并发回退。每组并发只有一次新操作，历史记录恰为试用 revision 1 和回退 revision 2，回退后指向基线。撤销发布授权后方案请求返回 403；删除来源消息后候选读取返回 403，恢复消息后读取成功。

候选与评估来自合成夹具，不证明真实模型质量提升；该模式提前结束仓储测试后续分支，不能把这次结果同时计为完整仓储回归。业务数据库与运行服务未改动。
