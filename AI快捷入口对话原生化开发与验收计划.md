# AI 快捷入口对话原生化开发与验收计划

> 文档版本：V1.0
> 编制日期：2026-08-22
> 适用项目：`/Users/hyw/Desktop/sbl_jedi`
> 当前状态：待评审
> 核心目标：将文档快捷入口改造成“预选 Skill 的普通 Agent 对话”，取消中央任务卡展示，同时保留后台长任务的持久化、恢复、取消、审计和产物交付能力。

## 1. 背景与问题

当前 AI 助手存在两条用户可感知的执行链路：

1. 普通对话通过 `send()`、`agent.sendMessage()` 和 JW Agent Runtime 执行，在聊天流中展示消息和工具过程。
2. 文档快捷入口通过 `runQuickTask()` 直接创建 `/ai/tasks`，再由 `AiTaskCards` 在聊天区域展示任务卡、阶段、百分比和下载入口。

两条链路造成以下问题：

- 快捷入口不像普通 Agent 调用 Skill，而像独立的任务管理产品。
- “补充项目数据或写作要求”与普通输入框割裂，当前会话语义不直观。
- 固定百分比不能反映 Agent 实际完成了哪些工作。
- 执行过程、失败原因、继续生成和后续修改无法自然地形成多轮对话。
- 对话下载入口和右侧产物中心在交互上重复，但缺少统一的产物身份。
- 旧任务卡契约限制了后续新增 Skill 快捷入口的扩展方式。

## 2. 产品原则

### 2.1 核心原则

快捷入口不是另一套文档任务系统，而是普通 Agent 输入框的 Skill 预设器。

点击快捷入口后的产品语义应当是：

```text
点击快捷入口
  → 预选对应 Skill
  → 聚焦普通输入框
  → 用户补充要求或附件（可选）
  → 点击普通发送按钮
  → Agent 在对话中直接执行并展示真实过程
  → 完成后在回复中提供下载
  → 同一产物同步显示在右侧文件栏
```

### 2.2 执行原则

- 正式文档必须由对应 Skill Agent 直接生成。
- 不修改任何 Skill 的 `SKILL.md`、references、scripts、assets 或模板。
- 不恢复旧 Plugin、旧模板编排或宿主兜底稿。
- 平台只负责准备资料、持久化执行、展示事实、恢复任务和登记产物。
- 业务质量、内容结构和验收结论由 Skill 与 Agent 自身规则决定。
- 不用固定百分比推测进度，只展示已真实发生的工具阶段、文件阶段和产物阶段。
- 投资提案必须读取当前项目全部可用资料和全部可用片段；完整资料研读失败时应明确失败，不得用抽样结果伪装完成。

### 2.3 长任务原则

界面采用普通对话体验，后台继续保留持久任务能力。不得将可能持续一至两小时的文档生成完全依赖单次浏览器 WebSocket：

- 浏览器刷新或断线后任务继续执行。
- 服务重启后可依据租约和检查点恢复。
- 用户能够取消、继续或从失败检查点重建任务。
- Token、模型调用、资料覆盖、产物和错误均可审计。
- 后台 `ai_task` 是实现细节，不再以任务卡形式出现在中央聊天流。

## 3. 范围

### 3.1 本期包含

| 快捷入口 | 预选 Skill | 任务类型 | 默认产物 |
|---|---|---|---|
| 合规性说明 | `generate-investment-compliance-note` | `compliance_statement` | DOCX |
| 投资提案 | `draft-investment-proposal` | `investment_proposal` | DOCX |
| 投资建议书（PPT） | `investment-committee-ppt` | `investment_recommendation_ppt` | PPTX/PDF |
| 尽调报告 | `draft-due-diligence-report` | `due_diligence_report` | DOCX |
| Q&A | `draft-investment-qa` | `project_qa` | DOCX |
| 上传模板 | `generate-document-from-template` | `custom_template_document` | DOCX/PPTX |

### 3.2 本期不包含

- 修改上述 Skill 的内部规则或模板。
- 修改模型供应商、模型选择策略或额度策略。
- 重写现有文档渲染器。
- 删除 `ai_tasks`、租约、重试、Token 或 `ai_artifacts` 等后台能力。
- 将未通过 Skill 验收的中间文件登记为正式下载产物。
- 恢复历史 Plugin 或历史模板生成链路。

## 4. 目标用户体验

### 4.1 快捷入口选择

用户点击“投资提案”等入口后：

1. 不打开独立任务表单。
2. 普通输入框上方出现唯一的 Skill 标签，例如 `Skill：draft-investment-proposal`。
3. 输入框占位文案变为“补充项目数据或写作要求（可选）”。
4. 自动绑定当前会话项目，但不把内部项目 ID 暴露在用户消息正文中。
5. 当前会话已上传附件保持可见；用户可继续增加或删除本轮附件。
6. 用户可关闭 Skill 标签，恢复普通对话。
7. 点击发送按钮后才创建消息和执行记录，单纯点击快捷入口不得创建后台任务。

### 4.2 用户消息展示

发送后，聊天记录中形成普通用户消息，至少包含：

- 目标文档名称。
- 所选 Skill 名称。
- 用户补充要求。
- 本轮附件名称。
- 当前项目绑定状态。

即使用户未填写补充要求，也应形成明确的自然语言指令，例如“使用 `draft-investment-proposal` 为当前项目生成投资提案”。

### 4.3 Agent 执行展示

中央聊天流使用普通 assistant 消息，不使用 `AiTaskCards`。建议把过程拆成可恢复的消息 part：

- `skill_selection`：已选 Skill、版本或摘要校验值。
- `document_run_event`：资料准备、工具调用、阶段变化、Token 变化和错误。
- `artifact`：正式产物、格式、版本、质量状态和下载地址。
- `run_control`：取消、继续执行或查看错误详情。

聊天区默认显示简洁阶段，详细工具日志放入“查看执行过程”折叠区。示例：

```text
正在调用 draft-investment-proposal Skill

已准备项目资料：37/37 份
已读取可用片段：1,254/1,254 条
已形成事实台账
已完成冲突事实裁决
已形成大纲
正在撰写正文
正在执行 Reviewer 审阅
正在渲染 DOCX
```

不得展示没有事实依据的固定 `82%`、`98%` 或基于耗时估算的完成比例。

### 4.4 完成与文件展示

成功后，同一条 assistant 回复追加正式产物信息：

```text
大衍科技投资提案_V18.docx
[下载 DOCX] [预览]
```

同时满足：

- 对话内下载按钮与右侧 `AiArtifactCenter` 使用同一 `ai_artifacts.id`。
- 只登记一份正式产物，不为两个展示位置复制文件。
- 对话和右侧文件栏的文件名、版本、格式、质量状态一致。
- 未完成、未通过 Skill 审阅或仅为中间稿的文件不得显示为正式下载。
- 后续修改生成新版本，并保留与上一个任务和产物的关联。

### 4.5 失败、取消和继续

失败不再显示“文档未完成”任务卡，而是显示普通 Agent 回复：

```text
生成暂时停止：模型认证失败。
当前资料准备和大纲检查点已经保存。

[继续执行] [查看错误详情]
```

行为要求：

- 取消按钮取消关联的 Agent run 和 `ai_task`，不能只停止前端动画。
- “继续执行”形成一条普通用户后续消息，并关联原任务检查点或重试链。
- 不可重试错误明确提示需要修正的资料、模型、模板或参数。
- 用户不需要重新填写已经保存的补充要求和附件。
- 取消、失败和成功终态在页面刷新后保持一致。

## 5. 目标技术架构

### 5.1 总体设计

采用“对话作为控制面，持久任务作为执行面，产物中心作为交付面”的架构：

```text
AiQuickActions
  → 设置 selectedQuickSkill
  → 复用 AIAssistantPage.send()
  → agent.sendMessage(skillName, attachments, project context)
  → Agent 调用 create_ai_task / 对应直接 Skill Agent
  → ai_tasks 持久执行、租约、检查点和审计
  → 任务事实转换为 agent_message_parts
  → MessageRow 原生渲染过程
  → ai_artifacts 同时供对话下载和右侧文件栏使用
```

### 5.2 事实源划分

| 数据 | 唯一事实源 |
|---|---|
| 用户要求、Skill 选择、本轮附件 | Agent 用户消息及其结构化上下文 |
| 执行状态、租约、取消、重试、Token | `ai_tasks` |
| 对话中的可见过程 | `agent_messages` / `agent_message_parts` |
| 正式下载文件 | `ai_artifacts` |
| 项目资料覆盖与引用来源 | `ai_task_sources` 及直接 Skill 工作区清单 |
| 业务质量判定 | Skill Agent 输出的 Reviewer/验收结果 |

### 5.3 消息与任务关联

推荐为 `ai_tasks` 增加可空关联字段：

- `trigger_message_id`：触发生成的用户消息 ID。
- `response_message_id`：承载过程和结果的 assistant 消息 ID。
- `skill_name`：本次实际绑定并调用的 Skill。
- `display_mode`：本期新任务使用 `chat`；历史任务保留兼容读取能力。

数据库迁移必须：

- 为消息关联建立外键或等价的仓储级完整性校验。
- 建立按 `conversation_id`、`response_message_id` 查询的索引。
- 不回填伪造的历史消息；历史任务继续保留原始审计数据。
- 对历史任务卡采用兼容展示或迁移策略，但新任务不得继续写入卡片专用契约。

### 5.4 消息 part 契约

建议新增或规范以下 payload：

```ts
type SkillSelectionPart = {
  type: 'skill_selection'
  runId: string
  taskId: string
  skillName: string
  skillVersion?: string
}

type DocumentRunEventPart = {
  type: 'document_run_event'
  runId: string
  taskId: string
  sequence: number
  stageCode: string
  message: string
  occurredAt: string
  facts?: Record<string, unknown>
}

type ArtifactPart = {
  type: 'artifact'
  runId: string
  taskId: string
  artifactId: string
  fileName: string
  format: string
  version: number
  qualityStatus: string
  downloadUrl: string
}
```

约束：

- `message` 只描述实际已发生的事实。
- `stageCode` 使用稳定机器值，中文展示文案可调整。
- 同一 `taskId + sequence` 必须幂等。
- `artifact` part 必须引用真实且有权限访问的 `ai_artifacts.id`。
- 不允许客户端根据轮询次数自行生成阶段或百分比。

### 5.5 事件生成规则

平台可观察并展示：

- Skill 工具是否已被实际调用。
- 项目资料文件数、成功解析数、可用片段总数和已研读片段数。
- 事实台账、冲突裁决、大纲、草稿、Reviewer、渲染文件是否真实出现。
- 模型调用次数和 Token 是否发生变化。
- 租约、重试、认证、额度、取消和超时状态。
- 正式产物是否完成登记。

平台不得代替 Skill 判断：

- 投资逻辑是否充分。
- 风险分析是否合格。
- 文档结构是否满足 Skill 标准。
- Reviewer 是否应该通过。
- 某个中间文件是否可以绕过 Skill 验收成为正式产物。

## 6. 开发计划

### 阶段 0：基线冻结与契约确认

目标：在修改前锁定现状，避免误删长任务能力或破坏已有产物。

开发任务：

1. 记录六个快捷入口当前请求参数、Skill 绑定和产物格式。
2. 记录普通 `send()`、`agent.sendMessage()`、Agent 工具和 `/ai/tasks` 的调用链。
3. 记录 `AiTaskCards`、`AiArtifactCenter`、取消、重试、轮询和 Token 展示现状。
4. 固化五个正式 Skill 与上传模板 Skill 的绑定测试。
5. 为现有生产行为保留截图、API 样例和数据库只读快照。

交付物：

- 当前链路基线说明。
- 新旧契约对照表。
- 数据库迁移与回滚草案。

完成门槛：产品、前端、服务端和测试人员确认目标不是“隐藏卡片”，而是统一发送和消息契约。

### 阶段 1：快捷入口改为输入框 Skill 预设

主要文件：

- `src/components/AiQuickActions.tsx`
- `src/pages/AIAssistantPage.tsx`

开发任务：

1. 将 `AiQuickActions` 的正式文档入口从 `onRunTask` 改为 `onSelectSkill`。
2. 在 `AIAssistantPage` 增加 `selectedQuickSkill` 状态。
3. 在普通输入区显示可关闭的 Skill 标签和对应占位文案。
4. 将“开始生成”统一为普通发送动作。
5. 复用已有附件上传、项目绑定、输入校验和防重复提交逻辑。
6. 单纯点击入口不创建消息、不调用 API、不创建 `ai_task`。
7. 上传模板保留模板分析步骤；分析完成后回到同一个普通输入框，再由发送动作触发生成。

完成门槛：六个入口均能形成普通用户消息，且网络请求中不再由快捷入口组件直接 POST `/ai/tasks`。

### 阶段 2：统一 Agent 发送与结构化上下文

主要文件：

- `src/pages/AIAssistantPage.tsx`
- `src/hooks/useJwAgent.ts` 或实际 Agent Hook
- `server/src/runtime/jwAgentRuntime.ts`
- Agent 请求与消息类型定义

开发任务：

1. 扩展普通 `send()` 的结构化参数，统一携带：
   - `skillName`
   - 当前项目 ID
   - 当前会话 ID
   - 本轮附件 ID/名称
   - 用户补充要求
   - 任务产物格式
2. 明确用户可见正文与系统结构化上下文的边界，避免把内部 ID、路径和实现提示直接展示给用户。
3. 服务端校验 Skill 与任务类型一一匹配，拒绝越权或错配调用。
4. Agent 调用正式任务工具时绑定稳定的用户、项目、会话和触发消息。
5. 保留防重复提交和幂等键，避免双击发送创建两个文档任务。

完成门槛：快捷入口和普通手动选择 Skill 使用同一个 Agent 请求契约，并产生等价的消息、任务和审计结果。

### 阶段 3：建立聊天消息与持久任务桥接

主要范围：

- MySQL schema 与迁移文件
- `agentAiTaskToolService.ts`
- AI task service/repository
- Agent message repository
- Socket.io 或现有实时事件通道

开发任务：

1. 建立 `trigger_message_id`、`response_message_id`、`skill_name` 和 `display_mode` 关联。
2. 创建一条普通 assistant 响应消息承载该文档 run。
3. 将任务实际事件幂等写入 `agent_message_parts`。
4. 任务更新时通过现有实时通道推送；断线时通过消息历史和任务快照补偿。
5. 页面刷新后重建同一 assistant 消息，而不是创建新的卡片或重复消息。
6. 保证取消、失败、重试、成功终态只发生一次。
7. 继续保留任务租约、超时、重试、Token 和产物登记能力。

完成门槛：关闭页面后任务继续，重新打开会话能看到相同的对话过程和终态。

### 阶段 4：聊天原生过程渲染

主要文件：

- `src/pages/AIAssistantPage.tsx`
- `MessageRow` 及消息 part 渲染组件
- 新增轻量的 Skill、执行事件、产物和错误 part 组件

开发任务：

1. 从新任务的 `conversationTimeline` 中移除 `AiTaskCards` 类型。
2. 在普通 `MessageRow` 中渲染 Skill 选择、执行阶段、工具详情和终态。
3. 同一 run 的阶段默认合并为简洁展示，完整日志可展开。
4. 取消固定百分比和进度条；如 Skill 自己输出可验证的完成计数，只展示计数，不转换为伪百分比。
5. 长任务运行期间允许页面正常滚动、切换右侧文件和查看历史消息。
6. 明确输入策略：默认允许用户继续普通问答；对运行中任务的补充要求必须通过“追加到本任务”或明确引用该 run，避免语义串线。

完成门槛：新建文档任务在中央区域全程只显示普通对话，不出现任务卡样式或固定进度百分比。

### 阶段 5：统一产物下载与右侧文件栏

主要文件：

- `src/components/AiTaskCards.tsx` 中的 `AiArtifactCenter`，后续可拆分为独立文件
- AI artifact API/service/repository
- 消息 artifact part 渲染组件

开发任务：

1. assistant 消息中的下载按钮引用同一 `artifactId` 和受保护下载接口。
2. 右侧文件栏继续从 `ai_artifacts` 查询，不从聊天正文解析文件路径。
3. 完成登记后同时触发消息 part 更新和右侧文件栏刷新。
4. 校验下载权限、项目权限、会话权限、归档状态和路径安全。
5. 统一版本号、文件名、格式、质量状态和生成时间。
6. 中间稿只可在调试/工作区查看，不进入正式产物中心。

完成门槛：对话下载和右侧下载返回相同文件摘要；越权用户均返回拒绝访问。

### 阶段 6：取消、继续执行与版本化

开发任务：

1. 将普通停止按钮与关联 `ai_task` 的取消请求统一。
2. 为 assistant 错误消息增加“继续执行”和“查看错误详情”。
3. 继续执行时创建清晰的重试链，保留原始 `retry_of_task_id` 或等价关联。
4. 把原补充要求、附件和检查点带入新 run，用户新增要求作为新的普通消息保存。
5. 成功后的修订请求生成新版本产物，不覆盖历史正式文件。
6. 认证、额度、资料不足、Skill 未调用、Reviewer 未通过和渲染失败使用不同错误代码与提示。

完成门槛：取消、刷新恢复、继续执行和生成新版本均可通过真实长任务验证。

### 阶段 7：清理旧展示契约与兼容历史数据

开发任务：

1. 新任务停止写入中央任务卡所需的前端专用状态。
2. 删除 `AIAssistantPage` 对新任务的 `AiTaskCards` 渲染分支。
3. 保留历史任务的只读访问方式，避免历史下载丢失。
4. 更新依赖“点击快捷入口直接 POST `/ai/tasks`”或“必须显示任务卡”的旧验收脚本。
5. 调整迁移基线、单服务边界和错误契约测试中的旧断言。
6. 搜索并移除用户可见的固定 `82%`、`98%`、任务卡专用文案和“历史未校验”等残留。

完成门槛：代码、自动化验收和构建产物中均不存在针对新任务的旧任务卡强制契约。

## 7. 自动化测试计划

### 7.1 前端单元与组件测试

| 编号 | 场景 | 预期结果 |
|---|---|---|
| FE-01 | 点击投资提案入口 | 只选中 `draft-investment-proposal`，不发请求 |
| FE-02 | 输入补充要求并发送 | 调用普通 `send()`，消息中保留要求和 Skill |
| FE-03 | 未填写补充要求直接发送 | 生成默认自然语言指令并正常发送 |
| FE-04 | 关闭 Skill 标签 | 恢复普通对话，不创建任务 |
| FE-05 | 双击发送 | 只创建一条用户消息和一个任务 |
| FE-06 | 上传本轮附件 | 附件名称、ID 和项目归属随 Agent 请求发送 |
| FE-07 | 收到多个 run event | 在同一 assistant 消息中按序显示 |
| FE-08 | 收到 artifact part | 对话显示下载按钮并刷新右侧文件栏 |
| FE-09 | 页面刷新 | 恢复消息、阶段和终态，不重复消息 |
| FE-10 | 新任务执行 | 中央聊天流不渲染 `AiTaskCards` |
| FE-11 | 固定 progress=82 的历史字段存在 | 新聊天展示不把该字段当作真实进度 |
| FE-12 | 运行中继续普通问答 | 新消息不会被错误追加为文档要求 |

### 7.2 服务端契约测试

| 编号 | 场景 | 预期结果 |
|---|---|---|
| BE-01 | Skill 与任务类型匹配 | 允许创建并记录实际 Skill |
| BE-02 | Skill 与任务类型错配 | 400/422，且不创建任务 |
| BE-03 | 跨项目/跨会话调用 | 403 或等价拒绝，且有审计记录 |
| BE-04 | 同一幂等键重复提交 | 返回同一任务，不重复执行 |
| BE-05 | 任务事件重复投递 | 同一 sequence 只产生一个消息 part |
| BE-06 | 任务成功 | artifact part 引用真实正式产物 |
| BE-07 | 任务失败 | assistant 消息终态和 `ai_tasks` 一致 |
| BE-08 | 任务取消 | 租约释放，终态不可再次被 worker 写回运行中 |
| BE-09 | 服务重启 | 租约到期任务可恢复，消息不重复 |
| BE-10 | 下载访问 | 所有者可下载，跨用户/项目访问被拒绝 |
| BE-11 | 历史任务 | 历史产物仍可查询和下载 |
| BE-12 | Token 更新 | 任务审计和可见统计一致，不显示伪零值 |

### 7.3 Skill 与资料覆盖测试

| 编号 | 场景 | 预期结果 |
|---|---|---|
| SK-01 | 六个快捷入口 | 实际调用各自唯一绑定的 Skill |
| SK-02 | Skill 未被 Agent 调用 | 任务失败，不生成宿主兜底稿 |
| SK-03 | 投资提案完整资料 | 每个可用项目文件至少有可审计的研读记录 |
| SK-04 | 投资提案全部片段 | 已研读片段数等于全部可用片段数 |
| SK-05 | 某一研读批次失败 | 明确失败或从检查点继续，不标记完成 |
| SK-06 | Reviewer 未通过 | 中间稿不登记为正式 artifact |
| SK-07 | Skill 生成正式文件 | 产物格式、文件存在性和质量状态满足 Skill 结果 |
| SK-08 | 旧 Plugin/模板链路 | 新任务调用链中不存在旧链路或宿主兜底稿 |

### 7.4 数据库与持久化测试

- 任务、触发消息、响应消息、项目和会话关联完整。
- 删除会话、项目或用户时遵守现有级联和保留策略。
- 消息 part 的顺序、幂等和终态约束可靠。
- artifact 与 task、conversation、project、user 一致。
- 重试任务能够追溯原任务，不覆盖原错误和 Token。
- 迁移向前执行和回滚均经过独立测试数据库验证。

### 7.5 安全测试

- 不允许客户端伪造 `skillName` 调用未授权 Skill。
- 不允许把其他项目的附件 ID 绑定到当前任务。
- 下载接口不接受任意文件路径，只接受受控 artifact ID。
- 消息 part 中的工具输出进行安全结构归一化，禁止脚本注入。
- 内部工作区路径、模型凭据和服务器错误栈不得显示给普通用户。
- 取消、重试、归档和下载均校验当前用户权限。

## 8. 人工验收计划

### 8.1 验收环境与数据

至少准备：

1. 一个拥有多类型资料、完整解析片段和历史会话的真实规模项目。
2. 一个资料不完整项目，用于验证明确失败。
3. 一个包含 DOCX、PDF、PPTX、XLSX 和图片资料的项目。
4. 一个合法自定义模板和一个不支持/损坏模板。
5. 管理员、项目成员和无项目权限用户各一个。

验收必须在独立测试数据库或明确隔离的验收项目中执行，不得使用业务生产数据库进行写能力测试。

### 8.2 快捷入口逐项验收

对六个入口分别执行以下步骤：

1. 进入绑定项目的 AI 助手会话。
2. 点击快捷入口，确认仅出现 Skill 标签并聚焦普通输入框。
3. 输入补充要求并上传本轮附件。
4. 点击发送，确认形成普通用户消息。
5. 确认 assistant 在聊天流中显示实际 Skill 调用。
6. 观察资料、事实台账、大纲、正文、Reviewer、渲染和产物阶段。
7. 确认没有任务卡和固定百分比。
8. 刷新页面，确认执行过程能够恢复。
9. 关闭浏览器后重新打开，确认长任务仍在运行或已进入正确终态。
10. 成功后分别从对话和右侧文件栏下载。
11. 对两个下载文件计算 SHA-256，确认内容完全相同。
12. 发起一条修订请求，确认生成新版本且旧版本仍可下载。

### 8.3 取消与故障验收

分别模拟：

- 用户主动取消。
- WebSocket 断开。
- 浏览器刷新和关闭。
- 主服务重启。
- 模型认证失败。
- 模型额度不足。
- 普通 403。
- 单次 Agent 超时。
- Skill 未调用。
- 资料研读批次失败。
- Reviewer 未通过。
- 渲染失败。
- artifact 登记失败。

每种故障必须确认：

- 对话显示的终态与数据库一致。
- 错误文案可行动，但不泄露凭据和内部堆栈。
- 不会无限自动重试或重复消耗模型额度。
- 可以恢复的错误保留检查点和继续入口。
- 不可恢复的错误明确要求重新创建或修正条件。
- 未完成文件不作为正式产物提供下载。

### 8.4 投资提案专项验收

投资提案额外要求：

1. 核对项目资料总数、可用资料数、片段总数和研读片段数。
2. 抽查每份资料至少一条研读或引用证据。
3. 确认没有文件级抽样、全局抽样、短内容过滤或近重复过滤导致可用片段缺失。
4. 检查事实台账、冲突裁决、大纲、草稿、Reviewer 和最终 DOCX 的实际文件证据。
5. 任意一批完整研读未完成时，任务不得进入 `succeeded`。
6. 最终引用清单和资料覆盖记录必须与实际输入一致。

## 9. 验收通过标准

以下条件必须全部满足：

### 9.1 产品体验

- 六个快捷入口均表现为普通输入框的 Skill 预设。
- 用户补充要求、附件和发送动作与普通 Agent 对话一致。
- 新任务中央区域完全不显示任务卡。
- 对话内展示真实阶段，不展示固定或推测百分比。
- 成功、失败、取消和继续执行均以普通消息表达。

### 9.2 链路正确性

- 每个入口实际调用唯一指定 Skill。
- 不调用旧 Plugin、旧模板生成或宿主兜底稿。
- 快捷入口和手动选择同一 Skill 的请求与结果契约一致。
- 当前项目、当前会话、本轮附件和用户要求绑定正确。
- 投资提案完成全部可用资料和片段研读。

### 9.3 稳定性

- 浏览器刷新、断线和服务重启不会丢失长任务。
- 同一发送动作不会创建重复任务或重复消息。
- 取消和重试链可审计，租约状态正确。
- 终态后不会被迟到事件改回运行中。

### 9.4 产物与安全

- 对话和右侧文件栏引用同一 artifact。
- 下载文件摘要一致，权限校验有效。
- 未通过 Skill 验收的中间稿不提供正式下载。
- 内部路径、凭据和错误堆栈不泄露。

### 9.5 工程门槛

- `npm run check:types` 通过。
- 相关 server unit tests 通过。
- 更新后的 `accept:ai-skills`、`accept:ai-api`、`accept:ai-task-lifecycle`、`accept:ai-task-persistence` 和 Agent 工具验收通过。
- 五种正式文档及上传模板的专项验收通过。
- `npm run build` 通过。
- 全量平台门禁中的既有失败与本次新增失败已明确区分；本次变更不得引入新的红灯。

## 10. 上线计划

### 10.1 上线前

1. 备份数据库并验证迁移回滚脚本。
2. 在测试环境完成六入口全链路验收。
3. 确认当前生产运行服务、分支、构建产物和健康检查基线。
4. 构建候选前端和服务端产物，不直接覆盖正在运行的版本。
5. 为新聊天原生入口增加功能开关，建议按用户或环境灰度。

### 10.2 灰度

建议顺序：

1. Q&A。
2. 合规性说明。
3. 尽调报告。
4. 投资提案。
5. 投资建议书（PPT）。
6. 上传模板。

每一阶段观察：任务创建成功率、Skill 实际调用率、完成率、取消率、恢复率、重复任务率、平均 Token、产物登记率和下载成功率。

### 10.3 正式切换

- 激活候选构建并重启单一 `cybernaut-app.service`。
- 检查组件健康接口、Socket.io、Agent Runtime、任务 worker、数据库和模型网关。
- 从线上页面实际创建一个隔离验收任务。
- 验证浏览器加载的是新前端资源。
- 验证聊天过程、右侧产物和下载文件，而不只检查服务 `active (running)`。

## 11. 回滚计划

### 11.1 回滚触发条件

- 快捷入口无法创建普通消息或任务。
- 新任务出现跨项目资料串用。
- 页面刷新导致执行记录或终态丢失。
- 取消无效、重复任务显著增加或任务无限重跑。
- 对话下载与右侧文件不一致。
- 正式产物权限绕过或内部路径泄露。
- 新链路完成率明显低于基线且无法快速修复。

### 11.2 回滚动作

1. 关闭聊天原生入口功能开关，阻止创建新模式任务。
2. 已创建的后台任务继续按原执行面完成或安全取消，不删除任务和产物。
3. 激活上一版前端和服务端候选构建。
4. 回滚只读兼容的数据库代码；除非迁移已证明安全可逆，否则不直接删除新增列和消息数据。
5. 重启服务并验证健康、旧任务查询和历史下载。
6. 记录失败任务、消息关联和产物状态，用于修复后重放验收。

## 12. 监控指标

上线后至少监控：

- `quick_skill_selected_total`
- `quick_skill_message_sent_total`
- `ai_task_created_total`，按 Skill 和任务类型分组
- `ai_task_duplicate_prevented_total`
- `ai_task_terminal_total`，按成功/失败/取消分组
- `ai_task_resume_total`
- `ai_task_event_delivery_lag_seconds`
- `ai_task_message_rebuild_failure_total`
- `ai_task_skill_not_invoked_total`
- `ai_task_source_coverage_ratio`
- `ai_task_artifact_registered_total`
- `artifact_download_total` 与下载失败率
- 每任务模型调用和 Token 用量
- 运行中但租约失效、终态后被写回运行中等异常计数

告警应优先覆盖：跨项目绑定、重复任务、事件积压、租约失效、Skill 未调用、产物登记失败和下载鉴权异常。

## 13. 交付清单

- [ ] 快捷入口 Skill 预设交互。
- [ ] 普通输入框统一发送契约。
- [ ] 用户消息与任务/assistant 消息关联。
- [ ] 持久任务事件转消息 part。
- [ ] 普通消息中的执行过程、错误与控制操作。
- [ ] 对话 artifact 下载组件。
- [ ] 右侧 `AiArtifactCenter` 同源刷新。
- [ ] 取消、恢复、重试与版本化。
- [ ] 历史任务与历史产物兼容。
- [ ] 数据库迁移和回滚脚本。
- [ ] 自动化测试与验收脚本更新。
- [ ] 六入口人工验收记录。
- [ ] 灰度、监控和回滚验证记录。

## 14. Definition of Done

本需求只有在以下事实同时成立时才算完成：

1. 用户可以把快捷入口理解为“已经选好 Skill 的普通 Agent 对话”。
2. 快捷入口与普通对话共用输入框、附件、发送、消息流和后续追问能力。
3. 正式文档仍由指定 Skill Agent 直接生成，Skill 本身未被修改。
4. 中央聊天区没有新任务卡、固定百分比或旧模板文案。
5. 后台任务能够跨刷新、断线和服务重启持续执行并恢复展示。
6. 对话下载和右侧文件栏引用同一个正式产物。
7. 投资提案完整读取当前项目全部可用资料和全部可用片段。
8. 自动化门禁、六入口全链路验收、权限测试和上线后现场验证全部通过。
