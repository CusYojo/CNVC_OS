# 共享线索库联网补全与 V3 评分迭代开发方案

> 文档版本：V2.4
> 编制日期：2026-08-26
> 适用项目：`/Users/hyw/Desktop/sbl_jedi`
> 关联规范：`评分规则.md`、`共享线索池1比1复刻开发与验收计划.md`
> 当前状态：核心链路已开发，完整 P0 验收、历史回补和生产发布尚未完成
> 本文性质：开发设计方案，不代表代码已完成、数据已回补或生产已发布

## 0. 2026-08-26 实施进度

本轮已落地的核心能力：

- 正式入池事件幂等创建 13 个独立专题任务；补全、评分分别使用持久化租约、重试和死信；
- 企业、项目、团队、科研成果实体及其关系独立存储，事实绑定实体 ID；显式标记为项目或团队的线索不会因为同时出现公司名称而退化为企业主体；
- 主体图按确定性规则创建：项目与运营公司通过`operated_by`关联，团队与项目/企业通过`core_team_of`关联，论文成果与共同署名团队通过`authored_by_team`关联；项目名、团队名不再写入法律公司别名；
- 专题事实按事实键路由到实际主体：团队成员和论文作者绑定团队，企业融资/股权/经营/客户/交易绑定公司，论文事实绑定科研成果；明确存在项目主体时产品、技术、竞争和项目画像优先绑定项目，避免全部事实挂在同一个主实体；
- 专题查询使用补全任务已确认的主实体类型和规范名称；项目即使同时披露运营公司，也按项目名称和`project`口径生成查询、缓存键和模型上下文，不回退成公司搜索；
- 法律公司全称、简称、品牌名和曾用名在实体标识中分别保存，同时保留去重后的检索别名；项目名和团队名继续禁止混入公司别名；
- `operating_company/commercialization_subject/technology_source/forming_company/financing.subject/patent.owner/ip.owner`等关系事实只有在已验证事实中的目标规范名称与已有实体精确匹配时，才把事实ID绑定到方向关系；关系事实被高等级新版本替代后，旧关系保留为`superseded`审计历史但不进入当前详情和快照；
- 补全接口继续维护企业、项目、团队、科研成果实体图、关系方向、确认状态和关系证据，供自动评分与管理工具使用；共享线索详情不展示“联网资料补全”、主体图、专题进度、事实浏览、重试或冲突裁决区块，只把带合法来源的`verified`事实投影到既有项目简介、主体基础信息、产品与商业化、团队成员、证据与动态区块；
- 普通详情不再下载完整补全状态、13专题错误、实体图、未验证候选或原文审计证据；改用`verified-profile`和`verified-facts`最小投影，只返回已验证介绍、介绍所依赖的去重公开来源、展示所需事实值和可打开来源，省略原文、定位符、页面哈希、证据等级、事实/主体/原始文档ID及版本字段；简介来源直接展示在原有简介/团队区块内，不新增“联网资料补全”版块；完整状态、冲突和审计事实读取统一要求`system.manage`权限；
- Web Search 仅发现候选 URL，宿主重新执行受控页面/PDF抓取、SSRF/重定向/robots/大小校验和原文引用匹配；
- 事实、原始文档、证据、冲突和不可变快照独立版本化；V3 只接收指定快照，并要求评分证据回溯 `factId/evidenceId`；
- 登记状态明确为注销的企业在入池前拒绝；公开情报正式提交边界会再次硬校验，脚本或内部服务不能绕过；存量线索核验为注销后退出有效池、取消全部未完成补全和评分并写入审计；
- 注销准入规则已同时下沉到正式线索统一提交边界和`Pipeline ready → enrichment`事务；Radar、Excel/BP、36Kr储备库在入池前已知明确注销时直接`rejected`，不创建正式线索、补全或评分任务；批量导入模板新增“登记状态”字段并单独记录排除结果；
- 公共列表和公共详情共用同一有效池可见性条件；`已注销/已删除/已合并/解析失败/质量拒绝`记录即使ID已知，也不能通过详情、`verified-profile`或`verified-facts`直接读取；系统管理员的补全状态、冲突、审计事实和评级历史接口可使用审计模式读取隐藏记录，确保“公共池不可见”不等于删除证据；
- 人工复核不能将原始证据中已明确注销的企业确认入池；已注销的审计留痕记录也不能转为专属项目；
- 主体歧义会暂停融资、股权、财务、专利和交易等重大专题；系统管理员可填写主体类型、规范名称、信用代码/域名/法人/注册地址和确认依据，确认后只恢复“因主体未确认而暂停”的专题，不覆盖证据冲突复核，也不清零既有尝试历史；
- arXiv/OpenAlex 采用独立 provider 和 canonical ID，作者、机构、许可、附件与未来日期按论文规则处理；
- 13专题状态、执行阶段、分页事实、主体确认、单专题重试和冲突裁决继续由受权限保护的管理接口承载，不进入共享线索详情；人工重试仍保留累计尝试次数并追加重试前状态、操作者、原因和时间审计；
- 13专题输出按事实键执行时间、单位、币种、范围、当前/历史任职、客户关系类型、产品版本/测试条件和资质/政策适用范围校验；不满足契约的候选不会入库，并计入拒绝率；
- 13专题事实字典已按计划进一步拆细：客户意向/试用/框架协议/试点/研究合作/正式客户，合同、订单、交付、回款，收入增速、现金、负债，产品价格/场景，竞品性能/价格/融资，市场币种/方法和政策生效时间均使用独立事实键，不再塞入模糊数组或通用文本；
- 客户关系由宿主执行类型化门禁：`customer.formal`必须在引用原文中出现采购、合同、订单、交付或回款依据，官网案例或模型自行填写的scope不能将意向对象升级为正式客户；
- 融资金额、估值、合同/订单/回款、企业经营和市场数据增加跨口径排斥规则；拟融资需求、募资目标、市场规模、合同金额和营业收入不能相互冒充；专利状态、产品发布状态和生产阶段必须使用宿主可识别的具体生命周期状态；
- 最新进展使用同一`instanceKey`绑定事件、事件发生日期和报道日期，任一缺失时整组候选被拒绝；访问时间继续由受控抓取证据确定性记录；
- 基本画像专题已扩充企业全称、成立时间、注册资本、法人、信用代码、登记状态、公司类型、注册地址、定位、主营业务、产品和客户类型；信用代码、专利号及资质/认证编号由宿主执行格式门禁；
- 来源等级由宿主按最终落地域名保守判定：监管披露为E1、政府/法院/知识产权等官方公共记录为E2，企业宣传、媒体及伪装官方后缀均保持E3；任何等级的数字、原始字符串和结构化对象内的字符串叶子都必须出现在原文引用中，网址值只能由同源URL确定性支持；单条结构化值限制为16KB，非市场政策事实的原文还必须命中当前主体名称或确认别名；
- 同一事实键出现多个不同值时由宿主拆分保存候选事实并生成冲突，不再静默合并成数组；模型显式声明冲突时，每个候选必须分别提交`value + quote + sourceUrls + period/unit/currency/scope`，并经过与普通事实相同的来源白名单、原页抓取、连续原文、主体、数值和口径门禁；少于两个可核验候选时不创建空壳冲突；
- 通过门禁的显式冲突候选先分别落成版本化事实和证据，再由宿主按`factKey + instanceKey`生成包含候选事实ID、直接来源和模型冲突原因的复核记录；管理接口可读取候选值、等级、当前/备选状态及直接来源，裁决幂等更新并保留证据，但普通详情不展示裁决入口；
- 融资轮次、股东、成员、客户、财务期间、产品、专利、资质、竞品、新闻等可重复事实使用`factKey + instanceKey`定位独立记录；不同实例可同时为当前事实，只有同一实例的不同值才进入冲突复核；
- 补全数据契约为`lead-enrichment-v2`，联网研究Prompt升级为`lead-topic-web-research-v6`；v6新增关系事实规范名称和证据绑定约束；历史事实通过`instanceKey=singleton`向前兼容，新快照显式保留实例键；
- 冲突选择“全部不采信并重跑”时，候选事实保留历史和证据，但统一标记`rejected + 非当前`，避免重跑无结果后再次混入正式快照；
- 同主体、同专题、同Prompt和同查询计划支持有时效缓存；仅确认主体允许复用，缓存命中后仍由宿主重新抓取原页并执行原文引用校验；
- V3结果按快照保存历史版本；系统管理员可在系统管理页恢复上一份有效V3结果，恢复操作不修改事实、证据和快照并写审计；共享线索详情继续遵守“不展示评级研判或评分入口”的既有契约；
- 快照完整保留专题运行ID、Prompt、模型、工具、次数、指标和时间用于审计，但语义哈希排除这些易变运行元数据；事实与证据未变化的新一轮专题运行复用原快照和评分绑定，不重复触发V3；
- V3评分适配器不再下发快照中的原始采集`summary/team`或专题运行指标，只下发主体名称/行业、实体关系、论文结构和基于已验证事实生成的企业/团队/项目介绍；
- V3宿主增加科研与交易适用性硬门禁：未成立公司且`financial_operations=not_applicable`时企业财务维度必须为`null`；`transaction_exit`为`missing/not_applicable`时不得声称估值或交易条款信息可用；
- 共享线索V3已移除无快照旧路径：没有就绪快照时只排队补全前置任务，不创建评分任务；手动评分和重试仅限系统管理员；
- 企业、团队和项目正式介绍只读取已验证画像事实，缺少项目介绍时明确展示缺口，不再使用企业介绍、旧评分文案或采集摘要代替；
- 提供相互独立的“停止新补全任务”“暂停Worker”“停止快照自动评分”运行开关；
- 专题失败按限流、网络、模型、解析、校验、预算分类并采用分类退避；不可重试、预算超限或达到上限进入死信；租约失效立即中断后续抓取/入库；
- 上游搜索/模型账号“额度不足、余额不足、支付要求、insufficient quota/credit”等计费故障优先覆盖网关返回的泛化`network/model`分类：当前专题直接进入`dead_letter`，Worker启动时也会把同类历史`retrying`任务隔离为死信，不再消耗重试次数；该处理只改变任务状态并保留错误与指标审计，不删除事实或来源；
- 网络、限流或模型连续失败达到阈值后打开60秒熔断；上游计费故障则立即打开独立的15分钟计费熔断（可由`LEAD_ENRICHMENT_BILLING_COOLDOWN_MS`配置），冷却期不再领取新网关专题；单专题本地预算超限仍只终止当前专题，不打开全局熔断；过期任务与总任务租约可标记`abandoned`并恢复领取，同一专题并发领取只有一个成功，实时新线索优先于历史回补；
- 注销处置与手动重试使用事务级状态锁；注销后的任务不能重新排队、冻结新快照或触发新评分，既有事实、证据、原始文档、快照和评级历史不物理删除；
- 提供默认只读的历史补全/OpenAlex 回填预览和批次结果报告，以及按专题、模型、证据等级、冲突、快照、主体歧义、校验拒绝、缓存、租约恢复、论文provider、Token、估算成本和耗时统计的管理指标接口；
- 历史补全批次支持按`network/rate_limit/model/parse/validation/unknown`错误类型只读预览和显式重排；预算超限不自动重试，已生成不可变快照或已注销主体不原地重排，执行次数和追加审计均保留；
- 发布脚本在停服前执行向前兼容迁移，候选产物验收、离线启动门禁或在线健康检查失败时回滚 Web/Server 构建对。

已经取得的自动化证据：类型检查通过；351 项 Server 测试中 347 通过、0 失败、4 项专用数据库测试按安全策略跳过；核心补全、批量入池、评分Agent、V3、OpenAlex、共享池 UI 和构建发布契约验收通过；新增覆盖公司名称分类、方向关系事实映射、快照介绍事实ID追溯、介绍文字的非空公开来源投影、上游额度不足计费熔断，以及普通详情只读最小投影和运营接口管理员权限；随机专用 MySQL 验收库已跑通 41 项业务检查及“创建→迁移→临时HTTP服务→删除”生命周期，通过未登录、普通用户、系统管理员三类真实会话验证401/403/200、普通用户携带有效CSRF仍不能触发五类管理写操作、注销记录不能通过公共列表/详情/投影直接读取且管理员仍可审计；既有关系证据、0049、冲突、注销和重试回归同时通过，临时服务和验收库已自动销毁。

只读真实联网灰度已对公开主体运行 1 个`basic_profile`专题：模型返回6条候选事实和19个搜索来源，宿主成功抓取6个原始页面，其中3条引用通过exactQuote匹配；该灰度没有写业务库。浏览器已在已发布的本地V2.4服务中分别检查真实企业线索和InsufficiencyBench论文线索：企业详情五个既有业务区块各出现一次；论文详情以“科研主体信息”替代“主体基础信息”，其余结构一致；补全运营区、专题进度、主体图和裁决入口均不可见，缺失事实保持待核验，论文的两个机构、论文许可和5位共同署名作者正常显示。尚未覆盖证据丰富的企业补全样本、普通用户角色、键盘和读屏。

2026-08-26 已在本地服务应用0049迁移并激活`build-20260826T042228827Z-17636-619f744b`，重启后的统一服务健康状态为`ready`，实际资源为`index-SjYBdSAH.js`且不含“联网资料补全”。新Worker启动时把12条历史“额度不足”重试隔离为死信；当前补全指标为`queued=15819`、`retrying=31`、`deadLetter=13`，计费熔断已打开，证明无效重试保护已生效。但上游搜索网关额度仍未恢复，因此真实业务补全链路仍未恢复，不能把“服务健康”理解为“补全成功”；恢复额度后仍需显式灰度。生产环境尚未应用0049或发布本构建。仍不得宣称完成的事项：恢复可用联网额度、使用v6契约执行13专题真实写回→快照→V3全链路、全量历史回补、普通用户/键盘/读屏验收、生产发布与在线资源核验、全部 P0 金标以及各负责人签字。以上未完成前，本方案状态不能改为“已完成”。

## 1. 结论与目标

本次迭代将当前“采集后只补企业介绍、官网和工商基础字段”的能力，升级为完整的证据化研究流水线：

```text
线索正式入池
  → 主体确认与实体消歧
  → 专题 0：企业、团队、项目基本画像
  → 12 类专项联网研究
  → 原始页面/PDF 获取与证据提取
  → 主体、时间、单位、口径和来源校验
  → 冲突裁决与人工复核
  → 冻结不可变事实快照
  → 生成 V3 评分输入
  → 自动排队 V3 评分
  → 发布评级、覆盖率、证据和缺口
  → 新事实触发增量补全与重新评分
```

本期核心目标：

1. 每条正式共享线索都具有可追踪的补全任务，而不是依赖一次性脚本。
2. 企业、团队、项目三个主体分别建模并明确相互关系。
3. 13 个研究专题可以独立执行、重试、复核、更新和审计。
4. 所有进入评分的非空事实都有来源 URL、原文证据、时间和主体绑定。
5. 网络上无法可靠获得的信息明确标记缺失，不由模型补写。
6. V3 只读取冻结的证据快照，不再从分散 JSON 临时拼接输入。
7. 事实快照发生变化后，旧评级自动过期，并且只触发一次新评分。
8. 继续禁止评分 Agent 临时联网，保证评级可复现和可审计。
9. 论文和科研成果使用独立来源适配与适用性规则，不因缺少企业融资、收入或股权资料被机械扣分。
10. 工商登记状态经可靠证据明确为“注销”的企业不得进入正式共享线索池；存量线索补全后确认注销时立即停止补全和评分并退出有效池。

## 2. 迭代立项时实现基线

本节记录立项时的原始能力和缺口，用于解释迭代范围；当前完成度以第0节的2026-08-26实施进度及验收文档为准。

立项时代码已经具备：

- Radar、公众号、论文、BP、批量导入和储备库等入池入口；
- 不可变 Pipeline 原始事件、状态迁移、Agent 决策和人工复核；
- Codex 内置联网搜索调用能力；
- 公司介绍、官网、注册资本、法人、成立时间、信用代码、登记状态、公司类型和注册地址补全；
- 来源 URL、字段证据和补全状态写回；
- `lead_score_jobs` 持久化评分队列、租约、重试、死信和恢复；
- 共享线索专用 `lead-rating-v3` 评分流程。

立项时缺口：

1. 联网补全主要依赖批量脚本或单条接口，不是所有正式入池事件的自动下一阶段。
2. 当前补全只覆盖 9 个企业基础字段，没有覆盖团队、客户、财务、技术、产业化、竞品、市场和交易信息。
3. 批量补全只处理能够确认法律公司全称的主体，项目、团队和科研成果缺少独立画像路径。
4. 搜索结果写入 `scoring.registry` 等位置，但 V3 输入仍主要读取 `radarProfile`、原摘要和原文，存在数据接线断点。
5. 补全完成后没有自动使旧评级过期，也没有自动重新排队评分。
6. 当前来源标题和 URL 会进入评分输入，但评分 Agent 禁止访问 URL，因此不能把 URL 当作正文证据。
7. 数据仍分散在 `leads`、`radarProfile`、`scoring` 和来源数组中，缺少统一事实、证据、冲突与快照模型。

论文专项基线（2026-08-25只读快照）：

- 273条有效论文线索均已有作者数据；
- 266条已有基于共同署名生成的研究团队和论文许可结构；
- 152条从原始页面确认了所属机构，另外121条机构未确认，不能把证据缺失视为系统成功或随意推断；
- 3条发现数据集链接，6条发现代码链接，但对应许可仍需分别核验；
- 7条OpenAlex记录仍显示为arXiv来源，缺少研究团队和权利结构，发布时间均异常位于未来；
- 其中1条OpenAlex记录的链接是补充材料XLSX而不是论文正文；
- 当前确定性回填只支持可识别arXiv ID的记录，OpenAlex尚无独立回填链路。

## 3. 范围

### 3.1 本期包含

- 正式入池后的自动补全任务触发；
- 企业、团队、项目主体识别和关系建模；
- arXiv、OpenAlex、DOI、出版社和仓储来源的论文身份规范化；
- 基本画像专题和 12 类专项联网研究；
- 搜索、原文获取、PDF/页面解析和结构化提取；
- 事实级来源、引用、证据等级、时效和主体绑定；
- 数值单位、币种、期间、范围和统计口径校验；
- 冲突检测、裁决和人工复核；
- 不可变事实快照；
- V3 评分输入适配、旧评分失效和自动重评；
- 历史共享线索分批回补；
- 管理端任务状态、失败重试、人工复核和审计；
- 共享线索页只读展示补全状态、更新时间和评级新鲜度；
- 自动化测试、受控真实搜索验收、灰度发布和回滚。

### 3.2 本期不包含

- 修改“我的专属项目”现有评分契约；
- 将网络搜索结果直接认定为审计财务或合同事实；
- 自动生成不存在的客户、订单、收入、估值或交易条款；
- 绕过数据源授权、付费墙、验证码或访问控制；
- 未经审批采购新的商业数据库；
- 将评分 Agent 改成可自由联网的自治 Agent；
- 删除历史 Pipeline、评分、审计或来源记录；
- 为追求覆盖率而降低 E1/E2/E3 证据门槛；
- 在共享线索列表恢复批量导入、Radar 同步等运营按钮。

## 4. 关键产品与技术决策

### 4.1 搜索与评分分离

- 研究 Agent 可以使用受控 Web Search/Web Fetch、页面解析和 PDF 提取能力。
- 评分 Agent 不使用任何网络或文件工具，只读取宿主冻结的证据快照。
- 综合分、覆盖率、评级资格和 A+ 门槛继续由宿主确定性计算。

### 4.2 事实优先，摘要后生成

- 模型先输出结构化候选事实，不直接输出可写库的最终介绍。
- 宿主验证事实后，再从已验证事实生成企业、团队和项目介绍。
- 介绍中的关键句必须能够追溯到事实和证据。

### 4.3 终态不等于有值

13 个专题全部到达终态后才能生成快照，但终态可以是：

- `completed`：主要目标字段已有可用证据；
- `partial`：只获得部分事实；
- `missing`：公开来源未披露；
- `not_applicable`：按项目类型或阶段不适用；
- `review`：存在主体或重大事实冲突；
- `failed` / `dead_letter`：技术失败，需重试或人工处理。

私有合同、流水、财务和交易条款在公开网络上查不到时必须进入 `missing`，不能无限阻塞，也不能编造。

### 4.4 评分语义边界

本迭代不调整 V3 七维权重、评级区间和现有共享线索评级展示决策。`评分规则.md` 中“无法评级显示待评级”与当前实现“完成审阅但证据不足显示 D”存在既有差异；如需调整，应形成单独产品决议和兼容迁移，不在补全迭代中隐式改变。

### 4.5 论文研究团队与商业化团队分离

- 论文全部署名作者确定性归入`researchTeam`，属于论文研究团队成员；
- 第一位作者标记`first_author`，只有原文明确同等贡献时才标记`joint_first_author`；
- 通讯作者、共同资深作者等角色必须由原文明确标记，不根据作者顺序推断；
- 论文作者不自动成为公司创始人、全职员工或`commercializationTeam`成员；
- 作者只有在公司任职、创业、技术转让或成果转化证据确认后，才能进入企业或商业化团队；
- 研究团队人数按规范化并去重后的全部署名作者计算。

### 4.6 注销企业准入红线

- 登记状态必须先经过确定性归一化；只有明确“注销/已注销”才命中本规则，不能把“吊销未注销”“迁出”“歇业”或状态未知擅自等同为注销；
- 采集或准入审查阶段已经确认注销时，Pipeline进入`rejected`且不得创建或合并正式线索；
- 批量导入模板应支持“登记状态”列；36Kr等储备源的`registrationStatus/businessStatus/regStatus/regStatusName/enterpriseStatus`应归一到同一准入字段；
- 明确注销的候选记录仅保留原始事件、排除原因和审计轨迹，不创建`leads`正式记录、实体、补全任务或评分任务；
- 人工复核不得绕过准入规则，已注销审计记录不得转为专属项目；
- 已入池后通过联网补全确认注销时，线索退出有效共享池，取消尚未执行的补全与评分任务，旧评级不得继续作为有效评级展示；
- 注销状态必须保留证据URL、原文引用、核验时间和处置审计，不做物理删除。

## 5. 目标架构

```text
lead_pipeline ready
        │
        ▼
Enrichment Orchestrator
        │
        ├── Entity Resolution / Relationship Graph
        │
        ├── Topic 00 Basic Profiles
        ├── Topic 01 Financing
        ├── Topic 02 Ownership
        ├── Topic 03 Founders & Team
        ├── Topic 04 Customers & Contracts
        ├── Topic 05 Financial Operations
        ├── Topic 06 Products
        ├── Topic 07 Technology & IP
        ├── Topic 08 Industrialization
        ├── Topic 09 Competition
        ├── Topic 10 Latest Developments
        ├── Topic 11 Market & Policy
        └── Topic 12 Transaction & Exit
                    │
                    ▼
         Search → Fetch → Extract
                    │
                    ▼
      Host Validation / Conflict Review
                    │
                    ▼
       Versioned Facts + Evidence Store
                    │
                    ▼
          Immutable Evidence Snapshot
                    │
                    ▼
       lead_score_jobs (snapshot bound)
                    │
                    ▼
              V3 Rating Result
```

## 6. 主体识别与关系图

### 6.1 主体类型

- `company`：明确法律公司主体；
- `project`：产品、技术或创业项目；
- `team`：尚未成立公司或公司不明确的创业团队；
- `research`：论文、实验室技术或科研成果；
- `unknown`：暂时无法确认。

### 6.2 实体状态

- `confirmed`：法律全称、域名、信用代码或权威来源确认；
- `claimed`：仅由企业材料声明；
- `inferred`：根据多条公开事实推断；
- `ambiguous`：存在多个同名主体；
- `missing`：无法定位。

### 6.3 必须建立的关系

- 项目运营主体；
- 融资主体；
- 技术来源主体；
- 知识产权权属主体；
- 核心团队与公司的任职关系；
- 产品品牌与公司主体关系；
- 高校、实验室与拟转化公司的关系。
- 论文、数据集、代码、模型权重、专利和技术转让主体关系；
- 研究团队、商业化团队和企业经营团队的关系。

`ambiguous` 状态不得自动写入融资、财务、股权、专利或交易事实，也不得生成正式 V3 评分。

## 7. 13 个研究专题

### 7.1 专题 00：企业、团队、项目基本画像

企业画像字段：

- 法律全称、简称、曾用名、品牌名；
- 成立时间、注册地、总部、官网；
- 企业定位、主营业务、核心产品、客户类型和发展阶段；
- 企业性质及与项目、团队、产品的关系；
- 基于已验证事实生成的客观企业介绍。

团队画像字段：

- 团队名称、形成时间和形成背景；
- 创始人、联合创始人、技术、产品、销售和运营负责人；
- 分工、履历、全职状态、团队规模、互补性和关键人员依赖；
- 与公司、项目、论文和知识产权的关系；
- 基于已验证事实生成的客观团队介绍。

项目画像字段：

- 项目名称、简称、历史名称和类型；
- 发起主体、所属公司或拟成立公司；
- 用户问题、解决方案、产品形态、技术路线、应用场景；
- 研发阶段、商业化阶段、已有验证和融资需求；
- 已实现、研发中、规划中和未验证能力的明确区分；
- 基于已验证事实生成的客观项目介绍。

### 7.2 专题 01 至 12

| 编号 | 专题 | 核心字段 | 关键防错规则 |
|---|---|---|---|
| 01 | 融资历史 | 轮次、日期、金额、币种、投资方、当轮估值 | 历史轮次不等于当前轮次；融资金额不等于估值或市场规模 |
| 02 | 股东股权 | 股东、持股比例、股东类型、最终受益人、数据日期 | 股权必须带快照时间；同名股东需消歧 |
| 03 | 创始人与团队 | 姓名、职位、教育、任职、创业经历、全职状态 | 顾问、教授、作者不自动视为全职团队 |
| 04 | 客户与订单 | 客户、产品、合同性质、金额、期间、交付、回款 | 意向、试用、框架协议不等于收入或稳定客户 |
| 05 | 财务经营 | 收入、增速、毛利、利润、现金流、回款、现金、负债 | 所有数字必须带期间、单位、币种、范围和审计属性 |
| 06 | 产品竞争力 | 产品、版本、参数、性能、价格、场景、产品矩阵 | 参数必须带版本和测试条件；宣传口径降为 E3 |
| 07 | 技术与知识产权 | 技术路线、指标、专利号、申请人、状态、权属 | 专利数量不等于壁垒；申请不等于授权 |
| 08 | 产业化能力 | 资质、认证、产能、良率、成本、交付、供应链 | 规划产能不等于实际产能；资质需核验有效状态和范围 |
| 09 | 竞品格局 | 竞品、产品、客户、场景、性能、价格、融资 | 目标用户、场景和交付物均匹配才可列直接竞品 |
| 10 | 最新经营进展 | 融资、产品、客户、订单、人事、诉讼、处罚、停产 | 区分事件发生时间和报道时间，旧新闻不作最新进展 |
| 11 | 市场与政策 | 市场规模、增速、地区、口径、竞争格局、政策原文 | 必须记录地区、年份、币种、统计范围和测算口径 |
| 12 | 交易与退出 | 本轮融资、投前/投后估值、比例、优先权、对赌、退出 | 公开信息只作补充，最终以 TS/协议等交易文件为准 |

### 7.3 论文和科研项目专用处理分支

论文项目继续使用13个通用专题，但在执行前必须经过论文来源适配器和科研适用性规划，不能把企业字段机械套到科研成果。

#### 7.3.1 论文身份和来源规范化

每条论文记录至少保存：

```text
provider: arxiv | openalex | crossref | publisher | repository
arxivId
openAlexId
doi
version
landingPageUrl
fullTextUrl
pdfUrl
publishedAt
updatedAt
sourceProvenance
```

处理规则：

- `Wxxxxxxxxxx`识别为OpenAlex Work ID，不得继续显示为arXiv；
- arXiv、OpenAlex、Crossref、DOI落地页和出版社页面使用独立适配器；
- DOI落地页、论文正文、PDF、数据集、代码仓库和补充材料分别标记内容类型；
- XLSX、ZIP、图片或补充材料不能充当论文正文；
- 发布时间超过当前合理边界、年份与版本冲突或无法解析时进入`review`；
- arXiv版本号必须保留，后续版本不得静默覆盖旧版本事实和评分。

#### 7.3.2 作者、贡献和研究团队

```text
paperAuthors: [{
  name,
  normalizedName,
  position,
  role,
  orcid,
  affiliations,
  identityStatus,
  evidenceUrl
}]
```

- 全部署名作者是`researchTeam`成员；
- 作者顺序来自来源原文，不允许模型重排；
- `joint_first_author`、`joint_senior_author`、`corresponding_author`只采信原文明确标记；
- ORCID、作者主页和机构主页由Codex联网搜索补充，但同名无法消歧时保持`ambiguous`；
- 论文作者和企业成员是两套关系，只有证据确认后才能关联。

#### 7.3.3 机构和逐人关系

- `affiliations`保存论文披露的全部机构；
- `authorAffiliations`保存原文能够明确确认的作者—机构关系；
- 论文只披露机构列表但无法逐人对应时，只展示机构列表，不强行分配；
- Codex可通过ORCID、作者主页、实验室主页和机构主页补充缺口，结果仍须主体消歧和来源校验。

#### 7.3.4 权利和研究产物

必须拆分：

- 论文使用许可和论文著作权主体；
- 数据集地址与数据许可；
- 代码仓库与代码许可；
- 模型权重地址与权重许可；
- 专利、申请人、权利人和法律状态；
- 知识产权所有人；
- 技术转让、许可和作价入股状态。

论文开放许可不等于数据、代码、模型或知识产权许可；arXiv托管不等于arXiv拥有成果；作者所属机构也不能自动推导为知识产权所有人。

#### 7.3.5 Codex科研联网补全

确定性解析论文原文后，Codex只搜索仍缺失的：

- ORCID、可信作者主页和当前所属机构；
- 实验室、研究团队和项目主页；
- 数据集、代码、模型权重及各自License；
- 专利、技术转让、成果转化和Spin-off公司；
- 产业合作、试点、科研资助和后续版本；
- 会议接收、奖项、代码发布和可验证商业化进展。

能够从arXiv/OpenAlex/出版社原文确定的作者、顺序、标题、摘要和许可优先确定性提取，不重复交给模型猜测。

#### 7.3.6 科研项目的13专题适用性

| 通用专题 | 科研项目适配规则 |
|---|---|
| 基本画像 | 标题、摘要、研究问题、方法、作者、机构和成果介绍 |
| 融资历史 | 查询科研资助、产业基金或Spin-off融资；不存在经营主体时为N/A |
| 股东股权 | 未成立公司时为N/A，不从机构关系推断股权 |
| 创始人与团队 | 全部作者为研究团队；商业化团队单独核验 |
| 客户与订单 | 区分研究合作方、数据提供方、试点方和正式客户 |
| 财务经营 | 未成立经营主体时为N/A |
| 产品竞争力 | 评价数据集、算法、模型、原型、工具和可交付成果 |
| 技术与知识产权 | 评价方法、指标、专利、许可、权属和可持续壁垒 |
| 产业化能力 | 评价TRL、复现性、代码数据开放、工程成本和场景适配 |
| 竞品格局 | 学术基线、相关工作和商业竞品分别建模 |
| 最新进展 | 版本、会议接收、奖项、代码、引用、转化和合作 |
| 市场与政策 | 评价成果应用市场、监管条件和政策限制 |
| 交易与退出 | 仅在技术转让、许可、作价入股或Spin-off融资时适用 |

科研项目不能因为企业融资、收入、股权和交易信息不适用而机械低分；不适用维度按V3的N/A和阶段适配规则处理。

## 8. 搜索、原文获取与提取

### 8.1 查询计划

每个专题根据主体别名、曾用名、品牌、创始人和产品名生成查询。论文项目还应使用arXiv ID、OpenAlex ID、DOI、作者、机构、数据集和代码仓库名。查询必须带实体约束，不允许只搜索泛行业关键词。

### 8.2 来源优先级

1. E1：监管文件、交易所披露、审计/财税/银行材料、正式合同、业务系统、客户确认、独立检测；
2. E2：工商、专利、政府、司法、官方资质、投资机构公告、权威行业研究；
3. E3：BP、公司官网宣传、管理层陈述、媒体报道和未经验证预测。

### 8.3 原文获取

- 搜索结果摘要只用于发现来源，不直接作为重大事实的最终证据；
- 应访问原始网页、PDF、公告、专利和监管页面；
- 保存标题、URL、发布者、发布时间、访问时间、原文摘录和内容哈希；
- PDF 和扫描件进入已有文档解析/OCR链，但不得把解析失败当作“未披露”。

### 8.4 模型输出边界

模型输出只是一组候选事实。每条候选事实至少包含：

```text
factKey
subjectId
value
unit
currency
period
scope
evidenceLevel
verificationStatus
sourceUrl
sourceTitle
publisher
publishedAt
accessedAt
exactQuote
confidence
```

## 9. 宿主验证与冲突裁决

### 9.1 确定性校验

- 来源 URL 必须来自本次受控搜索或已授权资料；
- `exactQuote` 必须存在于获取的页面快照；
- 关键值必须能在引用中定位；
- 数值必须具有单位、币种、期间和适用范围；
- 公司、人物、专利、客户和投资方必须完成主体绑定；
- 官网必须是企业自有域名；
- 信用代码、专利号、资质编号、日期和金额执行格式校验；
- 来源内容疑似 Prompt Injection 时作为不可信文本处理，不能改变工具权限和系统规则。
- 除市场与政策专题外，原始来源正文必须出现当前主体规范名称或已确认别名；只在搜索摘要中出现主体、正文实际讨论其他公司的候选必须拒绝；
- 证据等级由宿主基于受控抓取后的最终 URL 分类，模型声明的“官方/监管/媒体”不能直接决定 E1/E2/E3；
- 所有等级的数字值均必须在 `exactQuote` 中出现，E1/E2 同样不能以高来源等级绕过值与引用一致性校验。

### 9.2 冲突规则

- E1 优先于 E2，E2 优先于 E3；
- 同等级来源优先使用主体更明确、时间更新、口径更完整的来源；
- 重大融资、财务、股权、客户和交易事实只有 E3 时保留为 `unverified`；
- 两个高等级来源冲突时保留双方事实并进入人工复核；
- 同一专题输出中同一`factKey`出现不同值时，宿主必须分别落候选事实、关联各自原文证据并创建冲突，禁止自动拼成一个数组；
- 重大真实性冲突未解决时，相关快照状态为 `review`，V3 应无法评级。

### 9.3 主体歧义人工确认

- `ambiguous/missing/unknown`主体的融资、股权、财务、知识产权和交易专题进入`review`，原因记录为`entity_confirmation_required`；
- 仅系统管理员可确认主体，至少填写主体类型、规范名称和确认依据，可补充信用代码、官网域名、法人和注册地址；
- 确认动作使用事务锁、检查线索仍处于有效共享池，并写入操作人、时间、前后主体、标识和理由审计；
- 确认后只恢复因主体歧义暂停且尚未实际执行的专题，不自动重跑证据冲突专题，不重置`execution_attempts`；
- 对完全相同确认请求幂等返回，不重复排队或重复写审计。

## 10. 建议数据模型

以下为建议的新表/对象，名称可在技术设计评审时调整：

### 10.1 `lead_entities` 与 `lead_entity_relations`

分别保存公司、项目、团队和科研成果主体，以及运营、融资、技术来源、研究团队、商业化主体和知识产权等方向性关系。主体保留规范名称、别名、权威标识和`confirmed/claimed/inferred/ambiguous/missing`状态；事实必须绑定实体ID，不能只绑定线索ID。

### 10.2 `lead_enrichment_jobs`

保存线索级补全任务：触发来源、状态、优先级、目标快照版本、租约、重试、错误和完成时间。

### 10.3 `lead_enrichment_topic_runs`

保存 13 个专题的每次运行：专题、Prompt版本、模型、工具集、查询、状态、Token、成本、时长和错误。

### 10.4 `lead_facts`

保存版本化结构化事实：字段键、稳定实例键、主体、值、单位、期间、范围、证据等级、验证状态、有效期和当前版本。版本唯一性以`leadId + subjectId + factKey + instanceKey + version`约束，不允许不同融资轮次、成员、股东或产品相互覆盖。

### 10.5 `lead_fact_evidence`

保存事实与原始来源的多对多关系：URL、页面哈希、原文引用、发布者、发布时间、访问时间和可靠性。

### 10.6 `lead_source_documents`

保存受控 Web Fetch 或 PDF 解析得到的不可变原文版本：规范 URL、最终 URL、抓取状态、HTTP 状态、内容类型、标题、发布者、发布时间、访问时间、正文哈希、提取正文和缓存有效期。搜索摘要只用于发现 URL，正式事实引用必须绑定该表的正文版本和内容哈希。

### 10.7 `lead_fact_conflicts`

保存冲突事实、冲突类型、候选值、自动裁决依据、人工裁决及操作人。

### 10.8 `lead_enrichment_snapshots`

保存供评分使用的不可变快照：13个专题状态、事实、证据索引、缺失项、冲突、覆盖率、快照哈希和冻结时间。

### 10.9 论文来源和科研关系投影

论文来源、版本、作者、贡献、机构、研究产物和权利可作为版本化事实存储，也可建立规范化投影表；无论物理实现如何，必须支持arXiv/OpenAlex独立来源、逐人机构关系、研究团队与商业化团队分离，以及新版本增量更新。

### 10.10 评分绑定

`lead_score_jobs` 或评分输入事件必须记录 `enrichmentSnapshotId`、`snapshotHash` 和 `ratingSchemaVersion`。同一线索、同一快照、同一评级版本只允许一个有效评分任务。

## 11. 任务状态机与编排

### 11.1 触发时机

- Pipeline Item 首次进入 `ready`；
- Excel、BP、Radar、储备库或公开信息入口创建/合并正式线索后；
- 人工确认主体或解决冲突后；
- 论文新版本、机构、作者贡献、代码、数据或License发生变化后；
- 上传新的项目材料、财务或交易文件后；
- 某类事实超过有效期；
- 管理员手工请求重新研究。

### 11.2 状态机

```text
queued
  → resolving_entity
  → planning
  → searching
  → fetching
  → extracting
  → validating
  → completed / partial / missing / not_applicable / review
  → snapshot_ready
  → scoring_queued
  → rated
```

技术失败路径：

```text
searching/fetching/extracting/validating
  → retrying
  → failed
  → dead_letter
  → manual_retry
```

### 11.3 并发和预算

- 同一法律主体的重复线索共享搜索结果，但分别保留实体映射和审计；
- 每个专题独立查询预算、页面预算、工具调用上限和超时；
- 同一域名执行速率限制和缓存；
- 优先处理主体明确、高关注、已转项目候选和已有高等级信号的线索；
- 网络或模型熔断时暂停新搜索，保留任务，不生成空白正式快照。
- 连续`network/rate_limit/model`失败达到5次后打开60秒熔断；解析、校验和业务缺失不计入网关熔断；任一成功请求重置连续失败计数；
- 专题与总任务均持有租约；租约过期后记录`abandoned`并回到可领取状态，`FOR UPDATE SKIP LOCKED`保证同一专题并发只产生一个领取者；
- 实时新线索使用更高优先级，历史回补使用低优先级批次，避免回补长期挤占在线任务。

## 12. V3 评分接线

### 12.1 统一输入适配器

V3 输入必须从冻结快照读取：

- 企业、团队、项目介绍；
- 13 个专题的已验证事实；
- 每条事实的 E1/E2/E3 和来源索引；
- 缺失、不适用、冲突和未验证字段；
- 项目阶段；
- 论文来源、版本、作者、机构、贡献、数据/代码/模型产物和权利状态；
- 交易信息可用状态；
- 快照 ID、哈希和冻结时间。

不得继续以 `radarProfile` 或 `scoring.registry` 的偶然字段位置决定评分是否能看到某项事实。

### 12.2 自动重评

```text
newSnapshotHash != currentRating.snapshotHash
  → currentRating.status = stale
  → enqueue(leadId, snapshotHash, lead-rating-v3)
```

快照未变化时不得重复评分。补全任务处于 `review` 或实体不明确时不得产生正式评级。

### 12.3 结果发布

评分结果记录：

- `snapshotId`、`snapshotHash`；
- V3 Schema、Prompt、模型和工具集版本；
- 模型运行、宿主计算和决策 ID；
- 评分完成时间和下次建议更新日期；
- 评级状态、覆盖率、置信度和缺失事项。

## 13. API 与页面方案

### 13.1 建议接口

- `GET /leads/:id/enrichment`：系统管理员读取总任务、13专题、缺失、冲突和快照状态；
- `POST /leads/:id/enrichment`：管理员发起或刷新研究；
- `POST /leads/:id/enrichment/entity/confirm`：系统管理员确认歧义主体，并只恢复因主体未确认而暂停的专题；
- `POST /leads/:id/enrichment/topics/:topic/retry`：重试单个专题；
- `POST /leads/:id/enrichment/conflicts/:id/resolve`：人工裁决；
- `GET /leads/:id/facts`：系统管理员分页读取完整审计事实和来源；
- `GET /leads/:id/verified-profile`：普通详情读取基于已验证事实生成的三类介绍及其去重HTTP(S)公开来源，不返回专题、实体图、原文或内部事实ID；
- `GET /leads/:id/verified-facts`：普通详情分页读取已验证且有HTTP(S)来源的最小事实投影，不返回原文、页面哈希和内部审计字段；
- `GET /lead-enrichment/metrics`：系统管理员查看专题状态、证据等级、冲突、快照、评分任务、Token、估算成本和耗时；
- `GET /leads/:id/ratings/history`：读取快照绑定的评级历史。

接口必须复用现有认证、权限、审计和错误契约。

### 13.2 共享线索页面

列表只展示处理与评分状态；详情沿用现有业务信息块，只读展示：

- 补全中、待复核、待评级、评级中、评级完成（具体等级）、评级过期、补全失败或评级失败；
- 基于已验证事实生成的企业、团队和项目介绍，以及直接显示在对应原有区块内的可打开来源；
- 投影到主体基础信息、产品与商业化、团队成员、证据与动态中的真实值；
- 可打开的线索原始来源，以及支撑已展示事实的公开来源；
- 无合法来源或未通过宿主校验的值显示`待核验`、`未披露`或空状态，不使用采集摘要、宣传文案、推荐理由替代。

共享线索详情不得展示独立“联网资料补全”区块、主体关系图、13专题完成度、事实浏览器、主体确认、补全重试、冲突裁决或批量回补入口。上述能力保留在受权限保护的管理接口/运营工具中，不恢复共享线索列表和详情已隐藏的运营按钮。

## 14. 数据更新策略

| 数据 | 建议有效期/触发方式 |
|---|---|
| 最新新闻和经营进展 | 7天或事件触发 |
| 融资、客户、产品、团队 | 30天 |
| 工商、股东和登记状态 | 90天 |
| 专利、资质、竞品、市场和政策 | 180天 |
| 论文新版本、数据集和代码发布 | 7天检测或事件触发 |
| 作者机构、研究团队和论文许可 | 90天或新版本触发 |
| 财务数据 | 新财报、材料上传或财年触发 |
| 交易条款、估值和退出 | 新融资或交易文件上传触发 |

仅重跑过期或受影响专题，再生成新快照，不要求每次全量重搜。

## 15. 分阶段开发计划

### 阶段 0：契约冻结和金标样本

- 确认 13 专题字段字典、证据等级、缺失和冲突语义；
- 确认无法评级的 D/待评级产品口径；
- 建立企业、项目、科研、主体歧义、重大冲突等金标样本；
- 建立arXiv、OpenAlex、同名作者、机构无法逐人映射、论文/数据/代码许可分离和未来日期样本；
- 冻结事实、证据、快照和评分输入 JSON Schema。

完成条件：字段契约和样本经产品、投资、后端和测试共同评审。

### 阶段 1：任务、实体和事实基础设施

- 数据表和迁移；
- 实体确认和关系图；
- 补全任务与13专题状态机；
- 事实、证据、冲突和审计服务；
- 租约、重试、死信、熔断和恢复。

完成条件：使用模拟搜索源可完整跑通任务并冻结快照。

### 阶段 2：基本画像和高价值专题

- 专题 00 基本画像；
- arXiv、OpenAlex、Crossref/DOI来源适配，论文正文与补充材料识别；
- 论文作者、贡献、机构、研究团队、研究产物和权利结构；
- 融资、团队、客户订单、财务、产品、技术知识产权、产业化；
- 页面/PDF原文获取；
- 数值、主体和引用校验。

完成条件：核心七维资料能够进入统一事实库，错误主体和无来源事实无法写入。

### 阶段 3：外围专题和冲突复核

- 股东、竞品、新闻、市场政策、交易退出；
- 跨来源冲突检测；
- 管理端人工复核和单专题重试；
- 更新有效期和增量研究。
- ORCID、作者机构、代码/数据License、专利、技术转让和Spin-off联网补全。

完成条件：13专题全部具备明确终态和审计链。

### 阶段 4：证据快照与 V3 自动重评

- 快照生成器；
- V3 统一输入适配器；
- 旧评分过期和幂等重评；
- 评分结果与快照绑定；
- 列表和详情投影更新。

完成条件：新事实只触发一次新评分，评分能够逐项回溯证据。

### 阶段 5：历史回补和灰度发布

- 只读预览历史覆盖和预算；
- 按优先级分批回补；
- 小流量真实搜索和评分灰度；
- 生产监控、成本、失败率和质量抽检；
- 完成发布、回滚和运行手册。

完成条件：验收文档全部 P0 门禁通过，历史回补状态和未完成范围明确披露。

## 16. 预期代码影响范围

预计涉及但不限于：

- `server/src/db/schema.ts` 和 MySQL 迁移；
- 新增 enrichment/fact/evidence/snapshot 服务；
- `server/src/routes/meta.ts` 或独立 enrichment 路由；
- `server/src/services/leadPublicIntelService.ts`；
- `server/src/services/leadCompanyWebSearchService.ts`；
- `server/src/services/inProcessAiWorkflowService.ts`；
- `server/src/services/leadRatingV3Service.ts`；
- `server/src/services/leadScoreJobService.ts`；
- `server/src/services/paperResearchMetadataService.ts`、`paperMetadata.ts`和论文来源适配器；
- `server/src/scripts/backfillPaperResearchMetadata.ts`及OpenAlex独立回填脚本；
- 运行时任务调度和运营可观测性；
- `src/types/index.ts`、共享线索列表/详情和运营页；
- 单元、契约、集成、浏览器和发布验收脚本。

实现时必须保护当前工作区未提交修改，不得使用全量覆盖或盲目 `git add -A`。

## 17. 发布与回滚

### 17.1 灰度顺序

1. 只写新表，不影响现有页面和评分；
2. 影子生成快照，但不触发正式评分；
3. 对金标和内部线索启用新快照评分；
4. 小比例共享线索自动补全和重评；
5. 逐批扩大；
6. 最后执行历史回补。

### 17.2 回滚原则

- `LEAD_ENRICHMENT_ACCEPT_NEW_JOBS=false`停止创建新补全任务；`LEAD_ENRICHMENT_ENABLED=false`暂停Worker；
- `LEAD_ENRICHMENT_AUTO_SCORE=false`允许继续冻结可审计快照，但不由新快照创建V3评分任务；
- 系统管理员在系统管理页选择评分历史并填写原因后，评分恢复读取上一份有效 V3 结果；
- 不删除已经产生的事实、证据、快照、运行和审计；
- 取消尚未开始的重评任务，不中断已经提交的数据库事务；
- 新增表保持只读，便于问题分析和后续恢复。

历史回补的只读结果报告使用：

```bash
npm run report:lead-enrichment-backfill -- --batch-id=<stable-id>
```

报告分列任务和专题状态、已搜索、已找到、无公开信息、复核中、失败、快照、已重评以及注销企业清退数量，不写入业务数据。

## 18. 风险与控制

| 风险 | 控制措施 |
|---|---|
| 同名主体串档 | 法律名称、信用代码、域名、法人和关系图联合消歧 |
| 模型虚构 | 搜索工具来源白名单、原文引用匹配、宿主校验、无证据不入库 |
| 搜索结果 Prompt Injection | 外部文本不可信、工具最小权限、结构化输出和宿主验证 |
| 私有数据无法公开获取 | 明确 missing，生成材料索取清单，不降低证据门槛 |
| 数字口径混乱 | 强制期间、单位、币种、范围和原文定位 |
| OpenAlex/arXiv来源混标 | 以provider、canonical ID和来源适配器路由，不使用展示名称判断来源 |
| 论文作者误当创业团队 | 研究团队、商业化团队和企业经营团队分开建模 |
| 论文许可误当IP归属 | 论文、数据、代码、模型、专利和IP权利分别存证 |
| 未来日期或附件充当正文 | 日期合理性、MIME类型、落地页和正文链接联合校验 |
| 注销企业进入或滞留线索池 | 工商状态确定性归一化、准入硬拦截、存量退出池和评分任务取消 |
| 任务成本失控 | 按缺口搜索、缓存、同主体复用、专题预算和优先级 |
| 补全后重复评分 | 快照哈希幂等键和唯一约束 |
| 新评分覆盖旧评分 | 版本化评级和原子切换，不删除历史结果 |
| 全量任务拖垮在线服务 | 独立并发、租约、背压、熔断、灰度和低峰回补 |

## 19. 完成定义

本迭代只有同时满足以下条件才可声明完成：

1. 新入池线索自动进入补全任务；
2. 企业、团队、项目介绍均来自已验证事实；
3. 13 个专题均有独立状态、证据、重试和审计；
4. 所有非空重大事实可追溯到原始来源；
5. 主体歧义和重大冲突不会自动进入正式评分；
6. 网络不可得信息明确缺失且不被编造；
7. V3 只读取冻结快照；
8. 快照变化自动使旧评分过期并只触发一次重评；
9. 评分 Agent 仍无联网和文件工具；
10. 自动化、受控真实搜索、灰度、生产构建、激活、重启和在线验证全部通过；
11. 历史回补进度和剩余范围单独报告，不把部分回补描述为全量完成。
12. arXiv和OpenAlex分别完成来源、作者、机构、权利、科研适用性和历史回填验收。
13. 明确注销的企业在所有采集入口均被拒绝，存量确认注销后不再出现在有效共享池且不会继续评分。
14. 融资轮次、成员、股东、客户、产品、专利等多条合法事实可同时入快照，不同实例不会被误判为冲突。
