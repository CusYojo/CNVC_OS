# 共享线索池查询与分页缺陷修复及验收计划

> 文档版本：V1.2
>
> 编制日期：2026-09-02
>
> 适用项目：`/Users/hyw/Desktop/sbl_jedi`
>
> 适用分支：当前 `main` 工作树
>
> 当前状态：源码修复、六层自动化/数据库/HTTP/候选 DOM 验收完成；本地激活、真实登录会话与生产发布未执行
>
> 计划范围：共享线索池列表的关键词搜索、六项筛选、URL 状态、分页、错误恢复及对应 API/测试；不调整线索详情、评分规则、采集链路和其他业务板块。

## 1. 背景与目标

本计划用于修复共享线索池当前已确认的查询与分页缺陷，使列表展示、筛选结果、URL、API 分页元数据和数据库权威字段保持一致，并补齐可重复执行的自动化及真实环境验收。

目标结果：

1. 列表中可见的标准行业、地区、阶段和团队信息均可被对应条件准确查询。
2. 搜索、筛选、分页、每页条数和浏览器前进/后退共享同一份 URL 状态。
3. 越界页自动回退到最后有效页；非法页码和非法筛选值不会产生空白页、假筛选或无边界查询。
4. API 返回的 `total`、`page`、`pageSize`、`totalPages` 与同一筛选快照下的列表数据一致。
5. 查询失败和重试失败均有明确错误状态，不吞掉异常、不把旧数据伪装成新条件结果。
6. 建立单元、契约、隔离 MySQL、HTTP、浏览器和真实数据库只读审计六层验收证据。

## 2. 范围与边界

### 2.1 本期包含

- `GET /api/leads` 查询参数校验和响应分页语义；
- `listLeads()` 的关键词、行业、地区、阶段、渠道、线索类型、更新时间组合条件；
- `SourcingPage` 的 URL 参数解析、搜索防抖、浏览器历史同步、分页和重试；
- 前后端筛选枚举及分组规则的一致性契约；
- 展示值与筛选集合一致性审计；
- 只读真实数据库验证和隔离环境写入型验收；
- 相关测试、验收脚本和文档更新。

### 2.2 本期不包含

- 修改线索采集、Radar、补全、评级或转项目业务；
- 修改线索详情页信息架构和样式；
- 修改权限、会话、CSRF、Origin 或审计安全边界；
- 未经确认对业务库执行地区回填、迁移或批量更新；
- 启动、重启、激活构建、生产发布或推送代码；
- 为查询性能顺手升级依赖或重构无关数据访问层。

### 2.3 数据与运行边界

- 源码修复不等于获得业务数据库写入授权。
- 存量地区修正必须先生成 preview，列出总数、旧值、新值、证据来源、置信度和回滚依据；经单独确认后才允许 apply。
- 写入型 MySQL 验收仅允许在确认隔离的测试库运行，并继续受 `ALLOW_MYSQL_ACCEPTANCE_WRITES` 等现有安全守卫约束。
- 未获本地激活授权时，只交付源码、测试和候选构建结果，不声明页面已生效。

## 3. 排查基线

以下数据来自 2026-09-02 对当前数据库的只读聚合，只作为修复基线；正式验收时必须重新计算，不得硬编码到代码或测试断言中。

| 基线项 | 当前结果 | 结论 |
|---|---:|---|
| 当前可见线索 | 2,235 条 | 默认每页 20 条时共 112 页 |
| 请求第 10,000 页 | `page=10000`、`totalPages=112`、`list=[]` | 越界页未回退 |
| 列表显示“其他”行业 | 180 条 | 展示集合 |
| `industry=其他` 查询 | 78 条 | 确认漏筛 102 条 |
| 展示地区与 `business_region` 不一致 | 22 条 | 地区展示与筛选口径不一致 |
| 团队关键词抽样 | 24 个关键词中 4 个漏搜 | `leads.team` 未进入关键词 SQL |
| 有效但无可选阶段桶 | 79 条 | 阶段选项覆盖不完整 |
| 非法 `region=火星` | 返回全部 2,235 条 | 非法地区被静默忽略 |
| `industry=%` | 返回全部 2,235 条 | 枚举和通配符未限制 |

当前已有正向能力：

- 六项筛选参数已从页面传递到 API；
- 多条件在 SQL 中按 `AND` 合并；
- 最新请求编号可阻止旧响应覆盖新结果；
- 默认 20 条并支持 10/20/50 条；
- 当前排序以 `created_at` 后接唯一 `id`，静态数据下顺序稳定；
- 当前渠道单筛选合计可覆盖 2,235 条可见线索。

## 4. 缺陷清单与完成定义

| 缺陷 ID | 优先级 | 缺陷 | 根因 | 完成定义 |
|---|---|---|---|---|
| `LP-QRY-001` | P0 | “其他”行业漏筛 | 展示把未命中规则的行业归为“其他”，SQL 只匹配原文“其他” | `industry=其他` 返回集合与列表派生标签为“其他”的集合完全相等 |
| `LP-QRY-002` | P0 | 地区看得到但筛不出/错选 | 列表使用多来源派生地区，查询只比较 `business_region` | 列表展示和查询统一使用同一权威地区；集合差异为 0 |
| `LP-QRY-003` | P0 | 团队关键词漏搜 | 关键词 SQL 未包含 `leads.team` | 任一有效团队字段关键词均可命中对应可见线索 |
| `LP-PAGE-001` | P0 | 越界页返回空列表 | 在得知真实总数前直接按请求页计算 offset | 非空结果的越界页回退最后有效页，响应和 URL 均返回实际页 |
| `LP-PAGE-002` | P0 | 非法页码导致 400/异常页码状态 | 前端用 `Number()` 后未验证有限整数 | `abc`、小数、0、负数统一规范到第 1 页，不产生 `NaN` 请求 |
| `LP-STATE-001` | P1 | 前进/后退被旧搜索词覆盖 | 搜索输入只在首次挂载读取 URL | 浏览器历史变化后输入框、请求和 URL 三者一致，且不会形成写回循环 |
| `LP-QRY-004` | P1 | 非法筛选值产生假筛选/全量查询 | API 的行业、地区、渠道、阶段不是严格枚举 | 非法 API 参数返回 400；页面 URL 中非法值被移除并回到第 1 页 |
| `LP-QRY-005` | P1 | 79 条有效阶段无可选桶 | 前端选项与服务端标准阶段集合不完整 | 所有认可阶段均能归入明确筛选桶，且展示值与筛选结果一致 |
| `LP-ERR-001` | P1 | 重试再次失败时吞错 | 重试分支只有 `finally`，没有统一 catch | 首次和重试失败使用同一错误处理；无未处理 Promise |
| `LP-TEST-001` | P0 | 验收只检查源码字符串，未证明结果正确 | 缺少集合、边界、HTTP 和 DOM 行为测试 | 六层验收全部具备可重复证据，写入测试仅在隔离库执行 |

## 5. 目标查询契约

### 5.1 URL 与 API 参数

目标参数：

| 参数 | 合法值 | 默认值 | 非法值处理 |
|---|---|---|---|
| `page` | 1 至服务端上限的正整数 | `1` | 页面规范为 `1`；直接 API 请求返回 400 |
| `pageSize` | API 为 1 至 100 的正整数；页面公开选项为 `10`、`20`、`50` | `20` | 页面规范为 `20`；直接 API 的 0、负数、小数或大于 100 返回 400 |
| `keyword` | 去首尾空格后 1 至 100 字符 | 空 | 超长 API 请求返回 400；空白值删除参数 |
| `leadType` | `company`、`research` | 空 | 非法值返回 400/页面删除 |
| `industry` | 产品批准的标准行业枚举 | 空 | 非法值返回 400/页面删除 |
| `stage` | 产品批准的标准阶段桶 | 空 | 非法值返回 400/页面删除 |
| `region` | 省级标准地区枚举 | 空 | 非法值返回 400/页面删除 |
| `channel` | 当前正式渠道枚举 | 空 | 非法值返回 400/页面删除 |
| `updatedRange` | `7d`、`30d`、`90d` | 空 | 非法值返回 400/页面删除 |

`source` 和 `sort` 当前不是页面公开控件。本期需要明确：若仍为内部兼容参数，必须设置长度/枚举限制并保留契约测试；不得接受无边界字符串。

### 5.2 URL 单一事实源

- URL 是搜索、筛选、页码和每页条数的导航事实源。
- 搜索框保留本地输入状态用于 300ms 防抖，但 URL 变化时必须同步回本地输入。
- 用户输入完成后只在值确实变化时更新 URL，并将页码重置为 1。
- 浏览器前进、后退、详情返回和直接粘贴 URL 均走同一解析函数。
- API 若将越界页修正为实际页，页面必须使用 `replace` 把 URL 同步为实际页，避免历史栈保留无效页。
- 快速切换条件继续只允许最后一次请求落入 store；旧请求失败也不得覆盖当前错误状态。

### 5.3 分页响应语义

```ts
type LeadListResponse = {
  list: LeadListItem[]
  total: number
  page: number          // 实际有效页，不是原始请求页
  pageSize: number       // API 允许 1-100；页面公开选项为 10/20/50
  totalPages: number    // total=0 时约定为 1
}
```

规则：

1. `total=0` 时返回 `page=1`、`totalPages=1`、`list=[]`。
2. `total>0` 且请求页超过总页数时，返回最后一页及其真实数据。
3. 计数和列表必须在同一筛选条件下完成；实现需避免并发变更造成明显的 `total`/`list` 自相矛盾。
4. 排序最后一项继续使用唯一 `id`；本期不改为前端分页或固定总数。

## 6. 修复设计

### 6.1 查询枚举与契约拆分

建议新增：

- `src/lib/leadPoolFilters.ts`：浏览器安全的筛选选项、URL 解析与合法值判断；
- `server/src/contracts/leadPoolQueryContract.ts`：服务端 Zod 查询契约、枚举和分页窗口函数；
- `server/tests/leadPoolQueryContract.test.ts`：导入前后端纯常量，断言合法值集合一致。

约束：

- 服务端运行时代码不得反向导入 React 页面或浏览器依赖；
- 前端不得导入数据库、文件系统或服务端密钥模块；
- 不继续依赖正则扫描 JSX 来证明前后端契约一致。

### 6.2 分页根修复

目标文件：

- `server/src/services/aiSummaryService.ts`
- `server/src/routes/meta.ts`
- `src/store/useAppStore.ts`
- `src/pages/SourcingPage.tsx`

实施步骤：

1. 抽取纯函数 `normalizeLeadListPage(requestedPage, pageSize, total)`。
2. 先得到过滤后的 `total`，计算 `actualPage` 和 `actualOffset`，再读取实际页。
3. 如为性能保留并行计数/首查，必须在确认越界时补查最后有效页，最终返回实际页；不能继续返回空的请求页。
4. `fetchLeads()` 返回本次响应或至少返回实际分页元数据，不再只返回 `void`。
5. 页面收到 `response.page !== URL page` 时用 `replace` 修正 URL。
6. 页码组件只使用合法实际页生成页码和禁用状态。
7. 删除、筛选后总页数缩小时复用同一回退机制，不额外实现第二套逻辑。

### 6.3 关键词搜索修复

关键词条件至少覆盖：

- `leads.name`
- `leads.companyName`
- `leads.industry`
- `leads.summary`
- `leads.source`
- `leads.team`
- 已入库 Radar/评分中的团队、机构、投资方结构化字段

要求：

- 继续参数化查询；不得字符串拼接 SQL；
- 对 `%`、`_` 等通配符语义做明确产品决定。推荐将用户关键词按普通文本处理并转义通配符；
- 设置关键词长度上限，避免无边界 JSON 全表扫描；
- 对空格、中文、英文大小写和特殊字符增加单元/API 用例。

### 6.4 行业“其他”一致性修复

1. 抽取唯一的标准行业分类规则及服务端查询条件构造器。
2. 普通行业：原始行业文本或已批准的 `sectorLabels` 任一命中即进入标准桶。
3. “其他”：原始值有效，但没有命中任何标准行业规则或已知标准赛道时进入；原文显式“其他”也应进入。
4. “待确认”不等于“其他”，空值和占位值不得混入“其他”。
5. 新增只读集合审计：

```text
displayedIndustryTagIds(industry) == filteredLeadIds(industry)
```

6. 至少覆盖人工智能、具身智能/机器人、半导体/芯片、多标签行业、其他、待确认六类。

### 6.5 地区权威口径修复

推荐决策：`leads.business_region` 作为共享线索池列表和筛选的唯一权威地区；派生结果只能作为待回填候选，不能在列表中生成一个无法由同一条件筛选的第二事实。

源码步骤：

1. 列表 DTO 的 `region`、`businessTags.region` 和 SQL 筛选统一读取标准化后的 `business_region`。
2. `business_region` 为空时列表显示“待确认”，不在读取时临时扫描摘要并伪装为权威值。
3. 采集、补全和人工确认链路继续通过 `resolveLeadBusinessRegion()` 形成候选，并按现有来源/置信度规则写入标准字段。
4. 建立地区不一致 preview，输出脱敏聚合及可审计明细文件；不直接 apply。

存量数据步骤（需另行授权）：

1. 对当前 22 条差异记录生成 `leadId`、旧标准值、候选值、来源、置信度和证据摘要。
2. 高置信工商注册地可进入自动回填候选；中置信主体名称/正文推断进入人工复核，不自动写入。
3. apply 必须幂等，并记录字段 provenance/审计与回滚清单。
4. 回填后重新运行展示/筛选集合一致性审计。

### 6.6 阶段筛选补齐

需要产品确认的推荐分组：

| 标准筛选桶 | 建议包含 |
|---|---|
| 种子轮 | 种子轮、种子+轮、种子++轮 |
| 天使轮 | 天使轮、天使+轮、天使++轮 |
| Pre-A 轮 | Pre-A、Pre-A+ |
| A 轮 | A、A1、A2、A+、A++ |
| Pre-B 轮 | Pre-B、Pre-B+ |
| B 轮 | B、B1、B2、B+、B++ |
| C 轮及以后 | Pre-C、C-F、Pre-IPO、IPO、PE |
| 战略融资 | 战略融资、战略投资 |
| 股权融资/轮次未披露 | 股权融资、新一轮融资、首轮融资、分拆增资融资、风险投资/联合投资 |
| 科研成果 | 论文类型线索 |

规则：

- 不再让 79 条已识别有效阶段无可选入口；
- 历史轮次描述不得冒充当前轮次；
- “未融资”“未披露”“待核验”是融资状态/数据状态，除非产品明确决定，否则不混入轮次桶；
- 线索类型和阶段按 AND 组合；科研成果与企业线索等不兼容组合应返回 0，并在前端可选择是否自动清理冲突条件。

### 6.7 搜索状态和错误恢复

1. 抽取 `loadLeadPage(request)`，统一首次加载和“重新加载”的 `loading/error/finally`。
2. URL `keyword` 变化时同步输入框并取消旧防抖计时器。
3. 本地输入只在防抖完成且与 URL 不同时写 URL。
4. 重试再次失败必须保留错误文案和当前查询条件。
5. 失败时不得把旧列表总数解释为当前查询结果；可保留旧行作为灰化缓存，但必须明确标注“读取失败，显示上次结果”，或统一隐藏旧行。
6. 组件卸载或请求失效后不得更新当前加载/错误状态。

## 7. 实施阶段

### 阶段 I0：基线冻结与测试先行

- [x] 记录当前分支、`git status --short` 和本次允许修改的文件。
- [x] 保存当前只读数据库聚合基线，不保存公司名称、凭据或完整业务内容。
- [x] 为 10 个缺陷添加定向测试、只读审计或可重复浏览器复现步骤。
- [x] 确认写入型测试目标为隔离 MySQL；未确认时保持只读。

完成标志：每个缺陷均有明确的失败证据和预期断言。

### 阶段 I1：查询契约与纯函数

- [x] 新增前端筛选常量/URL 解析模块。
- [x] 新增服务端查询 Zod 契约和分页窗口函数。
- [x] 严格限制关键词长度、筛选枚举和每页条数。
- [x] 添加前后端枚举一致性测试。

完成标志：非法参数稳定失败，合法参数不改变现有 API 字段形状。

### 阶段 I2：服务端查询与分页

- [x] 修复实际页计算和越界回退。
- [x] 将 `leads.team` 纳入关键词条件。
- [x] 统一行业展示/筛选规则，重点修复“其他”。
- [x] 统一地区权威字段。
- [x] 补齐批准后的阶段分组。
- [x] 保留唯一 ID 排序兜底。

完成标志：服务级集合测试和分页边界测试全部通过。

### 阶段 I3：前端 URL、分页和重试

- [x] 前端只接受合法 URL 查询值。
- [x] 修复搜索输入与前进/后退同步。
- [x] 使用 API 实际页修正 URL。
- [x] 统一首次加载和重试错误处理。
- [x] 验证详情返回后筛选、页码、每页条数和滚动位置。

完成标志：MemoryRouter/组件测试覆盖关键历史导航和错误分支。

### 阶段 I4：隔离环境与真实只读验收

- [x] 在隔离 MySQL 执行写入型 fixture 验收。
- [x] 在当前真实数据库执行只读集合审计，不插入、不更新、不删除。
- [ ] 若获授权，启动当前项目本地服务并完成 HTTP/浏览器验收。
- [x] 地区存量数据只生成 preview；当前变更候选为 0，不执行 apply。

完成标志：数据库、API 和 DOM 三层证据相互一致。

### 阶段 I5：候选构建、激活与交付

- [x] 运行类型检查、相关测试、平台门禁和生产构建；平台门禁的既有失败单独记录。
- [ ] 只有用户明确要求本地生效时，才按项目生命周期停止确认归属的旧 PID、激活候选并启动。
- [ ] 验证 `/api/health`、组件健康、实际静态资源和登录后 DOM。
- [ ] 生产发布、Git 提交和推送分别取得授权并分别报告。

完成标志：明确区分源码完成、测试通过、本地生效、生产发布四种状态。

## 8. 自动化测试计划

### 8.1 单元与契约测试

建议文件：

- `server/tests/leadPoolQueryContract.test.ts`
- `server/tests/leadPoolPagination.test.ts`
- `server/tests/leadPoolIndustryFilter.test.ts`
- `server/tests/leadPoolRegionContract.test.ts`
- `src/pages/SourcingPage.test.tsx` 或项目现有等价组件测试位置

必测用例：

- [x] `UNIT-001` `page` 的 `abc`、小数、0、负数、超上限处理。
- [x] `UNIT-002` API `pageSize` 只允许 1-100 的整数；页面控件仅产生 10/20/50。
- [x] `UNIT-003` 非法行业、地区、阶段、渠道返回契约错误。
- [x] `UNIT-004` 前后端筛选枚举完全一致。
- [x] `UNIT-005` 0 条、1 条、整页、末页、越界页窗口计算。
- [x] `UNIT-006` “其他”与“待确认”严格分离。
- [x] `UNIT-007` 多标签行业和 sector label 映射。
- [x] `UNIT-008` 标准阶段全部进入批准的筛选桶；真实库未归桶数量为 0。
- [x] `UNIT-009` 搜索输入与 URL 外部变化双向同步且不循环。
- [x] `UNIT-010` 重试失败仍保持错误状态，且过期请求错误不会覆盖当前状态。

### 8.2 隔离 MySQL 验收

必须先确认数据库隔离并开启现有写入守卫。fixture 至少包含：

- 相同 `created_at`、相同分数但不同 UUID 的 7 条线索；
- 原始行业不含“其他”但应派生为“其他”的线索；
- `business_region` 有值、为空及派生候选冲突的线索；
- 团队字段只存在于 `leads.team` 的线索；
- Pre-B、股权融资、新一轮融资和科研成果线索；
- 组合筛选能唯一命中的线索。

断言：

- [x] `MYSQL-001` 逐页无重复、无遗漏，重复读取顺序一致。
- [x] `MYSQL-002` 越界页返回最后有效页数据。
- [x] `MYSQL-003` 删除或筛选导致页数收缩时返回新的实际页。
- [x] `MYSQL-004` 展示行业集合与对应筛选集合一致。
- [x] `MYSQL-005` 展示地区集合与对应筛选集合一致。
- [x] `MYSQL-006` 团队关键词命中直接团队列。
- [x] `MYSQL-007` 单筛选和多筛选总数等于 SQL 事实集。
- [x] `MYSQL-008` 所有 fixture 在 finally 中精确清理，失败也不残留。

### 8.3 HTTP API 验收

在已登录测试会话和隔离/获准环境执行：

- [x] `API-001` 默认请求返回 200 及完整分页字段。
- [x] `API-002` 非法页码、每页条数、行业、地区、阶段、渠道返回 400 `INVALID_ARGUMENT`。
- [x] `API-003` 越界页返回 200、实际末页和非空末页数据。
- [x] `API-004` 0 结果返回 `page=1`、`totalPages=1`。
- [x] `API-005` 团队关键词命中预期线索。
- [x] `API-006` “其他”行业和地区集合与数据库审计一致。
- [x] `API-007` 多条件按 AND 组合，不出现任一条件泄漏。
- [x] `API-008` 列表 DTO 不返回完整 Radar 正文、完整 scoring、内部错误或敏感字段，仅保留批准的列表投影键。
- [x] `API-009` 未登录请求保持现有鉴权行为。

### 8.4 浏览器交互验收

建议使用真实登录态和当前项目实际页面：

- [x] `UI-001` 首次进入默认第 1 页、20 条/页。
- [x] `UI-002` 搜索防抖后 URL、输入框、列表和总数一致。
- [x] `UI-003` 六项筛选分别验证选中状态、URL 和真实结果。
- [x] `UI-004` 多条件组合后总数和行内容正确。
- [x] `UI-005` 翻页、切换 10/20/50 条后状态正确。
- [x] `UI-006` 直接访问越界页自动落到末页，URL 被修正。
- [x] `UI-007` 直接访问非法参数不会出现空白选中项或假筛选。
- [x] `UI-008` 浏览器前进/后退恢复不同搜索词和筛选。
- [x] `UI-009` 进入详情再返回，搜索、筛选、页码、每页条数和滚动位置恢复。
- [x] `UI-010` 首次失败和重试失败均展示错误；重试成功恢复列表。
- [x] `UI-011` 快速连续切换条件时旧响应不覆盖新结果。
- [x] `UI-012` 空结果与越界页使用不同语义，不把越界误报为无匹配线索。

以上 UI 项使用最终候选版同哈希前端资源、真实 MySQL 只读查询和注入测试用户的浏览器 fixture 完成；它证明候选页面行为，但不替代真实 HttpOnly 登录会话、当前本地服务激活或生产验收。

### 8.5 真实数据库只读审计

建议新增只读脚本 `server/src/scripts/leadPoolQueryAudit.ts`，默认仅 preview：

- 输出可见总数及各筛选聚合；
- 比较每个标准行业的展示 ID 集合与筛选 ID 集合；
- 比较标准地区展示/筛选集合；
- 统计有效阶段未归桶数量；
- 抽样验证团队直接列关键词；
- 验证末页、末页+1 和最大允许页；
- 输出聚合和脱敏 ID 哈希，不输出完整企业资料；
- 不调用 `ensureSchema()`，不插入 fixture，不写审计表。

验收阈值：

- [x] `AUDIT-001` 行业展示/筛选集合差异为 0。
- [x] `AUDIT-002` 地区展示/筛选集合差异为 0。
- [x] `AUDIT-003` 已批准有效阶段未归桶数量为 0。
- [x] `AUDIT-004` 团队关键词确定性样本漏搜数量为 0。
- [x] `AUDIT-005` 越界页返回实际末页，元数据与列表一致。
- [x] `AUDIT-006` 所有列表审计查询由 MySQL `repeatable read + read only` 事务强制只读；紧邻双快照 2235 条且变化 0。

## 9. 推荐验证命令

以下命令以实际 `package.json` 为准，执行前再次核对环境：

```bash
npm run accept:lead-pool-ui
node --env-file-if-exists=.env --import tsx --test server/tests/leadPoolQueryContract.test.ts server/tests/leadPoolEnteredAt.test.ts
npm run audit:lead-pool-query
npm run accept:lead-pool-http
npm run fixture:lead-pool-browser
npm run check:types
npm run test:server
npm run check:platform
npm run build
```

注意：

- `npm run test:server` 会加载 `.env`，执行前必须确认不会连接业务库执行写入测试。
- 写入型 `npm run accept:stable-pagination` 只能在确认隔离的 MySQL 测试库运行，并显式设置 `ALLOW_MYSQL_ACCEPTANCE_WRITES=true`；脚本会在导入数据库客户端前执行隔离库守卫。
- 真实数据库只读审计应使用独立 preview 命令，不能复用会调用 `ensureSchema()` 的脚本。

## 10. 验收证据与交付格式

最终交付必须分层报告：

| 层级 | 必须证据 | 不得替代为 |
|---|---|---|
| 源码 | 相关文件 diff、修改范围、无关改动未触碰 | README 或计划文字 |
| 自动化 | 测试命令、通过数、失败分类 | “代码看起来正确” |
| 数据库 | 隔离 fixture 结果、真实库只读集合审计 | Worker ready 或健康检查 |
| HTTP | 实际状态码、分页字段和筛选响应 | 直接调用 service 函数 |
| 浏览器 | 登录后 URL、选中条件、总数、行内容和前进/后退 DOM 证据 | 构建成功或 JSX 检查 |
| 本地生效 | 活跃 PID、监听端口、健康、实际静态资源哈希 | 仅生成候选构建 |
| 生产发布 | 发布命令、生产健康、线上资源及真实 DOM | Git push 或本地运行 |

交付结论模板：

```text
源码：已完成/未完成
测试：已通过 X 项；未通过或未执行 Y 项
真实数据库只读审计：已通过/未执行
隔离 MySQL 写入验收：已通过/未执行
HTTP 验收：已通过/未执行
登录后 DOM 验收：已通过/未执行
本地版本生效：是/否
生产发布：是/否
剩余风险：...
```

## 11. 回滚计划

### 11.1 源码回滚

- 保留修复前 commit/补丁基线；
- 回滚仅针对本计划确认的文件，不覆盖用户其他未提交修改；
- 前后端查询契约必须成组回滚，避免页面发送服务端不识别的枚举；
- 回滚后重新运行旧版查询、类型和最小 HTTP 验收。

### 11.2 地区数据回滚

- 仅适用于经授权执行的存量回填；
- apply 前保存逐行 `leadId`、旧三元组 `businessRegion/businessRegionSource/businessRegionConfidence` 和版本/时间依据；
- 回滚必须按精确 ID 和预期新值条件更新，防止覆盖 apply 后的人工修改；
- 回滚后重新运行地区集合审计并核对审计链。

## 12. 风险与决策项

| 决策/风险 | 推荐处理 | 决策状态 |
|---|---|---|
| 地区以派生展示还是标准列为准 | 推荐 `business_region` 为唯一列表/筛选权威，派生仅用于待回填候选 | 已按推荐口径实施，无数据写入 |
| Pre-B、股权融资等阶段如何分桶 | 采用第 6.6 节推荐分组，评审后固化前后端枚举 | 已按推荐分组实施 |
| 关键词 `%`、`_` 是否作为通配符 | 推荐按普通文本转义，避免 URL 构造任意通配查询 | 已按普通文本转义 |
| `source`、`sort` 是否继续对外兼容 | 无页面使用则限制为内部契约；不得无边界开放 | 已限制为 `source<=1000`、`sort=latest/score` |
| 计数后查询的并发一致性 | 优先同一只读事务/一致快照；至少保证越界修正和稳定排序 | 已使用 `repeatable read + read only` 同一事务 |
| 关键词 JSON 扫描性能 | 先记录代表性查询基线；本期不盲目建索引或迁移 | 已记录 2235 条只读基线：默认页中位 506.9ms，跨 JSON/多列关键词中位 1113.7ms；列为后续性能专项，不在本轮引入未经评审的生成列或索引迁移 |

## 13. 最终完成门禁

只有同时满足以下条件，才可将本计划标记为“修复完成”：

- [x] 10 个缺陷 ID 均有源码修复或经批准的产品决策结论。
- [x] 行业、地区、阶段和团队查询的真实只读集合差异均为 0。
- [x] 越界、非法参数、0 结果、末页和每页条数边界全部通过。
- [x] 浏览器前进/后退、详情返回和重试错误路径通过候选版本真实 DOM 验收。
- [x] `check:types`、相关单元/API/隔离 MySQL 测试通过；`check:platform` 的既有无关 source-gap 97/7 失败已记录证据。
- [x] 没有削弱鉴权、权限、审计、CSRF、Origin 和数据隔离边界。
- [x] 没有修改无关业务、编译产物、用户材料或现有未提交工作。
- [x] 源码完成、测试通过、本地生效和生产发布四类状态分别如实报告。

## 14. 本轮实施与验收记录（2026-09-02）

### 14.1 已完成

- 查询契约：前后端筛选枚举、URL 解析、关键词长度、页码和每页条数边界已收口。
- 服务端：修复“其他”行业补集、标准地区一致性、团队字段关键词、阶段桶、通配符转义及越界页回退。
- 前端：修复非法 URL、历史导航搜索词同步、API 实际页回写、统一重试错误处理和“清空筛选”丢失父级 `view=leads` 参数的问题。
- 回归：新增查询契约单测、只读真实库集合审计、只读 HTTP、候选浏览器 fixture 和隔离 MySQL 写入验收；未执行任何业务数据库写入。
- 构建：最终生产候选构建成功，使用测试监听保护阻止自动激活，结果为 `activated: false`。

### 14.2 当前证据

| 层级 | 结果 |
|---|---|
| 类型检查 | `npm run check:types` 通过 |
| 定向测试 | 16/16 通过，覆盖查询契约、行业/地区派生、分页、URL 双向同步、连续重试、过期请求、通配符和只读一致快照契约 |
| 源码验收 | `npm run accept:lead-pool-ui` 通过 |
| 真实库只读审计 | 2235 条；33 个行业、34 个地区、10 个阶段、24 个团队关键词样本通过；认可阶段 1580 条、未归桶 0；“其他”102 条；越界第 10000 页回退第 112 页；所有查询使用 MySQL 只读可重复读事务 |
| 只读 HTTP 验收 | 默认 20 条；越界第 10000 页回退第 112 页且末页 15 条；0 结果规范为第 1/1 页；团队关键词命中；“其他”102 条、北京 412 条、组合 13 条；8 个非法参数均返回 400 `INVALID_ARGUMENT`；无 Cookie 请求返回 401 `AUTH_REQUIRED`；列表仅返回批准的 scoring/Radar 投影 |
| 候选浏览器 DOM | 默认 2235 条/20 行；搜索框及状态入口均限制 100 字符；六筛选分别为科研 273、人工智能 298、Pre-B 3、北京 412、36氪 1862、近 7 天 367；“其他+北京”13 条且逐行匹配；10/20/50 条分页、末页回落、非法 URL、搜索防抖、前进/后退、清空筛选、空结果、连续失败重试和慢请求竞态均通过 |
| 详情返回 | 返回后恢复 `view=leads`、北京筛选、50 条/页、50 行及列表 `scrollTop=7608.5`；详情页存在“返回共享线索池”入口 |
| 地区回填 preview | 只读扫描 2796 条、可解析 2316 条、未解析 480 条、变更候选 0；服务启动不再自动回填，显式 apply 受 `ALLOW_LEAD_REGION_BACKFILL=true` 保护 |
| 隔离 MySQL 写入验收 | 使用进程级 `DB_DATABASE=sbl_jedi_acceptance_20260828` 与 `ALLOW_MYSQL_ACCEPTANCE_WRITES=1` 执行；MYSQL-001～008 全部通过，覆盖同值稳定排序、越界/收缩回落、“其他”、权威地区与派生冲突、团队关键词、Pre-B/股权融资/新一轮融资、AND 组合及 review 唯一 ID 排序 |
| 隔离库清理复核 | 验收前库表数为 0；初始化后保留 197 张结构表。脚本自身清理断言通过，独立查询再次确认 `pagination-acceptance` 线索 0、项目 0；项目 `.env` 未修改，业务库未执行 DDL/DML |
| 查询性能只读基线 | 2235 条规模、每个场景预热后重复 5 次：默认页 494.8/506.9/530.9ms（最小/中位/最大），跨 JSON/多列关键词 1032.9/1113.7/1125.4ms，“其他”中位 581.7ms，北京中位 498.8ms，“其他+北京”中位 515.7ms，越界回退中位 689.9ms；未执行 `EXPLAIN ANALYZE`、DDL 或索引变更 |
| 全量服务端测试 | 873 项中 866 通过、5 跳过、2 个既有 FDE fixture 失败；失败文件不在本轮修改范围 |
| 平台门禁 | 类型和数据库边界通过；单服务边界因既有 source-gap 97/7 固定约束失败，相关守卫文件与 `HEAD` 一致 |
| 候选资源一致性 | 浏览器验收加载 `index-08eD7Uu2.js`、`index-DeyTiQvN.css`，与最终候选构建输出哈希一致 |
| 构建生命周期纠偏 | 一次中间构建因保护端口环境变量使用错误而短暂替换本地构建指针；4100 当时无监听、未启动服务，随后使用项目 `rollback:build` 恢复前一版 `dist/server-dist`，错误构建保留在可恢复目录。之后按 `NODE_ENV=test + CYBERNAUT_BUILD_ACTIVATION_PORT` 正确阻止激活 |
| 最终候选构建 | `build-20260902T053951746Z-58195-3cc7de86` 成功；5 个 Web 文件、1366 个服务端文件；`activated: false` |

### 14.3 尚未执行

- 未启动或重启当前项目本地服务；HTTP 和 DOM 证据来自挂载真实 `metaRouter`、真实 MySQL 只读查询及候选静态资源的隔离验收进程，不声明当前 4100 运行版本已生效。
- 未使用真实 HttpOnly 已登录会话；无 Cookie 请求已经过真实 `requireAuth` 验证为 401，但登录成功后的会话续期、Cookie 和本地运行链仍待本地激活验收。浏览器 fixture 使用注入测试用户，不削弱生产鉴权代码。
- 地区存量回填已完成只读 preview，当前候选为 0；没有执行 apply 或修改业务数据。
- 未激活本地候选、未发布生产、未提交或推送 Git。

因此，本轮可判定为“10 项缺陷源码修复及计划要求的单元、契约、隔离 MySQL、真实库只读、HTTP 和候选 DOM 验收完成，最终候选构建完成”。本地激活、真实 HttpOnly 登录会话和生产发布属于需另行授权的运行/发布步骤，未执行且不声明已生效或已发布。
