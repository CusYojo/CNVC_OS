# 尽调工作台 MVP 验证清单

## 边界

- 路由：`/due-diligence?project=<项目 ID>`。
- 项目、成员、材料和风险继续以既有平台为权威来源；尽调表只存储这些对象的 ID 引用。
- 本模块不包含方法论中心或自进化中心。
- 数字分身的浏览器语音输入使用 Web Speech API：不上传录音。识别文本必须由用户确认并点击保存，才会写入版本历史。

## 首次部署

1. 使用迁移账户执行 `npm run db:migrate`，应用 `0120_add_due_diligence_workbench.sql`。
2. 启动平台并以具备某项目访问权的用户登录。
3. 从左侧“尽调工作台”进入，或从项目详情页点击“尽调工作台”。

## 闭环验收

1. 选择一个已有项目，创建核查问题，分配负责人，并关联该项目已有的风险和材料。
2. 将问题状态依次更新为“核查中”“待确认”“已完成”，刷新页面确认状态仍存在。
3. 创建现场访谈，填写议程、实时记录和访谈纪要；创建远程插问并在现场确认；刷新页面确认记录和插问仍存在。
4. 新建个人数字分身，保存文字规则和案例；在 Chrome 中授权麦克风，确认语音识别文本后保存，确认版本号增加；使用“模拟追问”验证规则参与追问提示。
5. 使用无项目权限的账号访问该项目对应 URL，确认服务端返回无权访问而不是泄露数据。
6. 在不支持 Web Speech API 的浏览器中点击语音输入，确认出现提示且文字编辑仍可用。
7. 回归 `/projects`、`/collaboration`、`/workflow` 和 `/risks`，确认原有项目、协同、OA 流程和风险主数据未被修改。
